---
phase: 10-auto-deploy-on-approval
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/app/(admin)/campaigns/[id]/page.tsx
  - src/app/(admin)/campaigns/[id]/DeployButton.tsx
  - src/app/(admin)/campaigns/[id]/DeployHistory.tsx
autonomous: true
requirements: [DEPLOY-02, DEPLOY-06, DEPLOY-07]

must_haves:
  truths:
    - Deploy Campaign button visible only when campaign status=approved and both approvals present
    - Confirmation modal shows campaign name, lead count, channels, sequence step count before deploy
    - Deploy history table on campaign detail shows timestamp, status, lead count, channels, error
    - Retry button appears on partial_failure/failed deploys for each failed channel
    - Deploy status visible on campaign detail page without polling (static after load)
  artifacts:
    - src/app/(admin)/campaigns/[id]/DeployButton.tsx
    - src/app/(admin)/campaigns/[id]/DeployHistory.tsx
  key_links:
    - DeployButton calls POST /api/campaigns/[id]/deploy
    - DeployHistory calls GET /api/campaigns/[id]/deploys
    - Both components embedded in existing campaign detail page
---

<objective>
Add the Deploy Campaign button, confirmation modal, and deploy history table to the campaign detail page.

Purpose: Admin needs a clear UI to trigger deploys, see confirmation before executing, and track deploy history with retry capability.
Output: DeployButton component (with confirmation modal), DeployHistory component (with retry), integrated into campaign detail page.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-auto-deploy-on-approval/10-CONTEXT.md
@.planning/phases/10-auto-deploy-on-approval/10-RESEARCH.md
@src/app/(admin)/campaigns/[id]/page.tsx

<interfaces>
<!-- API contracts from Plan 03 -->
```typescript
// POST /api/campaigns/[id]/deploy
// Request: empty body (or ?retry=email|linkedin for retry)
// Response: { deployId: string, status: "pending" | "retrying" }

// GET /api/campaigns/[id]/deploys
// Response: { deploys: CampaignDeploy[] }
// CampaignDeploy: { id, campaignId, campaignName, workspaceSlug, status, emailStatus, linkedinStatus, leadCount, emailStepCount, linkedinStepCount, emailBisonCampaignId, emailError, linkedinError, error, channels, retryChannel, createdAt, completedAt }
```

<!-- Campaign detail page structure (inspect before modifying) -->
<!-- The page likely fetches campaign data and displays status, content preview, approval status -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeployButton with confirmation modal</name>
  <files>
    src/app/(admin)/campaigns/[id]/DeployButton.tsx
  </files>
  <action>
Create src/app/(admin)/campaigns/[id]/DeployButton.tsx as a "use client" component.

**Props:**
```typescript
interface DeployButtonProps {
  campaignId: string;
  campaignName: string;
  status: string;
  leadsApproved: boolean;
  contentApproved: boolean;
  channels: string[];
  leadCount: number;
  emailStepCount: number;
  linkedinStepCount: number;
}
```

**Behavior:**
1. Only render the button when ALL conditions are met:
   - status === "approved"
   - leadsApproved === true
   - contentApproved === true
   - If none of these, return null (button not shown)

2. Button styling: Brand yellow background (#F0FF7A), dark text (#18181b), rounded, font-semibold. Use Tailwind classes. Icons from lucide-react (Rocket or Send icon).

3. On click: show a confirmation modal (use a simple React state-driven modal, not a library). Modal contains:
   - Title: "Deploy Campaign"
   - Campaign name in bold
   - Stats grid:
     - Leads: {leadCount}
     - Channels: {channels.join(", ")}
     - Email Steps: {emailStepCount} (only if channels includes "email")
     - LinkedIn Steps: {linkedinStepCount} (only if channels includes "linkedin")
   - Warning text: "This will push leads and content to the selected channels. This action cannot be undone."
   - Two buttons: "Cancel" (secondary) and "Deploy Now" (brand yellow)

4. On "Deploy Now" click:
   - Set loading state
   - POST to `/api/campaigns/${campaignId}/deploy`
   - On success: show success message, optionally refresh the page via router.refresh()
   - On error: show error message in the modal

5. Modal overlay: dark semi-transparent background, centered white card, rounded-xl, shadow-2xl. Close on overlay click or Cancel button.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DeployButton renders only for approved campaigns with both approvals. Confirmation modal shows campaign summary before deploying. Calls POST /api/campaigns/[id]/deploy. Brand yellow styling. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create DeployHistory table and integrate into campaign detail page</name>
  <files>
    src/app/(admin)/campaigns/[id]/DeployHistory.tsx
    src/app/(admin)/campaigns/[id]/page.tsx
  </files>
  <action>
1. Create src/app/(admin)/campaigns/[id]/DeployHistory.tsx as a "use client" component.

**Props:**
```typescript
interface DeployHistoryProps {
  campaignId: string;
}
```

**Behavior:**
- Fetch deploy history from GET /api/campaigns/{campaignId}/deploys on mount (useEffect + fetch)
- Display as a table with columns: Timestamp, Status, Channels, Leads, Email Steps, LI Steps, Error
- Status column: color-coded pill badge
  - complete: green bg, "Complete" text
  - partial_failure: amber bg, "Partial Failure" text
  - failed: red bg, "Failed" text
  - running: blue bg, "Running" text
  - pending: gray bg, "Pending" text
- Channels column: parse JSON channels array and show as comma-separated badges
- Error column: truncated to 50 chars with tooltip on hover showing full error
- If a deploy is in partial_failure or failed status, show a "Retry" dropdown/button next to the error:
  - If emailStatus === 'failed': "Retry Email" button
  - If linkedinStatus === 'failed': "Retry LinkedIn" button
  - On click: POST /api/campaigns/{campaignId}/deploy?retry={channel}
  - Show loading state, refresh table on success
- If no deploys: show "No deploys yet" placeholder text
- Tailwind styling: zinc-800 header row, alternating row backgrounds (zinc-900/zinc-950), text-sm

2. Update src/app/(admin)/campaigns/[id]/page.tsx:
- **Read the file first** to understand its current structure
- Import DeployButton and DeployHistory components
- Add DeployButton to the page, passing the required props from the campaign data. Position it near the top status area or after the approval section.
- Add DeployHistory section below the campaign content area. Wrap in a section with a heading "Deploy History".
- Compute emailStepCount and linkedinStepCount from the campaign's emailSequence and linkedinSequence (parse JSON, count array length).
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DeployHistory fetches and displays deploy records with status badges and retry buttons. Campaign detail page includes DeployButton (conditional on status=approved) and DeployHistory section. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- DeployButton only renders when status === 'approved' && leadsApproved && contentApproved
- Confirmation modal shows accurate counts (leads, steps, channels)
- Deploy history table shows all deploys with correct status colors
- Retry buttons only appear for failed/partial_failure deploys
- Campaign detail page imports and renders both components
- All styling uses Tailwind with brand color #F0FF7A
</verification>

<success_criteria>
- `npx tsc --noEmit` passes with no errors
- DeployButton.tsx and DeployHistory.tsx exist as client components
- Campaign detail page integrates both components
- Button correctly hidden when campaign is not in approved state
</success_criteria>

<output>
After completion, create `.planning/phases/10-auto-deploy-on-approval/10-05-SUMMARY.md`
</output>
