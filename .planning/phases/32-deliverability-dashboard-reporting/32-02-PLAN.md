---
phase: 32-deliverability-dashboard-reporting
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/app/(admin)/deliverability/page.tsx
  - src/components/deliverability/domain-health-cards.tsx
  - src/components/deliverability/sender-health-table.tsx
  - src/components/deliverability/activity-feed.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Admin can navigate to /deliverability from the sidebar"
    - "Deliverability page shows domain health cards with SPF/DKIM/DMARC badges and blacklist status"
    - "Each sender shows a 30-day bounce rate sparkline and warmup progress bar"
    - "Activity feed shows recent EmailHealthEvent timeline with color-coded status transitions"
    - "Workspace dropdown filter narrows all three sections"
  artifacts:
    - path: "src/app/(admin)/deliverability/page.tsx"
      provides: "Admin deliverability page with three sections"
      min_lines: 80
    - path: "src/components/deliverability/domain-health-cards.tsx"
      provides: "Domain health card grid with DNS badges"
      min_lines: 60
    - path: "src/components/deliverability/sender-health-table.tsx"
      provides: "Sender table with sparklines and warmup bars"
      min_lines: 80
    - path: "src/components/deliverability/activity-feed.tsx"
      provides: "Event timeline with color-coded transitions"
      min_lines: 50
    - path: "src/components/layout/sidebar.tsx"
      provides: "Deliverability nav item in email group"
      contains: "Deliverability"
  key_links:
    - from: "src/app/(admin)/deliverability/page.tsx"
      to: "/api/deliverability/*"
      via: "fetch calls to all four API endpoints"
      pattern: "fetch.*api/deliverability"
    - from: "src/components/deliverability/sender-health-table.tsx"
      to: "recharts"
      via: "LineChart sparkline rendering"
      pattern: "LineChart|ResponsiveContainer"
    - from: "src/components/layout/sidebar.tsx"
      to: "/deliverability"
      via: "nav item href"
      pattern: "deliverability"
---

<objective>
Build the full admin Deliverability page with domain health cards, sender table (sparklines + warmup bars), activity feed, and add sidebar navigation.

Purpose: This is the primary deliverability visibility surface for admins. All data comes from Plan 01's API endpoints.
Output: Complete /deliverability page with three sections + sidebar link.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-deliverability-dashboard-reporting/32-01-SUMMARY.md
@src/components/senders/sender-health-panel.tsx
@src/components/layout/sidebar.tsx
@src/app/(admin)/intelligence/page.tsx

<interfaces>
<!-- API response shapes from Plan 01 -->

GET /api/deliverability/summary → {
  domains: { total, healthy, atRisk, worst: { domain, overallHealth } },
  senders: { total, healthy, elevated, warning, critical },
  recentEvents: [...]
}

GET /api/deliverability/domains → [{
  domain, spfStatus, dkimStatus, dkimSelectors: string[],
  dmarcStatus, dmarcPolicy, blacklistHits: string[],
  blacklistSeverity, overallHealth, lastDnsCheck, lastBlacklistCheck,
  activeSenderCount
}]

GET /api/deliverability/senders → [{
  id, emailAddress, workspaceSlug, emailBounceStatus,
  emailBounceStatusAt, warmupDay, warmupStartedAt,
  currentBounceRate, sparklineData: [{ date, bounceRate }],
  consecutiveHealthyChecks
}]

GET /api/deliverability/events → {
  events: [{ id, senderEmail, senderDomain, workspaceSlug,
    fromStatus, toStatus, reason, bouncePct, detail, createdAt }],
  hasMore, nextCursor
}

<!-- Sidebar nav item pattern (from sidebar.tsx line 128-134) -->
{ href: "/deliverability", label: "Deliverability", icon: ShieldCheck }
Add to "email" group items array, after "Intelligence Hub" entry.

<!-- Sparkline pattern (from sender-health-panel.tsx) -->
Uses recharts: LineChart, Line, ResponsiveContainer
Height ~40-48px, no axes, responsive container wrapper.

<!-- UI components available -->
Card, CardHeader, CardTitle, CardContent from @/components/ui/card
Table, TableHeader, TableRow, TableHead, TableBody, TableCell from @/components/ui/table
Badge from @/components/ui/badge
Select, SelectTrigger, SelectValue, SelectContent, SelectItem from @/components/ui/select
Button from @/components/ui/button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain health cards + sender table components</name>
  <files>src/components/deliverability/domain-health-cards.tsx, src/components/deliverability/sender-health-table.tsx</files>
  <action>
Create two client components for the deliverability page sections.

**domain-health-cards.tsx**
- Props: `{ domains: DomainData[] }` where DomainData matches the /api/deliverability/domains response shape
- Render a responsive grid of cards: `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4`
- Each card shows:
  - Domain name as card title
  - DNS badges row: SPF (green if "pass", red if "fail"/"missing"), DKIM (green if "pass", yellow if "partial", red if "fail"/"missing"), DMARC (green if "pass", red if "fail"/"missing")
    - Use Badge component with appropriate bg colors: `bg-emerald-100 text-emerald-800` (pass), `bg-yellow-100 text-yellow-800` (partial), `bg-red-100 text-red-800` (fail)
  - Blacklist status: show "Clear" chip (green) if no hits, or "X blacklists" chip (red) with blacklistHits count
  - Active sender count: "X senders" text
  - Overall health chip: healthy=green, warning=amber, critical=red, unknown=gray
  - Last checked timestamp (relative, e.g. "2h ago")
- Empty state: "No domains found" message

**sender-health-table.tsx**
- Props: `{ senders: SenderData[] }` where SenderData matches /api/deliverability/senders response
- "use client" component (recharts requires client-side rendering)
- Sortable table with columns:
  1. Email Address (text)
  2. Workspace (text, small badge)
  3. Health Status (colored chip: green=healthy, yellow=elevated, orange=warning, red=critical)
  4. 30-day Bounce Rate sparkline: use recharts `<ResponsiveContainer width={120} height={32}><LineChart data={sparklineData}><Line type="monotone" dataKey="bounceRate" stroke={colorByStatus} strokeWidth={1.5} dot={false} /></LineChart></ResponsiveContainer>`
     - Color sparkline stroke by emailBounceStatus: healthy=green, elevated=yellow, warning=orange, critical=red
     - If sparklineData is empty, show "No data" text
  5. Current Bounce % (formatted to 1 decimal, e.g. "2.3%"), or "—" if null
  6. Warmup Progress: show progress bar if warmupDay > 0 using `<div className="h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-brand" style={{ width: \`${(warmupDay/28)*100}%\` }} /></div>` with "Day X/28" label. If warmupDay === 0, show "Not started" label.
  7. Last Checked (relative timestamp from emailBounceStatusAt)
- Client-side sorting via useState: click column headers to toggle sort direction
- Import `formatDistanceToNow` from `date-fns` for relative timestamps
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit src/components/deliverability/domain-health-cards.tsx src/components/deliverability/sender-health-table.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Domain health cards render DNS badges and blacklist status per domain; sender table renders sparklines via recharts and warmup progress bars with client-side sorting</done>
</task>

<task type="auto">
  <name>Task 2: Activity feed + page layout + sidebar link</name>
  <files>src/components/deliverability/activity-feed.tsx, src/app/(admin)/deliverability/page.tsx, src/components/layout/sidebar.tsx</files>
  <action>
**activity-feed.tsx**
- Props: `{ initialEvents: EventData[], workspaceFilter?: string }` where EventData matches /api/deliverability/events response items
- "use client" component with useState for events array and hasMore/nextCursor state
- Render reverse-chronological list of events, each as a row:
  - Left border or dot color-coded by toStatus: green (healthy), yellow (elevated), orange (warning), red (critical)
  - Timestamp (relative using date-fns formatDistanceToNow)
  - Sender email
  - Transition text: "fromStatus → toStatus" with arrow
  - Reason chip (bounce_rate, blacklist, step_down, manual)
  - Detail text if available (action taken)
- "Load more" button at bottom when hasMore is true
  - On click: fetch `/api/deliverability/events?cursor=${nextCursor}${workspaceFilter ? '&workspace=' + workspaceFilter : ''}` and append to events array
- Default shows first 20 events (passed as initialEvents prop)
- Empty state: "No health events recorded yet"

**deliverability/page.tsx**
- "use client" component
- Fetch all four API endpoints on mount using useEffect + useState:
  - `/api/deliverability/summary`
  - `/api/deliverability/domains`
  - `/api/deliverability/senders`
  - `/api/deliverability/events`
- Workspace filter: Select dropdown at top of page, populated from unique workspaceSlug values from senders data
  - On change: re-fetch all endpoints with `?workspace=slug` query param
  - "All Workspaces" option clears filter
- Page structure:
  - `<Header title="Deliverability" description="Domain health, sender status, and auto-rotation activity" />`
  - `<div className="p-6 space-y-6">`
    - Workspace filter Select at top right
    - Section 1: Domain Health Cards (Card wrapper with title "Domain Health")
    - Section 2: Sender Health Table (Card wrapper with title "Sender Status")
    - Section 3: Activity Feed (Card wrapper with title "Health Events")
- Loading state: show skeleton/spinner while data loads
- Import Header from `@/components/layout/header`

**sidebar.tsx**
- Import `ShieldCheck` from `lucide-react`
- Add nav item to the "email" group items array (after the Intelligence Hub entry at line ~132):
  `{ href: "/deliverability", label: "Deliverability", icon: ShieldCheck }`
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit src/components/deliverability/activity-feed.tsx src/app/\(admin\)/deliverability/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Deliverability page renders all three sections with workspace filter; sidebar shows "Deliverability" link in email group navigating to /deliverability</done>
</task>

</tasks>

<verification>
- /deliverability page loads and shows domain cards, sender table, and activity feed
- Sidebar contains "Deliverability" link with ShieldCheck icon
- Domain cards show SPF/DKIM/DMARC green/red badges
- Sender table has inline recharts sparklines
- Warmup progress bars render for senders with warmupDay > 0
- Activity feed supports "Load more" pagination
- Workspace filter re-fetches all sections
</verification>

<success_criteria>
Admin can navigate to /deliverability from sidebar and see domain health cards with DNS badges, sender table with 30-day sparklines and warmup bars, and a paginated activity feed — all filterable by workspace.
</success_criteria>

<output>
After completion, create `.planning/phases/32-deliverability-dashboard-reporting/32-02-SUMMARY.md`
</output>
