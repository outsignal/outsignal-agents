---
phase: 71-foundation-constants-interface-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/channels/constants.ts
  - src/lib/channels/types.ts
  - src/lib/channels/registry.ts
  - src/lib/channels/index.ts
  - src/lib/channels/__tests__/constants.test.ts
  - src/lib/channels/__tests__/registry.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "All channel types, action types, sender types, and status strings are importable as typed constants from src/lib/channels/constants.ts"
    - "ChannelAdapter interface is defined with all 7 methods and compiles cleanly"
    - "getAdapter(channel) resolves an adapter from the registry and throws for unknown channels"
    - "UnifiedLead, UnifiedAction, UnifiedMetrics, UnifiedStep, CampaignChannelRef are importable from src/lib/channels/types.ts"
    - "senderMatchesChannel() correctly handles the 'both' tri-state"
  artifacts:
    - path: "src/lib/channels/constants.ts"
      provides: "All string enum constants and derived types"
      contains: "CHANNEL_TYPES"
    - path: "src/lib/channels/types.ts"
      provides: "ChannelAdapter interface and unified types"
      contains: "interface ChannelAdapter"
    - path: "src/lib/channels/registry.ts"
      provides: "Map-based adapter registry with getAdapter, registerAdapter, getAllAdapters, clearAdapters"
      exports: ["registerAdapter", "getAdapter", "getAllAdapters", "clearAdapters"]
    - path: "src/lib/channels/index.ts"
      provides: "Barrel export for all channel module exports"
    - path: "src/lib/channels/__tests__/constants.test.ts"
      provides: "Exhaustiveness tests for constants"
    - path: "src/lib/channels/__tests__/registry.test.ts"
      provides: "Registry resolution and error handling tests"
  key_links:
    - from: "src/lib/channels/types.ts"
      to: "src/lib/channels/constants.ts"
      via: "import type { ChannelType }"
      pattern: "import.*ChannelType.*constants"
    - from: "src/lib/channels/registry.ts"
      to: "src/lib/channels/types.ts"
      via: "import type { ChannelAdapter }"
      pattern: "import.*ChannelAdapter.*types"
    - from: "src/lib/channels/index.ts"
      to: "src/lib/channels/constants.ts, types.ts, registry.ts"
      via: "barrel re-exports"
      pattern: "export.*from"
---

<objective>
Create the entire channel adapter foundation: typed constants for all string enums, the ChannelAdapter interface with unified types, the Map-based adapter registry, and tests.

Purpose: Eliminate raw string comparisons (source of 6 production bugs), define the contract that all channel adapters will implement in Phase 72, and provide the registry factory that consumers will use from Phase 73 onward.

Output: `src/lib/channels/` directory with constants.ts, types.ts, registry.ts, index.ts, and two test files.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-foundation-constants-interface-registry/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.ts with all string enum domains and derived types</name>
  <files>src/lib/channels/constants.ts</files>
  <action>
Create `src/lib/channels/constants.ts` with ALL string enum domains identified in the research. Use `as const` objects (NOT TypeScript enums -- the codebase uses zero enums). Derive union types from each const object using `(typeof X)[keyof typeof X]` pattern.

Include these exact const objects with these exact values (from grep analysis in RESEARCH.md):

1. `CHANNEL_TYPES` = { EMAIL: "email", LINKEDIN: "linkedin" } -- the adapter discriminator
2. `SENDER_CHANNELS` = { EMAIL: "email", LINKEDIN: "linkedin", BOTH: "both" } -- Sender model's tri-state
3. `WORKSPACE_PACKAGES` = { EMAIL: "email", LINKEDIN: "linkedin", EMAIL_LINKEDIN: "email_linkedin", CONSULTANCY: "consultancy" }
4. `LINKEDIN_ACTION_TYPES` = { CONNECT: "connect", CONNECTION_REQUEST: "connection_request", MESSAGE: "message", PROFILE_VIEW: "profile_view", CHECK_CONNECTION: "check_connection" }
5. `CONNECTION_REQUEST_TYPES` = [LINKEDIN_ACTION_TYPES.CONNECT, LINKEDIN_ACTION_TYPES.CONNECTION_REQUEST] as const -- helper array for Prisma `{ in: [...] }` queries
6. `LINKEDIN_ACTION_STATUSES` = { PENDING: "pending", RUNNING: "running", COMPLETE: "complete", FAILED: "failed", CANCELLED: "cancelled", EXPIRED: "expired" }
7. `SENDER_STATUSES` = { SETUP: "setup", ACTIVE: "active", PAUSED: "paused", DISABLED: "disabled" }
8. `SENDER_HEALTH_STATUSES` = { HEALTHY: "healthy", WARNING: "warning", PAUSED: "paused", BLOCKED: "blocked", SESSION_EXPIRED: "session_expired" }
9. `CAMPAIGN_STATUSES` = { DRAFT: "draft", INTERNAL_REVIEW: "internal_review", PENDING_APPROVAL: "pending_approval", APPROVED: "approved", DEPLOYED: "deployed", ACTIVE: "active", PAUSED: "paused", COMPLETED: "completed", ARCHIVED: "archived" }
10. `DEPLOY_CHANNEL_STATUSES` = { PENDING: "pending", RUNNING: "running", COMPLETE: "complete", FAILED: "failed", SKIPPED: "skipped" }
11. `DEPLOY_STATUSES` = { PENDING: "pending", RUNNING: "running", COMPLETE: "complete", PARTIAL_FAILURE: "partial_failure", FAILED: "failed" }
12. `CONNECTION_STATUSES` = { NONE: "none", PENDING: "pending", CONNECTED: "connected", FAILED: "failed", EXPIRED: "expired" }
13. `SESSION_STATUSES` = { NOT_SETUP: "not_setup", ACTIVE: "active", EXPIRED: "expired" }

For each const object, export a derived type (e.g., `export type ChannelType = (typeof CHANNEL_TYPES)[keyof typeof CHANNEL_TYPES]`).

Include the `senderMatchesChannel(senderChannel: SenderChannel, target: ChannelType): boolean` helper function that returns true if senderChannel is BOTH or equals target. This encapsulates the tri-state logic so consumers never write `channel: { in: ['linkedin', 'both'] }` directly.

IMPORTANT: This file must NOT import from any other file in the channels module. It is the leaf dependency.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit src/lib/channels/constants.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 13 const objects exported with derived types. senderMatchesChannel helper exported. File compiles with zero errors. No imports from other channel module files.</done>
</task>

<task type="auto">
  <name>Task 2: Create types.ts with ChannelAdapter interface and unified types, registry.ts with Map-based factory, and index.ts barrel</name>
  <files>src/lib/channels/types.ts, src/lib/channels/registry.ts, src/lib/channels/index.ts</files>
  <action>
**types.ts** -- Create `src/lib/channels/types.ts` importing ONLY `ChannelType` from `./constants`. Define these types and interfaces:

- `CampaignChannelRef` -- { campaignId: string, workspaceSlug: string, campaignName: string, emailBisonCampaignId?: number }. MUST carry all identifiers needed by ANY adapter (campaignId for internal, campaignName+workspaceSlug for LinkedIn, emailBisonCampaignId for email).
- `DeployParams` -- { deployId: string, campaignId: string, campaignName: string, workspaceSlug: string, sequence: UnifiedStep[] }
- `DeployResult` -- { success: boolean, error?: string }
- `UnifiedLead` -- { id: string, email?: string, linkedInUrl?: string, name?: string, company?: string, title?: string, channel: ChannelType, status: string, addedAt?: Date }
- `UnifiedAction` -- { id: string, channel: ChannelType, actionType: string, status: string, personId?: string, personName?: string, personEmail?: string, detail?: string, performedAt: Date, campaignName?: string }
- `UnifiedMetrics` -- shared required fields (sent, replied, replyRate) + optional email fields (opened, openRate, bounced, bounceRate) + optional LinkedIn fields (connectionsSent, connectionsAccepted, acceptRate, messagesSent, profileViews). Channel discriminator field `channel: ChannelType`.
- `UnifiedStep` -- { stepNumber: number, channel: ChannelType, type: string, delayDays: number, subjectLine?: string, bodyHtml?: string, messageBody?: string, triggerEvent?: string }
- `ChannelAdapter` interface -- { readonly channel: ChannelType, deploy(params: DeployParams): Promise<DeployResult>, pause(ref: CampaignChannelRef): Promise<void>, resume(ref: CampaignChannelRef): Promise<void>, getMetrics(ref: CampaignChannelRef): Promise<UnifiedMetrics>, getLeads(ref: CampaignChannelRef): Promise<UnifiedLead[]>, getActions(ref: CampaignChannelRef): Promise<UnifiedAction[]>, getSequenceSteps(ref: CampaignChannelRef): Promise<UnifiedStep[]> }

IMPORTANT: types.ts imports from constants.ts only. Never from registry.ts. No external dependencies.

**registry.ts** -- Create `src/lib/channels/registry.ts` importing `ChannelType` from `./constants` and `ChannelAdapter` from `./types`. Implement:
- `const adapters = new Map<ChannelType, ChannelAdapter>()`
- `registerAdapter(adapter: ChannelAdapter): void` -- sets adapter in map keyed by adapter.channel
- `getAdapter(channel: ChannelType): ChannelAdapter` -- gets from map, throws descriptive error if not found (include registered adapter list in error message and "Did you call initAdapters()?" hint)
- `getAllAdapters(): ChannelAdapter[]` -- returns all registered adapters as array
- `clearAdapters(): void` -- clears the map (for testing only, add JSDoc noting this)

**index.ts** -- Create `src/lib/channels/index.ts` as barrel export:
```
export * from "./constants";
export * from "./types";
export { registerAdapter, getAdapter, getAllAdapters, clearAdapters } from "./registry";
```
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit src/lib/channels/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>ChannelAdapter interface defined with 7 methods. All 5 unified types exported. Registry has getAdapter/registerAdapter/getAllAdapters/clearAdapters. Barrel export compiles cleanly. Import chain is one-way: constants <- types <- registry <- index.</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for constants exhaustiveness and registry behavior</name>
  <files>src/lib/channels/__tests__/constants.test.ts, src/lib/channels/__tests__/registry.test.ts</files>
  <action>
**constants.test.ts** -- Create tests using vitest:
1. Test that CHANNEL_TYPES has exactly 2 values ("email", "linkedin")
2. Test that SENDER_CHANNELS has exactly 3 values ("email", "linkedin", "both")
3. Test that LINKEDIN_ACTION_TYPES has exactly 5 values
4. Test that CONNECTION_REQUEST_TYPES contains both "connect" and "connection_request"
5. Test senderMatchesChannel:
   - ("email", "email") => true
   - ("linkedin", "linkedin") => true
   - ("both", "email") => true
   - ("both", "linkedin") => true
   - ("email", "linkedin") => false
   - ("linkedin", "email") => false
6. Test that CAMPAIGN_STATUSES has exactly 9 values
7. Test that DEPLOY_STATUSES has exactly 5 values

**registry.test.ts** -- Create tests using vitest:
1. Import clearAdapters, registerAdapter, getAdapter, getAllAdapters
2. Use beforeEach to call clearAdapters()
3. Test: getAdapter throws for unregistered channel with descriptive error message containing the channel name
4. Test: registerAdapter + getAdapter round-trip -- register a mock adapter with channel "email", then getAdapter("email") returns it
5. Test: getAllAdapters returns all registered adapters
6. Test: registering a second adapter for the same channel replaces the first
7. Test: clearAdapters empties the registry

For mock adapters, create a minimal object satisfying the ChannelAdapter interface with vi.fn() for all methods and a readonly channel property.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx vitest run src/lib/channels/__tests__/ 2>&1 | tail -30</automated>
  </verify>
  <done>All constants exhaustiveness tests pass. All registry tests pass (register, resolve, error, clear, getAllAdapters). Mock adapter satisfies ChannelAdapter interface.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit src/lib/channels/index.ts` -- zero type errors
2. `npx vitest run src/lib/channels/__tests__/` -- all tests pass
3. Verify import chain is one-way: constants.ts has zero imports from the channels module, types.ts imports only from constants.ts, registry.ts imports from constants.ts and types.ts
4. Verify all 13 const objects are exported from constants.ts
5. Verify ChannelAdapter interface has exactly 7 methods (deploy, pause, resume, getMetrics, getLeads, getActions, getSequenceSteps) plus readonly channel property
</verification>

<success_criteria>
- `src/lib/channels/` directory exists with 4 source files and 2 test files
- All files compile with zero TypeScript errors
- All tests pass
- Every string enum domain identified in RESEARCH.md is represented as a typed constant
- ChannelAdapter interface is importable and has all 7 methods
- getAdapter() resolves registered adapters and throws for unknown channels
- Phase 72 can immediately import from `src/lib/channels` and start implementing adapters
</success_criteria>

<output>
After completion, create `.planning/phases/71-foundation-constants-interface-registry/71-01-SUMMARY.md`
</output>
