---
phase: 46-skill-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claudeignore
  - src/lib/sanitize-output.ts
  - src/lib/__tests__/sanitize-output.test.ts
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Claude Code sessions in this project never see .env file contents"
    - "CLI wrapper stdout passed through sanitizeOutput strips all known secret formats"
    - "PII (emails, names, workspace slugs) is NOT stripped by sanitizer"
  artifacts:
    - path: ".claudeignore"
      provides: "Secret file exclusion from Claude Code context"
      contains: ".env"
    - path: "src/lib/sanitize-output.ts"
      provides: "Stdout sanitization utility"
      exports: ["sanitizeOutput"]
    - path: "src/lib/__tests__/sanitize-output.test.ts"
      provides: "Unit tests for sanitization"
      min_lines: 30
  key_links:
    - from: "src/lib/sanitize-output.ts"
      to: "CLI wrapper scripts (Phase 48)"
      via: "import { sanitizeOutput }"
      pattern: "export function sanitizeOutput"
---

<objective>
Create the security foundation that gates all downstream CLI skill work: `.claudeignore` to prevent secret files from entering Claude Code context, and `sanitize-output.ts` to strip credentials from CLI wrapper stdout.

Purpose: CVE-2025-59536 credential exposure risk mitigation. No skill file can safely be invoked without these protections.
Output: `.claudeignore` file, `sanitize-output.ts` utility with unit tests.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-skill-architecture-foundation/46-CONTEXT.md
@.planning/phases/46-skill-architecture-foundation/46-RESEARCH.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claudeignore and sanitize-output.ts</name>
  <files>.claudeignore, src/lib/sanitize-output.ts</files>
  <action>
    1. Create `.claudeignore` at project root with these patterns:
       - `.env`, `.env.*`, `.env*.local` (secrets)
       - `*.pem`, `*.key`, `*.p12`, `*.pfx` (certificate files)
       - `prisma/dev.db`, `prisma/dev.db-journal` (local database)
       - `.next/`, `dist/`, `.trigger/` (build artifacts that may contain env interpolation)
       - `.vercel/` (deployment config with project IDs/tokens)
       - `node_modules/`, `worker/node_modules/` (dependencies)
       - `coverage/` (test output)
       - `.DS_Store` (OS artifacts)
       Group patterns with comment headers for readability.

    2. Create `src/lib/sanitize-output.ts` exporting a `sanitizeOutput(output: string): string` function.
       - Define a `SecretPattern` interface: `{ pattern: RegExp; type: string }`
       - Include these specific patterns (per locked decision on secrets-only scope):
         - `DATABASE_URL=\S+` -> `[REDACTED:DATABASE_URL]`
         - `postgres(ql)?://[^\s"']+` -> `[REDACTED:DATABASE_URL]`
         - `sk-ant-[A-Za-z0-9_-]+` -> `[REDACTED:ANTHROPIC_KEY]`
         - `sk-[A-Za-z0-9]{20,}` -> `[REDACTED:OPENAI_KEY]`
         - `tr_[A-Za-z0-9]{20,}` -> `[REDACTED:TRIGGER_KEY]`
         - `re_[A-Za-z0-9]{20,}` -> `[REDACTED:RESEND_KEY]`
         - `xoxb-[A-Za-z0-9-]+` -> `[REDACTED:SLACK_TOKEN]`
         - `vercelblob_rw_[A-Za-z0-9]+` -> `[REDACTED:BLOB_TOKEN]`
         - Named env var assignments: `(ANTHROPIC_API_KEY|OPENAI_API_KEY|TRIGGER_SECRET_KEY|RESEND_API_KEY|EMAILBISON_API_KEY|EMAILGUARD_API_TOKEN|INGEST_WEBHOOK_SECRET|API_SECRET|BLOB_READ_WRITE_TOKEN|SLACK_BOT_TOKEN)=\S+` -> `[REDACTED:ENV_VAR_NAME]`
         - Authorization Bearer: `Authorization:\s*Bearer\s+\S+` -> `[REDACTED:BEARER_TOKEN]`
       - The function is a pure string transformation. It must NOT import `dotenv`, read `.env`, or access `process.env`. It operates only on the string passed to it.
       - Pattern order matters: specific patterns (sk-ant-) before generic patterns to get precise redaction types.
       - Use `gi` flags on env var assignment patterns (case-insensitive), `g` on token prefix patterns.
  </action>
  <verify>
    <automated>npx tsx -e "const { sanitizeOutput } = require('./src/lib/sanitize-output'); const out = sanitizeOutput('DATABASE_URL=postgresql://u:p@host/db sk-ant-abc123 rise workspace'); console.log(out.includes('[REDACTED:DATABASE_URL]') && out.includes('[REDACTED:ANTHROPIC_KEY]') && out.includes('rise workspace') ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <done>.claudeignore blocks .env*, .next/, dist/, .trigger/, .vercel/, node_modules/, prisma dev.db. sanitizeOutput strips DATABASE_URL, API keys, tokens but preserves PII and workspace names.</done>
</task>

<task type="auto">
  <name>Task 2: Write sanitize-output unit tests</name>
  <files>src/lib/__tests__/sanitize-output.test.ts</files>
  <action>
    Create comprehensive unit tests for `sanitizeOutput`. Use the existing test framework (vitest or jest — check project config). Test cases:

    1. **Redacts DATABASE_URL assignment**: Input containing `DATABASE_URL=postgresql://user:super_secret@neon.tech/db` -> output contains `[REDACTED:DATABASE_URL]`
    2. **Redacts postgresql:// URL without assignment prefix**: Input `connecting to postgresql://user:pass@host/db` -> redacted
    3. **Redacts Anthropic API key**: `sk-ant-api03-abc123def456ghi789` -> `[REDACTED:ANTHROPIC_KEY]`
    4. **Redacts OpenAI API key**: `sk-proj-abc123def456ghi789jkl012` -> `[REDACTED:OPENAI_KEY]`
    5. **Redacts Trigger.dev key**: `tr_dev_abc123def456ghi789jkl` -> `[REDACTED:TRIGGER_KEY]`
    6. **Redacts Resend key**: `re_abc123def456ghi789jkl012` -> `[REDACTED:RESEND_KEY]`
    7. **Redacts Slack bot token**: `xoxb-123456-789012-abcdefgh` -> `[REDACTED:SLACK_TOKEN]`
    8. **Redacts Vercel Blob token**: `vercelblob_rw_abc123def456` -> `[REDACTED:BLOB_TOKEN]`
    9. **Redacts named env vars**: `EMAILBISON_API_KEY=abc123secret` -> `[REDACTED:EMAILBISON_API_KEY]`
    10. **Redacts Bearer token**: `Authorization: Bearer eyJhbGciOiJIUzI...` -> `[REDACTED:BEARER_TOKEN]`
    11. **PRESERVES workspace names**: Input containing `Workspace: rise` -> output still contains `Workspace: rise`
    12. **PRESERVES email addresses**: Input containing `april@rise.co` -> output still contains `april@rise.co`
    13. **PRESERVES campaign names**: Input containing `Rise Q2 Email` -> output still contains `Rise Q2 Email`
    14. **PRESERVES person names**: Input containing `FIRSTNAME=April` -> output still contains `FIRSTNAME=April`
    15. **Handles multiple secrets in one string**: Input with DATABASE_URL + API key + Slack token -> all three redacted
    16. **Returns unchanged string with no secrets**: Plain text input -> returned unchanged

    If the project uses vitest, import from `vitest`. If jest, use jest globals. Check `package.json` for test runner.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx vitest run src/lib/__tests__/sanitize-output.test.ts --reporter=verbose 2>/dev/null || npx jest src/lib/__tests__/sanitize-output.test.ts --verbose 2>/dev/null</automated>
  </verify>
  <done>All 16 test cases pass. Sanitizer correctly strips credentials while preserving PII and business data.</done>
</task>

</tasks>

<verification>
1. `.claudeignore` exists at project root with .env*, .next/, dist/, .trigger/, node_modules/ patterns
2. `src/lib/sanitize-output.ts` exports `sanitizeOutput` function
3. Unit tests pass — secrets redacted, PII preserved
</verification>

<success_criteria>
- .claudeignore blocks all secret file patterns from Claude Code context
- sanitizeOutput strips 10+ known secret formats with [REDACTED:type] replacements
- No false positives: workspace slugs, email addresses, person names survive sanitization
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/46-skill-architecture-foundation/46-01-SUMMARY.md`
</output>
