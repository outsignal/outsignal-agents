---
phase: 49-specialist-cli-skill-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/deliverability-rules.md
  - .claude/rules/onboarding-rules.md
  - .claude/rules/intelligence-rules.md
  - .claude/rules/writer-rules.md
  - .claude/rules/research-rules.md
  - .claude/rules/leads-rules.md
  - .claude/rules/campaign-rules.md
autonomous: true
requirements: [SKL-09]

must_haves:
  truths:
    - "deliverability-rules.md contains comprehensive diagnostic flow, warmup strategy, sender rotation, and DNS guidance — not a stub"
    - "onboarding-rules.md contains full workspace setup, domain config, inbox provisioning, and campaign scaffolding workflow — not a stub"
    - "intelligence-rules.md contains analytics methodology, cross-client benchmarking, insight generation, and optimization recommendations — not a stub"
    - "All 7 rules files reference dist/cli/ tool paths instead of camelCase TypeScript tool function names"
    - "All 7 rules files contain a Memory Write Governance section specifying which files the agent may/must-not write to"
  artifacts:
    - path: ".claude/rules/deliverability-rules.md"
      provides: "Full deliverability agent behavioral rules"
      contains: "## Diagnostic Flow"
    - path: ".claude/rules/onboarding-rules.md"
      provides: "Full onboarding agent behavioral rules"
      contains: "## Onboarding Workflow"
    - path: ".claude/rules/intelligence-rules.md"
      provides: "Full intelligence agent behavioral rules"
      contains: "## Analysis Methodology"
    - path: ".claude/rules/writer-rules.md"
      provides: "Updated writer rules with CLI refs + memory governance"
      contains: "## Memory Write Governance"
    - path: ".claude/rules/research-rules.md"
      provides: "Updated research rules with CLI refs + memory governance"
      contains: "## Memory Write Governance"
    - path: ".claude/rules/leads-rules.md"
      provides: "Updated leads rules with CLI refs + memory governance"
      contains: "## Memory Write Governance"
    - path: ".claude/rules/campaign-rules.md"
      provides: "Updated campaign rules with CLI refs + memory governance"
      contains: "## Memory Write Governance"
  key_links:
    - from: ".claude/rules/*-rules.md"
      to: "dist/cli/*.js"
      via: "CLI tool references in rules"
      pattern: "node dist/cli/"
---

<objective>
Flesh out 3 stub rules files and update 4 existing rules files for CLI agent context.

Purpose: Rules files are the behavioral backbone referenced by every skill file via @-import. The 3 new agent rules (deliverability, onboarding, intelligence) are currently stubs (15 lines each) and must be comprehensively authored before their skill files are created. The 4 existing rules files (writer, research, leads, campaign) need two targeted updates: (1) replace any camelCase TypeScript tool function references with `node dist/cli/*.js` CLI invocation patterns, and (2) add a Memory Write Governance section.

Output: 7 fully authored .claude/rules/ files ready for @-import by skill files.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-specialist-cli-skill-files/49-CONTEXT.md
@.planning/phases/49-specialist-cli-skill-files/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out 3 stub rules files (deliverability, onboarding, intelligence)</name>
  <files>
    .claude/rules/deliverability-rules.md
    .claude/rules/onboarding-rules.md
    .claude/rules/intelligence-rules.md
  </files>
  <action>
Replace each stub with comprehensive behavioral rules. Read the existing stub first, then overwrite with full content.

**deliverability-rules.md** — Author these sections based on the codebase's existing deliverability infrastructure (domain-health cron, EmailGuard, bounce monitoring, warmup auto-start):
- Purpose (expand from stub)
- Tools Available: table listing `node dist/cli/sender-health.js --slug {slug}`, `node dist/cli/domain-health.js --slug {slug}`, `node dist/cli/bounce-stats.js --slug {slug}`, `node dist/cli/inbox-status.js --slug {slug}` with descriptions
- Diagnostic Flow: step-by-step (1. start with sender-health + domain-health for full picture, 2. if bounce rate > 5% run bounce-stats for trend, 3. if blacklisted recommend immediate pause, 4. if SPF/DKIM/DMARC misconfigured provide exact DNS records needed)
- Warmup Strategy Rules: ramp-up guidance (start 5-10/day, increase 20% per week, pause if bounce > 3%, auto-start threshold), warmup duration expectations (3-4 weeks minimum)
- Sender Rotation: load balancing across inboxes, recovery protocol for flagged senders (pause, rotate, wait 7 days)
- Alert Interpretation: how to read domain-health output (SPF pass/fail, DKIM alignment, DMARC policy), what each blacklist means
- Memory Write Governance: may write to learnings.md (deliverability patterns), must NOT write to profile.md or campaigns.md. Append format with ISO timestamp.
- Voice: direct, technical, actionable. This agent should feel like a deliverability specialist, not a generic advisor.

**onboarding-rules.md** — Author these sections based on the codebase's onboarding workflow:
- Purpose (expand from stub)
- Tools Available: `node dist/cli/workspace-create.js`, `node dist/cli/member-invite.js`, `node dist/cli/workspace-get.js --slug {slug}`, `node dist/cli/workspace-package-update.js --slug {slug} --file /tmp/{uuid}.json`
- Onboarding Workflow: step-by-step checklist (1. create workspace with name/slug/vertical, 2. configure package modules, 3. guide DNS setup — SPF/DKIM/DMARC records for sending domains, 4. inbox provisioning — create inboxes in EmailBison, 5. member invites, 6. initial campaign scaffolding — create first campaign entity)
- DNS Configuration Guide: exact SPF/DKIM/DMARC record formats the client needs to add, where to check propagation
- Pre-Flight Checklist: what must be verified before first campaign sends (DNS propagated, inboxes warmed 3-4 weeks, ICP configured, at least one campaign in draft)
- Memory Write Governance: may write to learnings.md (onboarding observations), feedback.md (client preferences noted during setup), must NOT write to profile.md or campaigns.md
- Voice: helpful, step-by-step, patient. New clients need guidance, not jargon.

**intelligence-rules.md** — Author these sections based on the codebase's analytics and intelligence features:
- Purpose (expand from stub)
- Tools Available: `node dist/cli/cached-metrics.js --slug {slug}`, `node dist/cli/insight-list.js --slug {slug}`, `node dist/cli/workspace-intelligence.js --slug {slug}`, `node dist/cli/campaigns-get.js --slug {slug}`. Also loads `cat .nova/memory/global-insights.md` (intelligence-only)
- Analysis Methodology: how to structure a workspace performance review (1. pull cached-metrics for reply/bounce/open rates, 2. pull insight-list for generated insights, 3. compare against cross-client benchmarks from global-insights.md, 4. identify top/bottom performing campaigns, 5. formulate actionable recommendations)
- Benchmarking Rules: how to compare workspaces fairly (same vertical, similar send volume, same channel type), what metrics matter most (reply rate > open rate > click rate), industry benchmarks (cold email: 1-3% reply good, 5%+ excellent; LinkedIn: 15-25% accept, 5-10% reply)
- Insight Generation: what constitutes an actionable insight (specific, measurable, has a recommended action), example format: "Rise reply rate dropped from 4.2% to 2.1% over 2 weeks — likely cause: subject line fatigue. Recommend: A/B test new subject lines."
- Optimization Recommendations: copy angle analysis (which value props get replies), send time analysis (if data available), sequence length analysis (drop-off per step), channel comparison (email vs LinkedIn for this ICP)
- Memory Write Governance: may write to learnings.md (analytics patterns), global-insights.md (cross-client patterns). Must NOT write to profile.md, campaigns.md, or feedback.md. Append format with ISO timestamp.
- Voice: analytical, data-driven, specific. Numbers over opinions.
  </action>
  <verify>
    <automated>wc -l .claude/rules/deliverability-rules.md .claude/rules/onboarding-rules.md .claude/rules/intelligence-rules.md | tail -1 && grep -c "## Memory Write Governance" .claude/rules/deliverability-rules.md .claude/rules/onboarding-rules.md .claude/rules/intelligence-rules.md && grep -c "node dist/cli/" .claude/rules/deliverability-rules.md .claude/rules/onboarding-rules.md .claude/rules/intelligence-rules.md</automated>
  </verify>
  <done>All 3 rules files are comprehensive (50+ lines each), contain Memory Write Governance sections, and reference dist/cli/ tool paths. No stubs remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update 4 existing rules files with CLI references and memory governance</name>
  <files>
    .claude/rules/writer-rules.md
    .claude/rules/research-rules.md
    .claude/rules/leads-rules.md
    .claude/rules/campaign-rules.md
  </files>
  <action>
For each of the 4 existing rules files, make two targeted additions:

**1. Replace camelCase tool function references with CLI commands.**

Read each file and find all occurrences of camelCase tool names. Replace them with the corresponding CLI invocation:

writer-rules.md replacements:
- `getWorkspaceIntelligence` → `node dist/cli/workspace-intelligence.js --slug {slug}`
- `searchKnowledgeBase(query=..., tags=..., limit=...)` → `node dist/cli/kb-search.js --query "..." --tags "..." --limit N`
- `getCampaignPerformance` → `node dist/cli/campaign-performance.js --slug {slug}`
- `getSequenceSteps` → `node dist/cli/sequence-steps.js --campaignId {id}`
- `getExistingDrafts` → `node dist/cli/existing-drafts.js --slug {slug}`
- `getCampaignContext` → `node dist/cli/campaign-context.js --campaignId {id}`
- `saveDraft` → `node dist/cli/save-draft.js --file /tmp/{uuid}.json`
- `saveCampaignSequence` → `node dist/cli/save-sequence.js --file /tmp/{uuid}.json`
- `generateKBExamples` → remove or replace with manual instructions (no CLI wrapper for this)

research-rules.md replacements:
- `saveWebsiteAnalysis` → `node dist/cli/website-analysis-save.js --file /tmp/{uuid}.json`
- `updateWorkspaceICP` → `node dist/cli/workspace-icp-update.js --slug {slug} --file /tmp/{uuid}.json`
- `searchKnowledgeBase` → `node dist/cli/kb-search.js --query "..." --tags "..." --limit N`
- Add tool references for `node dist/cli/website-crawl.js --url {url}`, `node dist/cli/url-scrape.js --url {url}`, `node dist/cli/workspace-get.js --slug {slug}`

leads-rules.md replacements:
- `buildDiscoveryPlan` → `node dist/cli/discovery-plan.js --file /tmp/{uuid}.json`
- `searchApollo` → `node dist/cli/search-apollo.js --file /tmp/{uuid}.json`
- `searchProspeo` → `node dist/cli/search-prospeo.js --file /tmp/{uuid}.json`
- `searchAiArk` → `node dist/cli/search-aiark.js --file /tmp/{uuid}.json`
- `searchLeadsFinder` → `node dist/cli/search-leads-finder.js --file /tmp/{uuid}.json`
- `searchGoogle` → `node dist/cli/search-google.js --file /tmp/{uuid}.json`
- `searchGoogleMaps` → `node dist/cli/search-google-maps.js --file /tmp/{uuid}.json`
- `searchEcommerceStores` → `node dist/cli/search-ecommerce.js --file /tmp/{uuid}.json`
- `extractDirectory` → `node dist/cli/extract-directory.js --url {url}`
- `deduplicateAndPromote` → `node dist/cli/discovery-promote.js --file /tmp/{uuid}.json`
- `findTargetList` → `node dist/cli/target-list-find.js --slug {slug} --name "{name}"`

campaign-rules.md replacements:
- `createCampaign` → `node dist/cli/campaign-create.js --file /tmp/{uuid}.json`
- `getCampaigns` → `node dist/cli/campaigns-get.js --slug {slug}`
- `findTargetList` → `node dist/cli/target-list-find.js --slug {slug} --name "{name}"`
- `updateCampaignStatus` → `node dist/cli/campaign-status.js --campaignId {id} --status {s}`
- `publishForReview` → `node dist/cli/campaign-publish.js --campaignId {id}`
- `createSignalCampaign` → `node dist/cli/signal-campaign-create.js --file /tmp/{uuid}.json`
- `activateSignalCampaign` → `node dist/cli/signal-campaign-activate.js --campaignId {id}`
- `pauseResumeSignalCampaign` → `node dist/cli/signal-campaign-pause.js --campaignId {id} --action pause|resume`
- `updateWorkspacePackage` → `node dist/cli/workspace-package-update.js --slug {slug} --file /tmp/{uuid}.json`

**IMPORTANT:** Only replace tool REFERENCES (in prose instructions). Do NOT change the structure, ordering, or behavioral rules themselves. These are additive edits, not rewrites.

**2. Add Memory Write Governance section to each file.**

Append a `## Memory Write Governance` section at the end of each file:

writer-rules.md governance:
- May write to: campaigns.md (copy wins/losses, strategy effectiveness), feedback.md (client tone preferences, approval patterns), learnings.md (ICP messaging insights)
- Must NOT write to: profile.md (seed-only)

research-rules.md governance:
- May write to: learnings.md (ICP discoveries, website analysis insights, vertical patterns)
- Must NOT write to: profile.md, campaigns.md, feedback.md

leads-rules.md governance:
- May write to: learnings.md (lead source quality, ICP refinements, discovery patterns), feedback.md (client list preferences)
- Must NOT write to: profile.md, campaigns.md

campaign-rules.md governance:
- May write to: campaigns.md (campaign performance notes), feedback.md (client approval patterns), learnings.md (campaign structure insights)
- Must NOT write to: profile.md

Each governance block follows the append format: `[ISO-DATE] — {concise insight}`. Only append if insight is actionable for future sessions.
  </action>
  <verify>
    <automated>grep -c "## Memory Write Governance" .claude/rules/writer-rules.md .claude/rules/research-rules.md .claude/rules/leads-rules.md .claude/rules/campaign-rules.md && grep -c "node dist/cli/" .claude/rules/writer-rules.md .claude/rules/research-rules.md .claude/rules/leads-rules.md .claude/rules/campaign-rules.md</automated>
  </verify>
  <done>All 4 existing rules files have Memory Write Governance sections appended and all camelCase tool function references replaced with dist/cli/ CLI invocations. No camelCase tool names remain in prose instructions.</done>
</task>

</tasks>

<verification>
1. All 7 rules files exist and are non-stubs (50+ lines each for the 3 new ones)
2. Every rules file contains `## Memory Write Governance`
3. Every rules file references `node dist/cli/` for tool invocations
4. No camelCase tool function names remain in prose instructions (getWorkspaceIntelligence, searchKnowledgeBase, etc.)
5. Existing behavioral rules in the 4 updated files are preserved — only CLI refs and governance blocks were added
</verification>

<success_criteria>
- 3 stub rules files (deliverability, onboarding, intelligence) are fully authored with comprehensive behavioral rules
- 4 existing rules files have CLI tool references and memory governance blocks
- All 7 files are ready for @-import by skill files in Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/49-specialist-cli-skill-files/49-01-SUMMARY.md`
</output>
