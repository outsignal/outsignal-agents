---
phase: 49-specialist-cli-skill-files
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/nova-writer.md
  - .claude/commands/nova-research.md
  - .claude/commands/nova-leads.md
  - .claude/commands/nova-campaign.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03, SKL-04, SKL-09]

must_haves:
  truths:
    - "Each skill file injects all 4 workspace memory files via ! cat before the agent's first turn"
    - "Each skill file contains a tool table referencing node dist/cli/ compiled scripts"
    - "Each skill file references its rules file via @.claude/rules/ syntax"
    - "Each skill file includes a Memory Write-Back reminder section"
    - "No skill file exceeds 200 lines"
  artifacts:
    - path: ".claude/commands/nova-writer.md"
      provides: "Writer agent skill file"
      contains: "nova-writer"
    - path: ".claude/commands/nova-research.md"
      provides: "Research agent skill file"
      contains: "nova-research"
    - path: ".claude/commands/nova-leads.md"
      provides: "Leads agent skill file"
      contains: "nova-leads"
    - path: ".claude/commands/nova-campaign.md"
      provides: "Campaign agent skill file"
      contains: "nova-campaign"
  key_links:
    - from: ".claude/commands/nova-writer.md"
      to: ".claude/rules/writer-rules.md"
      via: "@ file reference"
      pattern: "@.claude/rules/writer-rules.md"
    - from: ".claude/commands/nova-*.md"
      to: ".nova/memory/{slug}/"
      via: "! cat shell injection"
      pattern: "cat .nova/memory/"
    - from: ".claude/commands/nova-*.md"
      to: "dist/cli/*.js"
      via: "tool table references"
      pattern: "node dist/cli/"
---

<objective>
Create 4 specialist skill files for agents with existing full rules files: writer, research, leads, campaign.

Purpose: These skill files are Claude Code commands that give each specialist agent its identity, tool table, workspace memory injection, and rules reference. Each follows the canonical pattern from the research (identity + tools + memory + rules + write-back) within 200 lines.

Output: 4 new .claude/commands/nova-{specialist}.md files loadable by Claude Code.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-specialist-cli-skill-files/49-CONTEXT.md
@.planning/phases/49-specialist-cli-skill-files/49-RESEARCH.md
</context>

<interfaces>
<!-- Canonical skill file structure from research. Every skill follows this template order. -->

```markdown
---
name: nova-{specialist}
description: {role description — when to invoke}
---

# Nova {Specialist} — {subtitle}

## Role
{1-2 sentence identity}

## Workspace Context
!`cat .nova/memory/$ARGUMENTS[0]/profile.md .nova/memory/$ARGUMENTS[0]/campaigns.md .nova/memory/$ARGUMENTS[0]/feedback.md .nova/memory/$ARGUMENTS[0]/learnings.md 2>/dev/null || echo "(No memory files found — workspace may not be seeded)"`

## Tools
All tools run from /Users/jjay/programs/outsignal-agents.

| Tool | Command | Purpose |
|------|---------|---------|
| ... | `node dist/cli/X.js --args` | ... |

## Rules
@.claude/rules/{specialist}-rules.md

## Memory Write-Back
{Which memory files this agent may append to + format}

$ARGUMENTS
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create nova-writer.md and nova-research.md skill files</name>
  <files>
    .claude/commands/nova-writer.md
    .claude/commands/nova-research.md
  </files>
  <action>
Create both skill files following the canonical pattern.

**nova-writer.md:**
- Frontmatter: name `nova-writer`, description: "Outsignal copywriter. Writes cold email and LinkedIn sequences for client campaigns. Use when generating outreach copy, revising drafts, or suggesting replies."
- Role: "You are Nova Writer, an expert cold outreach copywriter for Outsignal clients. You write email and LinkedIn sequences that get replies, following client-specific tone, ICP, and historical feedback."
- Workspace Context: `! cat` injection of all 4 memory files using `$ARGUMENTS[0]` as slug, with `2>/dev/null || echo "(No memory files found)"` guard
- Tools table (8 tools from research): workspace-intelligence.js, campaign-performance.js, sequence-steps.js, existing-drafts.js, campaign-context.js, kb-search.js, save-sequence.js, save-draft.js. Include args patterns from the research tool tables.
- Rules: `@.claude/rules/writer-rules.md`
- Memory Write-Back: copy wins/losses → campaigns.md, observed preferences → feedback.md, ICP/targeting insights → learnings.md
- End with `$ARGUMENTS`
- MUST be under 200 lines. The writer-rules.md handles all behavioral rules — the skill file is identity + tools + memory only.

**nova-research.md:**
- Frontmatter: name `nova-research`, description: "Outsignal business intelligence analyst. Crawls and analyzes client websites to extract ICP, value props, case studies, and competitive insights."
- Role: "You are Nova Research, a business intelligence analyst for Outsignal. You crawl client websites and extract actionable data for cold outbound campaigns."
- Workspace Context: same `! cat` injection pattern
- Tools table (5 tools): website-crawl.js, url-scrape.js, workspace-get.js, website-analysis-save.js, workspace-icp-update.js. Include args patterns.
- Rules: `@.claude/rules/research-rules.md`
- Memory Write-Back: ICP discoveries and website insights → learnings.md only
- End with `$ARGUMENTS`
- MUST be under 200 lines.
  </action>
  <verify>
    <automated>wc -l .claude/commands/nova-writer.md .claude/commands/nova-research.md && grep -c 'cat .nova/memory' .claude/commands/nova-writer.md .claude/commands/nova-research.md && grep -c '@.claude/rules/' .claude/commands/nova-writer.md .claude/commands/nova-research.md</automated>
  </verify>
  <done>Both files exist, are under 200 lines, inject memory via ! cat, reference their rules file via @, and contain tool tables with dist/cli/ paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create nova-leads.md and nova-campaign.md skill files</name>
  <files>
    .claude/commands/nova-leads.md
    .claude/commands/nova-campaign.md
  </files>
  <action>
Create both skill files following the canonical pattern.

**nova-leads.md:**
- Frontmatter: name `nova-leads`, description: "Outsignal lead discovery agent. Finds, scores, and manages target lists of prospects. Use when discovering leads, building lists, scoring against ICP, or exporting to EmailBison."
- Role: "You are Nova Leads, a lead discovery and list management specialist for Outsignal. You find prospects, build target lists, score against ICP criteria, and manage the lead pipeline."
- Workspace Context: same `! cat` injection pattern
- Tools table (22 tools from research): people-search.js, people-query.js, list-create.js, list-add-people.js, list-get.js, list-get-all.js, list-score.js, list-export.js, discovery-plan.js, discovery-promote.js, search-apollo.js, search-prospeo.js, search-aiark.js, search-leads-finder.js, search-google.js, search-google-maps.js, search-ecommerce.js, extract-directory.js, check-google-ads.js, check-tech-stack.js, target-list-find.js, kb-search.js. Include args patterns.
- The tool table will be long (22 rows). Keep it compact: one-line purpose descriptions. This is the agent most likely to approach the 200-line budget. If nearing the limit, abbreviate purpose descriptions to stay within budget.
- Rules: `@.claude/rules/leads-rules.md`
- Memory Write-Back: lead source quality and ICP refinements → learnings.md, client list preferences → feedback.md
- End with `$ARGUMENTS`

**nova-campaign.md:**
- Frontmatter: name `nova-campaign`, description: "Outsignal campaign manager. Creates campaigns, links target lists, manages campaign lifecycle, and publishes for client review."
- Role: "You are Nova Campaign, the campaign lifecycle manager for Outsignal. You create campaigns, link target lists, manage status transitions, and handle signal campaign workflows."
- Workspace Context: same `! cat` injection pattern
- Tools table (9 tools): campaign-create.js, campaign-get.js, campaign-list.js, target-list-find.js, campaign-status.js, campaign-publish.js, signal-campaign-create.js, signal-campaign-activate.js, signal-campaign-pause.js. Include args patterns.
- Rules: `@.claude/rules/campaign-rules.md`
- Memory Write-Back: campaign performance notes → campaigns.md, client approval patterns → feedback.md, campaign structure insights → learnings.md
- End with `$ARGUMENTS`
- Both files MUST be under 200 lines.
  </action>
  <verify>
    <automated>wc -l .claude/commands/nova-leads.md .claude/commands/nova-campaign.md && grep -c 'cat .nova/memory' .claude/commands/nova-leads.md .claude/commands/nova-campaign.md && grep -c '@.claude/rules/' .claude/commands/nova-leads.md .claude/commands/nova-campaign.md</automated>
  </verify>
  <done>Both files exist, are under 200 lines, inject memory via ! cat, reference their rules file via @, and contain tool tables with dist/cli/ paths. nova-leads.md covers all 22 tools in a compact table.</done>
</task>

</tasks>

<verification>
1. All 4 skill files exist in .claude/commands/
2. Each file has YAML frontmatter with name and description
3. Each file contains `! cat .nova/memory/$ARGUMENTS[0]/` for memory injection
4. Each file contains `@.claude/rules/{specialist}-rules.md`
5. Each file contains a tool table with `node dist/cli/` commands
6. Each file contains Memory Write-Back section
7. Each file ends with `$ARGUMENTS`
8. No file exceeds 200 lines: `wc -l .claude/commands/nova-*.md`
</verification>

<success_criteria>
- 4 specialist skill files created and loadable by Claude Code
- All follow the canonical template (identity + tools + memory + rules + write-back)
- All within 200-line budget
- All tool references use dist/cli/ paths (no npx tsx, no camelCase functions)
</success_criteria>

<output>
After completion, create `.planning/phases/49-specialist-cli-skill-files/49-02-SUMMARY.md`
</output>
