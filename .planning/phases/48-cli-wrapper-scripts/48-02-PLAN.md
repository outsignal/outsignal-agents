---
phase: 48-cli-wrapper-scripts
plan: "02"
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - scripts/cli/workspace-intelligence.ts
  - scripts/cli/campaign-performance.ts
  - scripts/cli/sequence-steps.ts
  - scripts/cli/kb-search.ts
  - scripts/cli/existing-drafts.ts
  - scripts/cli/campaign-context.ts
  - scripts/cli/save-sequence.ts
  - scripts/cli/save-draft.ts
  - scripts/cli/website-crawl.ts
  - scripts/cli/url-scrape.ts
  - scripts/cli/website-analysis-save.ts
  - scripts/cli/workspace-icp-update.ts
  - scripts/cli/campaign-create.ts
  - scripts/cli/campaign-get.ts
  - scripts/cli/campaign-list.ts
  - scripts/cli/target-list-find.ts
  - scripts/cli/campaign-status.ts
  - scripts/cli/campaign-publish.ts
  - scripts/cli/signal-campaign-create.ts
  - scripts/cli/signal-campaign-activate.ts
  - scripts/cli/signal-campaign-pause.ts
autonomous: true
requirements: [CLI-01]
must_haves:
  truths:
    - "All 8 writer domain scripts compile and return valid JSON when called with correct args"
    - "All 5 research domain scripts compile and return valid JSON when called with correct args"
    - "All 9 campaign domain scripts compile and return valid JSON when called with correct args"
    - "Scripts that accept JSON file input (save-sequence, save-draft, campaign-create, signal-campaign-create, website-analysis-save, workspace-icp-update) correctly parse the file"
    - "kb-search is a single shared script — not duplicated per agent"
  artifacts:
    - path: "scripts/cli/workspace-intelligence.ts"
      provides: "Writer tool: full workspace data for copy writing"
    - path: "scripts/cli/kb-search.ts"
      provides: "Shared tool: knowledge base search (used by writer, leads, orchestrator)"
    - path: "scripts/cli/campaign-list.ts"
      provides: "Campaign tool: list campaigns for workspace"
    - path: "scripts/cli/website-crawl.ts"
      provides: "Research tool: crawl website pages"
    - path: "dist/cli/"
      provides: "21 compiled CJS bundles for writer + research + campaign domains"
  key_links:
    - from: "scripts/cli/*.ts"
      to: "scripts/cli/_cli-harness.ts"
      via: "import { runWithHarness }"
      pattern: "runWithHarness"
    - from: "scripts/cli/save-sequence.ts"
      to: "src/lib/agents/writer.ts"
      via: "import tool execute function"
      pattern: "execute"
---

<objective>
Create all wrapper scripts for the Writer (8), Research (5), and Campaign (9) agent domains — 21 scripts total (workspace-get already exists from Plan 01, kb-search is shared).

Purpose: These three domains cover the core campaign creation workflow (research -> write -> campaign). Leads and orchestrator follow in Plan 03.

Output: 21 new scripts in scripts/cli/, all compiled to dist/cli/, each independently testable.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-cli-wrapper-scripts/48-CONTEXT.md
@.planning/phases/48-cli-wrapper-scripts/48-RESEARCH.md
@.planning/phases/48-cli-wrapper-scripts/48-01-SUMMARY.md

<interfaces>
<!-- Executor must read these source files to find the exact tool execute() signatures -->

Writer tools (src/lib/agents/writer.ts):
- writerTools.getWorkspaceIntelligence.execute({ slug }) -> workspace + websiteAnalysis
- writerTools.getCampaignPerformance.execute({ workspaceSlug }) -> campaign metrics
- writerTools.getSequenceSteps.execute({ workspaceSlug, campaignId }) -> sequence steps
- writerTools.getExistingDrafts.execute({ workspaceSlug, campaignName? }) -> drafts
- writerTools.getCampaignContext.execute({ campaignId }) -> campaign + sequences + targetList
- writerTools.saveCampaignSequence.execute({ campaignId, sequences }) -> saved sequences
- writerTools.saveDraft.execute({ workspaceSlug, campaignName, sequences, notes? }) -> saved draft

Research tools (src/lib/agents/research.ts):
- Look for tool definitions: crawlWebsite, scrapeUrl, saveWebsiteAnalysis, updateWorkspaceICP
- workspace-get already created in Plan 01

Campaign tools (src/lib/agents/campaign.ts):
- Look for: createCampaign, getCampaign, listCampaigns, findTargetList, updateCampaignStatus, publishForReview, createSignalCampaign, activateSignalCampaign, pauseResumeSignalCampaign

Shared tool (src/lib/agents/shared-tools.ts):
- searchKnowledgeBase.execute({ query, tags?, limit? })

From Plan 01 (cli-harness pattern):
```typescript
import { config } from "dotenv";
config({ path: ".env" }); config({ path: ".env.local" });
import { runWithHarness } from "./_cli-harness";
// import tool function
const [,, arg1, arg2] = process.argv;
runWithHarness("script-name <arg1> [arg2]", async () => {
  if (!arg1) throw new Error("Missing required argument: arg1");
  return toolFunction.execute({ arg1 });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Writer + Research + Campaign wrapper scripts</name>
  <files>
    scripts/cli/workspace-intelligence.ts
    scripts/cli/campaign-performance.ts
    scripts/cli/sequence-steps.ts
    scripts/cli/kb-search.ts
    scripts/cli/existing-drafts.ts
    scripts/cli/campaign-context.ts
    scripts/cli/save-sequence.ts
    scripts/cli/save-draft.ts
    scripts/cli/website-crawl.ts
    scripts/cli/url-scrape.ts
    scripts/cli/website-analysis-save.ts
    scripts/cli/workspace-icp-update.ts
    scripts/cli/campaign-create.ts
    scripts/cli/campaign-get.ts
    scripts/cli/campaign-list.ts
    scripts/cli/target-list-find.ts
    scripts/cli/campaign-status.ts
    scripts/cli/campaign-publish.ts
    scripts/cli/signal-campaign-create.ts
    scripts/cli/signal-campaign-activate.ts
    scripts/cli/signal-campaign-pause.ts
  </files>
  <action>
IMPORTANT: Before writing ANY script, read the source agent file to find the exact execute() function signature and how it's exported. The tool functions are NOT standalone exports -- they are properties of objects or module-level constants. You must trace the import path.

For each script, follow the exact pattern from Plan 01's workspace-get (dotenv at top, runWithHarness, positional args, throw on missing required args).

**Critical patterns by input type:**

1. Simple slug/ID scripts (most scripts): `const [,, slug] = process.argv;`
   - workspace-intelligence, campaign-performance, campaign-get, campaign-list, campaign-publish, campaign-context, signal-campaign-activate, target-list-find, url-scrape

2. Multi-arg scripts: `const [,, arg1, arg2] = process.argv;`
   - sequence-steps: `<workspaceSlug> <campaignId>` (parseInt campaignId)
   - existing-drafts: `<workspaceSlug> [campaignName]` (campaignName optional)
   - campaign-status: `<campaignId> <newStatus>`
   - signal-campaign-pause: `<campaignId> <pause|resume>`
   - kb-search: `<query> [tags] [limit]` (limit default 10, parseInt)
   - website-crawl: `<url> [maxPages]` (maxPages optional, parseInt, default 5)

3. JSON-file input scripts (for complex object inputs):
   - save-sequence: `<campaignId> <jsonFile>` — read file, parse JSON, pass as sequences arg
   - save-draft: `<workspaceSlug> <jsonFile>` — read file, parse JSON for { campaignName, sequences, notes? }
   - campaign-create: `<workspaceSlug> <jsonFile>` — read file, parse JSON for campaign params
   - signal-campaign-create: `<workspaceSlug> <jsonFile>` — read file, parse JSON
   - website-analysis-save: `<workspaceSlug> <jsonFile>` — read file, parse JSON
   - workspace-icp-update: `<slug> <jsonFile>` — read file, parse JSON
   - Use `import { readFileSync } from "fs"` and `JSON.parse(readFileSync(jsonFile, "utf8"))`

**Tool function import strategy:**
The agent files define tools inside objects that are NOT exported. You need to either:
(a) Extract the execute() logic into the wrapper directly (replicate the Prisma queries), OR
(b) Refactor the agent files to export the tool objects — but this risks breaking existing code.

RECOMMENDED: Option (a) — replicate the database/API calls directly in each wrapper. The tool execute() functions are typically 5-20 lines of Prisma queries or API client calls. This keeps wrappers self-contained and avoids modifying agent files. Read each tool's execute() body and replicate it.

For tools that call EmailBison API (campaign-performance, campaign-list, campaign-create, etc.), import `getClientForWorkspace` from `@/lib/workspaces`.

**Default limits for large-result scripts:**
- kb-search: default limit 10
- campaign-list: return all (typically <50 per workspace)
- existing-drafts: return all

**kb-search is shared:** One script used by writer, leads, and orchestrator agents. Import `searchKnowledgeBase` from `@/lib/agents/shared-tools` (it IS exported).
  </action>
  <verify>
    npm run build:cli 2>&1 | tail -3
    # Must compile all scripts without errors
    # Then spot-check 3 scripts:
    node dist/cli/kb-search.js "cold email" 2>&1 | head -3
    node dist/cli/campaign-list.js rise 2>&1 | head -3
    node dist/cli/workspace-intelligence.js rise 2>&1 | head -3
  </verify>
  <done>21 wrapper scripts created in scripts/cli/, all compile to dist/cli/, spot-checked scripts return valid JSON envelopes with real data</done>
</task>

</tasks>

<verification>
1. `npm run build:cli` exits 0 — all 22 scripts (21 new + workspace-get from Plan 01) compile
2. `ls dist/cli/*.js | wc -l` shows 22 files
3. `node dist/cli/kb-search.js "cold email best practices"` returns `{ "ok": true, "data": [...] }`
4. `node dist/cli/campaign-list.js rise` returns `{ "ok": true, "data": [...] }`
5. `node dist/cli/save-sequence.js` returns `{ "ok": false, "error": "Missing required argument: campaignId" }`
6. `node dist/cli/website-crawl.js https://outsignal.ai` returns `{ "ok": true, "data": {...} }`
</verification>

<success_criteria>
- All 21 writer/research/campaign scripts exist in scripts/cli/
- All compile to dist/cli/ via npm run build:cli
- Each returns wrapped JSON envelope (ok:true with data, or ok:false with error+usage)
- JSON-file input scripts correctly parse file contents
- sanitize-output applied to all output via cli-harness
</success_criteria>

<output>
After completion, create `.planning/phases/48-cli-wrapper-scripts/48-02-SUMMARY.md`
</output>
