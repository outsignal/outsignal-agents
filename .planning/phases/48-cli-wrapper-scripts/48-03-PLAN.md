---
phase: 48-cli-wrapper-scripts
plan: "03"
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - scripts/cli/people-search.ts
  - scripts/cli/list-create.ts
  - scripts/cli/list-add-people.ts
  - scripts/cli/list-get.ts
  - scripts/cli/list-get-all.ts
  - scripts/cli/list-score.ts
  - scripts/cli/list-export.ts
  - scripts/cli/discovery-plan.ts
  - scripts/cli/discovery-promote.ts
  - scripts/cli/search-apollo.ts
  - scripts/cli/search-prospeo.ts
  - scripts/cli/search-aiark.ts
  - scripts/cli/search-leads-finder.ts
  - scripts/cli/search-google.ts
  - scripts/cli/extract-directory.ts
  - scripts/cli/check-google-ads.ts
  - scripts/cli/check-tech-stack.ts
  - scripts/cli/search-google-maps.ts
  - scripts/cli/search-ecommerce.ts
  - scripts/cli/workspace-list.ts
  - scripts/cli/workspace-package-update.ts
  - scripts/cli/campaigns-get.ts
  - scripts/cli/replies-get.ts
  - scripts/cli/sender-health.ts
  - scripts/cli/people-query.ts
  - scripts/cli/proposal-list.ts
  - scripts/cli/proposal-create.ts
  - scripts/cli/domain-health.ts
  - scripts/cli/bounce-stats.ts
  - scripts/cli/inbox-status.ts
  - scripts/cli/notification-health.ts
  - scripts/cli/insight-list.ts
  - scripts/cli/cached-metrics.ts
autonomous: true
requirements: [CLI-01, CLI-04]
must_haves:
  truths:
    - "All 18 leads domain scripts compile and return valid JSON when called with correct args"
    - "All 9 orchestrator domain scripts compile and return valid JSON when called with correct args"
    - "All 6 deliverability/intelligence domain scripts compile and return valid JSON when called with correct args"
    - "people-search defaults to 50 results, overridable via second arg"
    - "discovery-promote accepts variadic runIds (3+ positional args)"
    - "All ~55 scripts across the phase compile in one npm run build:cli pass"
  artifacts:
    - path: "scripts/cli/people-search.ts"
      provides: "Leads tool: search people with default limit 50"
    - path: "scripts/cli/workspace-list.ts"
      provides: "Orchestrator tool: list all workspaces"
    - path: "scripts/cli/sender-health.ts"
      provides: "Orchestrator tool: inbox health per workspace"
    - path: "scripts/cli/domain-health.ts"
      provides: "Deliverability tool: domain rollup health data"
    - path: "scripts/cli/bounce-stats.ts"
      provides: "Deliverability tool: sender bounce statistics"
    - path: "dist/cli/"
      provides: "All ~55 compiled CJS bundles for all 7 agent domains"
  key_links:
    - from: "scripts/cli/people-search.ts"
      to: "@/lib/db (prisma)"
      via: "Prisma people query with limit"
      pattern: "prisma\\.lead\\.findMany|prisma\\.person"
    - from: "scripts/cli/domain-health.ts"
      to: "src/lib/domain-health/snapshots.ts"
      via: "import computeDomainRollup"
      pattern: "computeDomainRollup"
    - from: "scripts/cli/sender-health.ts"
      to: "src/lib/agents/orchestrator.ts"
      via: "replicate getSenderHealth execute() logic"
      pattern: "sender.*health"
---

<objective>
Create all wrapper scripts for the Leads (18), Orchestrator (9), and Deliverability/Intelligence (6) agent domains — 33 scripts total.

Purpose: Complete the CLI wrapper script inventory. After this plan, every tool function across all 7 agent domains has a callable CLI script.

Output: 33 new scripts in scripts/cli/, all compiled to dist/cli/, full inventory testable.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-cli-wrapper-scripts/48-CONTEXT.md
@.planning/phases/48-cli-wrapper-scripts/48-RESEARCH.md
@.planning/phases/48-cli-wrapper-scripts/48-01-SUMMARY.md

<interfaces>
<!-- Executor must read these source files to find the exact execute() function signatures -->

Leads tools (src/lib/agents/leads.ts) — 18 tools:
- searchPeople: execute({ query?, status?, limit? }) — DEFAULT LIMIT 50
- createList: execute({ workspaceSlug, name })
- addPeopleToList: execute({ listId, personIds }) — jsonFile input
- getList: execute({ listId })
- getLists: execute({ workspaceSlug? })
- scoreList: execute({ listId, workspaceSlug })
- exportListToEmailBison: execute({ listId, workspaceSlug })
- buildDiscoveryPlan: execute({ workspaceSlug, sources, filters }) — jsonFile input
- deduplicateAndPromote: execute({ workspaceSlug, runIds }) — variadic runIds
- searchApollo/Prospeo/AiArk/LeadsFinder/Google/GoogleMaps/EcommerceStores: execute({ workspaceSlug, ...filters }) — jsonFile input
- extractDirectory: execute({ workspaceSlug, url })
- checkGoogleAds: execute({ workspaceSlug, domains }) — jsonFile input
- checkTechStack: execute({ workspaceSlug, domain })

Orchestrator tools (src/lib/agents/orchestrator.ts) — 9 data tools (skip delegation tools):
- listWorkspaces: execute({}) — no args
- getWorkspaceInfo: execute({ slug }) — already covered by workspace-get in Plan 01, but orchestrator version is richer
- updateWorkspacePackage: execute({ workspaceSlug, ...fields }) — jsonFile input
- getCampaigns: execute({ workspaceSlug }) — NOTE: different from campaign-list (this is orchestrator's version)
- getReplies: execute({ workspaceSlug, limit? })
- getSenderHealth: execute({ workspaceSlug })
- queryPeople: execute({ workspaceSlug?, status?, limit? })
- listProposals: execute({ status? })
- createProposal: execute({ ...proposalFields }) — jsonFile input

Deliverability/Intelligence (library functions, not AI SDK tools):
- domain-health: computeDomainRollup from src/lib/domain-health/snapshots.ts
- bounce-stats: evaluateSender from src/lib/domain-health/bounce-monitor.ts
- inbox-status: checkAllWorkspaces from src/lib/inbox-health/monitor.ts (or equivalent)
- notification-health: Direct Prisma query (check src/app/api/notification-health/route.ts for the query)
- insight-list: generateInsights from src/lib/insights/generate.ts (confirm exists)
- cached-metrics: snapshotWorkspaceCampaigns from src/lib/analytics/snapshot.ts (confirm exists)

Harness pattern (from Plan 01):
```typescript
import { config } from "dotenv";
config({ path: ".env" }); config({ path: ".env.local" });
import { runWithHarness } from "./_cli-harness";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Leads domain wrapper scripts (18 scripts)</name>
  <files>
    scripts/cli/people-search.ts
    scripts/cli/list-create.ts
    scripts/cli/list-add-people.ts
    scripts/cli/list-get.ts
    scripts/cli/list-get-all.ts
    scripts/cli/list-score.ts
    scripts/cli/list-export.ts
    scripts/cli/discovery-plan.ts
    scripts/cli/discovery-promote.ts
    scripts/cli/search-apollo.ts
    scripts/cli/search-prospeo.ts
    scripts/cli/search-aiark.ts
    scripts/cli/search-leads-finder.ts
    scripts/cli/search-google.ts
    scripts/cli/extract-directory.ts
    scripts/cli/check-google-ads.ts
    scripts/cli/check-tech-stack.ts
    scripts/cli/search-google-maps.ts
    scripts/cli/search-ecommerce.ts
  </files>
  <action>
Read `src/lib/agents/leads.ts` to find all 18 tool execute() functions. Replicate each tool's database/API logic in its wrapper.

**Input patterns:**

1. Simple single-arg scripts:
   - list-get: `<listId>` (parseInt)
   - list-get-all: `[workspaceSlug]` (optional)
   - extract-directory: `<workspaceSlug> <url>`
   - check-tech-stack: `<workspaceSlug> <domain>`

2. Multi-arg scripts:
   - people-search: `[query] [limit]` — limit defaults to 50, parseInt. Query can be empty string for "all".
   - list-create: `<workspaceSlug> <name>`
   - list-score: `<listId> <workspaceSlug>`
   - list-export: `<listId> <workspaceSlug>`
   - search-google: `<workspaceSlug> <query> [mode]` — mode defaults to "web"

3. Variadic args:
   - discovery-promote: `<workspaceSlug> <runId1> [runId2] [runId3...]` — collect all args from index 4+ as runIds

4. JSON-file input (complex filter objects):
   - list-add-people: `<listId> <jsonFile>` — expects { personIds: number[] }
   - discovery-plan: `<workspaceSlug> <jsonFile>` — expects { sources, filters, ... }
   - search-apollo: `<workspaceSlug> <jsonFile>` — expects search filters
   - search-prospeo: `<workspaceSlug> <jsonFile>` — expects search filters
   - search-aiark: `<workspaceSlug> <jsonFile>` — expects search filters
   - search-leads-finder: `<workspaceSlug> <jsonFile>` — expects search filters
   - check-google-ads: `<workspaceSlug> <jsonFile>` — expects { domains: string[] }
   - search-google-maps: `<workspaceSlug> <jsonFile>` — expects search filters
   - search-ecommerce: `<workspaceSlug> <jsonFile>` — expects search filters
   - Use `import { readFileSync } from "fs"` and `JSON.parse(readFileSync(jsonFile, "utf8"))`

**For discovery-promote variadic pattern:**
```typescript
const [,, workspaceSlug, ...runIdStrs] = process.argv;
const runIds = runIdStrs.map(id => parseInt(id, 10));
```

**people-search default limit:** Must cap at 50 by default to prevent context overflow in agent sessions. Allow override via second arg.
  </action>
  <verify>
    npm run build:cli 2>&1 | tail -3
    node dist/cli/people-search.js "" 5 2>&1 | head -5
    node dist/cli/list-get-all.js rise 2>&1 | head -5
  </verify>
  <done>18 leads domain scripts created, all compile, people-search respects default limit, JSON-file scripts parse input correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create Orchestrator + Deliverability/Intelligence wrapper scripts (15 scripts)</name>
  <files>
    scripts/cli/workspace-list.ts
    scripts/cli/workspace-package-update.ts
    scripts/cli/campaigns-get.ts
    scripts/cli/replies-get.ts
    scripts/cli/sender-health.ts
    scripts/cli/people-query.ts
    scripts/cli/proposal-list.ts
    scripts/cli/proposal-create.ts
    scripts/cli/domain-health.ts
    scripts/cli/bounce-stats.ts
    scripts/cli/inbox-status.ts
    scripts/cli/notification-health.ts
    scripts/cli/insight-list.ts
    scripts/cli/cached-metrics.ts
  </files>
  <action>
**Orchestrator scripts (8 new — workspace-get already exists from Plan 01):**

Read `src/lib/agents/orchestrator.ts` to find all tool execute() functions. Skip the 4 delegation tools (delegateToResearch/Leads/Writer/Campaign) — those are agent orchestration, not data tools.

Input patterns:
- workspace-list: no args
- workspace-package-update: `<workspaceSlug> <jsonFile>`
- campaigns-get: `<workspaceSlug>` — NOTE: name is campaigns-get (not campaign-list) to avoid conflict with campaign agent's campaign-list
- replies-get: `<workspaceSlug> [limit]` — limit default 20
- sender-health: `<workspaceSlug>`
- people-query: `[workspaceSlug] [status] [limit]` — all optional, limit default 50
- proposal-list: `[status]` — optional status filter
- proposal-create: `<jsonFile>` — expects proposal fields

**Deliverability/Intelligence scripts (6):**

These wrap library functions (not AI SDK tools). Read the source files to find the actual function signatures:

1. domain-health: `<workspaceSlug>`
   - Check `src/lib/domain-health/snapshots.ts` for `computeDomainRollup` or similar
   - If that function doesn't exist by name, check what the domain-health API route calls

2. bounce-stats: `<workspaceSlug>`
   - Check `src/lib/domain-health/bounce-monitor.ts` for `evaluateSender` or similar
   - May need to iterate over all senders for the workspace

3. inbox-status: `[workspaceSlug]`
   - Check `src/lib/inbox-health/monitor.ts` or `src/lib/inbox-health/` directory
   - If no standalone function exists, replicate the Prisma query from the relevant API route

4. notification-health: `[range]` — range defaults to "24h"
   - Check `src/app/api/notification-health/route.ts` for the query logic
   - Replicate the Prisma query directly

5. insight-list: `<workspaceSlug>`
   - Check `src/lib/insights/generate.ts` — if it doesn't exist, check src/lib/insights/ directory
   - If the module doesn't exist, create a stub that returns `{ ok: true, data: { message: "Insight generation not yet implemented" } }`

6. cached-metrics: `<workspaceSlug>`
   - Check `src/lib/analytics/snapshot.ts` for `snapshotWorkspaceCampaigns` or similar
   - If unclear, check admin dashboard pages that display cached metrics

**IMPORTANT:** For deliverability/intelligence scripts, if the backing function doesn't exist or has a different signature than expected in the research doc, adapt accordingly. These are library functions, not AI SDK tools — import them directly.

After creating all scripts, run `npm run build:cli` once to compile the full inventory.
  </action>
  <verify>
    npm run build:cli 2>&1 | tail -3
    node dist/cli/workspace-list.js 2>&1 | head -5
    node dist/cli/sender-health.js rise 2>&1 | head -5
    node dist/cli/domain-health.js rise 2>&1 | head -5
    ls dist/cli/*.js | wc -l
    # Should show ~55 total scripts
  </verify>
  <done>14 new scripts created (8 orchestrator + 6 deliverability/intelligence), all compile alongside existing scripts, full inventory of ~55 scripts verified in dist/cli/</done>
</task>

</tasks>

<verification>
1. `npm run build:cli` exits 0 — all ~55 scripts compile in one pass
2. `ls dist/cli/*.js | wc -l` shows approximately 55 files
3. Leads spot checks:
   - `node dist/cli/people-search.js "" 3` returns `{ "ok": true, "data": [...] }` with max 3 results
   - `node dist/cli/list-get-all.js` returns `{ "ok": true, "data": [...] }`
4. Orchestrator spot checks:
   - `node dist/cli/workspace-list.js` returns `{ "ok": true, "data": [...] }` with workspace list
   - `node dist/cli/sender-health.js rise` returns `{ "ok": true, "data": {...} }`
5. Deliverability spot checks:
   - `node dist/cli/domain-health.js rise` returns valid JSON envelope
   - `node dist/cli/notification-health.js` returns valid JSON envelope
6. Error cases: any script called with missing required args returns `{ "ok": false, "error": "Missing...", "usage": "..." }`
</verification>

<success_criteria>
- All 33 leads/orchestrator/deliverability/intelligence scripts exist in scripts/cli/
- Full inventory (~55 scripts) compiles via single npm run build:cli
- Each script returns wrapped JSON envelope with sanitized output
- Default limits prevent context overflow (people-search: 50, replies-get: 20, people-query: 50)
- Every agent domain has complete CLI coverage — no tool function left unwrapped
</success_criteria>

<output>
After completion, create `.planning/phases/48-cli-wrapper-scripts/48-03-SUMMARY.md`
</output>
