---
phase: 47-client-memory-namespace
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - .nova/ARCHITECTURE.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04, MEM-05, MEM-06]

must_haves:
  truths:
    - "All workspace directories exist under .nova/memory/ with 4 populated files each"
    - "Global-insights.md exists with seeded cross-client benchmarks"
    - ".nova/ARCHITECTURE.md Section 6 reflects the actual 4-file schema (profile, campaigns, feedback, learnings)"
    - "Re-running seed --all overwrites profile.md but preserves campaigns.md, feedback.md, learnings.md"
  artifacts:
    - path: ".nova/ARCHITECTURE.md"
      provides: "Updated memory namespace documentation"
      contains: "profile.md"
    - path: ".nova/memory/rise/profile.md"
      provides: "Seeded workspace memory for Rise"
      contains: "Branded Merchandise"
    - path: ".nova/memory/global-insights.md"
      provides: "Cross-client intelligence benchmarks"
      contains: "Cross-Client Benchmarks"
  key_links:
    - from: ".nova/ARCHITECTURE.md"
      to: ".nova/memory/{slug}/"
      via: "Section 6 documents the 4-file schema"
      pattern: "profile\\.md.*campaigns\\.md.*feedback\\.md.*learnings\\.md"
---

<objective>
Run the seed script against all workspaces, verify output correctness, validate re-run safety, and update ARCHITECTURE.md to reflect the actual 4-file memory schema.

Purpose: Confirm the memory infrastructure works end-to-end and document the finalized schema so future phases (48, 49) can reference it accurately.
Output: Seeded memory files for all workspaces, updated ARCHITECTURE.md
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-client-memory-namespace/47-CONTEXT.md
@.planning/phases/47-client-memory-namespace/47-01-SUMMARY.md
@.nova/ARCHITECTURE.md
@scripts/nova-memory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute seed script and verify all workspace memory</name>
  <files>.nova/memory/</files>
  <action>
1. Run `npx tsx scripts/nova-memory.ts seed --all` and capture output
2. Verify ALL workspace directories were created by listing `.nova/memory/*/`
3. For each workspace, verify:
   - profile.md exists and contains workspace name, vertical (or placeholder), governance header
   - campaigns.md exists with campaign table or empty table if no campaigns
   - feedback.md exists with scaffold and governance header
   - learnings.md exists with scaffold and governance header
   - .gitkeep exists in each slug directory
4. Verify global-insights.md exists at `.nova/memory/global-insights.md` with cross-client benchmarks section
5. Verify gitignore works: `git check-ignore .nova/memory/rise/profile.md` returns the path
6. Verify .gitkeep is NOT ignored: `git check-ignore .nova/memory/rise/.gitkeep` returns empty
7. Test re-run safety:
   - Append a test line to `.nova/memory/rise/campaigns.md`: `<!-- test: this line must survive re-seed -->`
   - Run `npx tsx scripts/nova-memory.ts seed rise`
   - Verify profile.md was overwritten (fresh timestamp in governance header)
   - Verify campaigns.md still contains the test line (was NOT overwritten)
   - Remove the test line after verification

If any verification fails, fix the issue in `scripts/nova-memory.ts` and re-run. Common issues to watch for:
- `JSON.parse` errors on null/empty CachedMetrics data fields
- Missing workspace fields outputting `undefined` instead of placeholder text
- Campaign channels field being null instead of empty JSON array
  </action>
  <verify>
    ls .nova/memory/*/profile.md | wc -l | grep -q "[0-9]" && echo "Workspaces seeded" && git check-ignore .nova/memory/rise/profile.md && echo "Gitignore works"
  </verify>
  <done>
    All DB workspaces have .nova/memory/{slug}/ directories with 4 memory files each. Profile.md contains real DB data. Global-insights.md has cross-client benchmarks. Gitignore correctly hides .md files but preserves .gitkeep. Re-running seed preserves accumulated files while refreshing profile.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ARCHITECTURE.md to reflect actual 4-file memory schema</name>
  <files>.nova/ARCHITECTURE.md</files>
  <action>
Update Section 6 ("Memory Namespace (Phase 47)") in `.nova/ARCHITECTURE.md` to replace the placeholder schema with the actual implemented schema from CONTEXT.md decisions.

Replace the current content:
```
.nova/memory/{slug}/
  icp.md
  campaigns.md
  contacts.md
  notes.md
```

With the actual 4-file schema:
```
.nova/memory/{slug}/
  profile.md       # Company, vertical, ICP, tone, channels, contacts, standing instructions
  campaigns.md     # Campaign history with performance data, copy wins/losses
  feedback.md      # Client approval patterns, observed preferences
  learnings.md     # ICP learnings, lead source effectiveness, vertical insights
```

Update the description text to match the implemented behavior:
- "4 files per workspace" (not the placeholder names)
- profile.md: "Regenerated by `nova-memory seed` -- agents never write to this file"
- campaigns.md, feedback.md, learnings.md: "Append-only with ISO timestamps. 200-line limit. Agents summarise oldest entries when approaching limit."
- Global insights: "`.nova/memory/global-insights.md` -- cross-client benchmarks, seeded from CachedMetrics. Only `nova-intel` agent writes here."
- Seed script: "`npx tsx scripts/nova-memory.ts seed [slug|--all]`"
- Remove the "Backup and restore via Vercel Blob" line (not implemented in this phase, deferred)

Also update the directory structure in Section 7 to show `memory/{slug}/` with the correct 4 file names instead of the placeholder names.
  </action>
  <verify>
    grep -q "profile.md" .nova/ARCHITECTURE.md && grep -q "feedback.md" .nova/ARCHITECTURE.md && grep -q "learnings.md" .nova/ARCHITECTURE.md && echo "ARCHITECTURE.md updated"
  </verify>
  <done>
    .nova/ARCHITECTURE.md Section 6 documents the actual 4-file schema (profile.md, campaigns.md, feedback.md, learnings.md) with correct governance rules, seed behavior, and global insights location. Section 7 directory structure reflects the real file names. No references to Vercel Blob backup (deferred).
  </done>
</task>

</tasks>

<verification>
1. `ls .nova/memory/*/profile.md | wc -l` returns 10 (all DB workspaces)
2. `head -2 .nova/memory/myacq/profile.md` shows governance header with "re-seed: overwrites this file"
3. `head -2 .nova/memory/myacq/campaigns.md` shows governance header with "APPEND only"
4. `cat .nova/memory/global-insights.md` shows "Cross-Client Benchmarks" section with average rates
5. `grep "profile.md" .nova/ARCHITECTURE.md` confirms updated documentation
6. `git status` shows .gitkeep files as new (trackable) and NO .md memory files (gitignored)
</verification>

<success_criteria>
- All 10 DB workspaces have seeded memory directories with real content
- Re-run safety confirmed: profile.md overwrites, other files preserved
- ARCHITECTURE.md accurately documents the implemented 4-file schema
- Global insights contain actual cross-client performance averages
- Git correctly ignores memory .md files while tracking .gitkeep scaffolding
</success_criteria>

<output>
After completion, create `.planning/phases/47-client-memory-namespace/47-02-SUMMARY.md`
</output>
