---
phase: 53-platform-expertise-input-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/leads-rules.md
autonomous: true
requirements:
  - LEAD-09
  - LEAD-02
  - LEAD-03

must_haves:
  truths:
    - "leads-rules.md contains a Platform Expertise section with consistent per-platform playbooks for all 6 active platforms"
    - "The existing Source Selection Guide is replaced and consolidated into Platform Expertise"
    - "Two-path search routing logic is documented with explicit decision tree"
    - "AI Ark two-step company-then-people workaround is documented as mandatory with hard-blocked filters listed"
    - "Each platform has identical template sections: Overview, Filters, Cost Model, Known Issues, Example Combos, Routing Guidance"
  artifacts:
    - path: ".claude/rules/leads-rules.md"
      provides: "Complete platform expertise, routing logic, and input validation rules for leads agent"
      contains: "## Platform Expertise"
  key_links:
    - from: ".claude/rules/leads-rules.md"
      to: "src/lib/agents/leads.ts"
      via: "loadRules('leads-rules.md') at agent startup"
      pattern: "loadRules"
---

<objective>
Rewrite leads-rules.md to replace the existing Source Selection Guide with a comprehensive Platform Expertise section covering all 6 active discovery platforms, plus a Two-Path Routing Decision Tree and Input Validation Rules section. This is the knowledge layer that makes the leads agent an expert on every platform before it runs a single search.

Purpose: The leads agent currently has a thin Source Selection Guide that does not document known bugs, cost models, filter compatibility, or routing logic. This caused a $100 waste when Prospeo received company names instead of domains. The Platform Expertise section prevents this class of error by giving the agent deep, structured knowledge about every platform.

Output: Updated `.claude/rules/leads-rules.md` with Platform Expertise replacing Source Selection Guide.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-platform-expertise-input-rules/53-CONTEXT.md
@.planning/phases/53-platform-expertise-input-rules/53-RESEARCH.md
@.claude/rules/leads-rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite leads-rules.md with Platform Expertise, Routing, and Validation Rules</name>
  <files>.claude/rules/leads-rules.md</files>
  <action>
Rewrite `.claude/rules/leads-rules.md` preserving the existing sections (Capabilities, Discovery Workflow steps 1-4, Discovery Rules, Interaction Rules, Conversational Refinement, Voice, Error Handling, Important Notes, Memory Write Governance) but REPLACING the "Source Selection Guide" section (lines 52-92) with three new sections:

**Section 1: Platform Expertise** (replaces Source Selection Guide)

Use a consistent template for each of the 6 active platforms. Each platform gets these identical subsections:
- Overview (what it does, database size, what it returns)
- Cost (per-call cost, per-lead cost, monthly budget guidance)
- Rate Limits (requests/sec, requests/min where known)
- Pagination (page size, token format, zero-based vs one-based)
- Results (identity only vs verified emails vs company-only)
- Supported Filters table (filter name, field name, notes)
- Known Issues (with workarounds)
- HARD-BLOCKED Filters (with reasons and alternatives)
- Example Filter Combos (Enterprise B2B, SMB/Local, Ecommerce where applicable)
- Routing Guidance (use when, skip when, always pair with)

Platform data to encode (all from 53-RESEARCH.md):

**Apollo:**
- FREE, 275M contacts, identity only (no emails), up to 100/page
- Pagination: pageToken string
- Filters: jobTitles, seniority, industries, locations, companySizes, companyDomains, keywords
- Seniority values: c_suite, vp, director, manager, ic
- Company sizes: 1-10, 11-50, 51-200, 201-500, 500+
- No known broken filters
- Routing: always include (free), basic filter set only, no SIC/funding/revenue

**Prospeo:**
- $0.002/credit, 1 credit per request, 25 results/page (fixed), identity only
- Pagination: 1-based page numbers
- 20+ filters including unique: SIC codes, years of experience, fundingStages, departments, revenueMin/Max, technologies, companyType, foundedYear, naicsCodes
- Company domains via company.websites -- MUST be actual domains, NOT company names
- Location format: "Country Name #CC" (e.g. "United Kingdom #GB")
- Headcount mapping: generic ranges auto-mapped to Prospeo's finer-grained bands
- KNOWN ISSUE: company.websites accepts company names without error but returns junk data (the $100 bug) -- HARD-BLOCK company names
- Routing: always include alongside AI Ark, use for SIC/experience filters

**AI Ark:**
- ~$0.003 per API call, up to 100/page, identity only
- Rate limits: 5 req/s, 300 req/min
- Pagination: zero-based page numbers
- Filters: jobTitles (SMART mode), seniority, industries, locations, companySizes (RANGE), companyDomains, revenue, funding, technologies, companyType, foundedYear, naicsCodes
- HARD-BLOCKED filters:
  - contact.department -- BUGGED: silently returns all records ignoring the filter
  - contact.keyword -- BROKEN: returns 400 "request not readable"
  - account.keyword on /v1/people -- returns 500 "cannot serialize"
- TWO-STEP WORKAROUND (MANDATORY for keyword searches): Search /v1/companies by keyword -> get domains -> use domains as account.domain filter on /v1/people. This is already implemented in the adapter code (searchCompanyDomainsByKeyword) but must be documented as a hard rule.
- Company type values: PRIVATELY_HELD, PUBLIC_COMPANY, NON_PROFIT, SELF_OWNED, PARTNERSHIP
- Funding stages: SEED, SERIES_A, SERIES_B, SERIES_C, VENTURE_ROUND, ANGEL, IPO
- Routing: always include alongside Prospeo, equal coverage peer

**Leads Finder (Apify):**
- ~$0.002/lead ($2/1K), returns VERIFIED EMAILS + phones + LinkedIn in one step
- No pagination -- single batch, all results returned at once
- Filters: jobTitles, seniority, industries, locations, companySizes, companyDomains, companyKeywords, departments, revenueMin/Max, fundingStages
- UNIQUE: only source that returns verified emails directly (skips enrichment step)
- Requires Apify paid plan (credits reset monthly)
- Routing: use when verified emails needed fast, supplement core 3 sources

**Google Maps (Apify):**
- ~$0.005 per search, COMPANY-LEVEL only (name, address, phone, website, rating, reviews, categories)
- No person data -- MUST be followed by people search using discovered domains
- Use case: local/SMB business discovery by category and location
- Routing: use for local/SMB ICP, always follow with Prospeo/AI Ark for people

**Ecommerce Stores (Apify):**
- ~$0.004/lead (pay-per-result), 14M+ store database, COMPANY-LEVEL only
- Returns: domain, store name, platform, email, phone, country, traffic, technologies, categories
- Filters: platform (Shopify, WooCommerce, BigCommerce, Magento), category, country, monthly traffic, keywords
- No person data -- MUST be followed by people search (Prospeo/AI Ark) using discovered domains
- Status: UNDER MAINTENANCE
- Routing: use for ecommerce ICP, always follow with people search

**Section 2: Two-Path Routing Decision Tree**

Add immediately after Platform Expertise. Encode this exact routing logic:

```
INPUT: ICP + optional company domains

IF company domains provided:
  PATH A (domain-based): Search Prospeo + AI Ark + Apollo by companyDomains
  PATH B (ICP-filter): Search Prospeo + AI Ark + Apollo by ICP filters
  RUN BOTH IN PARALLEL, dedup after
  VERIFY DOMAINS: quick DNS check for valid/current domains before burning credits

IF ICP-filter only (no domains):
  ALWAYS use all three: Apollo (free) + Prospeo + AI Ark
  Add Apify sources when ICP calls for them:
    - Ecommerce ICP -> Ecommerce Stores first, then Prospeo/AI Ark for people
    - Local/SMB ICP -> Google Maps first, then Prospeo/AI Ark for people
    - Need verified emails fast -> Leads Finder

IF company keyword search:
  AI Ark: MUST use two-step workaround (companies -> domains -> people)
  Prospeo: Direct companyKeywords filter works
  Apollo: keywords filter works
```

Include the requirement that the agent explains WHY it chose the routing in its plan presentation (e.g. "Using domain-based search on Prospeo because you provided 104 company domains. Also running AI Ark ICP filters to catch companies not on your list.").

**Section 3: Pre-Search Input Validation Rules**

Add after Two-Path Routing. Document these 5 check types with examples:

1. Company Name vs Domain: companyDomains entries without a dot or with spaces = HARD BLOCK
2. Missing Required ICP Fields: no jobTitles AND no seniority AND no industries AND no companyDomains = HARD BLOCK
3. Filter-Platform Mismatch: AI Ark departments/keywords = HARD BLOCK; Apollo SIC codes/yearsExperience = WARNING
4. Budget Exceeded: estimated cost > remaining daily budget = WARNING (not hard block)
5. ICP Mismatch: search filters vs workspace ICP = WARNING

State that these rules are enforced at TWO layers: (1) the agent reads and follows these rules during plan generation, and (2) the discovery-validation.ts CLI module enforces them as a safety net before execution.

**Important formatting notes:**
- Keep the file under 400 lines (the 200-line budget note is aspirational -- this is a comprehensive rewrite that subsumes the old guide)
- Use markdown tables for filter listings to keep them scannable
- Keep the consistent template structure so each platform is quickly scannable
- Preserve ALL existing sections outside Source Selection Guide unchanged (Discovery Workflow, Discovery Rules, Interaction Rules, etc.)
  </action>
  <verify>
    <automated>grep -c "## Platform Expertise" .claude/rules/leads-rules.md && grep -c "HARD-BLOCKED" .claude/rules/leads-rules.md && grep -c "Two-Path Routing" .claude/rules/leads-rules.md && grep -c "Pre-Search Input Validation" .claude/rules/leads-rules.md && ! grep -c "## Source Selection Guide" .claude/rules/leads-rules.md</automated>
  </verify>
  <done>
    - leads-rules.md contains "Platform Expertise" section with all 6 platforms using consistent template
    - "Source Selection Guide" section is fully replaced (no longer present)
    - AI Ark hard-blocked filters (contact.department, contact.keyword) are documented
    - AI Ark two-step workaround documented as MANDATORY
    - Prospeo company-name-vs-domain bug documented with HARD BLOCK
    - Two-Path Routing Decision Tree present with parallel execution logic
    - Pre-Search Input Validation Rules present with all 5 check types
    - All existing sections (Discovery Workflow, Discovery Rules, etc.) preserved unchanged
  </done>
</task>

</tasks>

<verification>
- `grep "## Platform Expertise" .claude/rules/leads-rules.md` returns a match
- `grep -c "### Apollo\|### Prospeo\|### AI Ark\|### Leads Finder\|### Google Maps\|### Ecommerce Stores" .claude/rules/leads-rules.md` returns 6 (one per platform)
- `grep "Source Selection Guide" .claude/rules/leads-rules.md` returns NO match (replaced)
- `grep "Two-Path Routing" .claude/rules/leads-rules.md` returns a match
- `grep "HARD-BLOCKED\|HARD BLOCK" .claude/rules/leads-rules.md` returns multiple matches
- `grep "contact.department\|contact.keyword" .claude/rules/leads-rules.md` returns matches (documented as broken)
- `grep "two-step\|two step" .claude/rules/leads-rules.md` returns matches (AI Ark workaround documented)
</verification>

<success_criteria>
The leads agent, upon loading leads-rules.md via loadRules(), has comprehensive platform expertise covering all 6 active platforms with consistent playbooks, knows the exact routing decision tree for domain-based vs ICP-filter searches, and understands all input validation rules before any search executes.
</success_criteria>

<output>
After completion, create `.planning/phases/53-platform-expertise-input-rules/53-01-SUMMARY.md`
</output>
