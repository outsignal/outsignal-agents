---
phase: 27-ai-insights-action-queue
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - src/app/(admin)/analytics/page.tsx
  - src/components/analytics/insights-tab.tsx
  - src/components/analytics/insight-card.tsx
  - src/components/analytics/objection-clusters.tsx
autonomous: false
requirements: [INSIGHT-03, INSIGHT-05]

must_haves:
  truths:
    - "Admin sees 'Insights' as 4th tab on analytics page alongside Performance, Copy, Benchmarks"
    - "Each insight card shows observation, evidence with trend arrows, suggested action, confidence badge, and category color accent"
    - "Admin can approve, dismiss, or snooze (3d/1w/2w) each insight with one or two clicks"
    - "Destructive actions show inline 'Confirm pause?' before executing"
    - "Objection cluster section shows percentage distribution across campaigns with AI commentary"
    - "Dismissed insights visible in collapsed 'Dismissed' section"
  artifacts:
    - path: "src/app/(admin)/analytics/page.tsx"
      provides: "4th tab 'Insights' added to analytics page"
    - path: "src/components/analytics/insights-tab.tsx"
      provides: "Main insights tab container with action queue and objection clusters"
      exports: ["InsightsTab"]
    - path: "src/components/analytics/insight-card.tsx"
      provides: "Individual insight card with approve/dismiss/defer controls"
      exports: ["InsightCard"]
    - path: "src/components/analytics/objection-clusters.tsx"
      provides: "Objection pattern cluster visualization with AI commentary"
      exports: ["ObjectionClusters"]
  key_links:
    - from: "src/app/(admin)/analytics/page.tsx"
      to: "src/components/analytics/insights-tab.tsx"
      via: "InsightsTab rendered when activeTab='insights'"
      pattern: "isInsightsTab.*InsightsTab"
    - from: "src/components/analytics/insights-tab.tsx"
      to: "/api/insights?workspace="
      via: "fetch on mount and workspace change"
      pattern: "fetch.*api/insights"
    - from: "src/components/analytics/insight-card.tsx"
      to: "/api/insights/[id]"
      via: "PATCH on approve/dismiss/snooze button click"
      pattern: "fetch.*api/insights.*PATCH"
    - from: "src/components/analytics/objection-clusters.tsx"
      to: "/api/analytics/replies/stats"
      via: "fetch objection distribution data"
      pattern: "objectionSubtype"
---

<objective>
Build the Insights tab UI on the existing analytics page with insight cards (approve/dismiss/defer), objection cluster visualization, and analytics page integration. After this plan, the admin can view and manage AI-generated insights through a polished, data-first interface.

Purpose: The admin needs a visual interface to review insights, take action on suggestions, and see objection patterns — this is the user-facing surface of the entire Phase 27 system.
Output: Insights tab with insight cards, action queue controls, objection cluster section, Dismissed section.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ai-insights-action-queue/27-RESEARCH.md
@.planning/phases/27-ai-insights-action-queue/27-01-SUMMARY.md
@.planning/phases/27-ai-insights-action-queue/27-02-SUMMARY.md

@src/app/(admin)/analytics/page.tsx
@src/components/analytics/benchmarks-tab.tsx

<interfaces>
<!-- From analytics page (existing) -->

From src/app/(admin)/analytics/page.tsx:
```typescript
// Tab pattern: TabChip components with nuqs URL state
const activeTab = params.tab || "performance";
const isPerformanceTab = activeTab === "performance";
const isCopyTab = activeTab === "copy";
const isBenchmarksTab = activeTab === "benchmarks";
// Add: const isInsightsTab = activeTab === "insights";

function handleTabChange(tab: string) { /* updates URL params */ }

// Render pattern:
<TabChip label="Performance" active={isPerformanceTab} onClick={() => handleTabChange("performance")} />
// Add: <TabChip label="Insights" active={isInsightsTab} onClick={() => handleTabChange("insights")} />
```

<!-- From Plan 01 types -->
From src/lib/insights/types.ts:
```typescript
export const CATEGORY_COLORS = {
  performance: "border-l-blue-500",
  copy: "border-l-purple-500",
  objections: "border-l-orange-500",
  icp: "border-l-emerald-500",
};
export const CONFIDENCE_COLORS = {
  high: "bg-green-100 text-green-800",
  medium: "bg-yellow-100 text-yellow-800",
  low: "bg-gray-100 text-gray-600",
};
```

<!-- API endpoints from Plan 02 -->
```
GET /api/insights?workspace={slug}&status=active  -> Insight[]
PATCH /api/insights/{id}  body: {action: "approve"|"dismiss"|"snooze", snoozeDays?: 3|7|14}
POST /api/insights  body: {workspaceSlug}  -> {count, message}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insight card and objection clusters components</name>
  <files>src/components/analytics/insight-card.tsx, src/components/analytics/objection-clusters.tsx</files>
  <action>
1. Create src/components/analytics/insight-card.tsx:
   - "use client" directive.
   - Props: insight (the insight object from API), onAction (callback after approve/dismiss/snooze for parent to refresh).
   - Card layout with left border color from CATEGORY_COLORS[insight.category] (4px solid).
   - Top row: Category badge (small pill with category label), Confidence badge (color-coded per CONFIDENCE_COLORS), generated date ("3 days ago" format).
   - Observation text: bold, data-first (this is just rendering the AI-generated observation string).
   - Evidence section: render each evidence item as a row with metric name, value, and change with trend arrow. Use TrendingUp/TrendingDown icons from lucide-react. Format change with color: positive = green, negative = red. Example: "Reply rate: 2.1% ↓ (-40% vs last week)".
   - Suggested action section: light background box showing actionDescription text.
   - Action buttons row:
     a. "Approve" button (green). If actionType is "pause_campaign", clicking shows inline confirmation: button text changes to "Confirm pause?" with a "Cancel" text button next to it. Use local state (confirmingPause: boolean). On confirm, call PATCH with {action: "approve"}.
     b. "Snooze" dropdown/popover with 3 options: "3 days", "1 week", "2 weeks". On select, call PATCH with {action: "snooze", snoozeDays: 3|7|14}.
     c. "Dismiss" button (muted/gray). Call PATCH with {action: "dismiss"}.
   - For executed/failed insights (status not "active"): show status badge instead of action buttons. If executionResult exists, show before/after in a small details section.
   - Loading state on action buttons while PATCH is in flight.

2. Create src/components/analytics/objection-clusters.tsx:
   - "use client" directive.
   - Props: workspace (string | null).
   - Fetch objection distribution on mount: GET /api/analytics/replies/stats?workspace={slug} (this endpoint already exists from Phase 23).
   - Parse objection subtype counts from the response.
   - Display horizontal bar chart (Recharts BarChart, horizontal) showing each objection type with its percentage and count.
   - Color bars by objection type (budget=red-400, timing=amber-400, competitor=violet-400, authority=blue-400, need=orange-400, trust=slate-400).
   - Below chart: "AI Commentary" section. Fetch insights where category="objections" from the insights API. Show the most recent objection-category insight observation text as commentary. If none, show "No AI commentary yet — insights generate weekly."
   - Total objection count displayed as header stat.
  </action>
  <verify>ls -la src/components/analytics/insight-card.tsx src/components/analytics/objection-clusters.tsx && grep -c "use client" src/components/analytics/insight-card.tsx</verify>
  <done>InsightCard renders observation, evidence with trend arrows, confidence/category badges, and approve/dismiss/snooze controls with inline confirmation for destructive actions. ObjectionClusters shows horizontal bar chart of objection distribution with AI commentary.</done>
</task>

<task type="auto">
  <name>Task 2: Insights tab container and analytics page integration</name>
  <files>src/components/analytics/insights-tab.tsx, src/app/(admin)/analytics/page.tsx</files>
  <action>
1. Create src/components/analytics/insights-tab.tsx:
   - "use client" directive.
   - Props: workspace (string | null).
   - State: insights array, loading boolean, refreshing boolean.
   - Fetch insights on mount and workspace change: GET /api/insights?workspace={slug}&status=active.
   - Also separately fetch snoozed insights that have expired (the API handles this — snoozedUntil less than now returns in "active" query).
   - Layout sections:
     a. Header row: "AI Insights" title + "Refresh Insights" button (calls POST /api/insights with workspaceSlug, shows spinner while refreshing, then re-fetches list).
     b. Active insights grid: render InsightCard for each active insight. Use a single-column layout (cards stack vertically, full width). Pass onAction callback that triggers re-fetch.
     c. ObjectionClusters component rendered below the insight cards as a dedicated section with a section header "Objection Patterns".
     d. Dismissed section: collapsible (details/summary HTML or a toggle). Fetch GET /api/insights?workspace={slug}&status=dismissed. Show dismissed insight cards in muted styling (opacity-60, no action buttons — read-only).
   - Empty state: if no insights, show "No insights yet. Insights are generated weekly, or click Refresh to generate now." with the Refresh button.
   - If no workspace selected (global view): show message "Select a workspace to view insights."

2. Update src/app/(admin)/analytics/page.tsx:
   - Add import for InsightsTab component.
   - Add isInsightsTab = activeTab === "insights" alongside existing tab booleans.
   - Add 4th TabChip after Benchmarks tab.
   - Add conditional render: {isInsightsTab && InsightsTab with workspace prop}
   - Lazy loading: only fetch insights data when Insights tab is active (same pattern as other tabs).
  </action>
  <verify>grep -n "insights" src/app/\(admin\)/analytics/page.tsx | head -10 && ls -la src/components/analytics/insights-tab.tsx</verify>
  <done>Insights tab appears as 4th tab on analytics page. Shows active insight cards with action controls, objection clusters section, dismissed insights in collapsible section, and Refresh button for manual regeneration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify insights UI and action flow</name>
  <files>src/app/(admin)/analytics/page.tsx</files>
  <action>
Human verification of the complete AI Insights and Action Queue system. What was built: insight generation pipeline, action executors, insight cards with approve/dismiss/snooze, objection clusters, weekly digest notification, and Insights tab on analytics page.

Steps to verify:
1. Start dev server: npm run dev
2. Navigate to http://localhost:3000/admin/analytics
3. Click "Insights" tab (4th tab) — should show empty state or insights if already generated
4. Select a workspace from dropdown
5. Click "Refresh Insights" button — should generate 3-5 insight cards
6. Verify each card shows: category badge with left border color, confidence badge (green/yellow/gray), observation text (data-first, leading with numbers), evidence with trend arrows, suggested action description
7. Click "Approve" on a non-pause insight — should execute and show "Executed" status
8. Click "Approve" on a pause-campaign insight — should show "Confirm pause?" inline, click Cancel, then Confirm
9. Click "Snooze" and select "1 week" — card should disappear from active queue
10. Click "Dismiss" on an insight — card should move to collapsed "Dismissed" section
11. Scroll to "Objection Patterns" section — should show horizontal bar chart of objection distribution with AI commentary
12. Verify the overall tone is direct and data-first (numbers, not fluff)

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>echo "Human verification checkpoint — visual and functional review"</verify>
  <done>Admin confirms insights UI looks correct, action flow works (approve/dismiss/snooze), objection clusters display properly, and overall tone is data-first.</done>
</task>

</tasks>

<verification>
1. Analytics page shows 4 tabs: Performance, Copy, Benchmarks, Insights
2. Insight cards render with correct category colors and confidence badges
3. Approve/dismiss/snooze actions work via PATCH API
4. Inline confirmation appears for pause_campaign actions
5. Objection clusters section shows bar chart and AI commentary
6. Dismissed section is collapsible and shows dismissed insights
7. Refresh button triggers manual insight generation
</verification>

<success_criteria>
- "Insights" tab is the 4th tab on analytics page, accessible via URL ?tab=insights
- Insight cards display observation, evidence with trend arrows, suggested action, confidence badge, and category accent
- One-click approve/dismiss/snooze with inline confirmation for destructive actions
- Objection cluster section shows percentage distribution bar chart with AI commentary
- Dismissed insights viewable in collapsed section
- Refresh button triggers on-demand insight generation
</success_criteria>

<output>
After completion, create `.planning/phases/27-ai-insights-action-queue/27-03-SUMMARY.md`
</output>
