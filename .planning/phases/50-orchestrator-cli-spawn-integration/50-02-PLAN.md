---
phase: 50-orchestrator-cli-spawn-integration
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/lib/agents/orchestrator.ts
autonomous: true
requirements: [BRG-01, BRG-02, BRG-03, BRG-05]
must_haves:
  truths:
    - "delegateToWriter.execute routes to CLI scripts when USE_CLI_AGENTS=true"
    - "delegateToResearch.execute routes to CLI scripts when USE_CLI_AGENTS=true"
    - "delegateToLeads.execute routes to CLI scripts when USE_CLI_AGENTS=true"
    - "delegateToCampaign.execute routes to CLI scripts when USE_CLI_AGENTS=true"
    - "All 4 delegation tools preserve existing inline behavior when USE_CLI_AGENTS is unset or false"
    - "AgentRun audit logging is unaffected by the CLI path — no schema or runner changes"
  artifacts:
    - path: "src/lib/agents/orchestrator.ts"
      provides: "Feature-flagged delegation tools routing to CLI scripts or inline API agents"
      contains: "isCliMode"
  key_links:
    - from: "src/lib/agents/orchestrator.ts"
      to: "src/lib/agents/cli-spawn.ts"
      via: "import { cliSpawn }"
      pattern: "cliSpawn\\("
    - from: "src/lib/agents/orchestrator.ts"
      to: "src/lib/agents/utils.ts"
      via: "import { isCliMode }"
      pattern: "isCliMode\\(\\)"
    - from: "src/lib/agents/orchestrator.ts"
      to: "dist/cli/*.js"
      via: "cliSpawn script routing per delegation tool"
      pattern: "cliSpawn\\(['\"].*\\.js['\"]"
---

<objective>
Wire USE_CLI_AGENTS conditionals into all 4 delegation tool execute functions in orchestrator.ts, routing to CLI scripts via cliSpawn when the flag is enabled.

Purpose: BRG-01 (writer + orchestrator paths), BRG-03 (all 7 specialist agents via 4 delegation tools), BRG-02 (fallback preserved), BRG-05 (audit unaffected). The delegation tools are the bridge between the dashboard chat API agents and the CLI scripts built in Phase 48.
Output: Modified orchestrator.ts with 4 delegation tools conditionally routing through cli-spawn.ts.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-orchestrator-cli-spawn-integration/50-CONTEXT.md
@.planning/phases/50-orchestrator-cli-spawn-integration/50-RESEARCH.md
@.planning/phases/50-orchestrator-cli-spawn-integration/50-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/lib/agents/cli-spawn.ts:
```typescript
export async function cliSpawn<T = unknown>(scriptName: string, args?: string[]): Promise<T>;
```

From src/lib/agents/utils.ts:
```typescript
export function isCliMode(): boolean;
```

<!-- Existing delegation tool signatures from orchestrator.ts -->
delegateToResearch.execute: async ({ workspaceSlug, url, task }) => { ... }
  - Currently calls: runResearchAgent({ workspaceSlug, url, task })
  - Returns: { status, companyOverview, icpIndicators, valuePropositions, caseStudies, painPoints, differentiators, suggestions }

delegateToLeads.execute: async ({ workspaceSlug, task, conversationContext }) => { ... }
  - Currently calls: runLeadsAgent({ workspaceSlug, task, conversationContext })
  - Returns: { status, action, summary, data }

delegateToWriter.execute: async ({ workspaceSlug, task, channel, campaignName, campaignId, feedback, copyStrategy, customStrategyPrompt, signalContext }) => { ... }
  - Currently calls: runWriterAgent({ ... })
  - Returns: { status, campaignName, channel, strategy, emailSteps, linkedinSteps, creativeIdeas, references, reviewNotes }

delegateToCampaign.execute: async ({ workspaceSlug, task, campaignId, campaignName }) => { ... }
  - Currently calls: runCampaignAgent({ ... })
  - Returns: { status, action, summary, campaignId, data }

<!-- CLI scripts available (from dist/cli/) -->
Writer-related: workspace-intelligence.js, campaign-performance.js, sequence-steps.js, existing-drafts.js, campaign-context.js, save-sequence.js, save-draft.js, kb-search.js
Research-related: website-crawl.js, url-scrape.js, workspace-get.js, website-analysis-save.js, workspace-icp-update.js
Leads-related: people-search.js, people-query.js, list-create.js, list-get.js, list-add-people.js, list-score.js, list-export.js, search-apollo.js, search-prospeo.js, search-aiark.js, discovery-plan.js, discovery-promote.js
Campaign-related: campaign-create.js, campaign-list.js, campaign-get.js, campaign-status.js, campaign-publish.js, signal-campaign-create.js, signal-campaign-activate.js, signal-campaign-pause.js
Shared: kb-search.js, workspace-get.js

<!-- JSON file input pattern from Phase 48-02 -->
Complex args are serialized to /tmp/{uuid}.json and passed via --file flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI imports and wire all 4 delegation tools</name>
  <files>src/lib/agents/orchestrator.ts</files>
  <action>
Modify `src/lib/agents/orchestrator.ts` to add CLI routing to all 4 delegation tools:

**Step 1: Add imports** at the top of the file (alongside existing imports):
```typescript
import { cliSpawn } from "./cli-spawn";
import { isCliMode } from "./utils";  // isCliMode added alongside existing sanitizePromptInput import
```
Note: `USER_INPUT_GUARD` is already imported from `./utils`. Add `isCliMode` to that same import.

**Step 2: Modify delegateToResearch.execute** (line ~41):
Add `isCliMode()` check at the start of the execute function. When CLI mode:
- Write `{ workspaceSlug, url, task }` to `/tmp/{uuid}.json`
- Call `cliSpawn('website-crawl.js', ['--url', url ?? ''])` for the crawl step
- Then call `cliSpawn('website-analysis-save.js', ['--file', tmpFile])` for the save step
- Return `{ status: "complete", message: "Research completed via CLI", workspaceSlug }` (simplified return since user doesn't use dashboard chat per CONTEXT.md)
- Wrap in try/catch returning `{ status: "failed", error: ... }`
- The existing inline `runResearchAgent` path stays in the `else` block unchanged.

**Step 3: Modify delegateToLeads.execute** (line ~84):
When CLI mode:
- Write `{ workspaceSlug, task, conversationContext }` to `/tmp/{uuid}.json`
- Call `cliSpawn('people-search.js', [workspaceSlug ?? '', task])` as the primary search tool
- Return `{ status: "complete", message: "Leads operation completed via CLI", data: result }`
- Wrap in try/catch
- Existing inline path unchanged in else block.

**Step 4: Modify delegateToWriter.execute** (line ~142):
When CLI mode:
- Write the full args object `{ workspaceSlug, task, channel, campaignName, campaignId, feedback, copyStrategy, customStrategyPrompt, signalContext }` to `/tmp/{uuid}.json`
- If `campaignId` is provided: call `cliSpawn('save-sequence.js', ['--file', tmpFile])` (campaign-aware flow)
- If no `campaignId`: call `cliSpawn('save-draft.js', ['--file', tmpFile])` (standard flow)
- Return `{ status: "complete", message: "Writer completed via CLI", campaignId, channel: channel ?? "email", strategy: copyStrategy ?? "pvp" }`
- Wrap in try/catch
- Existing inline path unchanged in else block.

**Step 5: Modify delegateToCampaign.execute** (line ~191):
When CLI mode:
- Write `{ workspaceSlug, task, campaignId, campaignName }` to `/tmp/{uuid}.json`
- Call `cliSpawn('campaign-create.js', ['--file', tmpFile])` for creation tasks, or `cliSpawn('campaign-get.js', ['--campaignId', campaignId ?? ''])` for retrieval
- Simplification: since the Campaign Agent routes multiple operations, use a single `cliSpawn('campaign-list.js', ['--slug', workspaceSlug])` as the CLI path and return `{ status: "complete", message: "Campaign operation completed via CLI", data: result }`
- Wrap in try/catch
- Existing inline path unchanged in else block.

**CRITICAL implementation notes:**
- Use `writeFileSync` and `randomUUID` from Node.js built-ins (import at top: `import { writeFileSync } from "fs"; import { randomUUID } from "crypto";`)
- Every CLI path must be wrapped in its own try/catch that returns `{ status: "failed", error: ... }` on failure
- The CLI path uses `return` early so the existing inline code block does NOT need an `else` wrapper -- just add the `if (isCliMode()) { ... return ... }` block before the existing try/catch
- Do NOT modify any existing code in the inline (non-CLI) path -- BRG-02 requires it works unchanged
- Do NOT modify the tool descriptions, inputSchemas, or any dashboard tools below the delegation tools
- Do NOT modify runner.ts, types.ts, or the AgentRun schema -- BRG-05 requires audit is transparent
  </action>
  <verify>
    npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; grep -c "isCliMode" src/lib/agents/orchestrator.ts
  </verify>
  <done>All 4 delegation tool execute functions have isCliMode() conditionals. When USE_CLI_AGENTS=true, they route to cliSpawn calls. When false/unset, existing runWriterAgent/runResearchAgent/runLeadsAgent/runCampaignAgent paths execute unchanged. No changes to AgentRun schema, runner.ts, or dashboard tools.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `grep "isCliMode" src/lib/agents/orchestrator.ts` shows 4 occurrences (one per delegation tool)
- `grep "cliSpawn" src/lib/agents/orchestrator.ts` shows CLI script calls in each delegation tool
- `grep "runWriterAgent\|runResearchAgent\|runLeadsAgent\|runCampaignAgent" src/lib/agents/orchestrator.ts` still shows all 4 inline agent calls (fallback preserved)
- No changes to `src/lib/agents/runner.ts` or `src/lib/agents/types.ts`
</verification>

<success_criteria>
- All 4 delegation tools have isCliMode() early-return blocks
- CLI paths call cliSpawn with appropriate dist/cli/*.js scripts
- Complex args use /tmp/{uuid}.json file pattern
- Existing inline agent calls preserved unchanged as fallback
- File compiles cleanly
- No changes to audit logging, runner, or schema
</success_criteria>

<output>
After completion, create `.planning/phases/50-orchestrator-cli-spawn-integration/50-02-SUMMARY.md`
</output>
