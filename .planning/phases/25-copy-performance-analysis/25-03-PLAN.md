---
phase: 25-copy-performance-analysis
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - src/app/(admin)/analytics/page.tsx
  - src/components/analytics/copy-tab.tsx
  - src/components/analytics/subject-line-rankings.tsx
  - src/components/analytics/element-multiplier-cards.tsx
  - src/components/analytics/top-templates-list.tsx
  - src/components/analytics/template-detail-panel.tsx
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, COPY-04, COPY-05]

must_haves:
  truths:
    - "Admin can switch between Performance and Copy tabs on the analytics page"
    - "Admin can see ranked subject lines with open rate, reply rate, and send volume"
    - "Admin can toggle between global and per-campaign subject line views"
    - "Admin can see multiplier cards per element showing global and vertical multipliers side by side"
    - "Low-confidence correlations appear dimmed but visible"
    - "Admin can see top 10 performing templates with element pills and full body text"
    - "All copy analysis views filter by workspace and vertical"
  artifacts:
    - path: "src/app/(admin)/analytics/page.tsx"
      provides: "Tab switching between Performance and Copy views"
      contains: "tab.*performance.*copy"
    - path: "src/components/analytics/copy-tab.tsx"
      provides: "Container component fetching all copy analysis data"
      min_lines: 50
    - path: "src/components/analytics/subject-line-rankings.tsx"
      provides: "Subject line table with global/per-campaign toggle"
      min_lines: 40
    - path: "src/components/analytics/element-multiplier-cards.tsx"
      provides: "Grid of multiplier cards with dual baselines and confidence indicators"
      min_lines: 50
    - path: "src/components/analytics/top-templates-list.tsx"
      provides: "Top 10 templates list with element pills"
      min_lines: 40
    - path: "src/components/analytics/template-detail-panel.tsx"
      provides: "Expanded view showing full email body and element tags"
      min_lines: 30
  key_links:
    - from: "src/app/(admin)/analytics/page.tsx"
      to: "src/components/analytics/copy-tab.tsx"
      via: "Conditional render based on tab state"
      pattern: "tab.*copy.*CopyTab"
    - from: "src/components/analytics/copy-tab.tsx"
      to: "/api/analytics/copy/*"
      via: "fetch calls to three copy analysis endpoints"
      pattern: "fetch.*api/analytics/copy"
    - from: "src/components/analytics/element-multiplier-cards.tsx"
      to: "correlations API response"
      via: "Renders multiplier values with color coding"
      pattern: "globalMultiplier.*verticalMultiplier"
---

<objective>
Copy tab UI — subject line rankings, element multiplier cards, and top templates

Purpose: Add a "Copy" tab to the existing analytics page displaying subject line rankings with open/reply rates, element-to-reply-rate multiplier cards with dual baselines, and top-performing email templates with structural element breakdown. All views filterable by workspace and vertical.

Output: 5 new components + modified analytics page with tab navigation.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-copy-performance-analysis/25-RESEARCH.md
@.planning/phases/25-copy-performance-analysis/25-02-PLAN.md

@src/app/(admin)/analytics/page.tsx (existing analytics page to add tab to)
@src/components/analytics/analytics-filters.tsx (existing filters component pattern)
@src/components/analytics/strategy-comparison-cards.tsx (card grid pattern)
@src/components/analytics/campaign-rankings-table.tsx (sortable table pattern)

<interfaces>
From 25-02 (API routes — will be created by Plan 02):

Subject lines response:
```typescript
{
  subjectLines: Array<{
    text: string;
    campaignCount: number;
    campaignName: string | null;
    step: number | null;
    totalSends: number;
    openRate: number;
    replyRate: number;
    isVariantB: boolean;
  }>;
  total: number;
  view: "global" | "per-campaign";
  filters: { workspace: string | null; vertical: string | null };
}
```

Correlations response:
```typescript
{
  correlations: Array<{
    element: string;
    displayName: string;
    globalMultiplier: number | null;
    globalSampleWith: number;
    globalSampleWithout: number;
    verticalMultiplier: number | null;
    verticalSampleWith: number;
    verticalSampleWithout: number;
    verticalName: string | null;
    lowConfidence: boolean;
  }>;
  ctaSubtypes: Array<{
    subtype: string;
    avgReplyRate: number;
    sampleSize: number;
  }>;
  totalStepsAnalyzed: number;
  filters: { workspace: string | null; vertical: string | null };
}
```

Top templates response:
```typescript
{
  templates: Array<{
    campaignId: string;
    campaignName: string;
    workspaceSlug: string;
    step: number;
    subjectLine: string;
    body: string;
    elements: BodyElements;
    replyRate: number;
    interestedRate: number;
    compositeScore: number;
    totalSends: number;
    copyStrategy: string | null;
  }>;
  total: number;
  filters: { workspace: string | null; vertical: string | null };
}
```

Existing page uses nuqs for URL state: `useQueryStates` with `parseAsString`.
Existing page uses `Header` component from `@/components/layout/header`.
Existing page uses `ErrorBanner` and `Skeleton` components.
AnalyticsFilters takes workspace, period, and change handlers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab navigation and vertical filter to analytics page</name>
  <files>
    src/app/(admin)/analytics/page.tsx
    src/components/analytics/analytics-filters.tsx
  </files>
  <action>
**Modify `analytics-filters.tsx`** to add an optional `vertical` filter dropdown:
- Add props: `vertical: string | null`, `onVerticalChange: (v: string | null) => void`, `showVertical?: boolean` (default false)
- When `showVertical` is true, render a Select dropdown (same style as workspace selector) listing available verticals
- Fetch verticals from a simple inline query: `GET /api/workspaces` already returns workspace data — extract unique vertical values client-side. Or hardcode the 7 known verticals from the Workspace model (Branded Merchandise, Recruitment Services, Architecture Project Management, B2B Lead Generation, Business Acquisitions, Umbrella Company Solutions, Paid Media Agency). Prefer fetching from workspaces API and extracting unique verticals.
- "All verticals" as default option

**Modify `analytics/page.tsx`** to add tab switching:
- Add `tab` to the nuqs query state: `tab: parseAsString.withDefault("performance")`
- After the AnalyticsFilters bar, render two tab chips: "Performance" and "Copy" (use the same ToggleChip pattern from analytics-filters.tsx or inline similar styled buttons)
- When tab is "performance": render existing strategy cards + campaign rankings (current content)
- When tab is "copy": render `<CopyTab />` component (created in Task 2)
- Pass workspace, period, and vertical (new) to CopyTab via props
- Add `vertical` to nuqs state: `vertical: parseAsString.withDefault("")`
- Pass `showVertical={tab === "copy"}` and vertical handlers to AnalyticsFilters so the vertical dropdown only shows on the Copy tab
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Analytics page has Performance/Copy tab toggle. Vertical filter appears on Copy tab. Tab state persisted in URL via nuqs. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create copy tab container and subject line rankings component</name>
  <files>
    src/components/analytics/copy-tab.tsx
    src/components/analytics/subject-line-rankings.tsx
  </files>
  <action>
**Create `copy-tab.tsx`** — container component for the Copy tab:
- Props: `workspace: string | null`, `period: string`, `vertical: string | null`
- Fetches data from all 3 copy analysis endpoints: `/api/analytics/copy/subject-lines`, `/api/analytics/copy/correlations`, `/api/analytics/copy/top-templates`
- Passes workspace and vertical as query params to all endpoints
- For subject-lines, also passes `view` param (managed as local state toggle)
- Shows loading skeletons per section (same pattern as main page)
- Shows error banners per section with retry buttons
- Layout: 3 vertical sections — Subject Line Rankings, Element Correlations, Top Templates
- Each section has a heading (h2) consistent with the Performance tab sections

**Create `subject-line-rankings.tsx`** — subject line table:
- Props: `subjectLines: SubjectLine[]`, `total: number`, `view: "global" | "per-campaign"`, `onViewChange: (v: "global" | "per-campaign") => void`
- Render a toggle between "Global" and "Per-campaign" views at the top of the section
- Table columns:
  - Subject Line (text, truncated to ~60 chars with full text on hover/title)
  - If per-campaign view: Campaign Name column, Step # column
  - If variant B: small "B" badge next to subject line text
  - Open Rate (formatted as X.XX%)
  - Reply Rate (formatted as X.XX%)
  - Sends (formatted number)
- Sort by the column headers (client-side sort is fine since data is pre-sorted from API — but allow clicking to re-sort locally)
- If no data: show "No subject line data available" empty state
- Use standard table styling from the project (rounded border, alternating rows if existing pattern)
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CopyTab container fetches from all 3 copy analysis endpoints and renders 3 sections. SubjectLineRankings shows sortable table with global/per-campaign toggle. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Element multiplier cards, top templates list, and template detail panel</name>
  <files>
    src/components/analytics/element-multiplier-cards.tsx
    src/components/analytics/top-templates-list.tsx
    src/components/analytics/template-detail-panel.tsx
  </files>
  <action>
**Create `element-multiplier-cards.tsx`** — multiplier cards grid:
- Props: `correlations: Correlation[]`, `ctaSubtypes: CtaSubtype[]`, `totalStepsAnalyzed: number`
- Render a grid of cards (2 cols on mobile, 3 cols on lg), one per element
- Each card shows:
  - Element display name (e.g., "Case Study", "Personalization") as card title
  - Global multiplier as large number: "2.1x" — green text if > 1.0, red if < 1.0, gray if null
  - If multiplier is null: show "N/A" with tooltip "All emails contain this element" or "No emails contain this element"
  - Vertical multiplier below global: smaller text, same color coding, with vertical name label
  - Sample size: "Based on N emails" in muted text
  - If `lowConfidence`: dim the entire card (opacity-50 or similar), add a small "Low confidence" badge
- Below the element grid, show a CTA subtype section:
  - Small heading: "CTA Type Breakdown"
  - Horizontal list or small bar chart showing average reply rate per CTA subtype (book_a_call, reply_to_email, visit_link, download_resource)
  - Show sample size per subtype
- Total steps analyzed count shown as a note below the grid

**Create `top-templates-list.tsx`** — top templates ranked list:
- Props: `templates: Template[]`, `total: number`, `onSelectTemplate: (t: Template | null) => void`
- Render an ordered list (1-10) of template cards
- Each card shows:
  - Rank number (#1, #2, etc.)
  - Campaign name + step number ("Rise Campaign - Step 1")
  - Subject line (truncated)
  - Composite score as a badge (e.g., "Score: 8.2")
  - Reply rate + interested rate as small metrics
  - Total sends
  - Element pills: small colored badges for each detected element (e.g., "CTA", "Case Study", "Personalization") — green/filled for present elements
  - Copy strategy badge if available
- Clicking a template card calls `onSelectTemplate(template)` to show detail panel
- If no templates: "Insufficient data for template ranking" empty state

**Create `template-detail-panel.tsx`** — expanded template view:
- Props: `template: Template | null`, `onClose: () => void`
- When template is not null, show a right-side panel or modal (use the same slide-out pattern from the replies side panel — translate-x CSS transition)
- Content:
  - Campaign name, workspace, step number
  - Subject line
  - Full email body text in a readable format (pre-wrap, max-height with scroll)
  - Element tags as larger pills below the body, each showing element name + whether present (filled green) or absent (outline gray)
  - Performance metrics: reply rate, interested rate, composite score, total sends
  - Close button
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Element multiplier cards render with green/red color coding, dual baselines, low-confidence dimming, and CTA subtype breakdown. Top templates list shows ranked cards with element pills. Template detail panel shows full body text with element tags. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Analytics page has working Performance/Copy tab toggle
- Copy tab shows subject line rankings, element correlations, and top templates sections
- All components handle empty/loading/error states
- Vertical filter dropdown appears on Copy tab
</verification>

<success_criteria>
- Tab navigation works with URL persistence (deep-linkable to Copy tab)
- Subject line rankings table shows open rate, reply rate, sends with global/per-campaign toggle
- Multiplier cards show global and vertical multipliers side by side with color coding
- Low-confidence correlations are visually dimmed but still visible
- Top 10 templates show element pills and clicking opens detail panel with full body text
- All views respond to workspace and vertical filter changes
</success_criteria>

<output>
After completion, create `.planning/phases/25-copy-performance-analysis/25-03-SUMMARY.md`
</output>
