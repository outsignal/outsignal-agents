---
phase: 51-memory-accumulation-and-full-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md
autonomous: true
requirements: [VAL-02, VAL-03]

must_haves:
  truths:
    - "Dashboard chat with USE_CLI_AGENTS=true returns a response without 500 error"
    - "Dashboard chat with USE_CLI_AGENTS=false returns a response without 500 error"
    - "VERIFICATION.md documents pass/fail for all 5 VAL requirements"
  artifacts:
    - path: ".planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md"
      provides: "Full validation report with pass/fail per requirement"
      contains: "VAL-01"
  key_links:
    - from: "USE_CLI_AGENTS=true"
      to: "POST /api/chat"
      via: "isCliMode() in utils.ts gates CLI delegation"
      pattern: "isCliMode"
    - from: "USE_CLI_AGENTS=false"
      to: "POST /api/chat"
      via: "isCliMode() returns false, routes to inline API agents"
      pattern: "runWriterAgent"
---

<objective>
Smoke-test the dashboard chat bridge in both CLI and API modes, then compile the final VERIFICATION.md documenting pass/fail results for all 5 VAL requirements.

Purpose: Confirm the dashboard bridge works in both modes (no regressions) and produce the milestone validation report that closes v7.0.
Output: VERIFICATION.md with pass/fail for VAL-01 through VAL-05.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-memory-accumulation-and-full-validation/51-CONTEXT.md
@.planning/phases/51-memory-accumulation-and-full-validation/51-RESEARCH.md
@.planning/phases/51-memory-accumulation-and-full-validation/51-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard chat smoke tests (CLI mode and API fallback)</name>
  <files>.planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md</files>
  <action>
Smoke-test the dashboard chat API in both feature flag states. These are simple pass/fail tests -- no quality comparison.

**Step 1 — CLI mode smoke test (VAL-02):**
1. Add `USE_CLI_AGENTS=true` to `.env` (use `printf 'USE_CLI_AGENTS=true\n' >> .env`)
2. Start the dev server: `npm run dev`
3. Open `http://localhost:3000` in the browser, log in, navigate to the chat interface
4. Send a simple message: "What workspaces do we have?"
5. Confirm: response returned without a 500 error. The response content quality is not being evaluated -- just that the route works.
6. Document result: PASS if response returned, FAIL if 500/crash

**Step 2 — API fallback smoke test (VAL-03):**
1. Remove or set `USE_CLI_AGENTS=false` in `.env` (edit the line to `USE_CLI_AGENTS=false` or remove it entirely)
2. Restart the dev server
3. Send the same message through the dashboard chat: "What workspaces do we have?"
4. Confirm: response returned without a 500 error. This validates the pre-v7.0 API agent path still works.
5. Document result: PASS if response returned, FAIL if 500/crash

**Step 3 — Clean up:**
Remove the `USE_CLI_AGENTS` line from `.env` so it returns to default (false) state. Stop the dev server.

**NOTE:** The dashboard requires a real NextAuth session (requireAdminAuth). Use the browser UI, not curl, to avoid auth issues. If the dev server or browser is not available in the executor's environment, document the smoke test as SKIPPED with reason and verify manually via `curl` with a session cookie if possible.

**Alternative if browser unavailable:** Verify the code path works by confirming `isCliMode()` returns the correct value:
- `node -e "process.env.USE_CLI_AGENTS='true'; const {isCliMode} = require('./src/lib/agents/utils'); console.log('CLI mode:', isCliMode())"` (may need to adjust for ESM/CJS)
- Check that `cli-spawn.ts` exports are importable without errors
- This is a weaker validation but confirms the code path exists
  </action>
  <verify>
Check `.env` does not contain `USE_CLI_AGENTS=true` after cleanup: `grep USE_CLI_AGENTS .env` should return nothing or `false`. Verify dev server starts cleanly: `npm run build` completes without errors (confirms all imports and types are valid).
  </verify>
  <done>Dashboard chat smoke-tested in both CLI mode (USE_CLI_AGENTS=true) and API fallback mode (USE_CLI_AGENTS=false). Both modes returned responses without 500 errors, or documented as SKIPPED with rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Compile VERIFICATION.md with all validation results</name>
  <files>.planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md</files>
  <action>
Compile the final validation report using results from Plan 01 (CLI agents + memory + token budget) and this plan's Task 1 (dashboard smoke tests).

Create `.planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md` with this structure:

```markdown
# Phase 51: Memory Accumulation and Full Validation -- VERIFICATION

**Date:** {today's date}
**Workspace:** Rise (primary test target)
**Milestone:** v7.0 Nova CLI Agent Teams

## Validation Results

| ID | Requirement | Result | Evidence |
|----|-------------|--------|----------|
| VAL-01 | E2E campaign generation via CLI | {PASS/FAIL} | {brief evidence} |
| VAL-02 | Dashboard chat with CLI delegation | {PASS/FAIL/SKIPPED} | {brief evidence} |
| VAL-03 | API fallback with USE_CLI_AGENTS=false | {PASS/FAIL/SKIPPED} | {brief evidence} |
| VAL-04 | Memory accumulation (2+ sessions) | {PASS/FAIL} | {files that grew, new entry count} |
| VAL-05 | No context overflow | {PASS/FAIL} | {token count, budget ceiling} |

## Detailed Results

### VAL-01: End-to-End Campaign Generation
{Which agents were invoked, what artifacts produced, any errors encountered}

### VAL-02: Dashboard Chat (CLI Mode)
{What was sent, what was returned, any errors}

### VAL-03: API Fallback
{What was sent, what was returned, any errors}

### VAL-04: Memory Accumulation
**Baseline (before sessions):**
| File | Bytes |
|------|-------|
| campaigns.md | {before} |
| feedback.md | {before} |
| learnings.md | {before} |
| global-insights.md | {before} |

**After sessions:**
| File | Bytes | Growth |
|------|-------|--------|
| campaigns.md | {after} | {+N bytes} |
| feedback.md | {after} | {+N bytes} |
| learnings.md | {after} | {+N bytes} |
| global-insights.md | {after} | {+N bytes} |

**New entries found:**
{List the actual timestamped entries added}

### VAL-05: Token Budget
| Scenario | Tokens |
|----------|--------|
| Current (post-test) | {N} |
| Projected mature (200-line cap) | ~5,000 |
| Ceiling (skill + memory) | ~11,500 |

**Ceiling recommendation:** {documented ceiling}

## Overall Status

{PASS/FAIL -- all 5 VAL requirements must pass for v7.0 milestone to be considered validated}
```

Fill in all values from Plan 01 SUMMARY and this plan's Task 1 results. If any requirement is FAIL, note the specific failure and whether it's a blocker or a known limitation.
  </action>
  <verify>
File exists: `ls .planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md`. Contains all 5 requirement IDs: `grep -c 'VAL-0[1-5]' .planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md` returns at least 5. Contains an Overall Status line: `grep 'Overall Status' .planning/phases/51-memory-accumulation-and-full-validation/51-VERIFICATION.md`.
  </verify>
  <done>VERIFICATION.md created with pass/fail results for all 5 VAL requirements. v7.0 milestone validation is documented and complete.</done>
</task>

</tasks>

<verification>
- Dashboard chat tested in CLI mode (USE_CLI_AGENTS=true) without 500 error
- Dashboard chat tested in API mode (USE_CLI_AGENTS=false) without 500 error
- VERIFICATION.md exists with pass/fail for VAL-01 through VAL-05
- .env cleaned up (no stale USE_CLI_AGENTS=true)
</verification>

<success_criteria>
1. Dashboard chat returns a response in CLI mode without crashing
2. Dashboard chat returns a response in API mode without crashing
3. VERIFICATION.md documents all 5 requirements with pass/fail and evidence
4. .env is clean after testing
</success_criteria>

<output>
After completion, create `.planning/phases/51-memory-accumulation-and-full-validation/51-02-SUMMARY.md`
</output>
