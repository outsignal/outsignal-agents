---
phase: 51-memory-accumulation-and-full-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nova/memory/rise/campaigns.md
  - .nova/memory/rise/feedback.md
  - .nova/memory/rise/learnings.md
  - .nova/memory/global-insights.md
autonomous: true
requirements: [VAL-01, VAL-04, VAL-05]

must_haves:
  truths:
    - "Individual CLI agents (writer, intelligence) complete sessions against Rise workspace without errors"
    - "Full pipeline via /nova rise completes research-leads-writer-campaign chain without errors"
    - "Memory files contain new ISO-timestamped entries after writer and intelligence sessions"
    - "Token budget for Rise workspace memory is documented with current and projected ceiling"
  artifacts:
    - path: ".nova/memory/rise/campaigns.md"
      provides: "Writer session copy wins appended"
      contains: "2026-"
    - path: ".nova/memory/rise/learnings.md"
      provides: "Intelligence session patterns appended"
      contains: "2026-"
  key_links:
    - from: "/nova-writer rise"
      to: ".nova/memory/rise/campaigns.md"
      via: "Agent appends ISO-timestamped insight after session"
      pattern: "\\[2026-"
    - from: "/nova-intelligence rise"
      to: ".nova/memory/rise/learnings.md"
      via: "Agent appends ISO-timestamped insight after session"
      pattern: "\\[2026-"
---

<objective>
Validate the Nova CLI agent system end-to-end against the Rise workspace: run individual specialist agents, verify memory accumulation after sessions, run the full orchestrator pipeline, and document the token budget ceiling.

Purpose: Prove that v7.0's CLI agent system works in production conditions -- agents load memory, produce output, write back learnings, and the full orchestrator chain completes without errors.
Output: Validated CLI agent system with documented memory accumulation evidence and token budget analysis.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-memory-accumulation-and-full-validation/51-CONTEXT.md
@.planning/phases/51-memory-accumulation-and-full-validation/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Individual agent tests and memory accumulation verification</name>
  <files>
    .nova/memory/rise/campaigns.md
    .nova/memory/rise/feedback.md
    .nova/memory/rise/learnings.md
    .nova/memory/global-insights.md
  </files>
  <action>
Run individual CLI agent sessions against the Rise workspace and verify memory accumulation. Execute these steps in order:

**Step 1 — Record baseline memory state:**
Run `wc -c .nova/memory/rise/campaigns.md .nova/memory/rise/feedback.md .nova/memory/rise/learnings.md .nova/memory/global-insights.md` and save the byte counts.
Also run `tail -3` on each file to record the last few lines.

**Step 2 — Run writer agent session:**
Invoke `/nova-writer rise` with the task: "Generate a 3-step PVP email sequence for Rise targeting UK marketing directors at sports teams. After generating, identify what you observed about Rise's copy preferences and record any actionable insight."
Wait for the session to complete. Note: the agent should load Rise memory at invocation and produce a sequence. If the session produces an actionable insight, the agent should append it to campaigns.md or feedback.md per its memory governance rules.

**Step 3 — Run intelligence agent session:**
Invoke `/nova-intelligence rise` with the task: "Analyze Rise's campaign performance across all available campaigns. Identify any patterns in reply rates, subject line effectiveness, or copy angle performance. Record any cross-client insights to global-insights.md."
Wait for the session to complete. The agent should append patterns to learnings.md and/or global-insights.md.

**Step 4 — Verify memory accumulation:**
Run `wc -c .nova/memory/rise/campaigns.md .nova/memory/rise/feedback.md .nova/memory/rise/learnings.md .nova/memory/global-insights.md` again.
Compare byte counts to baseline from Step 1.
Run `tail -5` on each file to inspect new entries.
Check that at least one file has grown and contains a new `[2026-` timestamped entry.

**IMPORTANT — Memory write-back is at the agent's discretion.** If the first writer session does not produce a write-back, run a second session with richer context: "Generate a creative-ideas sequence for Rise targeting UK e-commerce brand managers. Compare the effectiveness of the 'branded kits' angle vs the 'merch volume' angle based on what you know about Rise. Record your observations."

**Pass criteria:** At least 2 of the 4 memory files show growth (new bytes) with valid ISO-timestamped entries after the combined writer + intelligence sessions.
  </action>
  <verify>
Run `wc -c .nova/memory/rise/campaigns.md .nova/memory/rise/feedback.md .nova/memory/rise/learnings.md .nova/memory/global-insights.md` and confirm at least 2 files have grown compared to baseline (seed state: campaigns.md ~957 bytes, feedback.md ~601 bytes, learnings.md ~913 bytes, global-insights.md ~1626 bytes). Run `grep -c '^\[2026-' .nova/memory/rise/learnings.md .nova/memory/rise/campaigns.md .nova/memory/global-insights.md` and confirm at least 1 new entry per file that grew.
  </verify>
  <done>Writer and intelligence agents completed sessions against Rise workspace. At least 2 memory files contain new ISO-timestamped entries proving memory accumulation works.</done>
</task>

<task type="auto">
  <name>Task 2: Full pipeline test and token budget documentation</name>
  <files>
    .nova/memory/rise/profile.md
    .nova/memory/rise/campaigns.md
    .nova/memory/rise/feedback.md
    .nova/memory/rise/learnings.md
    .nova/memory/global-insights.md
  </files>
  <action>
Run the full orchestrator pipeline and document the token budget ceiling.

**Step 1 — Full pipeline test (VAL-01):**
Invoke `/nova rise` with the request: "Create a full campaign for Rise targeting UK marketing directors at companies with 50-500 employees."
This should trigger the orchestrator to delegate through the research-leads-writer-campaign chain. The pipeline will create real DB records (draft campaigns, discovered leads) -- this is expected and harmless per CONTEXT.md.

Wait for the pipeline to complete. Document:
- Which agents were invoked (research, leads, writer, campaign)
- Whether any agent errored
- What artifacts were produced (campaign entity, lead list, copy sequences)
- Total approximate session duration

**Step 2 — Token budget estimation (VAL-05):**
Run: `cat .nova/memory/rise/profile.md .nova/memory/rise/campaigns.md .nova/memory/rise/feedback.md .nova/memory/rise/learnings.md .nova/memory/global-insights.md | wc -c`
Divide the byte count by 4 for a rough token estimate.

Document the token budget analysis:

| Scenario | Tokens |
|----------|--------|
| Current state (after test sessions) | {measured} |
| Projected mature (200-line cap on all 4 files) | ~5,000 |
| Projected mature + global-insights | ~6,250 |
| Skill file + rules overhead | ~5,250 |
| Total ceiling (skill + memory, mature) | ~11,500 |

**Budget ceiling recommendation:** Memory for any single workspace (4 files) should stay under 10,000 tokens (40,000 bytes). The 200-line max per file enforced by governance headers provides a natural cap. This is well within Claude's 200K context window.

**Pass criteria for VAL-01:** Pipeline completes without fatal errors. At least writer + campaign agents are invoked. A draft campaign entity exists in the DB afterward.
**Pass criteria for VAL-05:** Current total memory tokens are under 10,000. Budget ceiling is documented.
  </action>
  <verify>
Confirm pipeline completed by checking: `node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.campaign.findMany({where:{workspace:{slug:'rise'}},orderBy:{createdAt:'desc'},take:1,select:{id:true,name:true,status:true,createdAt:true}}).then(c=>{console.log(JSON.stringify(c));p.\$disconnect()})"` -- should show a draft campaign created during the test. Confirm token budget: `cat .nova/memory/rise/profile.md .nova/memory/rise/campaigns.md .nova/memory/rise/feedback.md .nova/memory/rise/learnings.md .nova/memory/global-insights.md | wc -c` returns a value under 40,000 bytes.
  </verify>
  <done>Full orchestrator pipeline completed for Rise. Token budget documented with current state under 10,000 tokens and ceiling recommendation of 40,000 bytes per workspace.</done>
</task>

</tasks>

<verification>
- Individual writer and intelligence agents completed sessions against Rise
- Memory files show new ISO-timestamped entries (at least 2 files grew)
- Full pipeline (research-leads-writer-campaign) completed without fatal errors
- Token budget documented with current and projected values
- Budget ceiling under 10,000 tokens confirmed
</verification>

<success_criteria>
1. Writer agent session produces copy output and potentially writes to campaigns.md or feedback.md
2. Intelligence agent session produces analysis and potentially writes to learnings.md or global-insights.md
3. At least 2 memory files contain new timestamped entries after test sessions
4. Full pipeline creates a draft campaign entity in the database
5. Token budget is under 10,000 tokens and ceiling is documented
</success_criteria>

<output>
After completion, create `.planning/phases/51-memory-accumulation-and-full-validation/51-01-SUMMARY.md`
</output>
