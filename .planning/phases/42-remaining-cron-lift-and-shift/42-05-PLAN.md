---
phase: 42-remaining-cron-lift-and-shift
plan: 05
type: execute
wave: 2
depends_on: [42-01, 42-02, 42-03, 42-04]
files_modified:
  - vercel.json
autonomous: false
requirements: [CRON-04, CRON-05, CRON-06, CRON-07, CRON-08, CRON-09, CRON-10, DECOMM-03]

must_haves:
  truths:
    - "All 12 Trigger.dev task files deploy successfully to Trigger.dev Cloud via npx trigger.dev@latest deploy"
    - "vercel.json no longer contains the /api/inbox-health/check cron entry"
    - "vercel.json no longer contains the /api/enrichment/jobs/process cron entry"
    - "All cron-job.org jobs for Phase 42 crons are disabled via API same day as Trigger.dev verification"
    - "Each Trigger.dev scheduled task appears in the dashboard with its configured cron schedule"
  artifacts:
    - vercel.json
  key_links:
    - "vercel.json crons array is empty after removing inbox-health and enrichment entries"
    - "cron-job.org jobs disabled via PATCH /jobs/{id} with enabled:false"
---

<objective>
Deploy all Phase 42 Trigger.dev tasks, remove Vercel-native crons from vercel.json, and disable cron-job.org jobs.

Purpose: This is the verification and cutover plan. All task files from Plans 01-04 are deployed to Trigger.dev Cloud, Vercel-native crons are removed to prevent double-processing, and cron-job.org jobs are disabled same-day.

Output: Successful Trigger.dev deployment, clean vercel.json, disabled cron-job.org jobs.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-CONTEXT.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-RESEARCH.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-01-SUMMARY.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-02-SUMMARY.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-03-SUMMARY.md
@.planning/phases/42-remaining-cron-lift-and-shift/42-04-SUMMARY.md

@vercel.json
@memory/infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Trigger.dev Cloud and remove Vercel-native crons</name>
  <files>vercel.json</files>
  <action>
**Step 1: Deploy all tasks to Trigger.dev Cloud:**

```bash
cd /Users/jjay/programs/outsignal-agents && npx trigger.dev@latest deploy
```

This deploys all task files discovered in `trigger/` directory (existing Phase 38-41 tasks plus all new Phase 42 tasks).

Wait for successful deployment. The CLI should report all tasks discovered and deployed.

**Step 2: Remove Vercel-native crons from vercel.json:**

Edit `vercel.json` to remove both cron entries. The file currently has:
```json
{
  "crons": [
    { "path": "/api/enrichment/jobs/process", "schedule": "0 6 * * *" },
    { "path": "/api/inbox-health/check", "schedule": "0 6 * * *" }
  ]
}
```

Replace with an empty crons array:
```json
{
  "crons": []
}
```

This prevents Vercel from firing the old monolith inbox-health and enrichment crons — both are now handled by Trigger.dev split tasks.

**Step 3: Deploy to Vercel** (to pick up vercel.json change + route modifications):

```bash
cd /Users/jjay/programs/outsignal-agents && npx vercel --prod
```

Run this in background.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && cat vercel.json | grep -c "inbox-health" && echo "FAIL: inbox-health still in vercel.json" || echo "PASS: inbox-health removed"</automated>
  </verify>
  <done>
- Trigger.dev deploy succeeds — all 12+ tasks visible in dashboard
- vercel.json has empty crons array
- Vercel production deployment initiated
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Trigger.dev dashboard and disable cron-job.org jobs</name>
  <files>vercel.json</files>
  <action>
Verify all Trigger.dev tasks are running and disable cron-job.org jobs.

**1. Verify Trigger.dev Dashboard:**
- Open Trigger.dev dashboard (https://cloud.trigger.dev)
- Navigate to the project (proj_difpmdhrzxdwuxzzeney)
- Confirm these new scheduled tasks appear with correct cron schedules:
  - `sync-senders` — `0 5 * * *`
  - `bounce-snapshots` — `0 8 * * *`
  - `deliverability-digest` — `0 8 * * 1`
  - `bounce-monitor` — `0 */4 * * *`
  - `poll-replies` — `*/10 * * * *`
  - `domain-health` — `0 8,20 * * *`
  - `inbox-check` — `0 6 * * *`
  - `inbox-sender-health` — `0 6 * * *`
  - `inbox-linkedin-maintenance` — `0 */6 * * *`
  - `invoice-processor` — `0 7 * * *`
  - `enrichment-job-processor` — `0 6 * * *`
- Confirm `campaign-deploy` appears as an on-demand task (not scheduled)

**2. Disable cron-job.org Jobs:**

Look up the job IDs for Phase 42 crons in the cron-job.org dashboard or via API:
```bash
curl -s -H "Authorization: Bearer $CRON_JOB_ORG_API_KEY" https://api.cron-job.org/jobs | jq '.jobs[] | {id, title, url, enabled}'
```

For each Phase 42 job (poll-replies, domain-health, bounce-monitor, sync-senders, bounce-snapshots, deliverability-digest, inbox-health), disable via API:
```bash
curl -X PATCH "https://api.cron-job.org/jobs/{JOB_ID}" \
  -H "Authorization: Bearer $CRON_JOB_ORG_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"job":{"enabled":false}}'
```

Note: The known job ID for domain-health is 7363961 (from MEMORY.md). Other job IDs must be looked up.

**3. Confirm No Double-Processing:**
- After disabling, wait for one poll-replies cycle (10 min) and verify in Trigger.dev dashboard that poll-replies ran successfully
- Check there are no duplicate webhook events in the DB from both systems
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && echo "Checkpoint: manual verification of Trigger.dev dashboard + cron-job.org disabling required"</automated>
  </verify>
  <done>
- All 11 scheduled tasks + 1 on-demand task visible in Trigger.dev dashboard with correct schedules
- All Phase 42 cron-job.org jobs disabled via API
- poll-replies has completed at least one successful Trigger.dev run
- No double-processing observed
  </done>
</task>

</tasks>

<verification>
1. `npx trigger.dev@latest deploy` succeeds
2. vercel.json has empty crons array
3. All 11 scheduled tasks + 1 on-demand task visible in Trigger.dev dashboard
4. cron-job.org Phase 42 jobs disabled (not deleted — can re-enable if needed)
5. poll-replies completes at least one successful run on Trigger.dev
</verification>

<success_criteria>
- Trigger.dev Cloud deployment successful with all Phase 42 tasks
- Vercel-native crons removed from vercel.json
- cron-job.org jobs disabled same day as Trigger.dev verification
- No double-processing between old and new systems
</success_criteria>

<output>
After completion, create `.planning/phases/42-remaining-cron-lift-and-shift/42-05-SUMMARY.md`
</output>
