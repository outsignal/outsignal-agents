---
phase: 41-ai-cron-migration
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified: []
autonomous: false
requirements: [CRON-01, CRON-02, CRON-03]

must_haves:
  truths:
    - "All three scheduled tasks appear in the Trigger.dev dashboard with their configured cron expressions"
    - "At least one successful run is recorded for each task in the Trigger.dev dashboard run history"
    - "cron-job.org jobs for retry-classification, generate-insights, and snapshot-metrics are disabled"
    - "No double-processing window — cron-job.org jobs disabled same day Trigger.dev tasks verified"
  artifacts: []
  key_links:
    - "Trigger.dev dashboard shows retry-classification, generate-insights, snapshot-metrics schedules"
    - "cron-job.org has 3 fewer active jobs"
---

<objective>
Deploy the three scheduled tasks to Trigger.dev production, verify they run successfully, and disable the corresponding cron-job.org jobs.

Purpose: This is the cutover step — the three most timeout-vulnerable crons move from cron-job.org + Vercel to Trigger.dev managed infrastructure. Disabling cron-job.org jobs prevents double-processing.

Output: Three verified Trigger.dev schedules running, three cron-job.org jobs disabled.
</objective>

<execution_context>
@/Users/jjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-ai-cron-migration/41-CONTEXT.md
@.planning/phases/41-ai-cron-migration/41-01-SUMMARY.md

@trigger/retry-classification.ts
@trigger/generate-insights.ts
@trigger/snapshot-metrics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy scheduled tasks to Trigger.dev</name>
  <files></files>
  <action>
Deploy the Trigger.dev tasks to production:

1. Run `npx trigger.dev@latest deploy` from the project root to deploy all tasks in `trigger/` to Trigger.dev Cloud.

2. Verify deployment succeeded by checking the output for all three new task IDs: `retry-classification`, `generate-insights`, `snapshot-metrics`.

3. If deployment fails, check for:
   - TypeScript compilation errors (fix and retry)
   - Missing env vars in Trigger.dev dashboard
   - Prisma binary target issues

The deploy command bundles all files in `trigger/` and pushes them to Trigger.dev Cloud. The `schedules.task()` declarations automatically register their cron schedules.
  </action>
  <verify>
    <automated>cd /Users/jjay/programs/outsignal-agents && npx trigger.dev@latest deploy 2>&1 | tail -20</automated>
  </verify>
  <done>
- Deploy command completes successfully
- Output confirms all three scheduled tasks are registered
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify scheduled tasks in Trigger.dev dashboard and disable cron-job.org jobs</name>
  <files></files>
  <action>
**Verify in Trigger.dev dashboard (https://cloud.trigger.dev):**

1. Navigate to the project's Schedules page
2. Confirm three new schedules exist:
   - `retry-classification` — cron: `*/30 * * * *`
   - `generate-insights` — cron: `0 */6 * * *`
   - `snapshot-metrics` — cron: `0 0 * * *`
3. Wait for the next scheduled run of retry-classification (max 30 min wait) or trigger it manually
4. Confirm the run completes successfully in the Runs page — check that:
   - Run status is "Completed" (not "Failed" or "Timed out")
   - Output shows `classified` count and no unhandled errors
   - Duration is visible (proves it ran beyond what Vercel would allow)

**Disable cron-job.org jobs (same day):**

1. Log into cron-job.org
2. Find and disable the jobs for:
   - retry-classification (every 30 min)
   - generate-insights (every 6 hours)
   - snapshot-metrics (daily at midnight)
3. Do NOT delete the jobs — just disable them (can re-enable if rollback needed)

**Decision #7 from CONTEXT.md:** Each job disabled the SAME DAY its Trigger.dev task is verified. No double-processing window.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — user verifies Trigger.dev dashboard + disables cron-job.org jobs"</automated>
  </verify>
  <done>
- All three scheduled tasks show in Trigger.dev dashboard with correct cron expressions
- At least one successful run recorded for retry-classification
- cron-job.org jobs for all three crons are disabled (not deleted)
- No double-processing window
  </done>
</task>

</tasks>

<verification>
1. Trigger.dev dashboard shows 3 new scheduled tasks with run history
2. retry-classification run log shows replies classified without timeout
3. cron-job.org has 3 disabled jobs (retry-classification, generate-insights, snapshot-metrics)
4. No errors in Trigger.dev run logs for the verified runs
</verification>

<success_criteria>
- All three AI cron jobs are running on Trigger.dev infrastructure
- cron-job.org equivalents are disabled
- At least one successful run proves the timeout constraint is eliminated
- No double-processing occurred during cutover
</success_criteria>

<output>
After completion, create `.planning/phases/41-ai-cron-migration/41-02-SUMMARY.md`
</output>
