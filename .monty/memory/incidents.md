<!-- incidents.md | monty | seeded: 2026-04-03 -->
<!-- Write governance: APPEND only, with ISO timestamp. Max 200 lines. -->

# Monty -- Incidents & QA Findings

<!-- Append entries as: [ISO date] -- [incident or finding] -->

[2026-04-15T16:45:00Z] -- INCIDENT (critical): BL-061 campaign-deploy CLI creates duplicate EB campaigns instead of reusing/transitioning existing ones. Triggered 2026-04-15 12:17-12:18 UTC when the new `scripts/cli/campaign-deploy.ts` was run against 17 campaigns (7 lime-recruitment + 10 1210-solutions). For each of the 11 email campaigns the deploy helper called `EmailBisonClient.createCampaign()` TWICE (first attempt + retry), producing 22 orphan EB drafts. All 22 subsequently failed mid-upload with 422 "The title field is..." on the sequence step POST, leaving the DB campaign stuck in status='deployed' but with a 'failed' CampaignDeploy record and zero-lead/zero-sender EB debris. Root cause: the deploy path has no "reuse existing emailBisonCampaignId if present" branch — every invocation (including retry within the same CLI run) calls createCampaign afresh. Trigger for recurrence: ANY future campaign-deploy run that retries a failed deploy WILL reproduce the bug until BL-062 lands. Impact: user-visible duplicates in the EB UI, DB campaigns stuck in 'deployed' status blocking re-approval flow, wasted EB campaign IDs (numeric sequence burned). Cleanup: 22 EB drafts deleted via `src/lib/emailbison/client.ts::deleteCampaign`; 11 Campaign rows flipped deployed→approved; 11 CampaignDeploy rows flipped failed→rolled_back. Secondary observation: 4 of the 5 1210-solutions LinkedIn deploys in the same batch failed with `prisma.campaignSequenceRule.createMany()` schema errors — separate from the EB duplication bug, needs its own investigation (possible BL-062 companion item). Classification: critical until BL-062 ships (blocks safe re-runs of the CLI).

(No incidents recorded yet)

[2026-04-15T13:00:00Z] -- BL-060 batch B1 QA review (commits fffe6413 chat.ts, d99c0d83 monty.ts). PASS / GO verdict. tsc clean, eslint no-restricted-imports count=0, npm run chat + npm run monty both exit 0 with correct signposts, no forbidden imports, both files < 80 lines, shebang + JSDoc match nova.ts pattern. 6 findings raised, all info/low severity: (1) triplicate signpost header pattern across nova/chat/monty is copy-paste-prone — candidate for shared `printLauncherHeader()` helper if a 4th launcher is added; (2) shebang `#!/usr/bin/env npx tsx` is decorative when invoked via `npm run chat` which goes through tsx directly — acceptable but cosmetic; (3) `.monty/memory/decisions.md` was not updated with a BL-060 batch B1 entry before this QA pass (orchestrator-only write, not a dev/QA defect); (4) signpost says "Interactive orchestrator sessions are now handled by Claude Code slash commands" which duplicates the heading line's "moved to Claude Code" — minor wording tightening opportunity; (5) monty.ts has no plural slash commands section like chat.ts does — if future monty specialists (`/monty-dev`, `/monty-qa`, `/monty-security`) become user-invocable slash commands, this signpost will need to grow; (6) chat.ts title still says "Outsignal Orchestrator" while the rest of the system/docs increasingly use "Nova" — consider renaming to "Nova" for consistency with `/nova` slash commands listed below it.

[2026-04-16T08:05:00Z] -- Phase 5.5 DB cleanup QA review (commit 184db22c `_deploy-rebuild-phase-5-5-clear-stale-ebid.ts`). GO verdict for Phase 6a canary. All 8 verification tasks passed. (1) 11 target campaigns all status='approved' and emailBisonCampaignId=null, updatedAt 2026-04-16T07:51:12-15Z within single 3s transaction window. (2) AuditLog action='campaign.emailbisoncampaignid.cleared' count=11, every entityId matches one of the 11 expected prefixes, every metadata object has phase='Phase 5.5', relatedCommit='ad3105de', and non-null numeric oldEbId in range 64-77 (64,66,67,70,71,72,73,74,75,76,77 — exactly the 11 deleted EB drafts from ad3105de). (3) Rollback history intact: every one of the 11 Campaigns has ≥1 CampaignDeploy with status='rolled_back'. (4) Out-of-scope active survivors verified: `cmneqhyd3…` (1210 LinkedIn Healthcare) and `cmnehed0a…` (Lime LinkedIn C5) both status='active', channels=["linkedin"], ebId=null, updatedAt 2026-04-15T12:17-18Z (22h old, well outside last-hour window). NOTE: DB has 21 status='active' Campaigns total (not 2 — PM's "exactly 2" referred to the 2 LinkedIn rollback survivors from the Phase 0 scope, not all-active); the other 19 are unrelated pre-existing active campaigns on outsignal/rise/lime/blanktag, all last-updated 2026-03-06 to 2026-04-10, none collateral. (5) Collateral check: 11 Campaigns updated in last 10min, all 11 in-scope, zero extras. (6) Idempotency re-check: `prisma.campaign.count({ status:'approved', emailBisonCampaignId:{not:null}, deploys:{some:{status:'rolled_back'}} })` = 0. Adjacent-stale probe: zero Campaigns with any rolled_back deploy AND non-null ebId — no stragglers cluster-wide. (7) Commit stat: only 1 file touched (the maintenance script, 190 lines added), zero src/ / schema.prisma / test changes. (8) `npx tsc --noEmit` clean (exit 0, no output). Findings (4): F1 low (idempotency UX) — re-running the script's dry-run now throws FATAL "Expected 11 candidates, found 0. Escalate." The hard-stop at line 59-63 conflates "exactly the 11 known stragglers" with "we are done" — a second ops invocation for sanity-check reasons exits non-zero and looks like a failure. Post-execution the script is effectively single-use. Suggestion for future one-shots: replace `if (n !== expected) throw` with `if (n === 0) { log("Nothing to do, script already executed"); return; }` then the expected-count assertion. Non-blocking for 6a since the job is done. F2 low (guard-logic verification) — confirmed email-adapter.ts:209-241: with emailBisonCampaignId=null the flow takes the `else` branch at line 235 (`ebClient.createCampaign`) — 404 safety (line 228-232) is only reachable when preExistingEbId is non-null, so the 11 cleared rows will take the fresh-create path cleanly. No residual risk from the guard. F3 info (audit-log schema self-describing) — metadata includes phase, relatedCommit, oldEbId, reason — auditors have full context. However `entityType='Campaign'` + `entityId=<cuid>` with no workspace slug in metadata means cross-workspace audit scans need a JOIN against Campaign to bucket by client. Minor — suggest adding workspaceSlug to metadata in future maintenance scripts. F4 info (hardcoded oldEbId in script memory) — `oldEbIds` map is built in-process from the initial findMany (lines 101-102) and used inside the transaction. If another process were to racily update the pre-tx rows between the findMany and the transaction start, the audited oldEbId could differ from what was actually cleared. Accept the risk for a manual one-shot run in a quiet ops window; not a pattern to replicate for automated flows. Confidence HIGH. No critical/high/medium findings. Phase 6a (1 email + 1 LinkedIn canary re-deploy) is CLEARED.

[2026-04-15T20:10:00Z] -- BL-068 Phase 1a + Phase 2 recon QA review. GO verdict for Phase 2 execution (code fix). All verification tasks A-E passed with high confidence. (A) DB state clean: `campaign.count({ deployedAt NOT NULL, status='approved', workspace ∈ [lime, 1210] })` = 0 (expected 0). AuditLog rows = 15 total (4 Phase 1 + 11 Phase 1a amendment), exactly matching the claim breakdown. Audit window 18:46:08Z - 18:54:40Z matches Phase 1a 19:50:00Z completion timestamp in decisions.md. (B) Commit-skip rationale holds: `git ls-files | grep bl068` returns ZERO tracked files — `_bl068-clear-deployedat.ts` never entered the index, so `rm` not `git rm` was correct. `git diff --cached` is empty. Tracked working-tree changes (email-adapter.ts, emailbison/client.ts, emailbison/types.ts, .env.example, memory files, 2 test files) pre-date Phase 1a and are explicitly preserved per brief §1c "preserve uncommitted working-tree files". Empty commit correctly avoided. (C) Recon claims 100% accurate: sequencing.ts:370-392 (createSequenceRulesForCampaign) and line 387 (`position: step.position`) verified; linkedin-adapter.ts:106-118 (postConnectRules map with `position: step.position`) verified; email-adapter.ts:126-140 (for-loop writing `position: step.position` into ebClient.createSequenceStep) verified; schema.prisma:1110 (`position Int` non-nullable, no default) verified; schema.prisma:1114 (`variantKey String?` with A/B comment block 1112-1115) verified; `npx prisma migrate status` confirms exactly 3 pending: add_sender_channel, make_person_email_nullable, add_inbox_alert_dedup_timestamps — none touch CampaignSequenceRule. (D) Shape drift CONFIRMED and broader than dev's single-example claim: ALL 4 stuck 1210 LinkedIn campaigns (cmneq1z3i, cmneq93i8, cmneqa5r5, cmneqixvz) have identical step keysets `[body, channel, delayDays, notes, stepNumber, subject, subjectB, type]` — zero have `position`. Lime email campaign cmnpwzv9e has `[body, delayDays, notes, position, subjectLine, subjectVariantB]` — has `position`, no `stepNumber`. Drift is systemic across channels, not a one-off. Root cause located: `src/lib/channels/types.ts:89` defines `UnifiedStep.stepNumber` but `src/lib/linkedin/sequencing.ts:320` defines `LinkedInSequenceStep.position` — two type systems for the same concept. Writer/save path emits `stepNumber` for LinkedIn (see src/lib/agents/writer.ts, src/lib/agents/types.ts) but `position` for email (agents/types.ts:99,123,133). (E) Phase 1 no-commit precedent genuinely applies: 2026-04-15T19:15:00Z decisions entry explicitly documents "NO commit — Phase 0 landed the schema in a prior commit, `prisma generate` clean, no tracked source files modified in Phase 1. Empty commit avoided per brief §1c. Maintenance script ... stays untracked." Phase 1a is structurally identical (DB + untracked script, zero tracked file changes). Not a stretch. Findings (6, below): F1 medium — shape drift is WIDER than BL-068 scope (stored-JSON vs type-system mismatch is a class of bugs not just this one); F2 low — typecast at linkedin-adapter.ts:64 (`as LinkedInSequenceStep[]`) masks runtime drift, silently produces `NaN - NaN` on line 86 sort; F3 low — linkedin-adapter.ts:90 `sorted.unshift({ position: 0, type: "profile_view", delayDays: 0 })` uses `delayDays` but LinkedInSequenceStep interface expects `delayHours` — preexisting secondary drift not in dev's recon; F4 info — Phase 2 code-only fix (defensive `step.position ?? step.stepNumber ?? idx + 1`) does not solve the upstream Writer inconsistency — consider a follow-up BL item to normalise UnifiedStep and the stored-JSON shape; F5 info — `variantKey` deferral is correct (nullable, by-design) but Writer does not emit it at all, so A/B variant paths are untestable end-to-end — separate from BL-068; F6 info — 8 untracked `_bl068-*.ts` maintenance scripts now present, none tracked; convention (per decisions.md 19:15Z) is `_` prefix = one-off tool, correctly applied. Confidence HIGH. No critical or high severity findings. Phase 2 cleared to proceed with the agreed code-only defensive-read fix + tests.

[2026-04-15T14:15:00Z] -- BL-060 step 7/8 QA review (commit 96bacc63 refactor(icp): relocate extractIcpCriteria). PASS / GO verdict. All 8 mandatory checks passed: (1) behavior preservation confirmed — campaign.ts:216 call site unchanged, caller passes string + uses result via JSON.stringify(icpCriteria) which preserves full schema shape; import path `@/lib/icp/extract-criteria` correct; (2) tests meaningful — happy-path asserts returned value equals mock, asserts generateObject called once, asserts prompt CONTAINS the input description string (protects against argument decoupling); schema tests cover valid payload, wrong-type (titles not array), missing required field (industries), and optional omission (keywords); 5/5 pass via `npx vitest run`; (3) eslint clean on both files, `npx eslint . | grep -c no-restricted-imports` = 0, `src/lib/icp/**` correctly present in allow-list at eslint.config.mjs; (4) tsc clean; (5) campaign.ts leftover check — NOVA_MODEL still imported (still used at line 378 `model: NOVA_MODEL`), no orphan generateObject/anthropic, both eslint-disable comments removed; (6) stale "Claude Haiku for cost efficiency" JSDoc removed from new file, correct one-line JSDoc preserved; (7) scope — exactly 3 files touched per `git show --stat`; (8) downstream importers of campaign.ts (9 CLI scripts) only import `campaignTools` — no one was importing the moved symbols, so no break. Findings (4, all low/info severity, none blocking): (A) [low / missing-test] test file does not verify that `anthropic(NOVA_MODEL)` is invoked with the correct model identifier — the happy-path asserts the prompt contains the description but never reads `callArgs.model`, so a future regression replacing `NOVA_MODEL` with a wrong literal would pass tests silently; suggest adding `expect(vi.mocked(anthropic)).toHaveBeenCalledWith(NOVA_MODEL)` (import NOVA_MODEL from @/lib/agents/types in the test); (B) [low / missing-test] schema tests do not cover the case where `keywords` is present but not an array — since keywords is optional z.array(z.string()).optional(), a caller passing `keywords: "b2b"` (string instead of array) would be rejected at runtime but this path is untested; add a safeParse test with `keywords: "b2b"` asserting success=false; (C) [info / pattern-inconsistency] `src/lib/icp/scorer.ts` (the sibling allow-listed file referenced in the eslint rule message) uses `temperature: 0` pinning for determinism per 2026-04-11 weekend audit decision, but the relocated `extractIcpCriteria` in extract-criteria.ts:34-38 does NOT pass a temperature parameter — default is 1, so ICP criteria extraction remains non-deterministic across identical inputs; this is pre-existing behavior (not introduced by this commit) but the relocation was the opportunity to align with the scorer.ts convention; recommend follow-up BL item to pin temperature: 0 for consistency with established ICP determinism policy; (D) [info / pattern-inconsistency] new file uses template literal with embedded `"${description}"` at line 37 with no escaping — if a user supplies an icpDescription containing a `"` or `${` sequence it flows into the prompt verbatim (prompt injection surface already existed pre-refactor, preserved as-is); signal campaign creation is admin-only via CLI so impact is low, but worth noting as inherited risk. Not blocking. Commit is safe to merge; 1 of 8 BL-060 files remaining (src/lib/agents/runner.ts per the earlier batch B2 QA note; campaign.ts is now effectively clean post-7/8).

[2026-04-15T15:30:00Z] -- BL-061 QA review (CLI deploy surface: src/lib/campaigns/deploy-campaign.ts, scripts/cli/campaign-deploy.ts, route.ts refactor, 2 test files / 34 tests). Verdict: SHIP-WITH-FIXES (no blocking critical/high, one medium behavioural-regression around main() error-handling, remainder low/info). Verification: `npx tsc --noEmit` clean. `npx vitest run` = 37 fail / 785 pass; stash-compared baseline = 37 fail / 751 pass; net +34 new tests all green, zero regressions. Orchestrator memo cited baseline as 709 but actual head baseline is 751 (35 existed already in the 34 new test delta window — cosmetic note, not a defect). 37 failures are the known BL-055 exclusionEntry mock-setup suite (api-routes, emailbison-client, notifications, promotion-icp-gate, channel-enrichment, linkedin-sender) — unchanged by this work. Parity audit vs pre-refactor route (commit 6dc4f1fc, 117 lines): (1) response shapes byte-identical for all 4 non-retry outcomes — happy path `{deployId, status:"pending"}`, 404 "Campaign not found", 400 "Both leads and content must be approved before deploying", 409 "Campaign is not in approved status (may have already been deployed)"; (2) retry branch untouched except campaign-load hoisted INTO the `if (retryChannel)` block (saves one getCampaign call on retry-with-missing-campaign — behaviorally equivalent because `retryChannel=null` skips to the helper which re-loads); (3) atomic updateMany({where:{id,status:'approved'}}) race guard preserved verbatim in deploy-campaign.ts:130-133; (4) tasks.trigger("campaign-deploy",{campaignId,deployId}) signature untouched — no fields added, no reordering; (5) auditLog row shape identical (action="campaign.deploy", entityType="Campaign", entityId=campaignId, metadata={campaignName, workspaceSlug, channels, deployId}) — NOTE: refactor ADDS `deployId` to metadata, which pre-refactor did not include (pre-refactor metadata was {campaignName, workspaceSlug, channels} only). That is a minor observable change in AuditLog rows (metadata is richer, not narrower) — flagged below as info. Findings (8): (A) [medium / behavioural-regression] src/lib/campaigns/deploy-campaign.ts:148-163 — if prisma.campaignDeploy.create() succeeds and tasks.trigger("campaign-deploy") subsequently throws (Trigger.dev outage, network failure, auth rotation), the campaign is left in status='deployed' with a CampaignDeploy row status='pending' that no worker will ever pick up, and the helper throws so no AuditLog row is written. The CLI main() then throws out of the for-loop, runWithHarness catches with ok:false, but the per-ID outcomes for IDs already-processed in that batch are LOST — they never made it into the results[] array. Net effect: operator sees one JSON error, cannot tell from stdout which of 17 IDs are now in zombie 'deployed' status. This is pre-existing risk INHERITED from the pre-refactor route (same ordering), so not a regression strictly speaking — but the CLI amplifies exposure because it runs serially and the spec explicitly asks for stop-on-first-failure + partial progress reporting. Suggestion: wrap the per-ID initiateCampaignDeploy() call in the CLI's for-loop in a try/catch so a thrown helper still records a per-ID error outcome in results[] before breaking, and emit a distinct "zombie deploy — verify manually" warning if the throw happened after updateMany. Separately consider compensating logic in the helper (wrap updateMany+create+trigger in a transaction with rollback on trigger fail, OR flip status back to 'approved' in a catch block). Severity medium not high because this only fires during Trigger.dev outage — operator recovery path exists (query CampaignDeploy rows with status='pending' AND createdAt recent) but it's not documented anywhere. (B) [low / missing-test] scripts/cli/campaign-deploy.ts:147-154 readStdin() and main() have ZERO direct test coverage. Tests cover the pure helpers (splitStdinIds, parseCliArgs, classifyResult) but never exercise: stdin → splitStdinIds → parseCliArgs wiring; the serialized for-loop; post-verify success path; stop-on-first-failure break; runWithHarness envelope output. A spec-conformance test harness that spawns the compiled CLI via child_process with piped stdin would close this gap — 34 tests sounds like coverage theatre for a 316-line script that ships 17 live deploys. Suggestion: add at minimum an integration test that mocks initiateCampaignDeploy + getCampaign and invokes main() directly, asserting: stdin path works, stop-on-first-failure breaks correctly, post-verify populates verifiedStatus/verifiedDeployedAt. (C) [low / missing-test] src/__tests__/deploy-campaign.test.ts — NO test exercises the specific race where updateMany count=0 AND the pre-load `campaign.status === 'approved'` (i.e., the campaign WAS approved when loaded but got deployed by a concurrent call between load + updateMany). In that scenario the current code falls into the `not_approved` branch (because neither 'deployed' nor 'active') which leaks a misleading error to the caller. Real-world: if two operators race the same ID, the second caller sees "not_approved" even though the true state is "already_deployed". Suggestion: expand the `returns code='already_deployed'` test to a second variant where the loaded status IS 'approved' but updateMany returns count=0 — assert code='not_approved' and acknowledge in a comment that this is a known ambiguity (or refetch post-updateMany to disambiguate). (D) [low / pattern-inconsistency] scripts/cli/campaign-deploy.ts default is LIVE (--dry-run opt-in) while scripts/maintenance/patch-content-approved.ts default is DRY-RUN (--apply opt-in). Dev agent self-flagged. Rationale exists (CLI is called by Campaign Agent which expects LIVE to "just work") but this divides the ops surface into two conventions. Suggestion: document the convention split in a comment block in campaign-deploy.ts so future maintainers don't flip the default to "match patch-content-approved"; consider adding --live-confirm flag as belt-and-braces for the 17-campaign batch use-case. (E) [info / behavioural-change] src/lib/campaigns/deploy-campaign.ts:170-175 auditLog metadata now includes `deployId` which the pre-refactor route did NOT include. This is a strict superset (richer audit trail, easier forensics) but any downstream AuditLog consumer / dashboard / Slack formatter that did `JSON.stringify(metadata)` and compared byte-for-byte would see drift. Checked: `grep -rn "action.*campaign.deploy" src/ scripts/` shows no consumer reads the metadata shape back, so risk is zero today, but noting for audit traceability. Not a regression — improvement. (F) [low / dead-code] scripts/cli/campaign-deploy.ts:204 `export interface SummaryEnvelope` is exported but never imported by any external file (checked via grep). Used internally only as main() return type. Suggestion: drop the `export` or add a test that asserts shape (which would justify the export). Low priority. (G) [info / pattern-inconsistency] scripts/cli/campaign-deploy.ts uses the `invokedAsScript` guard at line 305-316 while sibling sister scripts/cli/campaign-publish.ts (23 lines) + campaign-status.ts call runWithHarness() at top level. The guard is correct HERE because tests import this file and need the pure helpers WITHOUT the script side-effect (runWithHarness would call process.exit). Siblings don't export testable helpers so don't need it. Worth a comment noting the reason (current comment says "not when imported in tests" which is adequate). Acceptable. (H) [info / observation] src/lib/campaigns/deploy-campaign.ts:98 comment says `channels defaults to ["email"]` after formatCampaignDetail but the pre-refactor route line 68 had an explicit `campaign.channels ?? ["email"]` fallback which the helper drops. Verified CampaignDetail.channels is typed `string[]` (non-optional) and set via `parseJsonArray(raw.channels) as string[] ?? ["email"]` in operations.ts:213, so the fallback is already enforced one level up. Dropping the redundant `?? ["email"]` is correct — just flagging that removing defensive nullish coalescing during a refactor is usually a pattern-consistency smell and deserves the explicit comment the dev added (line 98). Confirmed safe. Blocking findings for SHIP: none. Finding (A) should be a follow-up BL item but should NOT block the 17-campaign deploy — the failure mode requires an active Trigger.dev outage which has not occurred in the past 30 days per incident history. Recommend: ship, run dry-run first across all 17, then live deploy with an operator watching stderr. File BL-NNN for finding (A)'s recovery/zombie-deploy scripting post-deploy.

[2026-04-15T13:30:00Z] -- BL-060 batch B2 QA review (commits 2166dd2e chat.ts color fix #F0FF7A->#635BFF, b4c37f19 delete scripts/setup-client.ts, d459554e delete scripts/lime-linkedin-writer.ts). PASS / GO verdict. Verification: `npx tsc --noEmit` clean, `npx eslint . | grep no-restricted-imports | wc -l` = 0, git log --format=full confirms all 3 commits carry exact spec titles + Co-Authored-By: Claude Opus 4.6 (1M context) trailer. `git diff --stat 3e933e1f..d459554e` confirms exactly 3 files touched: chat.ts (+1/-1), lime-linkedin-writer.ts (-69), setup-client.ts (-369). Zero unrelated scope creep. Color fix verified: scripts/chat.ts line 14 `chalk.bold.hex("#635BFF")` matches monty.ts line 14 and nova.ts lines 36/81. Deletions verified via `ls`: both files gone. Residual references (2 total, both acceptable): .monty/memory/backlog.json:629 (BL-060 notes, orchestrator-only write) and .monty/memory/decisions.md:49 (batch B2 pre-delegation log). Zero references in src/, scripts/, package.json, docs/, .claude/. 5 findings raised, all info/low severity: (1) [info] commit 2166dd2e is a cosmetic color fix bundled into a BL-060 batch but correctly NOT claiming BL-060 in its subject — commit body self-declares "not a BL-060 task but bundled with the in-flight batch" which is honest and traceable; (2) [low] setup-client.ts commit body flags "hardcoded API tokens are pre-existing in git history — out of scope for BL-060, flagged separately" but QA cannot find a separate backlog entry for credential exposure — if those tokens are still valid and reachable in public git history this warrants a dedicated BL-NNN for credential rotation, not just a trailing note on a delete commit (PM action item: confirm whether a BL item exists, create one if not); (3) [info] backlog.json:629 BL-060 notes field still lists all 8 original offender files (5 now complete: analyze-website, generate-copy, chat, monty, setup-client, lime-linkedin-writer); stale but orchestrator-only write governance; (4) [info] #635BFF hex literal now duplicated across 3 files (chat.ts:14, monty.ts:14, nova.ts:36+81) — 4 occurrences total — candidate for shared `BRAND_PURPLE` constant if a 4th launcher lands; (5) [low] chat.ts line 15 heading still reads "Outsignal Orchestrator — moved to Claude Code" while every slash command listed below is `/nova*` — batch B1 QA raised this as finding #6; the color fix makes the stale branding more visually prominent without fixing it. Dev did not need to fix this (out of scope for batch B2) but it remains an open doc-drift item. 3 of 8 BL-060 files remaining (src/lib/agents/runner.ts, src/lib/agents/campaign.ts, plus whichever count reconciles with PM's running tally — QA observes batch A closed 2, batch B1 closed 2, batch B2 closed 2, total 6 of 8).

[2026-04-15T19:40:00Z] -- Monty Phase 0 (BL-068 rollback) QA review. Verdict: VERIFIED WITH FINDINGS (ship, file follow-ups). Core rollback claims all hold: 4 Campaign rows (cmneqa5r50003p8rk322w3vc6, cmneq1z3i0001p8ef36c814py, cmneqixvz0003p871m8sw9u7o, cmneq93i80001p8p78pcw4yg9) = status='approved', contentApproved=true, leadsApproved=true, emailBisonCampaignId=null; 4 CampaignDeploy rows = status='rolled_back' with "--- BL-068 ROLLBACK 2026-04-15 ---" suffix appended to `error` field; 4 AuditLog rows exist with action='campaign.status.bl068_rollback', rolledBackBy='Monty Phase 0', full before/after transitions, full verbatim Prisma trace preserved on CampaignDeploy.linkedinError/error and BL-068 notes. Commit f3e831a2 touches ONLY .monty/memory/backlog.json (+11 lines), has Co-Authored-By trailer, 4 commits ahead of origin/main (f3e831a2, ad3105de, 535b3d1e, 468eb16b) confirming no remote push. BL-068 ticket is parseable JSON, has all 9 required keys, verbatim trace + root-cause hypothesis + Phase 2 scope note all present. Working tree diff unchanged by Phase 0 (files modified 13:11-13:19, rollback ran 18:34, commit 19:35). Scope discipline clean: only 4 expected AuditLog rows in the 18:00Z+ window, no other 1210 or cross-workspace mutations today by Monty. 1210 API token in DB (31|0E00...) differs from historical setup-client.ts value (17|WZWC...) confirming dev agent's key-mismatch claim — though note the token lives in Workspace.apiToken (DB), not .env.local as implied by the brief wording. Findings (3): (A) [medium / data-integrity] All 4 rolled-back Campaigns still have `deployedAt` = 2026-04-15T12:18Z populated despite status='approved'. prisma/schema.prisma:791 documents deployedAt as "When deployed to EmailBison/LinkedIn" but these campaigns were never actually deployed (no EB ID, no LinkedIn rules inserted — Prisma validation rejected atomically pre-insert). Leaving deployedAt populated means any downstream query `WHERE deployedAt IS NOT NULL` (dashboards, intelligence agent, cached-metrics) will treat these as live-deployed campaigns. Fix: include `deployedAt: null` in the rollback UPDATE (or file a follow-up ticket). (B) [medium / trace-fidelity] AuditLog.metadata.stackTraceExcerpt is the string "Invalid prisma.campaignSequenceRule.createMany() invocation: Argument position is missing." (90 chars, no backticks, no newlines, no data payload) — it's a summary, not a real excerpt. The full verbatim trace IS preserved on CampaignDeploy.linkedinError + BL-068 notes so forensic value is not lost, but the brief explicitly asked for "REAL trace containing 'campaignSequenceRule.createMany' and 'Argument `position` is missing'" — the backticks around `position` were stripped during JSON escaping, so a grep for the exact phrase `Argument \`position\` is missing` against AuditLog metadata will miss. Not blocking because source-of-truth rows retain the verbatim trace. Suggestion: next rollback helper should either store the full multi-line trace (JSONB can hold it) or rename the field stackTraceSummary to avoid the implied fidelity claim. (C) [low / governance-drift] Monty Phase 0 is a "campaign operations" mutation (flipping Campaign.status on 4 1210-solutions LinkedIn campaigns, writing Nova-namespace business rows) performed by the platform-engineering agent. Per delegation-rules.md this is arguably a boundary breach — campaign lifecycle transitions normally route through Nova → Campaign Agent. The justification (cleanup of a platform-bug-induced zombie state) is defensible but sets precedent for future "just this once" cross-team writes. Suggestion: BL-NNN to formalise an emergency-rollback carve-out in the rules, naming Monty as the agent allowed to reverse platform-bug-caused zombie deploys with an AuditLog requirement. Phase 1 NOT blocked — all rollback effects are reversible, core state is consistent, memory + backlog are clean, commit hygiene is tight.

[2026-04-15T20:20:00Z] -- QA FINDING (high): BL-068 fix (commit 8016c97f) is three-part within the channel-adapter path ONLY. Two additional readers of `campaign.linkedinSequence` still contain the silent-cast + NaN-sort bug and will reproduce BL-068 on their own code paths once stepNumber-shaped JSON reaches them: (1) `src/app/api/linkedin/plan/route.ts:195-205` - daily planner does `) as LinkedInSequenceStep[]` with local interface declaring `position: number` required, then `a.position - b.position` sort (NaN when undefined); (2) `src/lib/pipeline/signal-campaigns.ts:390-403` - signal campaign deploy has identical cast + sort pattern. Signal campaigns auto-deploy without client approval so silent corruption risk is higher. Neither site throws; both produce non-deterministic step ordering downstream. 6/6 unit tests pass, baseline 37 BL-055 failures unchanged, tsc clean. Verdict GO for Phase 3 but BL-069 scope must be expanded to enumerate all 3 reader sites (adapter/planner/signal-campaigns) and the 3 local `LinkedInSequenceStep` interface duplicates that need consolidation into `src/lib/linkedin/types.ts`.

[2026-04-15T20:50:00Z] -- Phase 3 EmailBison deploy rebuild adversarial QA. NO-GO verdict (conditional): 1 critical, 3 high, 5 medium, 3 low, 2 info. Test count independently verified: WITHOUT Phase 3 uncommitted files 793 pass / 53 fail; WITH Phase 3 806 pass / 40 fail. Phase 3 IMPROVES the suite by +13 pass / -13 fail — dev's claim "identical to baseline" is actually underselling. Scope claim is MISLEADING: dev said "touched ONLY email-adapter.ts + decisions.md" but `git diff --stat` shows src/lib/emailbison/client.ts (+105), src/lib/emailbison/types.ts (+38), .env.example (+3), 2 new test files (email-adapter-deploy.test.ts, emailbison-client-schedule.test.ts) — the client methods (createSchedule, updateSchedule, getSchedule, attachTagsToCampaigns) and their types are all new. Per EB spec cross-check (docs/emailbison-dedi-api-reference.md lines 152-198, 260-269, 1118-1127) the new client methods match verbs and paths exactly. Findings below (ordered by severity, most recent QA of this refactor).

F1 [CRITICAL] race-condition: src/lib/channels/email-adapter.ts:181-217 — Campaign.emailBisonCampaignId has NO unique constraint in prisma/schema.prisma:757 (only `@@index`, not `@@unique` and NOT in a transaction). Two concurrent executeDeploy() invocations for the same campaignId will BOTH observe preExistingEbId=null on the Step 1 read and BOTH call createCampaign() — reproducing exactly the BL-061 duplicate-EB-campaign bug the refactor is supposed to fix. The read-then-write between prisma.campaign.findUnique (via getCampaign) and prisma.campaign.update (Step 2) is racy. The retryDeployChannel path (deploy.ts:204) makes this especially reachable: admin clicks "retry" twice in quick succession, or the Trigger.dev task is dispatched twice due to idempotency-key drift. Mitigation options: (a) add @unique to Campaign.emailBisonCampaignId + catch P2002 on Step 2 and re-read; (b) wrap Step 1-2 in a `prisma.$transaction` with `SELECT ... FOR UPDATE` (Postgres only — SQLite dev will noop); (c) acquire a row-level advisory lock keyed on campaignId before Step 1. Handover §5 explicitly requires "idempotent re-run" — without a concurrency guard the re-run is only idempotent in the single-invoker case. FIX REQUIRED before Phase 4.

F2 [HIGH] idempotency-gap: src/lib/channels/email-adapter.ts:253-274 — Step 3 sequence-step upsert only calls getSequenceSteps when preExistingEbId != null. But the bug path is a deploy where Step 1 SUCCEEDED (createCampaign OK, ebCampaignId returned) and Step 3 PARTIALLY SUCCEEDED (e.g. 2 of 5 steps POSTed, then 422 on step 3). On re-run the same Campaign row still has emailBisonCampaignId set from the earlier Step 2 write, so preExistingEbId IS non-null — this branch is correct for true re-runs. BUT the INNER loop (line 260-274) increments `emailStepCount` for every step the agent re-POSTs, so emailStepCount is a count of EXISTING + NEWLY-POSTED on this re-run, which is correct for re-runs only because existingSteps.length was used as the starting point (line 259). However the local interface `EmailSequenceStep.position` is optional (line 83 — `position: number` is required but the cast at line 241 is `as EmailSequenceStep[]` applied to `unknown[]` returned by getCampaign). If a stored emailSequence step is missing `position`, `existingPositions.has(undefined)` is always false and that step is re-POSTed on every re-run — silent duplication. BL-068 was exactly this class of bug (position vs stepNumber shape drift). No Zod parse on emailSequence; the BL-068 discipline is NOT applied symmetrically.

F3 [HIGH] spec-mismatch / dead-code: src/lib/channels/email-adapter.ts:433-444 — Step 8 tags is documented as an intentional no-op because Workspace lacks an ebTagIds field. But the problem is: the 17 stuck 1210+lime campaigns have no tag attachment in the CURRENT production flow either, so "preserve existing no-op" preserves a FUNCTIONAL GAP, not a behaviour. The attachTagsToCampaigns client method (+its tests) was shipped in this Phase 3 but never wired. That's dead code on arrival. Escalation note in decisions.md lets this slide as "schema follow-up" but the handover §5 has 10 steps — step 8 being a silent no-op violates the "10-step target flow" contract. Either the step should be removed from DEPLOY_STEP numbering (so the compensating-tx `[step:N]` tags never reference a dead stage), OR the field should land now as Phase 3b before Phase 4. Ambiguity: `[step:8]` will never appear in emailError, so operators have no way to distinguish "tags not attached because no workspace config" from "tag attachment silently failed".

F4 [HIGH] error-swallowed: src/lib/channels/email-adapter.ts:657-674 — getSequenceSteps() wraps the EB API call in `try { ... } catch {}` (line 672) and falls through to the DB fallback on ANY error — rate limit, auth failure, 500, network, the lot. No logging. This is a preexisting pattern from pre-Phase-3 but Phase 3 kept it. Violates the Phase 3 hard rule "every catch block: either logs or rethrows, never silently swallows". The comment "// Fall through to Campaign.emailSequence fallback" is not a log. At minimum add `console.warn(`[email-adapter] getSequenceSteps EB fetch failed for campaignId=${ref.emailBisonCampaignId}: ${err}`)`. Same issue at line 709 — `catch {}` with no log on JSON.parse failure.

F5 [MEDIUM] compensating-tx-gap: src/lib/channels/email-adapter.ts:488-520 — Step 2 is `Promise.all([campaignDeploy.update, campaign.update])`. If the campaignDeploy update succeeds but the campaign update rejects (transient DB blip, FK issue), Promise.all rejects with the second error but the first write has already committed. On retry, CampaignDeploy.emailBisonCampaignId is set but Campaign.emailBisonCampaignId is null → Step 1 idempotency check (line 185 reads campaign.emailBisonCampaignId) will be null → createCampaign is called AGAIN → orphan EB campaign (the very thing BL-061 filed). The correct pattern is a prisma.$transaction or: persist to Campaign FIRST (single source of truth), THEN CampaignDeploy. Handover §5 Step 2 is "mirror onto Campaign + CampaignDeploy" — the mirroring order matters. Recommend: campaign.update sequentially first, then campaignDeploy.update.

F6 [MEDIUM] spec-mismatch: src/lib/channels/email-adapter.ts:71-77 — LAUNCHED_STATUSES = {queued, launching, active}. The inline comment describes the happy transition but the EB reference doc (docs/emailbison-dedi-api-reference.md) actually does NOT enumerate campaign statuses anywhere I could grep — the spec documents the `resume` verb (line 117-121) and `pause` (109-113) and `archive` (125-129) but not the valid resulting state machine. Dev's set is an EDUCATED GUESS from prior behaviour. If EB returns e.g. `"draft"` or `"resumed"` (plausible values), Step 10 throws even though resume succeeded — campaign is live but DB says failed. Extensibility concern: if EB adds `"warming_up"` or renames `"launching"` → `"ramping"`, every deploy flips to failed despite being healthy. Recommend: convert to a REJECT-LIST pattern (reject {draft, paused, completed, failed, archived, cancelled}; accept everything else) — more resilient to EB API drift and still catches the BL-061-style "didn't take" case.

F7 [MEDIUM] race-condition: src/lib/channels/email-adapter.ts:302-313 — Step 4 dedup check is `findFirst WebhookEvent where eventType='EMAIL_SENT'`. A lead who was CREATED into EB on a prior attempt but hasn't been EMAILED yet (EB is draft) will have NO EMAIL_SENT event, so this re-creates the lead on every retry. EB createLead is claimed idempotent-by-email in the comments (line 282) but the client source at line 549 is a plain POST with no upsert semantics documented in the EB reference. If EB errors 422 on duplicate email, the whole retry fails at Step 4. Worse: if EB returns a DIFFERENT lead.id (new record, dup email), the `createdLeadIds[]` array accumulates orphan IDs and attach-leads may succeed against a ghost record. Needs: either an EB-side "findOrCreateLead" with the `findLeadByEmail()` method already in client.ts:728, or verification that EB createLead truly dedupes server-side (spec is silent).

F8 [MEDIUM] idempotency-gap: src/lib/channels/email-adapter.ts:371-392 — Step 5 schedule upsert. On a TRUE re-run where Step 5 previously failed mid-flight (schedule partially created on EB), `preExistingEbId != null` so we GET the schedule — but getSchedule returns `null` on 404 AND returns empty `{}` when EB omits data envelope (client.ts:393). `existingSchedule != null` treats both as "exists" (because `{}` is truthy). If EB actually returns `{}` meaning "not configured", we PUT (updateSchedule) instead of POST (createSchedule), and PUT on a non-existent schedule is spec-undefined (EB docs only document PUT for update). Recommend: also check `Object.keys(existingSchedule).length > 0` or parse the response shape with Zod.

F9 [MEDIUM] compensating-tx-gap: src/lib/campaigns/deploy.ts:131-144 — executeDeploy auto-transitions Campaign.status from "deployed" → "active" when CampaignDeploy.status === "complete". The EmailAdapter zero-leads short-circuit (line 339-353) marks emailStatus=complete with `emailError="no_leads_to_deploy"`. finalizeDeployStatus then sees allComplete=true, status="complete", and the auto-activate fires (line 135-138). Campaign gets flipped to "active" with zero leads deployed to EB. This is arguably wrong — a zero-leads deploy should stay in a distinct status (e.g. remain "deployed" or introduce "deployed_no_leads") so the admin notices. Handover §5 doesn't address this ambiguity.

F10 [LOW] documentation-gap: src/lib/channels/email-adapter.ts:505 — `[step:N]` encoding is a pragma that has NO parser anywhere in the codebase. grep for `\[step:` returns only the write-site. Dashboards, retry tooling, and the UI deploy page will show the raw `"[step:5] schedule update failed: 422..."` string to operators. Acceptable short-term but the pattern needs to be either (a) documented in schema.prisma comment on CampaignDeploy.emailError with an example, or (b) parsed by the retryDeployChannel route to auto-resume from the failing step. Currently retry always resets emailStatus="pending" and re-runs from Step 1 — idempotency guards will skip already-done work but the `[step:N]` tag is purely informational.

F11 [LOW] pattern-inconsistency: src/lib/channels/email-adapter.ts:241 — `(campaign.emailSequence ?? []) as EmailSequenceStep[]`. Unchecked cast on `unknown[]` from operations.ts:64. This is EXACTLY the BL-068 antipattern (Phase 2 banned silent casts on EB-shape data). Internal emailSequence JSON is a different source than EB API responses, but the class of bug is identical: writer-stored shape may drift (stepNumber vs position, delayDays vs delayHours) and the cast hides it until runtime. Recommend: `EmailSequenceSchema = z.array(z.object({ position: z.number(), ... })).parse(campaign.emailSequence ?? [])` — matches Phase 2 discipline symmetrically.

F12 [LOW] pattern-inconsistency: src/lib/channels/email-adapter.ts:332 — `await new Promise(resolve => setTimeout(resolve, 100))` throttle hardcoded inside the lead-creation loop. EmailBisonClient.RATE_LIMITS already exports `delayBetweenCalls: 20` (client.ts:40) — the adapter ignores the client's own declared rate-limit config and uses a different value. Duplication violates the "use established client" rule in api-client-rules.md. Extract into either client.ts (as a public `withRateLimit` wrapper) or import RATE_LIMITS and use its value.

F13 [INFO] missing-test: src/__tests__/email-adapter-deploy.test.ts:1-392 — the new test suite is happy-path + zero-leads + zero-senders but has NO test for: (a) Step 1 idempotency path (preExistingEbId != null → getCampaign reuse), (b) Step 1 404 rethrow (isRecordNotFound), (c) Step 3 partial sequence upsert (existingPositions dedup), (d) Step 5 updateSchedule vs createSchedule branching, (e) Step 10 wrong-status rejection. Dev claims "email-adapter-deploy.test.ts improved 1/5 → 3/3" which is true for the happy paths it covers but the idempotency guards — the whole POINT of Phase 3 — have zero test coverage. Phase 4 should land these.

F14 [INFO] scope-drift: dev's scope claim "touched ONLY email-adapter.ts + decisions.md" is materially incorrect. `git diff --stat HEAD`: email-adapter.ts +355, client.ts +105, types.ts +38, .env.example +3, 2 untracked test files (392 + 225 lines), .nova/memory/yoopknows/learnings.md +2, memory files. This wasn't a harmful scope violation (the client.ts additions are spec-accurate and the tests are improvements), but the claim misrepresents the blast radius of the review the PM is greenlighting. For Phase 4 the dev should list ALL files modified, not just the primary surface.

Baseline verification: stashed Phase 3 uncommitted files (email-adapter.ts, client.ts, types.ts), ran `npx vitest run` → 793 pass / 53 fail. Popped stash, re-ran → 806 pass / 40 fail. Phase 3 net IMPROVES the suite by 13 tests. Dev's "identical to baseline" claim was wrong but in their favour. Handover §4 citing 809/37 does NOT match either count — likely stale or included a different set of uncommitted files at the time.

Verdict: NO-GO on Phase 4 until F1 (race) and F2+F3+F4 (idempotency/dead-code/swallowed-error) are addressed. F5-F8 should be triaged into Phase 3b or filed as follow-ups. F9-F14 are acceptable carry-forward.

[2026-04-15T21:30:00Z] -- Phase 3.5 BL-070 QA review (commit 5a13ceba close concurrent-deploy race via @unique on Campaign.emailBisonCampaignId). Verdict: SHIP-WITH-FIXES. `npx tsc --noEmit` clean. `npx vitest run src/lib/channels/__tests__/email-adapter-race.test.ts` 1/1 pass. Migration file present and well-formatted. Schema correctly drops redundant `@@index([emailBisonCampaignId])` now that `@unique` implies a btree index. Happy-path behaviour preserved. 9 findings follow — 1 high, 3 medium, 5 low/info. None block Phase 4 GO in principle, but findings F1 and F2 are strongly recommended before Phase 4 to avoid repeating BL-070 on the signal-campaign code path.

Finding F1 [high / pattern-inconsistency] src/lib/agents/campaign.ts:283-329 — PARALLEL RACE on `Campaign.signalEmailBisonCampaignId` NOT covered by this fix. The signal campaign activation path (`activateSignalCampaign`) reads workspace, calls `ebClient.createCampaign({ name })` at line 298, writes `signalEmailBisonCampaignId` at line 320-329 via `prisma.campaign.update`. `prisma/schema.prisma:785` declares `signalEmailBisonCampaignId Int?` with NO `@unique` constraint. Two concurrent activations of the same signal campaign (admin double-click, Trigger.dev retry, chat agent double-dispatch) would reproduce BL-070 EXACTLY on this sibling code path — two createCampaign calls on EB, one write wins, one EB draft orphaned. BL-070 and Phase 3.5 only closed the static-campaign deploy race. Suggestion: add `@unique` to `signalEmailBisonCampaignId`, write a migration mirroring this one, and apply the same P2002-catch pattern at src/lib/agents/campaign.ts:320. Alternatively file as BL-073 and block Phase 4 on it (signal campaigns are evergreen — an orphan in prod stays orphaned until a reconciler ships, which per BL-072 is still unwritten).

Finding F2 [medium / test-failure potential] src/lib/channels/email-adapter.ts:271-313 — the P2002 catch does NOT inspect `err.meta.target`. It only checks `err.code === "P2002" && preExistingEbId == null`. Today this is safe because the `prisma.campaign.update({ data: { emailBisonCampaignId } })` call sets exactly ONE unique-constrained field and can only violate that constraint. But Campaign has other uniques — `@@unique([workspaceSlug, name])` at schema line 802 does not fire on this update (name isn't set), but any FUTURE addition of `@unique` to `emailBisonSequenceId` (line 761) would cause this catch to misfire: it would treat an unrelated P2002 as the BL-070 race and delete a live EB campaign. Suggestion: narrow the catch to `err.code === "P2002" && (err.meta?.target as string[] | undefined)?.includes("emailBisonCampaignId") && preExistingEbId == null`. One extra predicate, eliminates a class of future-regression bugs. Also add a test: future-unique on other Campaign field fires P2002 -> catch should rethrow, not delete EB campaign.

Finding F3 [medium / missing-test] src/lib/channels/__tests__/email-adapter-race.test.ts — does not cover 3 of the 4 documented failure modes in the email-adapter code: (a) orphan `ebClient.deleteCampaign` throws at line 300-308 (BL-072 fallback — the whole reason BL-072 was filed). Test should assert adapter CONTINUES to resume/verify against winner and logs the "CLEANUP FAILED" warning. Currently only the happy cleanup is tested. (b) re-read returns null at line 287-294 (defensive rethrow path) — test should assert the thrown error message mentions "re-read returned null" and is tagged `[step:2]` in the final CampaignDeploy.emailError. (c) P2002 fires in `preExistingEbId != null` branch — code at line 275 rethrows in this case (correct) but no test verifies that. Without these tests, the three explicit defensive branches in the Phase 3.5 code are unexercised and would silently regress. Minimum add: three more `it()` cases covering each branch.

Finding F4 [medium / missing-test] src/lib/channels/__tests__/email-adapter-race.test.ts — NOT a true concurrency test. The test simulates the LOSER'S view only, with `prisma.campaign.update` hard-coded to reject P2002 on the first call. A real race test would use `Promise.all([adapter.deploy(params), adapter.deploy(params)])` against an in-memory or transactional test DB (or a stubbed Prisma with a sequenced gate). The current approach CANNOT catch: winner-also-fails-later bugs, ordering bugs where the re-read happens before winner's write commits, or deadlock-style stalls where both deploys hit the unique at once. Test documentation says "catches P2002 on Campaign.update, deletes the orphan EB campaign, and completes the deploy against the winning EB campaign ID" — accurate to what the test does, but the PR narrative implies concurrent-race coverage that this mock-level test does NOT provide. Suggestion: add an integration test (Testcontainers-backed Postgres or prisma-mock's sequenced update) that runs Promise.all and asserts exactly one EB campaign remains persisted. If integration-test scaffolding isn't feasible in this phase, DOCUMENT the gap loudly in the test file's top-level comment and file as a BL-074 for Phase 5.

Finding F5 [medium / migration-safety] prisma/migrations/20260415000000_add_unique_campaign_emailbison_id/migration.sql — migration preamble CLAIMS "Pre-check (run against prod before generating this migration) confirmed zero duplicate non-null emailBisonCampaignId rows" but this claim is only in a comment, not a defensive check in the DDL itself. If a duplicate got introduced between pre-check and deploy-time (unlikely but possible — the BL-061 incident on 2026-04-15 produced EB duplicates; if any of those mapped to the same Campaign before cleanup landed this would trigger), `CREATE UNIQUE INDEX` fails partway through with no rollback. Also: `CREATE UNIQUE INDEX` (not `CONCURRENTLY`) takes ACCESS EXCLUSIVE on Campaign for the duration of the index build — blocking all Campaign reads/writes. On today's Campaign row count it's sub-second, but a deploy-hygiene standard would use `CREATE UNIQUE INDEX CONCURRENTLY` which Prisma migrations do NOT generate by default. Suggestion: add a `DO $$ BEGIN IF EXISTS (SELECT "emailBisonCampaignId" FROM "Campaign" WHERE "emailBisonCampaignId" IS NOT NULL GROUP BY "emailBisonCampaignId" HAVING COUNT(*) > 1) THEN RAISE EXCEPTION 'duplicate emailBisonCampaignId rows present — resolve before applying this migration'; END IF; END $$;` preamble. For future migrations on larger tables, consider a Prisma convention for `CREATE INDEX CONCURRENTLY` (requires custom migration workflow since Prisma wraps DDL in a txn). This one can ship as-is for Phase 3.5 given Campaign size, but note it as a pattern-to-establish before any similar unique index lands on a large table (e.g. Person, TargetListPerson).

Finding F6 [low / pattern-inconsistency] src/lib/channels/email-adapter.ts:314-317 — `campaignDeploy.update` moved OUT of the `Promise.all` and now runs AFTER `campaign.update`. This is behaviourally correct (needs `ebCampaignId` reassigned post-race), but changes the atomicity guarantees: previously both writes went in parallel, so a crash between them could leave the system with EITHER both written OR neither written; now it is sequential, so a crash after Campaign.update (or between it and CampaignDeploy.update) leaves Campaign.emailBisonCampaignId set but the CampaignDeploy row without the ID. This is a recoverable state (operator can SELECT the EB ID from Campaign and patch CampaignDeploy) but is strictly less consistent than the prior Promise.all — the prior parallel form had the same failure mode because Prisma Promise.all doesn't transact, but the new sequential form widens the window. Suggestion: wrap both updates in a `prisma.$transaction([...])` so they commit atomically, and apply the same race-catch logic to the transaction. Low-severity because recovery is manual but trivial; noting because "moved out of Promise.all" is the kind of silent consistency change that deserves a comment in the source.

Finding F7 [low / missing-test] src/lib/channels/__tests__/email-adapter-race.test.ts:143-149 — `targetListPerson.findMany` mock returns a single lead with email="a@acme.com", and `webhookEvent.findFirst` returns null. But the race loser, having switched `ebCampaignId` from 777 to 555 (the winner's EB campaign), now runs Steps 4-10 AGAINST THE WINNER'S EB CAMPAIGN. The winner may have ALREADY completed those steps (leads already attached, senders attached, resume already called). The test mocks all downstream EB calls as idempotent-successful, but does NOT assert the idempotency story — it doesn't verify that re-running createLead / attachSenderEmails / resumeCampaign on a campaign that the winner already activated is safe. Code comments claim "idempotent on re-run" but the test doesn't exercise the "winner finished first, loser runs steps 3-10 second" sequence. Suggestion: add a test where ebClient.resumeCampaign mock THROWS a 4xx "already active" error on the loser's call, and verify the adapter either catches-and-continues (if that's the contract) or fails cleanly with a step:9 tag.

Finding F8 [low / documentation] src/lib/channels/email-adapter.ts:259-263 — comment claims "The idempotent reuse branch in Step 1 can never trigger P2002 because it reuses the already-persisted ID — the update becomes a no-op write of the same value." This is TRUE for the specific case where the stored `preExistingEbId` matches what Step 1 re-reads, BUT the comment would be incorrect in a hypothetical scenario where Step 1's idempotent reuse reads an ID from memory (the loaded `campaign` object at line 205) and between then and line 267 another process wipes `emailBisonCampaignId` to null AND a third process writes a new ID. Update becomes writing `preExistingEbId` onto a row that now has a different non-null value → P2002. This is astronomically unlikely in practice but the comment's absolutism is slightly misleading. Suggestion: soften to "In practice the idempotent reuse branch does not trigger P2002 because the update writes the same value that was already persisted; a P2002 here would indicate concurrent manual mutation, which we intentionally do not attempt to recover from." Low priority docs.

Finding F9 [info / consistency] `prisma/schema.prisma:802` still has `@@unique([workspaceSlug, name])` on Campaign — healthy separation of concerns, not a conflict with the new `@unique` on emailBisonCampaignId. Verified via Grep. The new `@unique` is on the correct field (`Int?` nullable, Postgres treats multiple NULLs as distinct under UNIQUE per the migration comment which is correct Postgres semantics). No dead code from the dropped `@@index` since Prisma doesn't generate helpers off @@index names. No other code path writes `emailBisonCampaignId` besides the race-guarded line 267 and the deploy-row writer on 314 (confirmed via grep output in review context: only email-adapter.ts:269/316 write the field; monitoring, webhooks, operations, analytics, lookup, helpers all READ it). Clean.

Phase 4 recommendation: GO with caveats. Phase 3.5 closes BL-070 on the static-campaign path and the code is sound in isolation. However, F1 (signal-campaign sibling race) is the highest-leverage follow-up — it's the same bug class in an adjacent code path, and if Phase 4 introduces any concurrency into signal-campaign activation (e.g. Trigger.dev schedule parallelism) it will fire. Suggest: file BL-073 for F1 NOW, and gate Phase 4 work that touches signal-campaign lifecycle on BL-073 landing. F2-F7 are quality improvements; none block Phase 4. If Phase 4 explicitly does NOT touch signal-campaign code, F1 can defer, but the risk should be documented in the Phase 4 plan.

[2026-04-16T08:30:00Z] -- Phase 6a canary deploy FAILURE adversarial QA. NO-GO for Phase 6b confirmed. Identical 422 error to 2026-04-15 pre-rebuild failure (deployId cmo00nd6g000pp8j53qk6h1ky), verified byte-for-byte from final-state.log. The Phase 3 rebuild DID NOT FIX the original bug. 1 CRITICAL, 2 HIGH, 3 MEDIUM, 2 LOW findings.

F1 [CRITICAL / test-failure] src/lib/emailbison/client.ts:250-268 — createSequenceStep() POSTs a malformed payload. The EB API reference (docs/emailbison-dedi-api-reference.md:260-269 deprecated AND 1326-1334 v1.1) explicitly requires the POST body to contain `title` (string, REQUIRED) and `sequence_steps` (array, REQUIRED). The client posts the flat object `{position, subject, body, delay_days}` — neither `title` nor `sequence_steps`. The 422 error text `"The title field is required"` + `"The sequence steps field is required"` is the EB API reporting EXACTLY that. EmailAdapter.ts:359-373 compounds the bug by calling createSequenceStep in a for-loop (one POST per step) while the EB endpoint expects a SINGLE POST with a batched array. The client is ALSO hitting the deprecated `/api/campaigns/{id}/sequence-steps` path when the live v1.1 path is `/api/campaigns/v1.1/{id}/sequence-steps`. Impact: EVERY email deploy is guaranteed to 422 at Step 3. Root cause for the Phase 6a canary failure. Suggestion: rewrite createSequenceStep to accept the full sequence array + title, POST a single v1.1 request, collapse the for-loop at email-adapter.ts:359-373, and add a contract test asserting `JSON.parse(postBody)` contains both `title` and `sequence_steps`.

F2 [HIGH / missing-test] src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts:191-195 + src/__tests__/emailbison-client.test.ts (no createSequenceStep POST test exists) + src/lib/channels/__tests__/email-adapter-race.test.ts:137,278 — NO test validates the POST body shape of createSequenceStep against the EB OpenAPI spec. Every mock is `handler: () => mockResponse({ data: { id: 1 } })` returning success regardless of payload. The integration test at line 191 explicitly says `// Step 3 — createSequenceStep` and still returns unconditional success. This is coverage theatre — the Phase 4 "integration test" proves the adapter CALLS createSequenceStep but proves nothing about the wire payload. A real contract test would have caught F1 on the first dry-run. Suggestion: add `expect(JSON.parse(opts.body as string))` body-shape assertions to at least one integration case, and add a direct client-level unit test that inspects the fetch body for `title` and `sequence_steps`.

F3 [HIGH / pattern-inconsistency] trigger/campaign-deploy.ts:14-19 + src/lib/campaigns/deploy.ts:176-197 — Trigger.dev task has `retry: { maxAttempts: 2 }`. When the first attempt throws the 422 at Step 3, executeDeploy catches, marks CampaignDeploy.emailStatus='failed' + emailError (email-adapter.ts:610-616), then RETHROWS (line 617) → propagates up through executeDeploy → Trigger.dev RETRIES the entire task. Second attempt creates a SECOND EB campaign because the catch in email-adapter.ts deliberately does NOT roll back Campaign.emailBisonCampaignId (line 598-602 comment: "we deliberately do NOT roll back Campaign.status or Campaign.emailBisonCampaignId here"). On retry, the reuse branch (line 212) hits getCampaign(78) → EB still has draft 78 → continues through Step 3 → 422 again. This explains why the final-state log shows EB 78 AND 79 (two orphans) but CampaignDeploy.emailBisonCampaignId flipped 78→79 between snapshots. Evidence: EB 78 created at 08:00:53Z, EB 79 created at 08:01:10Z (17s apart — consistent with Trigger.dev 5s retry backoff + reprocessing). The P2002 race guard at email-adapter.ts:266-313 does NOT fire because this is serial (same deployId, same campaignId, different task invocations) — not a P2002 concurrent race. Compensating-tx was designed for parallel deploys only. The test `email-adapter-race.test.ts` tests the CONCURRENT race exclusively and provides zero coverage of the RETRY path. The compensating-tx system did not fire at all for this failure — EB 78 became orphaned with no delete, EB 79 was created fresh. Suggestion: either (a) set retry.maxAttempts=1 on the Trigger.dev task since the adapter owns its own retry via withRetry, OR (b) add an idempotent orphan-cleanup to the catch block that deletes the current ebCampaignId if Step 3+ fails BEFORE rethrowing.

F4 [MEDIUM / pattern-inconsistency] src/lib/campaigns/deploy-campaign.ts:130-158 + src/lib/campaigns/deploy.ts:174-197 — Campaign.status rollback gap. initiateCampaignDeploy flips Campaign.status approved→deployed OPTIMISTICALLY (line 130-133) BEFORE the Trigger.dev task fires. When the deploy subsequently fails (422 in this case), the outer catch in executeDeploy (deploy.ts:174-197) only updates CampaignDeploy.status — it does NOT roll back Campaign.status, does NOT clear Campaign.deployedAt, does NOT clear Campaign.emailBisonCampaignId. End-state: Campaign row says status='deployed', deployedAt=non-null, emailBisonCampaignId=79 — anyone looking at Campaign alone would conclude success. Only CampaignDeploy.status='failed' in the sibling table reveals the truth. Commit 184db22c (Phase 5.5) was required because THIS EXACT drift happened after the 2026-04-15 rollback. The fix was a one-shot SQL cleanup, not a systemic rollback hook. Suggestion: in executeDeploy's catch block, if CampaignDeploy.status transitioned to 'failed' AND Campaign.deployedAt was set by this deploy invocation, flip Campaign.status back to 'approved', clear deployedAt + emailBisonCampaignId, and emit an audit log row. Otherwise every failed deploy leaves zombie state requiring manual Phase-5.5-style maintenance.

F5 [MEDIUM / missing-test] scripts/cli/campaign-deploy.ts:283-310 — the CLI reports `verdict=deployed verifiedStatus=deployed` for this run even though the deploy failed ~30s later. The post-verify at line 285 just reads Campaign.status (which was flipped to 'deployed' optimistically at line 132 of deploy-campaign.ts) — it does NOT poll CampaignDeploy.status. The CLI printed `success=1 failure=0 total=1` to stderr; the deploy genuinely failed. For a canary run where the whole point is to detect failure, the CLI has no feedback loop with the async task. Suggestion: after verifiedStatus lookup, also fetch the most recent CampaignDeploy for the campaign and include CampaignDeploy.status + .emailError in the outcome — OR poll CampaignDeploy.status='complete' vs 'failed' with a timeout (e.g. 60s) before declaring success.

F6 [MEDIUM / pattern-inconsistency] scripts/maintenance/_phase6a-preflight.ts:33-44 — canary campaign IDs are HARDCODED. The script is single-use (a second run against the same campaigns after deploy would fail the `emailBisonCampaignId=null` check now that id=79 is persisted). Directory convention established by sibling scripts (`_bl054-*.ts`, `_bl068-*.ts`, `_bl070_*.ts`) is BL-ID-prefixed + date-suffixed for one-shots (e.g. `_deploy-rebuild-phase-5-5-clear-stale-ebid.ts`). This script is named `_phase6a-preflight.ts` which drops the date and does not include a BL ID. Reusable variant would need `--ids` plus `--expectedLeadCount` as CLI args. Suggestion: either (a) rename to `_phase6a-preflight-2026-04-16.ts` to signal single-use + remove after cleanup, OR (b) accept `--ids` + `--channel` + `--expectedLeadCount` to generalise for Phase 6b + future canaries.

F7 [LOW / pattern-inconsistency] src/lib/channels/email-adapter.ts:282-313 — the BL-070 race guard catch block re-reads Campaign to get the winning EB ID, then calls ebClient.deleteCampaign on the orphan. But if Step 3+ fails LATER in the flow (like Phase 6a did), the ebCampaignId stored on Campaign is NOT an orphan — it's the half-deployed campaign. The same ID gets fully orphaned only if the outer flow fails without cleanup. There is no Step 3+-failure orphan-cleanup parallel to the Step 2 race-guard cleanup. Suggestion: acknowledge this asymmetry in email-adapter.ts comments, OR extend the outer catch (line 590-618) to delete ebCampaignId if currentStep >= 3 AND the adapter cannot recover.

F8 [LOW / missing-test] src/lib/channels/__tests__/email-adapter-race.test.ts tests P2002 scenarios only. Zero tests cover the retry-from-Trigger.dev scenario (same deployId re-entering adapter.deploy because Trigger.dev retried the task). A test that invokes adapter.deploy twice serially with preExistingEbId transitioning from null → <id> and asserts no duplicate createCampaign + no orphan delete would have surfaced F3. Suggestion: add a retry-idempotency test that covers the serial-retry path distinct from the concurrent-race path.

Summary: the Phase 3 rebuild's idempotency + compensating-tx story holds ONLY for concurrent-race scenarios. Serial-retry + wire-payload-shape were never actually tested end-to-end. The Phase 6a canary exposed both simultaneously. Top finding (F1) is a direct wire-format bug that has been latent since before Phase 3 and every test since has mocked around it rather than validated the contract. Retry.maxAttempts=2 on the Trigger.dev task (F3) explains the dual-EB-orphan mystery without needing to invoke the BL-070 race guard at all.

---

[2026-04-16T08:45:00Z] — Phase 6a-rollback QA (script: scripts/maintenance/_phase-6a-rollback-email-canary.ts, untracked; decisions.md entry [2026-04-16T08:15:00Z]). Adversarial review after monty-dev executed rollback. tsc clean, eslint clean, no tests touched. **Verdict: CONCERN — does NOT block Phase 6.5 fix work, but the dev's explanation of the first-attempt failure is incorrect and propagating it into memory risks future agents chasing the wrong ghost.**

F1 [HIGH / pattern-inconsistency] scripts/maintenance/_phase-6a-rollback-email-canary.ts:151,184 — catch block narrows `e as EmailBisonApiError` and reads `err.isRecordNotFound || err.status === 404`. But src/lib/emailbison/client.ts:328-341 `getCampaign()` can throw TWO unrelated error classes: `EmailBisonApiError` (from request() on HTTP non-2xx, has `.status` + `.isRecordNotFound` getter) AND `EmailBisonError` from src/lib/emailbison/types.ts:336 (thrown when EB returned 200 with empty/missing `data` — has `.statusCode`, NO `.status`, NO `.isRecordNotFound` getter). On `EmailBisonError`: `err.status === undefined`, `err.isRecordNotFound === undefined`, so BOTH conditions fail and the script rethrows "Post-delete GET returned non-404 error: status=undefined" — which is **exactly the error signature the dev reported** on attempt 1. The dev's decisions.md entry [2026-04-16T08:15:00Z] calls this a "transient fetch error or retry-exhaustion race" — that is wrong. The actual root cause is a class-mismatch bug in the rollback script itself: EB returned 200-with-empty-body (plausible during EB's async delete queue window per docs/emailbison-dedi-api-reference.md:572-589 "deletion is queued and processed in the background"), which triggered the `EmailBisonError('CAMPAIGN_NOT_FOUND', 404, ...)` throw at client.ts:334-339, which the catch cannot recognise. Suggestion: (a) correct the memory entry to reflect the real root cause, (b) widen the catch in both places (lines 150-161 and 183-194) to `err instanceof EmailBisonError && err.statusCode === 404` OR import both classes and check instanceof for each. This same bug pattern likely exists in other callers of `getCampaign` — grep `isRecordNotFound || .status === 404` across the codebase for sibling instances before Phase 6.5.

F2 [MEDIUM / pattern-inconsistency] scripts/maintenance/_phase-6a-rollback-email-canary.ts:101-134 — re-run ergonomics do NOT support the "idempotent" framing in the dev's decisions.md entry. The script is a one-shot-with-resumable-failure-window, not idempotent. Re-running AFTER a successful execution aborts at line 102 (`campaign.status !== "deployed"` → `EXPECTED Campaign status=deployed, got approved. STOP`). The only reason the dev's 2nd `--execute` attempt succeeded is that attempt 1 crashed BEFORE reaching the Step 2 transaction (no DB mutation) AND EB's async delete had completed during the intervening window (attempt 1's DELETE actually succeeded, so attempt 2's pre-delete GET hit a genuine 404). If attempt 1 had managed to commit the DB transaction before dying (it didn't here, but the sequence is order-dependent), attempt 2 would have failed the preflight and given no path forward. Sibling precedent scripts (_deploy-rebuild-phase-5-5-clear-stale-ebid.ts) have the same non-idempotent pattern but do not claim idempotency in their memory entries. Suggestion: the pattern is fine for one-shots — DON'T call it idempotent in decisions.md. For future one-shots consider adding a "already-applied" early-exit branch that detects post-state (`status === "approved" && emailBisonCampaignId === null && deploy.status === "rolled_back"`) and exits success-zero.

F3 [MEDIUM / pattern-inconsistency] scripts/maintenance/_phase-6a-rollback-email-canary.ts:396-416 — collateral window differs from sibling script without justification. Phase 5.5 uses `Date.now() - 2 * 60 * 1000` (2 minutes); this script uses `txStart - 5_000` (5 seconds). 5 seconds is a tight window that IS actually correct on the main axis (anything inside the transaction writes `updatedAt` equal to `txStart` or slightly after — so a 5s pre-buffer + "now" upper bound catches the tx'd row reliably). HOWEVER: the window only covers Campaign rows — CampaignDeploy rows are NOT collaterally checked even though this script uniquely mutates one. If another process had touched a CampaignDeploy row during the same window, we would never notice. Suggestion: add a mirror `prisma.campaignDeploy.findMany({where:{updatedAt:{gte:collateralWindowStart}}})` check asserting `length === 1 && id === DEPLOY_ID`. Zero-cost safety rail.

F4 [MEDIUM / missing-test] scripts/maintenance/_phase-6a-rollback-email-canary.ts (entire file) — zero tests, zero dry-run-against-fixture coverage. The script has ~400 lines of preflight/verify logic with multiple error-handling branches (12 `throw new Error` paths). The argument "maintenance scripts don't need tests" is reasonable for truly one-shot throwaways, BUT the F1 class-mismatch bug is exactly the kind of thing a 10-line vitest with a mocked `getCampaign` throwing `EmailBisonError('CAMPAIGN_NOT_FOUND', 404, ...)` would have caught pre-execution. Since monty-dev burned a full `--execute` attempt on this bug and misdiagnosed the cause, the cost of "no test" was at least the risk-exposure window of one failed DELETE mid-flight. Suggestion: either (a) formalise a shared `runRollback(...)` helper in src/lib/maintenance/ with unit tests, OR (b) at minimum include a mocked-EB smoke test alongside any one-shot that calls EB + mutates DB.

F5 [LOW / pattern-inconsistency] scripts/maintenance/_phase-6a-rollback-email-canary.ts:276-293 — AuditLog metadata unconditionally records `deletedEbCampaignId: EB_CAMPAIGN_ID` even on attempt 2 where the script SKIPPED the delete (line 153-155 early-return when preDeleteExists === false). The audit claims the rollback deleted 79; on attempt 2, EB's async queue from attempt 1 had already completed the delete. Factually the net effect is correct (79 IS deleted post-script), but the audit log attributes the delete to THIS invocation when it was a side-effect of the prior crashed invocation. Suggestion: make metadata honest — add a `ebDeleteFiredInThisRun: boolean` flag driven by `preDeleteExists`, preserving forensic clarity.

F6 [LOW / governance] decisions.md [2026-04-16T08:15:00Z] — the "OUT-OF-SCOPE OBSERVATION" about EB 78 orphan is persisted in decisions.md prose and the dev says they "flagged [it] for orchestrator". But BL-076's notes only describe the abstract Trigger.dev retry pattern; no backlog item explicitly says "Delete EB 78 orphan after Phase 6.5 lands". An orchestrator's next session reads backlog.json first, not prose in decisions.md. More importantly: **BL-074's notes explicitly said "DO NOT delete orphan EB 78 + EB 79 yet — forensic evidence for F3"** — and this rollback deleted EB 79. The rollback brief from the PM superseded that hold on 79 (intentionally — EB 79 was load-bearing on DB state), but the same-day conflict between BL-074's stated hold and the rollback brief was never reconciled in memory. Suggestion: (a) append a note to BL-074 that EB 79 was deleted in the 6a-rollback per PM override + what forensic info was captured before deletion; (b) file a separate BL entry for "Delete EB 78 post-Phase-6.5" so it doesn't get lost.

F7 [LOW / pattern-inconsistency] scripts/maintenance/_phase-6a-rollback-email-canary.ts:33,LINKEDIN_CANARY_ID — module-scope constant `LINKEDIN_CANARY_ID` hardcodes a second campaign ID just for the "LinkedIn-canary-unchanged" sanity check. This is a nice belt-and-braces, but the check at line 368-394 reads `linkedinFinal.status === "approved" && emailBisonCampaignId === null && updatedAt strictly before tx start`. If the LinkedIn canary was touched AFTER this script was written (unlikely but possible — e.g. another agent updated LinkedIn canary status mid-session), the hard-coded expected status fails for a reason unrelated to THIS rollback. Suggestion: compare byte-identical fields against the preflight snapshot (`linkedInInitialUpdatedAt`, already captured at line 99) rather than asserting fixed literals — if the LinkedIn canary moved for any reason, the diff will be visible and useful.

F8 [INFO / pattern-consistency — NOT a defect] The script is genuinely well-structured compared to the sibling scripts. Pattern symmetry with _deploy-rebuild-phase-5-5-clear-stale-ebid.ts is strong: callback-style $transaction, preflight gates with hard stops, dry-run/--execute split, post-tx verification + collateral check, 4-phase comment structure matching the decisions.md plan. The preflight's 6 hard-stop gates are MORE defensive than Phase 5.5's `length === 11` guard. The tx error-suffix concatenation is safe. The AuditLog metadata fields match the brief. This is info-only: the script is production-quality for a one-shot, which is why a 3-finding minimum required actually looking at semantic bugs rather than stylistic nits.

Phase 6.5 gate verdict: **CONCERN, does NOT block**. The rollback achieved its goal — DB is clean, Campaign cmneqixpv can be re-deployed once BL-074 + BL-075 + BL-076 are fixed. The F1 memory-rot risk is the highest-impact finding: if a future agent trusts "transient fetch retry-exhaustion race" as gospel, they'll miss a real bug class in the EB client that likely affects other callers. F6 (BL-074 hold on EB 79 overridden by brief without reconciliation) is a PM-ownership issue, not a dev issue. F4 (zero tests on a 400-line CLI that mutates production DB + calls external APIs) is the strategic finding worth raising for future maintenance-script conventions. EB 78 cleanup tracking needs a discrete backlog item or it will fall through the cracks.


[2026-04-16T10:15:00Z] -- Phase 6.5b Bundle A adversarial QA (commit ad22e6db). Verdict: PASS (ship). Gates green: tsc clean (exit 0 no output), eslint no-restricted-imports count=0, vitest 825 pass / 37 fail / 1 todo (matches claim of 822->825 +3 contract tests, zero new failures). Scope verified: 9 files / 319 insertions / 86 deletions, no campaign-deploy.ts / schema.prisma / canary-ID touches. BL-074 correctly flipped open->done, BL-077+BL-078 still open, existing [UPDATE 2026-04-16T09:45Z] prose preserved, [CLOSED] note appended pure-append. F1 (getSequenceSteps) migration to `/campaigns/v1.1/{id}/sequence-steps` verified against docs/emailbison-dedi-api-reference.md line 1318. F2 callers (trigger/ooo-reengage.ts:232, src/lib/agents/campaign.ts:314) migrated cleanly with title sourced from in-scope vars (campaignName / campaign.name). F7 deprecated singular method removed, repo-wide grep of `createSequenceStep\b` returns zero active source/test callers (only .monty/planning/docstring/comment references). 6 genuine findings, all medium/low/info — none blocking.
  F1 [medium, missing-test]: trigger/ooo-reengage.ts + src/lib/agents/campaign.ts — two migrated callers have ZERO test coverage at either the adapter or integration level. `grep -rln activateSignalCampaign|oooReengage` returns only the source files themselves (and the email-adapter unit test, which covers the third caller). The migration flips the semantics from N serial per-step POSTs to 1 atomic batched POST; if either callsite has a bug passing `title`/step shape wrong, no CI signal catches it before prod. Pre-existing coverage gap (not introduced by Bundle A), but Bundle A is the commit that touched these files to fix the 422 root cause and shipped no test for either callsite. Suggestion: add at least a smoke-level mock-fetch test for each — (a) ooo-reengage fallback-sequence path (delayDays=0 clamp through the wire), (b) activateSignalCampaign email path (verify title = campaign.name is passed). Alternatively: file a BL-followup for coverage and reference it in the commit message. Severity medium because the 422 root cause lived unnoticed precisely because these callers had no contract test — shipping the fix without backfilling coverage leaves the same blind spot.
  F2 [low, test-coverage-weakening]: src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts:325,522 — GET regex widened from `/\/campaigns\/10001\/sequence-steps/` to `/\/campaigns\/(v1\.1\/)?10001\/sequence-steps/` (accepts BOTH v1 and v1.1). Commit message calls this "defensive robustness" but the practical effect is that an accidental revert of client.ts getSequenceSteps to the deprecated v1 path would still pass these integration tests. Path enforcement has been *moved* to the contract test at client-sequence-steps.test.ts:346 (`expect(url).toBe(${EB_BASE}/campaigns/v1.1/...)` + `expect(url).not.toMatch(/campaigns/${CAMPAIGN_ID}/sequence-steps/)`), which is strict — so the defense-in-depth argument holds — but the integration test layer lost its path assertion. Suggestion: tighten the integration regex to v1.1-only (drop the `(v1\.1\/)?` optional group). If the dev wanted forward-compat for future EB path renames, that's what the contract test's negative assertion already catches. Severity low because coverage still exists at the contract layer.
  F3 [low, pattern-inconsistency]: src/lib/emailbison/client.ts:298 — `const raw = Array.isArray(parsed.data) ? parsed.data : parsed.data.data;` relies on Zod's discriminated union narrowing to distinguish array vs `{data: array}`. This works but reads confusingly next to `createSequenceSteps` at line 430 which uses a non-union schema and so does a direct `parsed.data.data.map(...)`. Two different parse-and-narrow patterns in the same class for what is conceptually the same "EB response shape normalization" problem. Suggestion: either extract a shared helper `normalizeSequenceStepsResponse(res): SequenceStep[]` used by both methods, or accept the asymmetry explicitly in a comment (getSequenceSteps needs to tolerate bare-array historical shapes; createSequenceSteps only sees the documented `{data}` shape because the API contract is newer). Severity low — purely readability; no correctness issue.
  F4 [low, test-coverage-gap]: src/lib/emailbison/__tests__/client-sequence-steps.test.ts — the 3 new getSequenceSteps contract tests cover happy path + bare-array + schema-drift (`{not_data: "oops"}`). Missing edge cases: (a) response is `null`, (b) response is `{data: null}`, (c) response is `{data: [ {id: "string-not-number"} ]}` (Zod should reject inner field drift too, not just the outer shape), (d) response has an item with BOTH `order` AND `position` populated (fallback precedence is `order ?? position`, but no test locks that in — a future refactor that swaps the order would silently flip behavior). Suggestion: add at least (c) and (d) — they exercise the passthrough schema and the documented fallback order respectively. Severity low because the core drift-guard already catches shape breakage at the outer level.
  F5 [low, dead-code-note]: src/__tests__/emailbison-client.test.ts lines 243-263, 397-448 — 6 pre-existing test failures in this file assert an IMAGINARY path `/campaigns/sequence-steps?campaign_id=X&page=1` that matches NEITHER the deprecated v1 path NOR the new v1.1 path. These tests have been failing through Phase 6.5a AND Phase 6.5b; they're baked into the 37-failure baseline. Bundle A was the logical moment to either fix or delete them (the dev migrated getSequenceSteps and could have updated its legacy assertions). The decision to leave them failing is defensible (preserve baseline stability; tackle in a cleanup bundle) but undocumented. Suggestion: file a BL for deleting/rewriting this file's getSequenceSteps block against the v1.1 contract, or add a comment at the top of the describe block explaining why these assertions are stale. Severity low — pre-existing technical debt, not a regression.
  F6 [info, pre-existing-orphan-risk-note]: trigger/ooo-reengage.ts:216-241 — `createCampaign` is called BEFORE `createSequenceSteps`. If the batched sequence-steps POST throws (422, 500, network), the EB campaign is orphaned (same failure mode as BL-076 on the main deploy path). The catch block at line 272-280 logs + re-throws to let Trigger.dev retry, but does NOT delete the orphan EB campaign. This is NOT introduced by Bundle A — the pre-migration code had the same orphan pattern per-step — but it's now a single-atomic-failure surface rather than N partial-success surfaces. Worth tracking as a sibling of BL-076 for the ooo-reengage path specifically. Suggestion: add ooo-reengage to the scope of BL-076's retry-semantics audit, or file BL-079 for catch-block-with-inline-cleanup on the ooo-reengage path. Severity info — no user-visible regression, just a pre-existing risk that's now more obvious in the migrated code.

[2026-04-16T13:30:00Z] -- Phase 6.5b Bundle B adversarial QA (commit be5e54fa). Verdict: BLOCK (Bundle C MUST land before this ships). Gates green: tsc clean (exit 0 no output), eslint no-restricted-imports count=0, vitest 832 pass / 37 pre-existing fail / 1 todo — matches claim exactly, +7 new passing tests over Bundle A baseline (825), zero new failures, same 6 pre-existing failing files (api-routes, emailbison-client, linkedin-sender, resend-notifications, channel-enrichment, promotion-icp-gate). Scope verified: 6 files changed (deploy.ts, email-adapter.ts comment, deploy-rollback.test.ts NEW, deploy-campaign.integration.test.ts extended, decisions.md, architecture.md), no schema change, no trigger/campaign-deploy.ts touch, no canary IDs touched, email-adapter.ts only comment-change confirmed. Atomic-transaction correctness VERIFIED: all 5 writes in the rollback path (currentDeploy.findUnique, campaignDeploy.update, campaignDeploy.findFirst sibling check, campaign.findUnique snapshot, campaign.updateMany rollback, auditLog.create) use `tx.` not `prisma.` — atomicity intact. `prisma.` only appears at the `$transaction(...)` entry at line 212. `auditLog(...)` fire-and-forget helper NOT used — correct (would break atomicity). findFirst sibling check IS inside the tx, BEFORE the Campaign rollback decision — serializable ordering correct. CampaignDeploy status='running' guard preserved at line 219. updateMany({status:'deployed'}) race-guard correct. campaignSnapshot read BEFORE updateMany so clearedEmailBisonCampaignId captures the pre-rollback value. Rethrow at line 297 preserved. SYSTEM_ADMIN_EMAIL imported from @/lib/audit matches operations.ts:880 precedent. 9 findings total, 1 critical / 2 high / 3 medium / 3 low.

  F1 [CRITICAL, correctness/integration-risk]: src/lib/campaigns/deploy.ts:97-101 vs Bundle B catch block — The rollback DEFEATS Trigger.dev's maxAttempts=2 retry semantics entirely. trigger/campaign-deploy.ts:15 configures `retry: { maxAttempts: 2 }` meaning the task is re-invoked on exception with the SAME payload (same campaignId, same deployId). On the FIRST attempt's catch: (a) CampaignDeploy.status flips to 'failed', (b) findFirst for inflight sibling returns NULL (no sibling CampaignDeploy row is ever created — only `deploy-campaign.ts:148` and `route.ts:50` create CampaignDeploys and neither runs on a Trigger.dev retry), (c) Campaign rollback fires unconditionally → status='approved', emailBisonCampaignId=null, deployedAt=null, (d) rethrow. Trigger.dev then schedules the SECOND attempt 5s later, re-invoking executeDeploy(campaignId, deployId). On re-entry: line 85-88 flips the terminal-state CampaignDeploy.status back to 'running' (state-machine violation — terminal→non-terminal, see F2), line 92 loads the Campaign via getCampaign — which now shows status='approved' — and line 97-101 THROWS `Campaign is not in 'deployed' or 'active' status (got 'approved'). Deploy aborted.` The retry cannot succeed because its precondition guard was invalidated by the first attempt's rollback. Result: every terminal deploy failure is now effectively UNRETRYABLE from Trigger.dev's perspective. Pre-Bundle-B behavior (broken in a different way) was: first attempt leaves Campaign.status='deployed' + emailBisonCampaignId set, retry re-enters email-adapter Step 1 CREATE_OR_REUSE idempotency path, getCampaign-verify-existing succeeds, retry resumes at the failed step — and SUCCEEDS for transient failures. Post-Bundle-B: retry cannot reach email-adapter at all. The brief flags this as "the most important adversarial question" for Bundle C interaction; answer is UNAMBIGUOUS: Bundle B's gate does NOT correctly handle Trigger.dev retries. The `findFirst` inflight-sibling check is designed for SCENARIO A (a second `initiateCampaignDeploy` call from the route/CLI while the first is in flight — that creates a sibling CampaignDeploy row and findFirst catches it) but the Trigger.dev retry case is SCENARIO B (SAME deployId re-invoked, NO sibling row exists) which the gate cannot detect. **This MUST be resolved before Phase 6.5c deploys.** Options: (a) Bundle C changes trigger/campaign-deploy.ts retry config to `maxAttempts: 1` (disable automatic Trigger.dev retries, rely on manual retryDeployChannel) — simplest, makes Bundle B's behavior correct by removing the conflicting retry path; (b) Bundle B's rollback should detect a Trigger.dev retry context and skip — but there is no Trigger.dev context passed into executeDeploy today; (c) Bundle C's retry-reuse fix (BL-076) changes email-adapter to handle the "Campaign.status='approved' + emailBisonCampaignId=null" case as "fresh deploy re-entry" — but then line 97-101 needs to be widened to accept 'approved' status on retry, which undermines the Bundle B guard's purpose. Most likely correct path: Option (a) + rename BL-076 to cover trigger retry config. Until one of these lands, Bundle B should NOT ship to production. Severity CRITICAL because this is a production behavior regression (retries work pre-Bundle-B, don't work post-Bundle-B) disguised as a correctness fix.

  F2 [HIGH, state-machine-violation]: src/lib/campaigns/deploy.ts:85-88 — `prisma.campaignDeploy.update({where: {id: deployId}, data: {status: "running"}})` is UNGUARDED. On a Trigger.dev retry where the CampaignDeploy row was just flipped to 'failed' by the first attempt's catch, this line silently flips 'failed' → 'running' — a terminal → non-terminal transition. The CampaignDeploy state machine per schema.prisma:815 is documented as `pending -> running -> complete | partial_failure | failed`, with the final three being terminal. Re-entering 'running' from 'failed' means: (a) a DB consumer sees the row's status oscillate, (b) the completedAt timestamp from the first attempt's catch (line 225 in the catch) is LEFT IN PLACE (status='running' + completedAt=<first-attempt-timestamp> is an impossible state), (c) the error field from the first catch (line 224) is LEFT IN PLACE too, so the second attempt's retry shows an already-failed error message while in 'running' state. This pre-dates Bundle B (it's pre-existing in line 85-88 from before this commit) but Bundle B's rollback logic makes the bug OBSERVABLE/EXPLOITABLE — before Bundle B, a retry hit email-adapter Step 1 and recovered; now it hits the status guard at line 97-101 and throws, leaving the CampaignDeploy row in the 'running'-with-completedAt frankenstate permanently. Suggestion: guard line 85-88 as `updateMany({where: {id: deployId, status: 'pending'}, data: {...}})` — on a retry, count=0 and we should either (a) throw "retry attempted on terminal CampaignDeploy" or (b) accept it as a new run, in which case also clear completedAt + error. Either way, unguarded status='running' write is wrong. Severity high because combined with F1 it leaves persistent DB rows in an impossible state after every terminal failure + Trigger.dev retry.

  F3 [HIGH, missing-test-coverage]: src/lib/campaigns/__tests__/deploy-rollback.test.ts — ZERO test coverage for the Trigger.dev retry scenario described in F1. The 6 cases cover: (1) happy path, (2) failure + no sibling → full rollback, (3) failure + sibling → skip rollback, (4) audit metadata shape, (5) Campaign already paused → count=0 skip, (6) adapter already flipped CampaignDeploy.status → preserve guard. None of these exercise "first attempt fires rollback THEN retry re-enters executeDeploy with Campaign.status='approved'" — the exact scenario that is BROKEN per F1. The brief explicitly asks (section 6): "verify this [retry-without-pending-sibling case] via a test case". The dev's case 5 description says "a retry is coming and will own the final state" but the test asserts only the COUNT=0 path (Campaign manually paused), not the retry re-entry path. Missing test: spin executeDeploy once, let it throw (simulating first-attempt failure), assert full rollback fired. Then re-invoke executeDeploy with the same (campaignId, deployId) to simulate Trigger.dev retry; assert it throws with `Campaign is not in 'deployed'...` error message — this locks in current broken behavior AND will loudly fail when Bundle C lands the fix. Without this test, Bundle C's retry fix has no regression signal. Severity high because this is the most important adversarial case called out in the brief and it is untested.

  F4 [HIGH, missing-test-coverage]: src/lib/campaigns/__tests__/deploy-rollback.test.ts — ZERO test coverage for the `erroredStep: null` case (error message without a `[step:N]` tag). Cases 2 and 4 both use messages containing `[step:6]` / `[step:7]` — `erroredStep` is always populated. If `err.message` is a plain string like "Network timeout" or the generic "Campaign not found: cmneq..." from line 94, the regex `/\[step:(\d+)\]/` returns null and `erroredStep` goes to the audit metadata as null. No test verifies (a) the audit write still happens, (b) metadata.erroredStep === null (not undefined, not missing key). Suggestion: add a 7th case with a non-tagged error message ("Campaign not found: bogus-id" thrown synchronously from the `throw new Error(\`Campaign not found...\`)` path at line 94) and assert `meta.erroredStep === null`. This also covers an edge case brief section 3 called out — "Does `erroredStep` extraction handle edge cases (no `[step:N]` tag in the error message → null)? Test coverage for the null case?" Severity high because a null-vs-undefined bug in audit metadata would be silent and hard to diagnose; explicit test locks the contract.

  F5 [MEDIUM, test-ordering-not-asserted]: src/lib/campaigns/__tests__/deploy-rollback.test.ts:239-264 — Case 2 asserts `txMock.campaignDeploy.findFirst`, `txMock.campaign.findUnique`, and `txMock.campaign.updateMany` were all called with specific shapes, but DOES NOT assert ORDER. The brief section 1 calls out: "The order of operations inside the tx matters: `findFirst` sibling check MUST run before the Campaign rollback decision... The Campaign snapshot read... happens BEFORE the update, inside the tx." The production code at deploy.ts:212-295 does enforce this order (sequential await), but if a future refactor reorders (e.g., moves findFirst after findUnique), the test would still pass because order assertions are missing. Order matters here because: (a) findFirst sibling check before snapshot means we don't waste a read on the sibling-skip path; (b) snapshot before updateMany is MANDATORY (snapshot captures what WAS cleared). Suggestion: use `vi.fn().mockImplementation(() => { order.push('findFirst'); return ... })` pattern and assert `expect(order).toEqual(['campaignDeploy.update', 'findFirst', 'campaign.findUnique', 'campaign.updateMany', 'auditLog.create'])`. Severity medium because current behavior is correct; this is test-strengthening for future refactor safety.

  F6 [MEDIUM, coverage-gap]: src/lib/campaigns/__tests__/deploy-rollback.test.ts — ZERO test for the case where the `prisma.$transaction` itself THROWS (serialization conflict, DB connection drop mid-tx, CHECK constraint violation). The code at deploy.ts:212-295 wraps the callback in $transaction; if the tx aborts, Prisma rejects the promise, the outer catch does NOT catch it (we're already IN the outer catch), and the rejection propagates... actually wait — it propagates to WHERE? The $transaction is inside the catch block. If $transaction throws, it is NOT caught by any other try/catch in this function — it unwinds past the `throw err` at line 297. The CATCH BLOCK ITSELF throws a DIFFERENT error than the original, hiding the original `err`. Caller (Trigger.dev run wrapper) sees a Prisma serialization error instead of the original adapter error. This means the audit log never gets written AND the CampaignDeploy.status is not flipped to 'failed' AND the original error message is lost. Suggestion: wrap the `$transaction(...)` call in its own try/catch that logs the tx failure AND still rethrows the ORIGINAL err: `try { await prisma.$transaction(...); } catch (txErr) { console.error('[deploy] Rollback tx failed:', txErr); }`. Then `throw err` (original) on line 297. Test: mock `$transaction` to reject, assert the original adapter error is still thrown, assert txErr is logged. Severity medium because serialization retries are rare but the failure mode is silent loss of audit/state.

  F7 [MEDIUM, pattern-concern]: src/lib/campaigns/deploy.ts:238-245 — `tx.campaignDeploy.findFirst({where: {campaignId, status: {in: ['pending', 'running']}, id: {not: deployId}}})`. The `{not: deployId}` exclusion is correct for self-exclusion, but the whole gate is built on the assumption that Scenario A (concurrent `initiateCampaignDeploy` from route/CLI creating a SECOND CampaignDeploy row) is the dominant retry case. Grep confirms: only 2 create sites (deploy-campaign.ts:148, route.ts:50), both external route/CLI initiators. Trigger.dev retry re-invokes the SAME (campaignId, deployId) payload and does NOT create a new CampaignDeploy row (F1). So the findFirst gate ONLY catches Scenario A and MISSES Scenario B entirely. If Scenario A is actually common in ops practice (admin clicks "deploy" twice, or CLI is run twice by racing operators), the gate is valuable. But the brief says Phase 6.5a canary surfaced the Scenario-B retry orphan (BL-076) — the more frequent real-world retry case. Suggestion: document in a code comment that this gate handles Scenario A (concurrent external initiate) only and does NOT handle Scenario B (Trigger.dev automatic retry); Scenario B is Bundle C scope. Without this comment, future maintainers will assume the gate is comprehensive. Severity medium because it's a documentation/design-clarity issue that will mislead later work.

  F8 [LOW, minor-improvement]: src/lib/campaigns/deploy.ts:210 — `const reason = message.length > 500 ? message.slice(0, 500) : message;` does a hard slice with no ellipsis. If the message is exactly 501 chars, the audit metadata shows the first 500 chars with no indication it was truncated. An operator reading the audit log sees a message that LOOKS complete but isn't. Suggestion: `const reason = message.length > 500 ? message.slice(0, 497) + '...' : message;` — preserves the 500-char budget and signals truncation. Severity low, purely a debuggability improvement.

  F9 [LOW, dead-code-concern]: src/lib/campaigns/deploy.ts:206 — `const stepMatch = message.match(/\[step:(\d+)\]/);` captures the digits via capture group 1 but then the next line discards it: `const erroredStep: string | null = stepMatch ? stepMatch[0] : null;`. `stepMatch[0]` is the FULL match like `"[step:6]"`, not just `"6"`. The audit metadata field is literally the string `"[step:6]"` including brackets. This works — test case 4 asserts `erroredStep: "[step:7]"` with brackets — but the naming `erroredStep` suggests a numeric step number (DEPLOY_STEP is keyed by integers at email-adapter.ts:37-48), and consumers reading the audit JSON see a bracketed string which is awkward to parse. Suggestion: either rename the field to `erroredStepTag` to match the convention, or use `stepMatch[1]` to capture just the digit string. Both valid; the current state is a minor inconsistency. Severity low.

  Notes: (1) integration test case 5 is well-constructed — drives through executeDeploy() not just the adapter, asserts all rollback fields + AuditLog + preserves case-3's adapter-level emailError [step:6] contract per brief requirement. (2) architecture.md entry is accurate and captures the findFirst-inside-tx pattern correctly. (3) email-adapter.ts comment update (lines 614-623) accurately describes the new contract. (4) No schema change, no trigger config change — scope boundary preserved. (5) The dev's own decisions.md note acknowledges "Bundle C will fix trigger retry config separately — do NOT touch trigger/campaign-deploy.ts" which aligns with my F1 finding — the dev KNEW Bundle C would need to fix the retry interaction but shipped Bundle B without a test asserting the current broken state. That test (F3) would have caught F1 at review time.

[2026-04-16T10:35:00Z] -- Phase 6.5b Bundle D adversarial QA (commit e1f7e253 refactor(emailbison): shared isNotFoundError helper). Verdict: SHIP-WITH-FIXES (4 findings total: 0 critical, 1 medium, 1 low, 2 info). GATES VERIFIED: tsc clean (exit 0, no output); eslint no-restricted-imports=0 (pre-existing 2247 unrelated errors); vitest 842 pass / 37 fail / 1 todo / 880 total — exactly matches claim (832 baseline + 10 new errors.test.ts cases). 6 files changed match claim (2 new: src/lib/emailbison/errors.ts + tests; 4 modified: email-adapter.ts, backlog.json, decisions.md, architecture.md). BL-078 flipped open→done with updatedAt=2026-04-16T10:25:00Z, notes APPENDED not replaced. BL-077 remains open. Untracked `_phase-6a-rollback-email-canary.ts` intact and not staged. Zero touches to trigger/, schema.prisma, deploy.ts. CORRECTNESS REVIEW: helper uses real `instanceof EmailBisonApiError` and `instanceof EmailBisonError` checks (no `as` casts, no `any`). Import graph verified — errors.ts imports from both client.ts + types.ts; client.ts does NOT import errors.ts (dev's circular-dep reasoning is ACCURATE). The 10 unit tests use real class constructors (not plain-object mocks), including one that constructs an EmailBisonApiError with a record_not_found body to exercise the isRecordNotFound getter. Enumerated all `throw new EmailBisonError(...)` sites in client.ts (8 total): only CAMPAIGN_NOT_FOUND is a not-found code; others (UNEXPECTED_RESPONSE x3, EMPTY_UPDATE, EMPTY_TAG_LIST, EMPTY_CAMPAIGN_LIST, EMPTY_SENDER_LIST) correctly NOT matched. Helper is semantically correct for the EmailBisonError class.

Finding F1 [medium / pattern-inconsistency, semantic-broadening] src/lib/channels/email-adapter.ts:234 migration broadens the manual-delete guard. Pre-commit: `err instanceof EmailBisonApiError && err.isRecordNotFound` → required HTTP 404 AND `data.record_not_found` body marker (client.ts getter at line 62-66: `status === 404 && !!data?.record_not_found`). Post-commit: `isNotFoundError(err)` → matches ANY EmailBisonApiError with `status === 404 OR isRecordNotFound === true` (note the `||` inside the helper, NOT `&&`). A plain HTTP 404 from EB without the record_not_found body marker (e.g. EB route-not-found, a newly-created campaign not yet visible on a read replica, token pointing at a different EB workspace where the ID is a valid integer but not owned by this token) now triggers the "manual delete — clear emailBisonCampaignId and re-deploy" error message. Pre-commit those cases fell through to `throw err` and surfaced as a generic deploy failure labeled failedAtStep=1. The misdiagnosis risk is narrow (EB does return 404 almost exclusively for record_not_found in practice per docs/emailbison-dedi-api-reference.md), but the operator-facing message is now WRONG for the token-mismatch / replica-lag subclass: an operator reading "EB campaign X no longer exists in EmailBison (manual delete?). Will not silently re-create — clear Campaign.emailBisonCampaignId first" would clear a valid EB ID pointing at a real campaign, producing actual orphans. Suggestion: narrow the helper OR the call site. Either (a) change the EmailBisonApiError branch of isNotFoundError from `e.status === 404 || e.isRecordNotFound === true` to `e.status === 404 && (e.parsedBody?.data?.record_not_found || e.body === '' || some explicit not-found predicate)` — i.e. keep the getter semantics; or (b) at the call site add `if (isNotFoundError(err) && (err instanceof EmailBisonError || (err instanceof EmailBisonApiError && err.isRecordNotFound)))` to keep the old strictness for EmailBisonApiError while still covering the EmailBisonError class. Option (b) is closer to the stated BL-078 goal ("match the class, not broaden the semantic"). Not BLOCKING because (i) the helper's broadening is called out in the JSDoc ("OR isRecordNotFound === true") and the dev thought about it, (ii) real-world EB 404 responses do almost always carry the record_not_found body per the EB docs, (iii) the test matrix covers the case explicitly — test 1 ("status=404 plain") asserts true. But this should be captured as a BL follow-up (BL-079 "isNotFoundError semantic tightening for email-adapter Step 1 guard") BEFORE BL-077 uses the helper to delete EB 78, because BL-077's delete path also needs to distinguish "gone" from "not yours".

Finding F2 [low / missing-test] no integration test exists for the email-adapter Step 1 manual-delete branch — neither pre- nor post-migration. Grepped `src/lib/channels/__tests__` for isNotFoundError, isRecordNotFound, manual-delete, CAMPAIGN_NOT_FOUND, "Will not silently re-create" — zero matches. The helper unit tests cover all 10 shape permutations in isolation, but the actual caller (email-adapter.ts:234) has no test that mocks ebClient.getCampaign throwing EmailBisonError(CAMPAIGN_NOT_FOUND, 404) and asserts the deploy fails at step 1 with the manual-delete message AND that emailBisonCampaignId is NOT cleared (only the operator should do that). The EmailBisonApiError path is also untested. Bundle C (email-adapter-race.test.ts) does not cover Step 1 — it covers the P2002 race at Step 2. Suggestion: add 2 cases to a new or existing email-adapter test file — (a) getCampaign throws EmailBisonError(CAMPAIGN_NOT_FOUND, 404) → deploy.status='failed', deploy.errorStep=1, deploy.errorMessage.includes('no longer exists in EmailBison'), campaign.emailBisonCampaignId UNCHANGED; (b) getCampaign throws EmailBisonApiError(404, '{data:{record_not_found:{campaign:123}}}') → same assertions. Not blocking because the helper itself is fully unit-tested and the call site is a 1-line change, but the migration's whole POINT is to catch the EmailBisonError class which previously fell through — having zero tests proving that end-to-end behaviour is a gap in the bundle's claimed scope.

Finding F3 [info / latent-bug] the deferred inbox route migration hides a REAL inconsistency that the helper was designed to fix. src/app/api/admin/inbox/email/actions/route.ts:189-213 and src/app/api/portal/inbox/email/actions/route.ts:192-217 catch in this order: (1) `if (err instanceof EmailBisonError)` returns HTTP `err.statusCode` with generic message "Action failed via email provider", then (2) `if (err instanceof EmailBisonApiError)` which has the 404 branch that soft-deletes the Reply row (Reply.deletedAt=new Date()) and returns a friendly "This reply no longer exists" message. A 200-with-empty-data from EB (EmailBisonError with statusCode=404) currently takes branch (1) — it does NOT soft-delete the Reply row, and the portal/admin UI sees a generic 404 instead of the intended "reply deleted" message. This is EXACTLY the bug pattern BL-078 exists to eliminate. The dev's "structural rewrite needed" claim is TECHNICALLY accurate (the two branches do different logging + body mapping), but calling this a "latent inconsistency, deferred to a future restructure ticket" understates the risk — any Reply action invoked on a reply that EB has async-deleted (which happens during the DELETE-queue cleanup window per docs §572-589, the same mechanism that triggered BL-078 in the first place) will give the user a generic error and leave an orphan Reply row un-tombstoned. Suggestion: file a BL item now (not "future restructure ticket") to unify under isNotFoundError. The restructure is ~15 lines: swap the catch order so a single `if (isNotFoundError(err)) { soft-delete Reply + return 404 friendly }` branch runs FIRST, then the existing class-specific branches handle non-404 logging and body extraction. Low severity because the happy path is EB returning HTTP 404 with record_not_found body (EmailBisonApiError branch works today), but the EmailBisonError path is a documented EB behaviour not a hypothetical. This also argues for merging BL-077 and BL-079 (my F1 suggestion) with this into a single "isNotFoundError rollout sweep" follow-up.

Finding F4 [info / documentation] architecture.md entry is substantive (WHY + HOW-TO-APPLY + first-use + 4 explicit rules) and NOT filler. Captures the circular-dep insight (rule 3) and the type-predicate-vs-boolean tradeoff (rule 1) accurately. One minor quibble: rule 1 says "predicate narrowing would falsely imply the error is one of N specific classes" — this is true but the REAL reason not to use a predicate is subtler: a predicate `e is EmailBisonApiError | EmailBisonError` would force callers to re-branch on instanceof anyway if they want to access class-specific properties, so the predicate adds no value. Worth a one-line tweak if the dev updates the memory entry; not a defect. Memory entry earns its keep.

Summary: 1 medium, 1 low, 2 info. The helper is correct, tests are thorough, migration is mechanically sound, backlog/memory hygiene is clean. The medium (F1) is the semantic-broadening concern that the dev's own JSDoc acknowledges but doesn't adequately justify — worth a BL-079 follow-up before BL-077 lands. The low (F2) is the integration-test gap on the only migrated call site — claim-vs-reality mismatch since the "10 unit tests" prove the helper works in isolation but not the 1 site that uses it. F3 is the latent-bug call that the deferred migration is riskier than labeled. Bundle D does NOT block Bundle C (Bundle C is deploy-flow concurrency, orthogonal). Confidence HIGH on the helper correctness, MEDIUM on the email-adapter migration due to the semantic-broadening.

[2026-04-16T16:30:00Z] -- Phase 6.5b Bundle C adversarial QA (commit 3cdd5c4a). Verdict: SHIP-WITH-FIXES (no critical blocker; 2 high findings are soft blocks that the dev should address before Phase 6.5c, but the coordinated B+C contract is coherent for the canonical retry case). Gates VERIFIED: tsc clean (exit 0 no output), eslint no-restricted-imports count=0, vitest 846 pass / 37 pre-existing fail / 1 todo — matches claim exactly. +4 new tests at deploy-rollback.test.ts (total 10 in that file, was 6). Pre-existing 37 failures are the same 6 files listed in Bundle B QA (api-routes, emailbison-client, linkedin-sender, resend-notifications, channel-enrichment, promotion-icp-gate) — no new failures introduced. Scope verified: 5 tracked code files + 3 memory files, git diff --stat matches dev claim. No trigger/campaign-deploy.ts touch (maxAttempts=2 preserved at trigger/campaign-deploy.ts:15). No prisma/schema.prisma touch. CampaignDeploy.emailBisonCampaignId column confirmed to exist at schema.prisma:828 (`emailBisonCampaignId Int?`) — the retry anchor. Canary IDs (cmneqixpv, cmneqixvz) untouched. EB 78 untouched (BL-077 preserved). Maintenance scripts (scripts/maintenance/_*) still untracked. BL-076 flipped done in backlog.json, BL-077 still open. 6 findings total, 0 critical / 2 high / 2 medium / 1 low / 1 info. Bundle B QA F1/F2/F3 assessment: F1 RESOLVED for the canonical retry case (anchor present + Campaign rolled back cleanly), PARTIALLY-RESOLVED for edge cases (see F1 below); F2 RESOLVED (state-machine guard is now race-safe updateMany for both pending and failed entry paths); F3 PARTIALLY-RESOLVED (4 new tests added but retry-happy test has a mocking-layer weakness — see F4 below).

  F1 [HIGH, behavior-regression] src/lib/campaigns/deploy.ts:126-180 — The retry contract is NOT symmetric across Step-1 vs Step-2+ failures. The restore branch at lines 126-168 requires `currentDeploy.emailBisonCampaignId != null` AND `currentDeploy.status === 'failed'`. This captures Step-2-onwards failures correctly (anchor was persisted before the non-idempotent step). But Step-1 failures (getCampaign returns null, workspace.apiToken missing, network error during ebClient.createCampaign before anchor write at email-adapter.ts:296-299, Zod parse failure on emailSequence) leave `CampaignDeploy.emailBisonCampaignId=null` — the defensive `else` branch at line 172-179 THROWS "unexpected status for executeDeploy entry: 'failed' (emailBisonCampaignId=null). Refusing to proceed." This is a REGRESSION vs pre-Bundle-B behavior. Pre-Bundle-B: Step-1 failure left Campaign in zombie 'deployed'+null state, but Trigger.dev retry could still re-run through the fresh-create path (getCampaign returns campaign with emailBisonCampaignId=null → preExistingEbId=null → createCampaign creates fresh EB campaign — which IS the right behavior because the prior attempt never created an EB campaign). Post-Bundle-C: Step-1 failure is NOT retryable by Trigger.dev; operator must manually delete the failed CampaignDeploy row and re-invoke initiateCampaignDeploy. The defensive throw is safe (no duplicate created) but the retry system is now crippled for the specific class of failures where a retry would be genuinely safe (Step 1 never created an EB campaign, so no duplicate risk exists). Note the false-symmetry in the dev's rationale comment at lines 172-176: "'running' implies concurrent task invocation; 'complete'/terminal implies a successful run we shouldn't re-run." This correctly classifies 'running'/'complete' but glosses over 'failed'+null, which is a LEGITIMATE retryable state (pre-Step-2 failure). Suggestion: widen the retry path to handle `currentDeploy.status='failed' AND currentDeploy.emailBisonCampaignId=null` — treat as "retry before anchor was set, safe to restart from Step 1." The restore tx in this case should only flip CampaignDeploy 'failed'→'running' (no Campaign restore needed since nothing was persisted to Campaign either). Additional complication: restore's Campaign.updateMany guard (`status='approved' AND emailBisonCampaignId=null`) will miss in the Step-1-failure case because Campaign.status is whatever initiateCampaignDeploy set it to (likely 'deployed'), and emailBisonCampaignId is still null (never persisted). So the widened path needs: (a) no Campaign.updateMany call if we detect "status='deployed' AND ebId=null" (nothing to restore, Campaign already in pre-adapter state), (b) still flip CampaignDeploy 'failed'→'running'. Severity HIGH because this breaks the retry system for a real class of failures — pre-Bundle-B these retries worked, post-Bundle-C they don't. Not a ship blocker (operator workaround exists: delete CampaignDeploy row manually) but must be resolved before Phase 6.5c or the retry system is degraded.

  F2 [HIGH, test-quality] src/lib/campaigns/__tests__/deploy-rollback.test.ts:478-614 (retry-happy case) — The commit message and QA brief BOTH claim this test "asserts SINGLE createCampaign call across TWO executeDeploy invocations with the same deployId — the dual-orphan regression gate." THIS CLAIM IS FALSE. The test mocks the entire EmailAdapter as a fake (`getAdapter: () => ({ deploy: adapterDeployMock })` at line 96), so `createCampaign` is never called in this test at all. The actual assertion at line 579 is `expect(adapterDeployMock).toHaveBeenCalledTimes(2)` — which only verifies the adapter wrapper was invoked twice (once per executeDeploy call). The test DOES NOT exercise email-adapter.ts's Step 1 reuse branch at all. Specifically, the test does not verify: (a) that on retry, email-adapter reads `campaign.emailBisonCampaignId` which has been restored to 78, (b) that email-adapter takes the `preExistingEbId != null` reuse path at line 215, (c) that ebClient.createCampaign is called ZERO times on the retry attempt. A real dual-orphan regression gate would use the integration test harness (deploy-campaign.integration.test.ts) with a real EmailBisonClient mock that counts createCampaign calls across two executeDeploy invocations. This mocking-layer weakness means the test WILL NOT catch: a regression where restore block fails to write Campaign.emailBisonCampaignId, a regression where email-adapter Step 1 stops reading `preExistingEbId` from Campaign, a regression where the adapter starts creating a fresh EB campaign on the reuse path. Severity HIGH because the test claims to be a regression gate but isn't — it's a state-transition smoke test. Suggestion: add a parallel integration test in deploy-campaign.integration.test.ts that drives through a real EmailAdapter with EmailBisonClient mocked at the HTTP boundary; asserts `mockClient.createCampaign.toHaveBeenCalledTimes(1)` across 2 executeDeploy calls with the same deployId. Without that integration test, Phase 6a's root regression (duplicate createCampaign) remains uncovered at the behavioral layer.

  F3 [MEDIUM, root-cause-math] Commit message + dev's decisions.md entry + deploy-rollback.test.ts:452-460 all claim "The 17s gap between EB 78 (08:00:53Z) and EB 79 (08:01:10Z) matches the 15s withRetry final delay exactly." VERIFIED AGAINST src/lib/utils/retry.ts:11-29: withRetry with default `maxRetries=3, delays=[1000, 5000, 15000]` runs attempts 0, 1, 2. After attempt 0 fails, waits `delays[min(0,2)] = 1000ms`. After attempt 1 fails, waits `delays[min(1,2)] = 5000ms`. Attempt 2 runs with NO post-wait (guard at line 22: `if (attempt < maxRetries - 1)`). The 15000ms delay is NEVER executed at the default maxRetries=3 — it's dead code in the default config. The TWO inter-attempt delays that DO fire are 1s and 5s. 17s is NOT "exactly 15s"; it doesn't match any single retry delay. A plausible alternative explanation: first createCampaign call took ~10-16s to return (EmailBison server-side processing or async 202 + client-side timeout), then delays[0]=1s wait, then second attempt fired → total elapsed between call 1 and call 2 = 11-17s. This explanation is consistent with the observed 17s gap but requires a long first-attempt latency, not a 15s retry delay. The CONCLUSION that withRetry could have caused a duplicate is still plausible (a long-latency first call + a 1s retry gap CAN produce the observed pattern), but the specific arithmetic claim in the commit message is wrong. Severity MEDIUM because the fix (remove withRetry from createCampaign) is still correct — client-side retry of a non-idempotent POST is always unsafe regardless of the specific delay that caused the Phase 6a incident. But the forensic claim in the commit message and memory file will mislead future incident investigators. Suggestion: update the commit message / memory entry to say "the observed 17s gap is consistent with a long first-attempt latency + withRetry's 1s or 5s retry delay creating a duplicate" rather than the false "exactly matches 15s" claim.

  F4 [MEDIUM, edge-case] src/lib/campaigns/deploy.ts:144-155 (restore Campaign.updateMany guard) — The guard `status='approved' AND emailBisonCampaignId=null` is narrowly correct for the happy retry path (Bundle B rollback fired cleanly → Campaign in ('approved', null) state). But it silently no-ops in 3 edge cases that produce different observable outcomes: (1) prior attempt's Bundle B rollback fired but hit `inflightSibling` gate → Campaign was NOT rolled back (case 3 of Bundle B tests) → Campaign is still in ('deployed', ebId) state → restore guard misses → no-op → status check at line 189 passes → adapter runs with Campaign.ebId=anchor (which matches the anchor, so OK). Case-1 is benign. (2) prior attempt's Bundle B rollback fired but hit `count=0` case (Campaign was manually paused mid-deploy, Bundle B test case 5) → Campaign is in ('paused', ebId) state → restore guard misses → no-op → status check at line 189 THROWS `Campaign is not in 'deployed' or 'active'`. Case-2 means operator-paused campaigns are unretriable; acceptable but produces confusing error. (3) prior attempt's restore ran BUT the restore's tx itself was interrupted mid-way (Campaign.updateMany ran, CampaignDeploy.updateMany did NOT) — next retry sees status='failed' + ebId set + Campaign already in ('deployed', anchor) → restore guard misses (status='deployed' not 'approved') → no-op → CampaignDeploy still in 'failed' state but execution proceeds → wait no, the restore tx is atomic so this can't happen unless Prisma $transaction itself is broken. Ignore case 3. Real concern: case 2 makes manually-paused campaigns produce a generic error rather than a targeted "campaign was paused during deploy, manual intervention required" message. Severity MEDIUM because edge case but possible (BL-075 Bundle B case 5 explicitly shows a pause-during-deploy scenario). Suggestion: before the status check at line 189, read the current Campaign.status and emit a more targeted error if it's 'paused' or 'archived': "Campaign ${id} was transitioned to '${status}' during deploy; retry requires operator to explicitly resume before re-invoking."

  F5 [LOW, missing-test] deploy-rollback.test.ts has NO test for the `retryDeployChannel` pathway (src/lib/campaigns/deploy.ts:397-448). This function bypasses the new executeDeploy entry logic entirely — it calls `adapter.deploy()` directly without running the state-machine guard or the retry-restore tx. trigger/campaign-deploy.ts at lines 21-27 shows Trigger.dev can invoke either path based on payload.retryChannel: `if (payload.retryChannel) await retryDeployChannel(...) else await executeDeploy(...)`. If a retryDeployChannel invocation fails terminally AND Trigger.dev retries it (maxAttempts=2 applies at the task level, not per-payload-type), the second retryDeployChannel invocation won't go through Bundle C's state-machine guard or restore block. This is a latent concurrency gap but probably not exploited in practice because manual retryDeployChannel is user-initiated (from admin UI), and Trigger.dev retrying a user-initiated retry is unusual. Severity LOW because not a production blocker, but the test surface is uneven. Suggestion: at least add a comment to retryDeployChannel explaining that it is NOT wrapped by the Bundle C entry guard and that callers (the Trigger.dev task) should understand retryChannel-initiated runs are single-attempt from a state-machine perspective.

  F6 [INFO, documentation] Commit message says "+4 retry integration tests" but 2 of those 4 are defensive-guard tests (both verify the throw path), 1 is retry-still-fails (terminal failure after retry — valuable), and only 1 (retry-happy) exercises the recovery path — and even that one has F2's mocking-layer weakness. Net NEW behavioral coverage is 1.5 tests (retry-happy as a state-transition smoke test + retry-still-fails as a terminal-retry audit gate). The 2 defensive-guard tests are negative assertions (the throw fires, adapter never runs) which is valuable but not integration coverage of the fix. Bundle B QA F3 ("zero retry-test coverage") is technically resolved, but the coverage added is thinner than the commit message implies. Severity INFO because the tests DO pass and do catch some regressions; this is more a narrative-vs-reality framing comment.

  Coordinated B+C contract assessment: COHERENT for the canonical retry path. The dev's 4-scenario analysis (Trigger.dev retry, manual retryDeployChannel, concurrent initiate, terminal failure) is largely accurate. Scenarios verified from code: (1) Trigger.dev automatic retry same deployId: rollback fires (Bundle B), restore fires on re-entry (Bundle C), reuse branch in email-adapter Step 1 prevents duplicate create. GOOD — the canonical happy path works. (2) Concurrent initiateCampaignDeploy (different deployId, same campaignId): Bundle B's sibling-gate at deploy.ts:330-341 skips rollback for the losing deploy, preventing clobber of the winner's Campaign state. GOOD. (3) Terminal no-retry (maxAttempts exhausted): Bundle B rollback fires on final attempt's catch, Campaign clean. GOOD. (4) Manual retryDeployChannel: does NOT pass through Bundle C's entry guard (F5 above) — uses the separate retryDeployChannel function at deploy.ts:397-448. BORDERLINE — works for the intended single-channel retry case but lacks the state-machine guard. **The concerning scenario not in the dev's analysis: (5) Step-1-failure retry** (F1 above) — retry attempts are DROPPED by the defensive guard, a regression vs pre-Bundle-B. This is the primary BLEMISH on the coordinated contract. The overall architecture (Bundle B clears Campaign, Bundle C restores from CampaignDeploy anchor on retry) is sound; the anchor-first write order (email-adapter.ts:296-299) is the correct choice; the idempotent restore guard is correct for the canonical case. But F1's regression means the "retry semantics stronger after Bundle B+C than before" claim is OVERSOLD — retries are stronger for Step 2+ failures, WEAKER for Step 1 failures.

Verdict summary: SHIP-WITH-FIXES. Does NOT block Phase 6.5c push for the canary re-run (the canonical retry path works), but the 2 HIGH findings should be addressed in a Bundle C.1 follow-up within the same phase: (F1) widen retry path to handle Step-1-failure-with-null-anchor, (F2) add integration-level dual-orphan regression test with a real EmailBisonClient mock counting createCampaign calls across retry attempts. Orchestrator's call on whether to spin Bundle C.1 now vs defer; if deferred, file as BL-079 (Step-1-retry regression) and BL-080 (dual-orphan integration test). Confidence HIGH on canonical retry correctness, MEDIUM on edge-case coverage, LOW on the "regression gate" claim in the retry-happy test.

[2026-04-16T15:15:00Z] -- Phase 6a-retry PREFLIGHT HALT QA (checkpoint review, not PR gate). Dev halted Phase 6a-retry at preflight because LinkedIn canary cmneqixvz's sole LinkedIn sender cmmxon6vi (Daniel, 1210-solutions) failed gate-12 with healthStatus='session_expired' AND sessionStatus='active' — field contradiction, not sender failure. Email canary 12/12 PASS. 5 findings:

  F1 [INFO, governance] Halt decision was CORRECT-BY-SPEC. Phase 6a-retry brief ([15:00Z] decisions.md entry) defines sequence: "6a-r.1 preflight read-only per campaign (12 gates each); 6a-r.2 fire email canary ...; 6a-r.3 fire LinkedIn canary ONLY on 6a-r.2 success". Hard rules include "STOP on first failure". The brief does NOT explicitly say "stop on preflight fail for EITHER canary blocks both", but the sequencing treats preflight as a pre-gate: if 6a-r.1 produces a BLOCKED verdict for either canary, the Tier 3 fire that depends on it does not proceed. Dev was conservative and halted both; an equally defensible reading is "email canary passed 6a-r.1, so 6a-r.2 may fire; LinkedIn canary failed 6a-r.1, so 6a-r.3 does not fire". The "stop on first failure" rule as written applies to FIRE failures (polling timeout, terminal status=failed, EB parity mismatch), not preflight gate-failures on a different canary. Recommendation: dev's halt was safe and brief-aligned; PM can either ratify the halt or explicitly narrow the rule to "preflight blocks fire ONLY for the same canary". Either is correct-by-spec. No structural bug.

  F2 [HIGH, pattern-inconsistency] The sessionStatus/healthStatus contradiction is a REAL data-integrity gap, not intentional design. Confirmed code paths: src/lib/linkedin/session-refresh.ts:58-64 atomically writes BOTH sessionStatus='expired' AND healthStatus='session_expired' (correct). src/app/api/extension/senders/[id]/expiry/route.ts:47-64 does the same atomic pair (correct). BUT worker/src/keepalive.ts:146 calls `api.updateSenderHealth(sender.id, "session_expired")` — which hits PATCH /api/linkedin/senders/[id]/health/route.ts — which updates `healthStatus` ONLY and leaves sessionStatus untouched (route.ts:39-47 body only reads `healthStatus`, `lastKeepaliveAt`, `linkedinProfileUrl`). Consequence: when the worker's keepalive endpoint call fails (401/403/non-200), it marks healthStatus='session_expired' while sessionStatus remains 'active'. Daniel's state fits this exactly — lastKeepaliveAt 2026-04-16T09:46:58Z is only ~4.75h stale (session-refresh.ts threshold is 12h, so it didn't fire there), but the worker's own keepalive attempt at ~09:46 likely got a 401 and called updateSenderHealth without the co-write. The health-check cron's Step 3.5 (src/lib/linkedin/health-check.ts:93-127) WILL auto-recover this on next 6am run — it sees healthStatus='session_expired' + sessionStatus='active' and flips healthStatus back to 'healthy'. But this papers over the real problem: the keepalive failed, which means the session likely IS broken, and auto-recovery will then cause worker actions to attempt with a dead cookie. The fix: PATCH /api/linkedin/senders/[id]/health/route.ts should co-write sessionStatus='expired' whenever healthStatus is set to 'session_expired' (or worker should call a dedicated endpoint that does both). File: src/app/api/linkedin/senders/[id]/health/route.ts:39-47 and worker/src/keepalive.ts:146. Recommend filing as BL-082 (medium/P3) — NOT blocking Phase 6a-retry but a real semantic bug that will keep producing these preflight halts until fixed.

  F3 [INFO, governance] If PM chooses option 2 (fire email-only now): BL-068 fix (LinkedIn-specific `position: undefined` bug in src/lib/channels/linkedin-adapter.ts via prisma.campaignSequenceRule.createMany) WILL NOT be exercised by an email-only canary. Email deploy path never calls linkedin-adapter.ts — it hits src/lib/channels/email-adapter.ts + EmailBisonClient.createSequenceSteps (v1.1). The 6.5b fix set under test is: BL-074 (createSequenceSteps v1.1 wire-shape + remaining callers) — exercised by email ✓. BL-075 (atomic Campaign.status rollback on terminal failure) — exercised by email ✓ (deploy.ts catch is channel-agnostic). BL-076 (Trigger.dev retry preExistingEbId reuse) — exercised by email ✓ (CampaignDeploy anchor is channel-agnostic). BL-078 (shared isNotFoundError helper) — exercised by email partially (email-adapter.ts:228 is one of the 2 callers of the helper; LinkedIn path also uses it, unexercised). BL-068 (position vs stepNumber Zod parse) — NOT exercised. Implication: Phase 6b.1 GO decision on an email-only canary leaves a LinkedIn-specific regression hole. If Phase 6b includes LinkedIn campaigns (15 remaining per [2026-04-16T00:00Z] entry includes LinkedIn), firing Phase 6b without LinkedIn canary validation is riskier. Recommendation: if PM chooses email-only now, file the LinkedIn canary re-fire as a separate gating step (call it 6a-retry-LI) BEFORE any LinkedIn campaign in Phase 6b, not as an absolute blocker on Phase 6b as a whole.

  F4 [MEDIUM, pattern-inconsistency] If PM chooses option 1 (refresh Daniel's session): two sub-paths and they behave differently. Sub-path A (admin clicks "Reactivate" in UI → POST /api/senders/[id]/reactivate): src/app/api/senders/[id]/reactivate/route.ts:37-53 sets healthStatus='healthy' + healthFlaggedAt=null but does NOT touch sessionStatus, sessionData, or lastKeepaliveAt. Preflight gate-12 filter (`sessionStatus === "active" && (healthStatus === "healthy" || healthStatus === "warning")` at _phase6a-preflight.ts:222-226) WOULD pass. But the session data is the same stale/broken cookie the worker already failed to keepalive — the preflight would green-light a canary that's going to fail at first worker action. THIS IS THE WORST OUTCOME: false-positive preflight enables a broken-cookie fire. Sub-path B (admin re-auths via Chrome extension → POST /api/extension/senders/[id]/cookies): cookies/route.ts:66-77 sets sessionData + sessionStatus='active' + healthStatus='healthy' + lastActiveAt=new Date() (all fresh). Preflight gate-12 would pass AND the cookie is fresh. This is the correct recovery. BUT — preflight would ALSO pass in sub-path A, and gate-12 gives PM no way to distinguish. Recommendation: before re-firing preflight in option 1, verify Daniel was re-authed via extension (check sessionConnectedAt bumped to today) not just admin-reactivated. OR: tighten preflight gate-12 to additionally require `lastKeepaliveAt` within the last 6h (less than keepalive interval), which catches the admin-reactivate-only case. File: scripts/maintenance/_phase6a-preflight.ts:208-231.

  F5 [INFO, governance] Other adversarial concerns on ANY option: (a) Phase 6.5c deploy aayzlcex9 is live per brief (both domains 200 OK) — no cache-layer concern on the Next.js app (route handlers re-execute on every request, no build-time static cache on POST /api/...). (b) Trigger.dev config: brief says "retry config untouched (maxAttempts=2 preserved)". If the Phase 6.5c deploy bumped the Trigger.dev task to a NEW deployment ID (aayzlcex9), any in-flight Trigger.dev run from Phase 6a would have been killed — but no Phase 6a run is in-flight (Phase 6a-rollback completed per [09:45Z]). New runs will pick up the current deployment. No stale-task concern. (c) EB 78 orphan (preserved for BL-077) is NOT a blocker — email canary will create EB 80+ (or whatever next numeric ID), not reuse 78. (d) One subtle concern: preflight's gate-8 ("targetList.people >0 (expect 44/27)") only asserts >0, not exact count. If TargetList.people drifted (someone deleted a person, dedup ran, etc.) the deploy would proceed with a different lead count than Phase 6a-retry brief assumed. Low probability since rollback completed 6h ago, but worth checking actual counts against expected before firing.

Verdict: dev's halt was CORRECT, F2 is a REAL bug (warrants BL-082), F3 flags a coverage hole in option 2, F4 flags a preflight blind-spot that could cause a false-positive re-try in option 1. Recommendation ranking: option 3 (file BL-082 + abort Phase 6a-retry, re-fire after fix) = safest. Option 1 (extension re-auth + verify via sessionConnectedAt bump, not admin-reactivate) = fast, acceptable if PM verifies the recovery path. Option 2 (email-only) = acceptable if PM explicitly narrows Phase 6b.1 to "email campaigns ONLY" until a separate LinkedIn canary validation fires. Do NOT block for the sake of blocking — halt was correct-by-spec, contradiction is a real code bug (F2), all three options are reasonable with caveats.

[2026-04-16T13:45:00Z] -- QA review (canary stage-only deploy / skipResume feature, uncommitted diff on deploy.ts + types.ts + email-adapter.ts + linkedin-adapter.ts + _canary-stage-deploy.ts + deploy-skip-resume.test.ts). VERDICT: conditional-PASS on the 5 critical foci; 3 non-blocking findings on adjacent paths. tsc --noEmit clean; new deploy-skip-resume.test.ts 2/2 pass; existing deploy-rollback + email-adapter-deploy + email-adapter-race + deploy-campaign.integration all pass (17 + 21 = 21 unique tests green with no regression).

  Critical-foci verdict (all PASS):
  (1) skipResume-path-must-not-call-resumeCampaign: PASS. grep `resumeCampaign\\b` across src/ shows exactly one deploy-path call site — `src/lib/channels/email-adapter.ts:636 await withRetry(() => ebClient.resumeCampaign(ebCampaignId))` — placed AFTER the `if (skipResume) { ... return; }` early-return at lines 618-633. The early-return writes CampaignDeploy.emailStatus='complete' + emailError='STAGED — ...' and returns BEFORE reaching line 635 `currentStep = DEPLOY_STEP.RESUME_LAUNCH` or the resumeCampaign call. Step 10 (verify getCampaign) at lines 650-659 is also bypassed. linkedin-adapter.ts has no resumeCampaign call at all (pull model). The `resume()` method at email-adapter.ts:736-745 is a separate lifecycle-API method invoked only by `src/app/api/campaigns/[id]/status/route.ts::resumeCampaignChannels` (pause/resume UX), NOT by any deploy path.
  (2) EB-campaign-stays-DRAFT: PASS. EB campaigns are created DRAFT by `ebClient.createCampaign` (confirmed at email-adapter.ts:257). Nothing between Step 1 and the skipResume early-return mutates EB campaign status (Steps 2-8 only add sequence steps, leads, schedule, senders, settings, tags — none change campaign state). resumeCampaign is the only EB status-mutating call in the deploy path, and it's skipped. Verified.
  (3) Campaign.status-stays-deployed: PASS. grep `status:\\s*"active"` scoped to src/lib/campaigns/ finds exactly ONE write in the deploy path — `deploy.ts:253` (inside `prisma.campaign.updateMany`), which is correctly gated by `finalizedDeploy.status === 'complete' && opts?.skipResume !== true` at line 249. The else-if branch at line 260-264 handles the skipResume=true case with just a log line (no DB write). No other code path under executeDeploy transitions Campaign.status to 'active'. Unit test `deploy-skip-resume.test.ts` explicitly asserts `prismaMock.campaign.updateMany).not.toHaveBeenCalled()` on the skipResume=true case and DOES fire on the default case — both pass.
  (4) BL-075 auto-rollback contract unchanged: PASS. Outer catch at deploy.ts:294-417 is untouched by this diff (git diff confirms: only lines 68-83, 92-95, 205-225, 234-264 are modified — none inside the catch). Catch block structure preserved: same `$transaction` callback, same inflight-sibling check, same Campaign.updateMany({status:'deployed'}) race-safe guard, same AuditLog write with erroredStep. Live-run evidence corroborates: script failed at Step 2 (BL-068 shape validation), rollback fired cleanly (Campaign reverted 'deployed'→'approved', ebId cleared), matches the BL-075 contract exactly.
  (5) skipResume-branch-placement: PASS. Branch is at email-adapter.ts:618-633, positioned (a) AFTER Step 8 completes — Step 8 is a documented no-op at line 597 ("intentional no-op — see above"), so "Step 8 completes successfully" is vacuous but Steps 1-7 all precede the branch inside the same try block; and (b) BEFORE line 635 `currentStep = DEPLOY_STEP.RESUME_LAUNCH` and line 636 resumeCampaign call. The branch is INSIDE the outer try/catch (lines 186-714) so any throw from Steps 1-7 still trips the catch and rethrows, which is what drove the observed live-run rollback from Step 2. Any throw from the CampaignDeploy.update call at lines 622-631 ALSO trips the outer catch (would tag emailError '[step:8] <msg>' since currentStep was last set to Step 8 equivalent at prior steps — actually re-reading, currentStep is left at ATTACH_TAGS=8 by Step 7's earlier assignment at line 579 — UPDATE_SETTINGS=7 then no reassignment before the skipResume branch — so a DB failure on the emailError write would tag `[step:7]`. Minor accuracy issue — see F2 below. Rollback would still fire correctly.)

  Non-blocking findings (all on adjacent paths, not the 5 critical foci):
  F1 [MEDIUM, correctness] `notifyCampaignLive` fires on skipResume=true. deploy.ts:285-292 sends the client-facing "Your campaign X is now live" Slack+email notification whenever `deployStatus === 'complete'`. On skipResume=true, CampaignDeploy.status IS 'complete' (the adapter sets emailStatus='complete'; finalizeDeployStatus rolls that to overall 'complete'), so `notifyCampaignLive` fires — but the EB campaign is actually in DRAFT and Campaign.status is 'deployed' (not 'active'). Result: the client gets a false "your campaign is live" email/Slack ping while the campaign is still in stage-only state awaiting PM launch. notifyCampaignLive does NOT read Campaign.status or CampaignDeploy.emailError to verify — it blindly sends on status='complete'. File: src/lib/campaigns/deploy.ts:285-292 + src/lib/notifications.ts:688-707. Suggestion: gate the notifyCampaignLive call behind `opts?.skipResume !== true`, symmetrical to the deploy.ts:249 auto-transition gate. This wasn't caught because the live canary run failed at Step 2 before reaching the notification path, so the bug is latent — will fire on the first successful skipResume run. Reachability: 100% on any successful skipResume deploy.
  F2 [LOW, pattern-inconsistency] skipResume branch does not set `currentStep` before the DB write. At email-adapter.ts:618-633, if the `prisma.campaignDeploy.update` call at line 622 throws (DB transient failure, etc.), the outer catch at line 699-713 will tag emailError as `[step:7] <msg>` because currentStep was last set at line 579 to `UPDATE_SETTINGS=7`. That's misleading — the failure isn't in Step 7, it's in the stage-complete write. Pattern precedent: every other step re-assigns currentStep at the top of its block (see lines 208, 294, 399, 436, 523, 552, 579, 635, 650). Suggestion: before line 622, add `currentStep = DEPLOY_STEP.ATTACH_TAGS;` (the virtual Step 8) or introduce a new STAGE_COMPLETE constant. Non-blocking because DB writes on our own table rarely fail, but if one does, the step tag is wrong.
  F3 [MEDIUM, missing-test] email-adapter.ts's skipResume branch (lines 618-633, 16 lines) is NOT exercised by any test. `deploy-skip-resume.test.ts` mocks the adapter entirely (vi.mock("@/lib/channels", () => ({ getAdapter: () => ({ deploy: adapterDeployMock }) }))), so only the deploy.ts-side gating is tested. src/__tests__/email-adapter-deploy.test.ts has 6 tests (fresh deploy, re-deploy, race, zero-senders, zero-leads) — none set `skipResume: true`. Consequence: the adapter's early-return, CampaignDeploy.update payload (emailStatus, emailStepCount, leadCount, emailError), and the specific 'STAGED — ...' string are untested. Regression risk: if someone changes the emailError literal or the update payload shape, no unit test catches it. Suggestion: add a test to src/__tests__/email-adapter-deploy.test.ts: "skipResume=true: skips Step 9 resumeCampaign + Step 10 verify, marks CampaignDeploy.emailStatus='complete' with 'STAGED' prefix, returns before resumeCampaign call". Verify ebMock.resumeCampaign not called and prismaMock.campaignDeploy.update called with emailError matching /^STAGED/. Same pattern already used by the "zero leads" and "zero senders" tests in that file.
  F4 [LOW, dead-code/contract-gap] `retryDeployChannel` (deploy.ts:424-475) does NOT accept or forward `skipResume`. If a skipResume deploy fails mid-flight (e.g. Step 2 shape validation, like the live canary), CampaignDeploy ends up in 'failed'/'partial_failure' with emailStatus='failed'. If admin then invokes the standard "retry this channel" UX (calls retryDeployChannel via trigger/campaign-deploy.ts:26 with retryChannel payload), the retry will call `adapter.deploy({...})` WITHOUT skipResume, which means Step 9 resumeCampaign WILL fire and the EB campaign WILL launch — contradicting the PM's original stage-only intent. No caller passes skipResume through retry today (it's a one-off script), so latent; but any future admin/UI wiring of skipResume needs to thread the flag through retryDeployChannel as well, otherwise "retry after stage-deploy failure" silently escalates to a full launch. Suggestion: either add `opts?: { skipResume?: boolean }` to retryDeployChannel (symmetric to executeDeploy) and read the original intent from CampaignDeploy metadata; or document loudly in the DeployParams comment that retryDeployChannel does NOT inherit skipResume (today it doesn't, and the trigger.dev task payload `CampaignDeployPayload` doesn't carry skipResume either, so Trigger.dev retries of a skipResume deploy would also silently escalate to launch — same gap in trigger/campaign-deploy.ts:21-35).
  F5 [INFO, pattern-inconsistency] LinkedIn adapter's `skipResume` local is parsed at line 97 but only used at line 207 (log suffix); the empty-sequence early-return branch at lines 119-132 never references it. If `campaign.linkedinSequence ?? []` has length 0 AND skipResume=true, the function returns at line 131 without ever touching the parsed variable. Compiler doesn't flag because TypeScript doesn't warn on conditionally-unused locals. Minor. Suggestion: either drop the parse (LinkedIn truly has no skipResume-specific behavior) OR include skipResume in the empty-sequence log message for consistency. Prefer the former — the current parse + log-suffix is decorative and adds ~13 lines of JSDoc explanation for 1 conditional string append. Keeping it is defensible for "future-proofing" but borderline dead code today.

  Overall: 5 critical foci all PASS, no resumeCampaign leaks on the skipResume path, Campaign.status stays 'deployed', EB campaign stays DRAFT, BL-075 rollback untouched. F1 is a latent correctness bug (false "campaign live" notification) that should be fixed before the skipResume script is re-run after BL-068 resolves. F3 (missing test) is the most addressable immediate gap — the adapter's skipResume branch has zero coverage. F4 documents a contract gap that will bite future admin-retry workflows if they build on skipResume. Recommend: fix F1 before next skipResume canary attempt, add F3 test, file F4 + F5 as low-priority backlog.

## 2026-04-16 — Canary Run E Step 3 failure: EB rejects empty subject on reply-in-thread follow-up

**Campaign:** cmneqixpv0001p8710bov1fga (1210 Facilities/Cleaning — canary)
**Run:** stage-only deploy via `scripts/maintenance/_canary-stage-deploy.ts`
**Failure:** Step 3 (upsert sequence steps) — EB 422 `sequence_steps.1.email_subject field is required`
**New orphan:** EB 81 (1210-solutions workspace, status=draft, 0 leads, 0 steps)
**Auto-rollback:** BL-075 fired correctly — Campaign.status=approved, emailBisonCampaignId=null, deployedAt=null

**Root cause (QA Finding 1, CRITICAL):** `_bl083_repair_1210_emailsequence.ts:109` stripped empty-string `subject: ""` during shape repair. This was a pre-existing reply-in-thread marker. Downstream, email-adapter.ts:405-412 emits `subject: undefined` → client.ts:380 sends `email_subject: ""` → EB v1.1 batch endpoint 422s.

**Deeper root cause:** email-adapter has no handling for reply-in-thread follow-up steps with empty subjects. Even preserving the empty string would not have fixed Run E — the adapter would still send `email_subject: ""` and EB would still reject. The repair script merely moved the failure from Step 2 (zod parse) to Step 3 (wire validation).

**Filed:** BL-085 (P1 high) — adapter fix + EB 81 cleanup + audit of other 1210 campaigns for same pattern.

**Impact scope:** Industrial/Warehouse, Green List, Construction likely have same pattern — must audit before attempting their deploys. Healthcare already canonical. Lime 6 presumed unaffected.

[2026-04-16T14:50:00Z] -- BL-085 Fix A+B (commit df0ed71d) QA review. Tolerant-parse on createSequenceSteps HTTP 200 + withRetry removal on Step 3. tsc clean (exit 0), client-sequence-steps.test.ts 14/14 pass, src/lib/channels/__tests__/ 80/80 pass. 7 findings raised.

**F1 [HIGH] withRetry matrix reasoning is INCORRECT — dev's Step 5 createSchedule KEEP decision relies on a wrong premise.** File: src/lib/channels/email-adapter.ts:610-617 + src/lib/utils/retry.ts:11-29. Dev's reasoning: "schedule is a singleton per campaign; duplicate POST returns 422 which isn't in the retry set {429,500,502,503,504}". The retry set referenced is EmailBisonClient.RETRYABLE_STATUSES (client.ts:85) — the INTERNAL HTTP retry inside `request()`. But `withRetry` (retry.ts:11) is an OUTER wrap that catches ALL thrown errors regardless of status code and retries 3 times with exponential delays [1000ms, 5000ms, 15000ms]. When EB returns 422, client.ts:118-121 throws EmailBisonApiError immediately (fails fast inside the client), and that throw bubbles to withRetry which THEN retries it. So a duplicate-schedule 422 triggers 3× redundant POST calls with 1s+5s+15s delays = ~21s of wasted latency before rethrow. This is the SAME amplifier pattern that caused BL-085's 3× sequence step insertion on Phase 6.5c canary. Step 5's newly-discovered `save_as_template required` 422 (PM's "Newly discovered issue #1") will now burn 21s of retry per deploy attempt instead of failing fast. Suggestion: either (a) extend the commit scope to remove withRetry from createSchedule AND createLead AND attachLeadsToCampaign AND attachSenderEmails POST calls using the same reasoning as Step 1 (createCampaign, line 245-257 comment) and Step 3 (createSequenceSteps, line 476-492 comment), or (b) make withRetry status-aware — inspect EmailBisonApiError.status inside the catch and skip retry for non-retryable statuses.

**F2 [HIGH] attachLeadsToCampaign and createLead dedupe assumptions are UNDOCUMENTED.** File: src/lib/channels/email-adapter.ts:544-552 (createLead), 582-584 (attachLeadsToCampaign) + docs/emailbison-dedi-api-reference.md:424-441, 1449-1462. Dev's audit matrix claims "EB dedupes by email" for createLead and "EB dedupes link" for attachLeadsToCampaign. The EB docs say the OPPOSITE for createLead — line 1458: "The email address of the contact. Must be unique and in a valid email format." That phrasing implies EB REJECTS duplicates (422), not silently dedupes. For attach-leads (docs line 424-441), there is NO mention of dedupe behaviour — only `allow_parallel_sending` and `lead_ids` are documented. Dev's matrix is asserting behaviour the docs do not confirm. If EB actually returns 422 on duplicate-email createLead, the outer withRetry will loop 3× on every duplicate person — 100ms-per-lead throttle + 1s/5s/15s retry delays mean a single duplicate would add 21s per person × N duplicates = potentially minutes of wasted retry on a second deploy run. In practice this is mitigated by the WebhookEvent EMAIL_SENT dedupe pre-check (lines 528-539), but that only filters persons whose emails have ALREADY been SENT — not persons who were merely created-as-leads on a prior failed deploy attempt. Suggestion: (a) run a controlled live probe against EB (POST /leads with an existing email → observe response code) and update the audit matrix with confirmed behaviour, or (b) switch createLead to findLeadByEmail (client.ts:929) check-then-create, or (c) remove withRetry from both POST calls per F1.

**F3 [MEDIUM] Tolerant-parse creates blind trust on HTTP 200 — no post-POST verification that steps were actually inserted.** File: src/lib/emailbison/client.ts:413-456 + src/lib/channels/email-adapter.ts:493-500. The design rationale at line 414 says "HTTP 200 is the success signal". But if EB degrades into a mode where it returns 200 + weird shape but DID NOT actually persist the steps, the adapter silently proceeds to Step 4 (attach leads + launch) with an empty sequence. The campaign would launch, queue leads, and fail to send anything (no steps to schedule). The `existingSteps` diff at line 400-402 only runs on the REUSE path (preExistingEbId != null), not on a fresh deploy — so fresh deploys have zero insertion verification. A 3-line fix: after `ebClient.createSequenceSteps(...)` at line 497, add a defensive `const verifySteps = await ebClient.getSequenceSteps(ebCampaignId)` and assert `verifySteps.length >= missingSteps.length`, throwing if insertion didn't take. This is cheap (one extra GET per deploy) and converts a silent-failure mode into a loud one. The Step 10 VERIFY_STATUS pattern already exists (line 48) — Step 3 could adopt the same philosophy.

**F4 [MEDIUM] Warn log signature is inconsistent with email-adapter.ts BL-085 warns — operational grep will miss it.** File: src/lib/emailbison/client.ts:452-454 + src/lib/channels/email-adapter.ts:445-447. The PM spec said "the warn log is the ONLY signal of EB shape drift" — so discoverability matters. client.ts:453 logs with prefix `[EmailBison] createSequenceSteps: HTTP 200 but response failed schema validation — tolerating drift ...` which does NOT include the ticket number "BL-085". email-adapter.ts:445-447 logs `[email-adapter] BL-085: Campaign ... has empty subjectLine ...` which DOES include "BL-085". Ops ship-runbook greppers searching `/BL-085/` in production logs will miss the tolerant-parse warn entirely. Suggestion: add `BL-085:` to the client.ts warn prefix so both signals are greppable under the same ticket tag, e.g. `[EmailBison] BL-085 createSequenceSteps: HTTP 200 ...`. Tests at client-sequence-steps.test.ts:344-346 check `/createSequenceSteps/`, `/tolerating drift/i`, `/Raw \(first 500 chars\)/` but not `/BL-085/` — update tests to assert the ticket tag once it's added.

**F5 [MEDIUM] Test assertion depth — the `(b'-regression)` canary test uses a FABRICATED response shape (`{data: {}, success: true, campaign_id: CAMPAIGN_ID}`), not the actual production shape.** File: src/lib/emailbison/__tests__/client-sequence-steps.test.ts:356-374 + scripts/maintenance/_bl085-probe-eb82.ts:36-38. The probe script at _bl085-probe-eb82.ts:36-38 explicitly says "Not firing — creating steps would mutate EB state; only GET probed above." So the actual 200-with-weird-shape body that caused the canary failure was never captured. The test comment at lines 356-358 acknowledges this: "Use the shape most likely to match what EB actually sent. Even if the exact production shape is different, the tolerant parse path covers all non-matching 200 responses." That's a fair design — the tolerant parse is intentionally shape-agnostic. But as a REGRESSION guard it's weak: if EB's actual shape happens to match a future schema update that we write to be tolerant-of, the test would still pass but the regression would recur. Suggestion: capture the real response body on the NEXT canary attempt (low-risk — add temporary `console.log(JSON.stringify(res))` in client.ts before the Zod parse OR fire the probe against a throwaway EB campaign ID) and pin the test to that real shape. The 5 table-driven hypothetical shapes are good defense-in-depth but cannot replace the actual production shape as a regression fixture.

**F6 [LOW] Tolerant-parse `rawPreview` branch misses a case and could show `"[object Object]"`.** File: src/lib/emailbison/client.ts:440-451. The preview-building logic handles three cases: `undefined` (line 441-442), `string` (443-444), and object-via-JSON.stringify (446-450). What it misses: `number`, `boolean`, `null`. If EB returned `null` as the body (JSON.parse("null") === null), `typeof res === 'object'` is true (null is object in JS), `JSON.stringify(null)` returns the string `"null"` → rawPreview = `"null"` which is acceptable. If EB returned a number literal (`JSON.parse("42") === 42`), `typeof res === 'number'`, none of the branches match, falls to the else branch at line 446 which calls `JSON.stringify(42)` = `"42"` — also acceptable. So in practice all JSON-parseable primitives work. The real gap is when `res` comes out as an object without a useful `toString` and JSON.stringify succeeds silently producing `{}` — but that's what the test covers at line 306. Low severity — preview is bounded to 500 chars and never throws. Leave as-is or simplify the three-branch logic to `const rawPreview = res === undefined ? "(empty body)" : typeof res === "string" ? res.slice(0,500) : (JSON.stringify(res)?.slice(0,500) ?? "(unserializable)");`.

**F7 [LOW] Pattern inconsistency: client.ts has TWO different responses to 200-with-weird-shape.** File: src/lib/emailbison/client.ts:260-313 (getSequenceSteps) vs 362-469 (createSequenceSteps). getSequenceSteps throws EmailBisonError on Zod fail (line 291-296 `throw new EmailBisonError("UNEXPECTED_RESPONSE", 200, ...)`). createSequenceSteps now TOLERATES Zod fail and returns [] with a warn (line 435-456). Both are defensible individually but the codebase now has two opposing responses to the same failure mode in adjacent methods. The reasoning comment at line 408-420 justifies the split (callers don't consume the POST response; they DO consume the GET response), which is technically correct. But future maintainers will have to re-discover this reasoning every time they touch either method. Suggestion: either (a) unify both on the tolerant pattern with a shared private helper `this.tolerantZodParse(res, schema, methodName)` — but that would silently mask drift on the READ path which is the WRONG trade-off — OR (b) leave split but add a SECTION banner comment at the top of client.ts documenting "createSequenceSteps → tolerant; getSequenceSteps → strict; RATIONALE: ..." so the contrast is findable by grep. Alternative (c): keep strict on both but add a `tolerantPost` flag on getSequenceSteps that lets callers opt-into tolerance — unlikely worth the API surface complexity.

Confidence HIGH. Critical findings: 0. High findings: 2 (F1, F2 — both concern the withRetry matrix reasoning). Medium findings: 3. Low findings: 2. Commit df0ed71d is safe to keep deployed for the Step 3 fix-scope, but F1+F2 should be addressed BEFORE the Step 5 save_as_template bug is patched — otherwise the Step 5 POST will hit the same withRetry amplifier the Step 3 fix just removed. Recommend filing a follow-up backlog item "BL-086: audit remaining withRetry wraps on non-idempotent POSTs" to extend the commit's reasoning symmetrically.

[2026-04-16T16:10:00Z] -- BL-086 + BL-087 QA review (commit ca2fe6a3 fix(retry+schedule): status-aware withRetry + createSchedule body shape). Verdict: PASS WITH CONCERNS. Both fixes do what they claim; no regressions. tsc clean (exit 0, zero diagnostics). Targeted vitest: src/lib/utils/__tests__/retry-status-aware.test.ts (26 tests) + src/__tests__/email-adapter-deploy.test.ts (6 tests) = 32/32 passed. Full suite: 885 passed / 37 failed, identical to dev's claimed baseline (37 pre-existing failures: api-routes, emailbison-client pagination, proposals, notifications, channel-enrichment, ICP gate — all unrelated to retry.ts or email-adapter.ts and reproduce on df0ed71d). Tests use REAL `EmailBisonApiError` and `EmailBisonError` class instances, not duck-typed stubs (verified line-by-line at retry-status-aware.test.ts:38-115; the explicit `{ status: 500 }` plain-object test at line 105 confirms duck-typing must NOT trigger retry — strong contract). Spec compliance for BL-087: docs/emailbison-dedi-api-reference.md POST /campaigns/{id}/schedule (lines 152-169) requires 10 fields + 1 optional (`save_as_template`). PUT (lines 181-198) requires all 11. DEFAULT_SCHEDULE constant (email-adapter.ts:135-147) sends all 11 with `save_as_template:false` — matches BOTH endpoints. Removal of inline `save_as_template:false` override at the updateSchedule call site (line 618) is safe: spread `{...DEFAULT_SCHEDULE}` carries the value through. BL-086 verification independently sound: stage-deploy canary post-commit advanced past Step 5 (BL-087 proven), Step 4 surfaced 422 in ~6s with single POST (BL-086 amplifier neutralization proven on real wire). Findings (5):

F1 [MEDIUM / pattern-inconsistency] src/lib/utils/retry.ts:42-44 vs src/lib/emailbison/client.ts:85 — `RETRYABLE_EB_STATUSES` in retry.ts is a manual copy of `EmailBisonClient.RETRYABLE_STATUSES`. The latter is `private static` so it cannot be imported, and there is no compile-time or test-time guarantee they stay in sync. The header comment at retry.ts:38-41 explicitly flags this ("kept in sync deliberately. If the inner client adds 408/425, this set should follow") but discipline-by-comment is exactly the BL-085-cascade smell pattern. Two diverging sets means: if the inner client adds 408 (request timeout) to its retry set but retry.ts isn't updated, a 408 surfaces from the client (deterministic non-retryable per retry.ts) and the outer adapter throws on first failure even though the client actually wanted to retry. Suggestion: either (a) make `EmailBisonClient.RETRYABLE_STATUSES` public/exported and import it in retry.ts, OR (b) co-locate both into `src/lib/emailbison/retry-statuses.ts` and import from both call sites. Plus add a vitest assertion `expect(RETRYABLE_EB_STATUSES).toEqual(new Set([...EmailBisonClient.RETRYABLE_STATUSES_PUBLIC]))` so the sets cannot drift silently.

F2 [MEDIUM / pattern-inconsistency] src/lib/emailbison/types.ts:201 — `CreateScheduleParams.save_as_template?: boolean` is still typed as OPTIONAL in the spec-mirroring TypeScript interface, even though BL-087's whole premise is "EB v1.1 made it required on POST in practice." This means: any future caller of `ebClient.createSchedule(...)` constructing a params literal WITHOUT `save_as_template` will type-check successfully and crash at runtime with the same 422 the canary just hit. Today the only caller is email-adapter.ts going through DEFAULT_SCHEDULE which happens to include the field — but that's coincidence, not enforcement. Suggestion: flip `save_as_template` to required (no `?`) in `CreateScheduleParams` to match observed EB v1.1 behaviour, and add a code comment citing the BL-087 canary 422. Risk if you don't: the next adapter (signal-campaign deploy, for example) builds its own params object and reproduces BL-087.

F3 [MEDIUM / missing-test] src/lib/channels/__tests__/email-adapter-race.test.ts:79 + src/lib/channels/__tests__/email-adapter-reply-thread.test.ts:79 + src/__tests__/email-adapter-deploy.test.ts:84 — all three email-adapter test files mock `withRetry` as a pass-through (`vi.mock("@/lib/utils/retry", () => ({ withRetry: <T>(fn) => fn() }))`). This means the BL-086 status-aware behaviour is unit-tested in isolation (retry-status-aware.test.ts) but NEVER exercised through the email-adapter integration tests. There is no automated test that proves "when ebClient.createLead throws EmailBisonApiError(422), the adapter calls createLead exactly once and surfaces the 422 to the outer try/catch." The dev's stage-deploy canary (Step 4 ~6s, single POST) is the only verification — and that's a manual run, not a CI gate. If a future refactor accidentally double-wraps a withRetry around a non-idempotent call (e.g. someone wraps `createSequenceSteps` in withRetry again), the unit tests still pass and the email-adapter tests still pass — only a canary catches it. Suggestion: add ONE integration test in email-adapter-deploy.test.ts that uses the REAL withRetry (no mock) plus a mock ebClient.createLead that throws `new EmailBisonApiError(422, ...)` — assert createLead was called exactly once and the error propagated to emailError. Re-mock withRetry for the rest of the suite.

F4 [LOW / pattern-inconsistency] src/lib/utils/retry.ts:111-114 — the retry loop sleeps "between" attempts using `attempt < maxRetries - 1` as the sleep gate. With defaults (maxRetries=3, delays=[1000,5000,15000]), the third delay value (15000) is UNREACHABLE: attempt 0 sleeps delays[0]=1s, attempt 1 sleeps delays[1]=5s, attempt 2 falls through to the post-loop throw with no sleep. The function-level docstring at line 88-89 still claims "Default: 3 attempts, delays of 1s, 5s, 15s" — but only [1s, 5s] are ever observed. Pre-existing in the old code (verified via `git show ca2fe6a3^:src/lib/utils/retry.ts`), not introduced by this commit, but worth flagging since the rewrite is the natural opportunity to fix it. Suggestion: either change the gate to `attempt < delays.length` (use all 3 delays as documented), OR update the docstring + default delays array to `[1000, 5000]` to match actual behaviour. Picking one closes the doc-vs-behaviour drift.

F5 [LOW / missing-test] src/lib/utils/__tests__/retry-status-aware.test.ts (entire file) — predicate matrix is comprehensive on the SUPPORTED error classes but does NOT cover: (a) `RateLimitError` (extends EmailBisonApiError with status=429) — should retry per the predicate, but no test pins it down; the BL-076/BL-085/BL-086 cascade is exactly about subclass relationships so this is worth one explicit test; (b) `AggregateError` (Node 18+ throws AggregateError when all promises in Promise.any reject — if a future caller passes a fn that rejects with AggregateError, current behaviour is correctly "no retry" but undocumented); (c) `DOMException` with name='AbortError' (Edge runtime / aborted fetch — currently "no retry" because not a TypeError; intentional but untested). The test at line 99-101 covers `new Error("something broke")` returning false, which catches the most important edge case. Suggestion: add a single `RateLimitError` subclass test asserting `isRetryableError(new RateLimitError(60)) === true` — protects against a future refactor moving `RateLimitError` to extend `Error` directly instead of `EmailBisonApiError`.

Confidence HIGH on the fix correctness. Concerns are about defense-in-depth (F1, F2, F3 — preventing the same class of bug recurring elsewhere) and minor doc/test polish (F4, F5). None block deploy. The fix should land; F1 + F2 should be filed as their own backlog item (defense-in-depth: enforce retry-status sync + tighten schedule type) before the next adapter-style refactor that touches schedule or retry semantics.

[2026-04-16T16:45:00Z] -- BL-088 + BL-089 + BL-090 QA review (commit 33a9c3c4 — idempotent batch upsert, canonical RETRYABLE_STATUSES, save_as_template required). tsc: CLEAN. Vitest (3 new/updated test files, 45 tests): ALL PASS. Build health: 904 pass / 37 fail / 1 todo (net +19, ZERO new failures per commit message — baseline 37 failures are pre-existing BL-055). Verdict: SHIP. No critical or high findings. 7 findings below (1 medium, 4 low, 2 info).

F1 [MEDIUM / dead-code / stale-comment] src/lib/channels/email-adapter.ts:42 — `DEPLOY_STEP.ATTACH_LEADS` comment still reads "// Step 4 — createLead per person + attach-leads batch". BL-088 switched Step 4 from per-lead `createLead` to batch `createOrUpdateLeadsMultiple`. The comment no longer describes what the step does — it describes the removed approach. Any operator reading `[step:4]` in an emailError and consulting this constant table will be confused. Suggestion: update to "// Step 4 — batch upsert via createOrUpdateLeadsMultiple (BL-088) + attach-leads batch".

F2 [LOW / missing-test] src/lib/channels/__tests__/email-adapter-step4-upsert.test.ts — no test covers the >500-lead chunking gap. The code at email-adapter.ts:584-587 acknowledges "EB caps this endpoint at 500 leads/request ... if a future workspace exceeds 500, chunk here in 500-lead batches" but the chunking is NOT implemented — it is a TODO comment. The current code sends ALL eligible leads in a single POST regardless of count. A workspace with 501+ eligible leads will hit EB's documented 500-lead cap and likely receive a 422 or silently truncated batch. There is no test for the 501-lead scenario and no runtime guard. Suggestion: either (a) implement chunking now (500-lead batches in a loop, accumulating returned IDs), or (b) add a runtime assertion that logs an explicit warn when `eligibleLeads.length > 500` so the operator is alerted rather than silently hitting a server-side cap. File a backlog item.

F3 [LOW / pattern-inconsistency] src/lib/utils/__tests__/retry-status-aware.test.ts:227-231 — the BL-089 "single source of truth" test iterates `EB_CLIENT_STATUSES` and verifies `isRetryableError` returns true for each. This is a NEAR-TAUTOLOGY because `isRetryableError` calls `RETRYABLE_EB_STATUSES.has(err.status)` which IS `EB_CLIENT_STATUSES` (same reference since BL-089 eliminated the local copy). The test would only catch a regression where a developer re-introduced a local copy AND broke its content — it cannot catch a developer who added to the local copy without updating the exported set. The second assertion at lines 245-251 (pinning the exact 5 members) is the actual guard and would catch divergence. The first assertion at 227-231 adds false confidence without adding real coverage. Suggestion: add a comment explicitly noting the tautology and pointing to the second test as the real regression guard, OR replace the first test with a reference-equality assertion: `expect(RETRYABLE_EB_STATUSES).toBe(EB_CLIENT_STATUSES)` — this would fail immediately if a local copy is re-introduced.

F4 [LOW / pattern-inconsistency] src/lib/pipeline/signal-campaigns.ts:370 and src/lib/leads/operations.ts:823 and src/mcp/leads-agent/tools/export.ts:192 — three call sites still use the old per-lead `createLead` POST. These are NOT email-adapter paths (as noted in the commit message: "createLead() preserved for non-canary callers in lib/leads/operations.ts + lib/pipeline/signal-campaigns.ts"), but `signal-campaigns.ts:370` in particular deploys leads to EB as part of the signal pipeline — it will 422 on workspace-retained leads for the same reason BL-088 fixed the email-adapter. The per-lead loop at signal-campaigns.ts:368-386 has no 422 handling (it catches and logs via `console.error` but continues), so an existing lead fails silently and is never attached to the EB campaign. A workspace with prior signal-campaign runs will have retained leads that silently fail every subsequent signal deploy. Suggestion: migrate signal-campaigns.ts to `createOrUpdateLeadsMultiple` under a separate backlog item. export.ts is an admin MCP tool with explicit error reporting so lower urgency; operations.ts is a lead-export path also lower urgency.

F5 [LOW / missing-test] src/lib/emailbison/__tests__/client-create-or-update-leads.test.ts — tolerant parse test cases at lines 234-293 verify that drift returns [] and emits a [BL-088] warn. However, none of the 6 "unexpected shape" cases assert that the warn message contains the raw response preview (the `raw=` segment of the warn string). Test line 269 only checks for `/\[BL-088\]/`, `/createOrUpdateLeadsMultiple/`, `/response drift/`, and `/raw=/` — `raw=` is the regex, not the content. If the raw preview generation silently fails (e.g. JSON.stringify throws on a circular reference and the inner try-catch produces "(unserializable)"), the warn still fires with `raw=(unserializable)` and the test still passes. No test pins that the actual response body is captured in the warn output. This is acceptable for the current implementation but means a future change that accidentally silences the raw preview would pass tests. Info-level concern, not blocking.

F6 [INFO / missing-test] Neither test file covers the >500-lead scenario for `attachLeadsToCampaign`. The `attachLeadsToCampaign` endpoint (docs line 424) has no documented cap on `lead_ids` array size, but EB APIs typically have undocumented limits. A deploy with 501 leads would successfully upsert in the first batch (if chunking were implemented) and then attempt to attach all 501 IDs in a single `attachLeadsToCampaign` call. If EB imposes a limit here too, the attach silently 422s and the deploy marks as failed at `[step:4]`. Not actionable without EB docs confirming behaviour, but worth noting as an untested boundary.

F7 [INFO / doc-gap] The `getSequenceSteps` v1.1 drift described in the dev commit report (post-deploy read returning unexpected shape after the canary) is pre-existing behaviour documented in the `getSequenceSteps` implementation comment (client.ts:287) — the fallback path is in place and was not introduced by this commit. No new backlog item needed unless the drift escalates to a hard failure. The tolerant Zod schema on `getSequenceSteps` handles both envelope shapes. Monitoring recommendation: confirm the v1.1 drift warn appears in production ops logs after next full deploy (not canary-only) to distinguish "canary-specific edge" from "production pattern."

[2026-04-16T17:45:00Z] -- BL-093 revision re-review (uncommitted; canary EB 88 still deployed). VERDICT: SHIP. tsc clean (exit 0). Targeted vitest 184/184 pass (src/lib/emailbison + src/lib/channels + src/lib/campaigns + email-adapter-deploy). F1 (CAMPAIGN_SENDER_ALLOCATION) genuinely fixed: ran scripts/maintenance/_bl093-derive-allocation.ts against live DB — confirmed 58-sender pool [631..660, 661, 662, 663, 666..690] (664/665 absent, 661/662/663 present). Hardcoded map in src/lib/channels/email-adapter.ts:209-220 matches the live round-robin output for all 5 buckets exactly. Test fixture ALL_1210_SENDER_IDS replaced with the live pool; new per-bucket pin tests (Construction, Green, Healthcare, Industrial, Facilities) + union-partition assertion (10 tests total) lock the map against silent drift. Derive-script `where` (workspace.slug + channel + healthStatus + emailBisonSenderId not null + orderBy emailBisonSenderId asc) is the field-equivalent twin of EmailAdapter.deploy Step 6 (line 836-846) — Sender.workspaceSlug is the FK column for the workspace.slug relation, so both queries select the same rows. F2 (buildSequenceStepsForEB extraction) genuinely fixed: helper exported at src/lib/channels/email-adapter.ts:286-346, called from EmailAdapter.deploy Step 3 (line 620-624) AND signal-campaign path agents/campaign.ts:316-318 via dynamic import. Subtle bug claim verified: helper computes firstStepPosition from FULL fullSequence BEFORE the emitFilter applies (lines 297-309), so a partial re-deploy that only emits step 2 still threads correctly under step 1's stored subject — pinned by the "deploy-path predicate" test in build-sequence-steps-for-eb.test.ts:139-171. Helper preserves variable-transform ordering (helper outputs canonical {FIRSTNAME}, client.createSequenceSteps applies transformVariablesForEB at the wire boundary; transform is idempotent per its own test:99-100). F3 (orphan revert script) genuinely fixed: _bl093-prefix-revert.ts is gone from disk; never committed (git log AD-filter empty), so no deletion in git status is correct. Replacement script _bl093-derive-allocation.ts is read-only (only Prisma findMany + console.log; no $transaction/update/delete/create/EB write calls). No regressions introduced to BL-085/86/87/88/89/90 — emailbison/client.ts diff is additive (transformVariablesForEB import + thread_reply forward); deploy test's existing assertions retained, only adds thread_reply: false on existing steps and orderBy on Step 6 query. Coverage gap (NOT blocking): no integration test exercises the runtime dynamic `await import("@/lib/channels/email-adapter")` from agents/campaign.ts — but the same dynamic-import pattern is used 3 lines above for EmailBisonClient and the helper itself has 9 unit tests covering signal-path scenarios. F4/F5/F6 deferred per dev report (filed BL-097/098/099 in decisions.md).

[2026-04-16T17:30:00Z] -- BL-093 4-bug fix QA review (uncommitted; canary EB 88 deployed). VERDICT: REVISE. tsc clean (exit 0). Affected-subset vitest 171/171 pass; full suite has 37 unrelated pre-existing failures (api-routes, emailbison-client legacy, promotion-icp-gate — Prisma mock setup issues, NOT BL-093 regressions). Critical finding: hardcoded CAMPAIGN_SENDER_ALLOCATION map in src/lib/channels/email-adapter.ts is WRONG for 3 of the 5 1210 campaigns. Verified against live DB via direct Prisma query: actual healthy email senders for 1210-solutions are 58 IDs in [631..690] EXCLUDING {664,665} and INCLUDING {661,662,663}. The hardcoded map matches actual round-robin only for buckets 3 (Industrial) and 4 (Facilities/canary) — the canary worked because its bucket happens to be correct. Buckets 0/1/2 are wrong: Construction map has 663 (should be 661), Green map has 664 (does not exist in DB → silently dropped to 11 senders attached not 12), Healthcare map has 665 (does not exist → silently dropped to 11 senders). The unit test fixture ALL_1210_SENDER_IDS in email-adapter-allocation.test.ts also doesn't match production (skips 661/662, includes 664/665 — exact inverse of live DB). The disjoint-bucket test passes only against the wrong fixture. When the other 4 1210 campaigns deploy, two will silently attach 11 instead of 12 senders, and Construction will attach the wrong sender (663 belongs to Healthcare per the actual round-robin algorithm the dev claimed to follow). Other findings: (a) maintenance script _bl093-prefix-revert.ts has stale ORPHAN_EB_ID=87 (canary is now EB 88 deployed) and a docstring referring to EB 85 — re-running it would silently revert the live canary (DB → status='approved', emailBisonCampaignId=null) while leaving EB 88 running orphan; pre-state guard only WARNS, doesn't HALT. (b) signal-campaign-activate path (src/lib/agents/campaign.ts:314 createSequenceSteps call) does NOT pass thread_reply, so signal campaign follow-up steps with empty subjectLine will hit the same 422 the BL-093 thread fix prevents in deploy path — regression-by-omission. (c) writer's subjectVariantB field is silently discarded by the email-adapter — pre-existing limitation but worth noting since the canary fixture explicitly contains variant B values. Required fixes before commit: correct the allocation map sender IDs for Construction/Green/Healthcare buckets; correct the test fixture to match live DB; update the failing test pins; update _bl093-prefix-revert.ts to either be deleted (one-shot already executed) or hardcode-guard against re-running on currently-deployed canary; consider adding thread_reply pass-through to signal-campaign path or document the gap explicitly.
