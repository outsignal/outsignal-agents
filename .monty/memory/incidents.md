<!-- incidents.md | monty | seeded: 2026-04-03 -->
<!-- Write governance: APPEND only, with ISO timestamp. Max 200 lines. -->

# Monty -- Incidents & QA Findings

<!-- Append entries as: [ISO date] -- [incident or finding] -->

[2026-04-15T16:45:00Z] -- INCIDENT (critical): BL-061 campaign-deploy CLI creates duplicate EB campaigns instead of reusing/transitioning existing ones. Triggered 2026-04-15 12:17-12:18 UTC when the new `scripts/cli/campaign-deploy.ts` was run against 17 campaigns (7 lime-recruitment + 10 1210-solutions). For each of the 11 email campaigns the deploy helper called `EmailBisonClient.createCampaign()` TWICE (first attempt + retry), producing 22 orphan EB drafts. All 22 subsequently failed mid-upload with 422 "The title field is..." on the sequence step POST, leaving the DB campaign stuck in status='deployed' but with a 'failed' CampaignDeploy record and zero-lead/zero-sender EB debris. Root cause: the deploy path has no "reuse existing emailBisonCampaignId if present" branch — every invocation (including retry within the same CLI run) calls createCampaign afresh. Trigger for recurrence: ANY future campaign-deploy run that retries a failed deploy WILL reproduce the bug until BL-062 lands. Impact: user-visible duplicates in the EB UI, DB campaigns stuck in 'deployed' status blocking re-approval flow, wasted EB campaign IDs (numeric sequence burned). Cleanup: 22 EB drafts deleted via `src/lib/emailbison/client.ts::deleteCampaign`; 11 Campaign rows flipped deployed→approved; 11 CampaignDeploy rows flipped failed→rolled_back. Secondary observation: 4 of the 5 1210-solutions LinkedIn deploys in the same batch failed with `prisma.campaignSequenceRule.createMany()` schema errors — separate from the EB duplication bug, needs its own investigation (possible BL-062 companion item). Classification: critical until BL-062 ships (blocks safe re-runs of the CLI).

(No incidents recorded yet)

[2026-04-15T13:00:00Z] -- BL-060 batch B1 QA review (commits fffe6413 chat.ts, d99c0d83 monty.ts). PASS / GO verdict. tsc clean, eslint no-restricted-imports count=0, npm run chat + npm run monty both exit 0 with correct signposts, no forbidden imports, both files < 80 lines, shebang + JSDoc match nova.ts pattern. 6 findings raised, all info/low severity: (1) triplicate signpost header pattern across nova/chat/monty is copy-paste-prone — candidate for shared `printLauncherHeader()` helper if a 4th launcher is added; (2) shebang `#!/usr/bin/env npx tsx` is decorative when invoked via `npm run chat` which goes through tsx directly — acceptable but cosmetic; (3) `.monty/memory/decisions.md` was not updated with a BL-060 batch B1 entry before this QA pass (orchestrator-only write, not a dev/QA defect); (4) signpost says "Interactive orchestrator sessions are now handled by Claude Code slash commands" which duplicates the heading line's "moved to Claude Code" — minor wording tightening opportunity; (5) monty.ts has no plural slash commands section like chat.ts does — if future monty specialists (`/monty-dev`, `/monty-qa`, `/monty-security`) become user-invocable slash commands, this signpost will need to grow; (6) chat.ts title still says "Outsignal Orchestrator" while the rest of the system/docs increasingly use "Nova" — consider renaming to "Nova" for consistency with `/nova` slash commands listed below it.

[2026-04-15T14:15:00Z] -- BL-060 step 7/8 QA review (commit 96bacc63 refactor(icp): relocate extractIcpCriteria). PASS / GO verdict. All 8 mandatory checks passed: (1) behavior preservation confirmed — campaign.ts:216 call site unchanged, caller passes string + uses result via JSON.stringify(icpCriteria) which preserves full schema shape; import path `@/lib/icp/extract-criteria` correct; (2) tests meaningful — happy-path asserts returned value equals mock, asserts generateObject called once, asserts prompt CONTAINS the input description string (protects against argument decoupling); schema tests cover valid payload, wrong-type (titles not array), missing required field (industries), and optional omission (keywords); 5/5 pass via `npx vitest run`; (3) eslint clean on both files, `npx eslint . | grep -c no-restricted-imports` = 0, `src/lib/icp/**` correctly present in allow-list at eslint.config.mjs; (4) tsc clean; (5) campaign.ts leftover check — NOVA_MODEL still imported (still used at line 378 `model: NOVA_MODEL`), no orphan generateObject/anthropic, both eslint-disable comments removed; (6) stale "Claude Haiku for cost efficiency" JSDoc removed from new file, correct one-line JSDoc preserved; (7) scope — exactly 3 files touched per `git show --stat`; (8) downstream importers of campaign.ts (9 CLI scripts) only import `campaignTools` — no one was importing the moved symbols, so no break. Findings (4, all low/info severity, none blocking): (A) [low / missing-test] test file does not verify that `anthropic(NOVA_MODEL)` is invoked with the correct model identifier — the happy-path asserts the prompt contains the description but never reads `callArgs.model`, so a future regression replacing `NOVA_MODEL` with a wrong literal would pass tests silently; suggest adding `expect(vi.mocked(anthropic)).toHaveBeenCalledWith(NOVA_MODEL)` (import NOVA_MODEL from @/lib/agents/types in the test); (B) [low / missing-test] schema tests do not cover the case where `keywords` is present but not an array — since keywords is optional z.array(z.string()).optional(), a caller passing `keywords: "b2b"` (string instead of array) would be rejected at runtime but this path is untested; add a safeParse test with `keywords: "b2b"` asserting success=false; (C) [info / pattern-inconsistency] `src/lib/icp/scorer.ts` (the sibling allow-listed file referenced in the eslint rule message) uses `temperature: 0` pinning for determinism per 2026-04-11 weekend audit decision, but the relocated `extractIcpCriteria` in extract-criteria.ts:34-38 does NOT pass a temperature parameter — default is 1, so ICP criteria extraction remains non-deterministic across identical inputs; this is pre-existing behavior (not introduced by this commit) but the relocation was the opportunity to align with the scorer.ts convention; recommend follow-up BL item to pin temperature: 0 for consistency with established ICP determinism policy; (D) [info / pattern-inconsistency] new file uses template literal with embedded `"${description}"` at line 37 with no escaping — if a user supplies an icpDescription containing a `"` or `${` sequence it flows into the prompt verbatim (prompt injection surface already existed pre-refactor, preserved as-is); signal campaign creation is admin-only via CLI so impact is low, but worth noting as inherited risk. Not blocking. Commit is safe to merge; 1 of 8 BL-060 files remaining (src/lib/agents/runner.ts per the earlier batch B2 QA note; campaign.ts is now effectively clean post-7/8).

[2026-04-15T15:30:00Z] -- BL-061 QA review (CLI deploy surface: src/lib/campaigns/deploy-campaign.ts, scripts/cli/campaign-deploy.ts, route.ts refactor, 2 test files / 34 tests). Verdict: SHIP-WITH-FIXES (no blocking critical/high, one medium behavioural-regression around main() error-handling, remainder low/info). Verification: `npx tsc --noEmit` clean. `npx vitest run` = 37 fail / 785 pass; stash-compared baseline = 37 fail / 751 pass; net +34 new tests all green, zero regressions. Orchestrator memo cited baseline as 709 but actual head baseline is 751 (35 existed already in the 34 new test delta window — cosmetic note, not a defect). 37 failures are the known BL-055 exclusionEntry mock-setup suite (api-routes, emailbison-client, notifications, promotion-icp-gate, channel-enrichment, linkedin-sender) — unchanged by this work. Parity audit vs pre-refactor route (commit 6dc4f1fc, 117 lines): (1) response shapes byte-identical for all 4 non-retry outcomes — happy path `{deployId, status:"pending"}`, 404 "Campaign not found", 400 "Both leads and content must be approved before deploying", 409 "Campaign is not in approved status (may have already been deployed)"; (2) retry branch untouched except campaign-load hoisted INTO the `if (retryChannel)` block (saves one getCampaign call on retry-with-missing-campaign — behaviorally equivalent because `retryChannel=null` skips to the helper which re-loads); (3) atomic updateMany({where:{id,status:'approved'}}) race guard preserved verbatim in deploy-campaign.ts:130-133; (4) tasks.trigger("campaign-deploy",{campaignId,deployId}) signature untouched — no fields added, no reordering; (5) auditLog row shape identical (action="campaign.deploy", entityType="Campaign", entityId=campaignId, metadata={campaignName, workspaceSlug, channels, deployId}) — NOTE: refactor ADDS `deployId` to metadata, which pre-refactor did not include (pre-refactor metadata was {campaignName, workspaceSlug, channels} only). That is a minor observable change in AuditLog rows (metadata is richer, not narrower) — flagged below as info. Findings (8): (A) [medium / behavioural-regression] src/lib/campaigns/deploy-campaign.ts:148-163 — if prisma.campaignDeploy.create() succeeds and tasks.trigger("campaign-deploy") subsequently throws (Trigger.dev outage, network failure, auth rotation), the campaign is left in status='deployed' with a CampaignDeploy row status='pending' that no worker will ever pick up, and the helper throws so no AuditLog row is written. The CLI main() then throws out of the for-loop, runWithHarness catches with ok:false, but the per-ID outcomes for IDs already-processed in that batch are LOST — they never made it into the results[] array. Net effect: operator sees one JSON error, cannot tell from stdout which of 17 IDs are now in zombie 'deployed' status. This is pre-existing risk INHERITED from the pre-refactor route (same ordering), so not a regression strictly speaking — but the CLI amplifies exposure because it runs serially and the spec explicitly asks for stop-on-first-failure + partial progress reporting. Suggestion: wrap the per-ID initiateCampaignDeploy() call in the CLI's for-loop in a try/catch so a thrown helper still records a per-ID error outcome in results[] before breaking, and emit a distinct "zombie deploy — verify manually" warning if the throw happened after updateMany. Separately consider compensating logic in the helper (wrap updateMany+create+trigger in a transaction with rollback on trigger fail, OR flip status back to 'approved' in a catch block). Severity medium not high because this only fires during Trigger.dev outage — operator recovery path exists (query CampaignDeploy rows with status='pending' AND createdAt recent) but it's not documented anywhere. (B) [low / missing-test] scripts/cli/campaign-deploy.ts:147-154 readStdin() and main() have ZERO direct test coverage. Tests cover the pure helpers (splitStdinIds, parseCliArgs, classifyResult) but never exercise: stdin → splitStdinIds → parseCliArgs wiring; the serialized for-loop; post-verify success path; stop-on-first-failure break; runWithHarness envelope output. A spec-conformance test harness that spawns the compiled CLI via child_process with piped stdin would close this gap — 34 tests sounds like coverage theatre for a 316-line script that ships 17 live deploys. Suggestion: add at minimum an integration test that mocks initiateCampaignDeploy + getCampaign and invokes main() directly, asserting: stdin path works, stop-on-first-failure breaks correctly, post-verify populates verifiedStatus/verifiedDeployedAt. (C) [low / missing-test] src/__tests__/deploy-campaign.test.ts — NO test exercises the specific race where updateMany count=0 AND the pre-load `campaign.status === 'approved'` (i.e., the campaign WAS approved when loaded but got deployed by a concurrent call between load + updateMany). In that scenario the current code falls into the `not_approved` branch (because neither 'deployed' nor 'active') which leaks a misleading error to the caller. Real-world: if two operators race the same ID, the second caller sees "not_approved" even though the true state is "already_deployed". Suggestion: expand the `returns code='already_deployed'` test to a second variant where the loaded status IS 'approved' but updateMany returns count=0 — assert code='not_approved' and acknowledge in a comment that this is a known ambiguity (or refetch post-updateMany to disambiguate). (D) [low / pattern-inconsistency] scripts/cli/campaign-deploy.ts default is LIVE (--dry-run opt-in) while scripts/maintenance/patch-content-approved.ts default is DRY-RUN (--apply opt-in). Dev agent self-flagged. Rationale exists (CLI is called by Campaign Agent which expects LIVE to "just work") but this divides the ops surface into two conventions. Suggestion: document the convention split in a comment block in campaign-deploy.ts so future maintainers don't flip the default to "match patch-content-approved"; consider adding --live-confirm flag as belt-and-braces for the 17-campaign batch use-case. (E) [info / behavioural-change] src/lib/campaigns/deploy-campaign.ts:170-175 auditLog metadata now includes `deployId` which the pre-refactor route did NOT include. This is a strict superset (richer audit trail, easier forensics) but any downstream AuditLog consumer / dashboard / Slack formatter that did `JSON.stringify(metadata)` and compared byte-for-byte would see drift. Checked: `grep -rn "action.*campaign.deploy" src/ scripts/` shows no consumer reads the metadata shape back, so risk is zero today, but noting for audit traceability. Not a regression — improvement. (F) [low / dead-code] scripts/cli/campaign-deploy.ts:204 `export interface SummaryEnvelope` is exported but never imported by any external file (checked via grep). Used internally only as main() return type. Suggestion: drop the `export` or add a test that asserts shape (which would justify the export). Low priority. (G) [info / pattern-inconsistency] scripts/cli/campaign-deploy.ts uses the `invokedAsScript` guard at line 305-316 while sibling sister scripts/cli/campaign-publish.ts (23 lines) + campaign-status.ts call runWithHarness() at top level. The guard is correct HERE because tests import this file and need the pure helpers WITHOUT the script side-effect (runWithHarness would call process.exit). Siblings don't export testable helpers so don't need it. Worth a comment noting the reason (current comment says "not when imported in tests" which is adequate). Acceptable. (H) [info / observation] src/lib/campaigns/deploy-campaign.ts:98 comment says `channels defaults to ["email"]` after formatCampaignDetail but the pre-refactor route line 68 had an explicit `campaign.channels ?? ["email"]` fallback which the helper drops. Verified CampaignDetail.channels is typed `string[]` (non-optional) and set via `parseJsonArray(raw.channels) as string[] ?? ["email"]` in operations.ts:213, so the fallback is already enforced one level up. Dropping the redundant `?? ["email"]` is correct — just flagging that removing defensive nullish coalescing during a refactor is usually a pattern-consistency smell and deserves the explicit comment the dev added (line 98). Confirmed safe. Blocking findings for SHIP: none. Finding (A) should be a follow-up BL item but should NOT block the 17-campaign deploy — the failure mode requires an active Trigger.dev outage which has not occurred in the past 30 days per incident history. Recommend: ship, run dry-run first across all 17, then live deploy with an operator watching stderr. File BL-NNN for finding (A)'s recovery/zombie-deploy scripting post-deploy.

[2026-04-15T13:30:00Z] -- BL-060 batch B2 QA review (commits 2166dd2e chat.ts color fix #F0FF7A->#635BFF, b4c37f19 delete scripts/setup-client.ts, d459554e delete scripts/lime-linkedin-writer.ts). PASS / GO verdict. Verification: `npx tsc --noEmit` clean, `npx eslint . | grep no-restricted-imports | wc -l` = 0, git log --format=full confirms all 3 commits carry exact spec titles + Co-Authored-By: Claude Opus 4.6 (1M context) trailer. `git diff --stat 3e933e1f..d459554e` confirms exactly 3 files touched: chat.ts (+1/-1), lime-linkedin-writer.ts (-69), setup-client.ts (-369). Zero unrelated scope creep. Color fix verified: scripts/chat.ts line 14 `chalk.bold.hex("#635BFF")` matches monty.ts line 14 and nova.ts lines 36/81. Deletions verified via `ls`: both files gone. Residual references (2 total, both acceptable): .monty/memory/backlog.json:629 (BL-060 notes, orchestrator-only write) and .monty/memory/decisions.md:49 (batch B2 pre-delegation log). Zero references in src/, scripts/, package.json, docs/, .claude/. 5 findings raised, all info/low severity: (1) [info] commit 2166dd2e is a cosmetic color fix bundled into a BL-060 batch but correctly NOT claiming BL-060 in its subject — commit body self-declares "not a BL-060 task but bundled with the in-flight batch" which is honest and traceable; (2) [low] setup-client.ts commit body flags "hardcoded API tokens are pre-existing in git history — out of scope for BL-060, flagged separately" but QA cannot find a separate backlog entry for credential exposure — if those tokens are still valid and reachable in public git history this warrants a dedicated BL-NNN for credential rotation, not just a trailing note on a delete commit (PM action item: confirm whether a BL item exists, create one if not); (3) [info] backlog.json:629 BL-060 notes field still lists all 8 original offender files (5 now complete: analyze-website, generate-copy, chat, monty, setup-client, lime-linkedin-writer); stale but orchestrator-only write governance; (4) [info] #635BFF hex literal now duplicated across 3 files (chat.ts:14, monty.ts:14, nova.ts:36+81) — 4 occurrences total — candidate for shared `BRAND_PURPLE` constant if a 4th launcher lands; (5) [low] chat.ts line 15 heading still reads "Outsignal Orchestrator — moved to Claude Code" while every slash command listed below is `/nova*` — batch B1 QA raised this as finding #6; the color fix makes the stale branding more visually prominent without fixing it. Dev did not need to fix this (out of scope for batch B2) but it remains an open doc-drift item. 3 of 8 BL-060 files remaining (src/lib/agents/runner.ts, src/lib/agents/campaign.ts, plus whichever count reconciles with PM's running tally — QA observes batch A closed 2, batch B1 closed 2, batch B2 closed 2, total 6 of 8).
