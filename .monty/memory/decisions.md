<!-- decisions.md | monty | seeded: 2026-04-03 -->
<!-- Write governance: APPEND only, with ISO timestamp. Max 200 lines. -->

# Monty -- Decisions Log

[2026-04-16T23:59:00Z] -- BL-105 post-fix render verification for 1210-solutions (5 active/approved) + lime-recruitment (7 active) LinkedIn campaigns. Tier 1 read-only: new `scripts/maintenance/_bl105-verify-1210-lime-render.ts` uses real `buildTemplateContext` + `compileTemplate` from `src/lib/linkedin/sequencing.ts` against first target-list lead per campaign (deterministic: addedAt ASC). Rendered steps 2-4. RESULT: all 12 campaigns PASS -- zero `{UPPERCASE}` residue, zero `{{camelCase}}` residue, zero legal-suffix leaks. Only 1 suffix-bearing sample lead (Kevin @ East Lancashire Services Ltd on 1210 Facilities/Cleaning) normalised to "East Lancashire Services" cleanly. Deferred sender-name issue confirmed (log-only, not fixed): "Daniel" hardcoded in 1210 Green List + Healthcare steps 2/3; "Lucy" hardcoded in all Lime C1/C3/C4/C5/C6/C7 steps. Brief expected 8 campaigns (1+7), DB returned 12 (5+7); 4 extra 1210 campaigns in `approved` status were included per brief. Anomaly: Lime C1 and C3 sequence JSON contains TWO `type=message` entries at position=2 (connection-request-note + first-message); script rendered both. Not a bug in the fix, shape of the existing data. Commit + push to main after.

[2026-04-17T01:45:00Z] -- COMPLETED BL-104 company-normaliser polish (Tier 2 code + Tier 3 EB delete + canary DB revert, PM pre-authorized end-to-end). Continuation of the prior monty-dev session that stream-timed-out mid-execution — normaliser logic (src/lib/emailbison/company-normaliser.ts, +244/-77 vs 7a895f4b) had already been written on disk and the F5 audit logged; this session verified correctness, completed the test suite, wired the adapter call site, executed the Tier 3 pre-fix + re-stage + verification, and pushed. Commit on main (post-push). New EB canary id 92.

NORMALISER (ALREADY ON DISK, VERIFIED): src/lib/emailbison/company-normaliser.ts (501 LOC). Legal+geo suffix lists retained from BL-103. New lanes: (1) stripTrailingRoundBracket helper — regex `^(.*?\S)\s*\([^)]+\)\s*$` strips trailing `(...)` only (square brackets preserved). Iterates via stripOnce after legal/geo fail. (2) isAmpersandAtomicBrand guard — walks back from matchIndex over whitespace and checks for preceding `&`; "Bain & Company" / "Mars & Co" preserve atomically while "Foo & Bar Co" strips to "Foo & Bar" and "Acme Ltd" still strips normally. Removed "& Co" from explicit suffix list (would double-strip). (3) MAX_ITERATIONS=10 hitCap tracking + console.warn('[company-normaliser] MAX_ITERATIONS hit for input — current=... raw=...') matching variable-transform.ts/sender-name-transform.ts convention. (4) Entry trim (raw.trim()) + per-iteration trim (next = stripOnce(current).trim()) so "Acme Corp  " no longer bypasses the `\s+SUFFIX\.?$` regex anchor. (5) Overloaded signature normalizeCompanyName(raw, domain?) with 4-arity overloads preserving backward-compat — existing 1-arg callers unaffected. truncateByDomain helper parses stem (strip www., split before first '.', lowercase, '-' → ' '), token-splits both sides, checks prefix match case-insensitively, truncates only when cleaned.length > stem.length (preserving original casing on survivors).

TESTS (THIS SESSION): src/lib/emailbison/__tests__/company-normaliser.test.ts rewritten +140 lines. 51/51 pass. Two obsolete BL-103 expectations updated per BL-104 spec: 'Bar (USA) Inc' was 'Bar (USA)' → now 'Bar' (bracket lane added); 'Foo Ltd (UK)' was 'Foo (UK)' → now 'Foo'. New describe("normalizeCompanyName (BL-104)") block covers every PM-spec pair: 5 bracket cases, 12 domain cases (incl. null/""/undefined fall-through + www-prefix), 5 trim cases, 5 ampersand cases, 1 warn-on-cap spy test (vi.spyOn console.warn with pathological 12-suffix chain), 1 idempotency-with-domain case.

ADAPTER CALL SITE: src/lib/channels/email-adapter.ts line 879 now `normalizeCompanyName(entry.person.company, entry.person.companyDomain ?? null) ?? undefined`. Person.companyDomain confirmed at prisma/schema.prisma:149 (indexed, nullable). `?? null` produces uniform call signature across all leads.

F5 NAMING COLLISION AUDIT (from prior session, confirmed this session): src/lib/normalize.ts#normalizeCompanyName is the title-case normaliser (called from normalizer/company.ts + api/people/enrich + api/companies/enrich — 3 callers). src/lib/normalizer/company.ts exports classifyCompanyName (NOT normalizeCompanyName — false positive in brief). src/lib/emailbison/company-normaliser.ts#normalizeCompanyName is the wire-boundary stripper (2 callers: email-adapter.ts + linkedin/sequencing.ts from [bpp20zwu2]). Only TWO true collisions (not three). Decision: leave wire-boundary name intact (backward-compat with [bpp20zwu2]); flag normalize.ts rename candidate in commit body for follow-up backlog.

TIER 3 PRE-FIX (scripts/maintenance/_bl104-prefix-revert.ts, throwaway/untracked): Workspace 1210-solutions apiToken resolved, EmailBisonClient constructed per BL-100 precedent (single-arg ctor). Pre-state: 1 EB campaign (91, status=draft). client.deleteCampaign(91) succeeded; post-delete getCampaignById(91) returned status='pending deletion' (accepted per BL-078/BL-100 async-delete semantic). Pre-state Campaign: status=deployed/ebId=91/deployedAt=2026-04-16T17:45:55.741Z, contentApproved=true, leadsApproved=true. Transaction: Campaign update (deployed→approved, ebId 91→null, deployedAt→null, contentApproved/leadsApproved preserved), CampaignDeploy cmo1rt1nu flipped complete→rolled_back with "; BL-104 rollback 2026-04-16 — EB 91 deleted, DB reverted for normaliser polish re-canary" appended to emailError, AuditLog cmo1tlzd7 action=campaign.status.bl104_canary_revert with metadata (reason, fromStatus=deployed, toStatus=approved, clearedEbId=91, clearedDeployedAt, campaignDeployId, phase=BL-104 pre-fix). Post-check: residual workspace Campaigns with non-null ebId = 0; EB post-delete campaign count: 1 with status='pending deletion' (accepted).

RE-STAGE: _canary-stage-deploy.ts line 105 verified `executeDeploy(campaignId, deploy.id, { skipResume: true })` hardcoded. Re-run produced EB 92 — status=draft, leadCount=44, emailStepCount=3, linkedinStatus=skipped, 11/58 senders allocated, schedule set, emailError='STAGED — resume pending PM review via EB UI or manual resumeCampaign call', deployedAt=2026-04-16T18:36:32.477Z.

VERIFICATION (scripts/maintenance/_bl104-verify.ts, throwaway/untracked): Fetched all 44 leads via paginated getCampaignLeads(92, page, 100). **Zero trailing LEGAL/GEO/bracket residue across 44/44 leads.** Sonnic proof: 1 match (duarte@sonnic.com), DB='Sonnic Support Solutions' + domain 'sonnic.com' → wire='Sonnic' (domain-truncated TRUE). C4SS proof: 1 match (anuj.popat@c4ss.co.uk), DB='C4SS (Contact 4 Support Services)' + domain 'c4ss.co.uk' → wire='C4SS' (bracket-stripped TRUE). Sequence steps (position/subject/body): step 1 contains literal {COMPANY} token preserved; step 2 and 3 bodies do not use {COMPANY} (writer choice, not a regression).

15 BEFORE/AFTER PAIRS (all domains verified case-insensitively). Category distribution in real fixture: 1 domain-truncated (Sonnic), 1 bracket-stripped (C4SS), 0 '& Co' / '& Company' in fixture (ampersand case verified via unit tests only — noted in verify output), 12 legal-only, 0 geo-only, 0 already-clean, 0 bracket-only. No fake pairs — category call-outs honest:
  1. duarte@sonnic.com: 'Sonnic Support Solutions' (sonnic.com) → 'Sonnic' [domain-truncated]
  2. anuj.popat@c4ss.co.uk: 'C4SS (Contact 4 Support Services)' (c4ss.co.uk) → 'C4SS' [bracket-stripped]
  3. kevin@elservices.co.uk: 'East Lancashire Services Ltd' (els-recruitment.co.uk) → 'East Lancashire Services' [legal-only]
  4. charlotte.nourse@nationwidefm.com: 'Nationwide Cleaning & Support Services Ltd' (nationwidecleaning-fm.co.uk) → 'Nationwide Cleaning & Support Services' [legal-only, & preserved mid-string]
  5. gl@firluk.co.uk: 'Facilities Infrastructure Recruitment Ltd' (firluk.co.uk) → 'Facilities Infrastructure Recruitment' [legal-only]
  6. chelsie@frontline-construction.co.uk: 'Frontline Construction Recruitment Limited' (frontline-construction.co.uk) → 'Frontline Construction' [legal-only + domain truncation — domain stem 'frontline construction' matches 2-token prefix of 'Frontline Construction Recruitment', truncates]
  7. paul@lawrencecleaning.co.uk: 'Lawrence Cleaning Ltd' (lawrencecleaning.co.uk) → 'Lawrence Cleaning' [legal-only]
  8. nicoletag@limefms.co.uk: 'Lime Fms Ltd' (limefms.co.uk) → 'Lime Fms' [legal-only]
  9. katie.morgan@bborecruitment.com: 'Bbo Recruitment Ltd' (bborecruitment.com) → 'Bbo Recruitment' [legal-only]
  10. adam.lander@goldcrestservices.com: 'Goldcrest Cleaning Limited' (goldcrestcleaning.com) → 'Goldcrest Cleaning' [legal-only]
  11. martin@capablecleaners.co.uk: 'Capable Cleaners Ltd' (capablecleaners.co.uk) → 'Capable Cleaners' [legal-only]
  12. joanne@frontline-construction.co.uk: 'Frontline Construction Recruitment Limited' (frontline-construction.co.uk) → 'Frontline Construction' [legal-only + domain truncation]
  13. dw@dmwrecruitment.co.uk: 'Dmw Recruitment Ltd' (dmwrecruitment.co.uk) → 'Dmw Recruitment' [legal-only — domain stem 'dmwrecruitment' is concatenated, no prefix match so domain rule no-ops]
  14. jay.adderley@ceworld.com: 'Cleanevent Services Ltd' (ceworld.com) → 'Cleanevent Services' [legal-only — domain stem 'ceworld' ≠ 'cleanevent', no match; legal strip alone]
  15. stephen@evergreenfs.uk: 'Evergreen Facilities Services Ltd' (evergreenfs.uk) → 'Evergreen Facilities Services' [legal-only — stem 'evergreenfs' no prefix match]

GATES: tsc clean; eslint 0 new errors (only pre-existing 'DeployResult' unused-import warning on email-adapter.ts from BL-100); vitest 1047 pass / 37 fail / 1 todo (baseline 996/37/1 → +51 new passing tests, 0 new regressions, same 6 pre-existing failed files).

DEVIATIONS / SURPRISES: (a) EB v1.1 sequence-step shape uses `position`/`subject`/`body` rather than `order`/`email_subject`/`email_body` — verify script needed a defensive cast to read both accessors; BL-093 latent shape-drift pattern, not a bug. (b) No '& Company' / '& Co' cases in the real 44-lead fixture, so ampersand preservation is verified via unit tests only (5 cases including Bain & Company, Mars & Co, Foo & Bar Co, Bain & Company Ltd, and Acme Ltd normal path). Verify script explicitly logged this gap — no fake pairs fabricated. (c) Brief expected pairs like "DMW Recruitment, dmwrecruitment.com → DMW Recruitment"; real fixture had 'Dmw Recruitment Ltd' casing so legal strip alone applies; same semantic outcome. (d) Two post-ladder domain-truncation examples (rows 6, 12) hit the prefix rule in-fixture beyond the Sonnic canonical case — confirms domain rule fires in-wire on realistic inputs, not only the fabricated unit test.

PRE-PUSH PARITY: commit scope only src/lib/emailbison/company-normaliser.ts + src/lib/emailbison/__tests__/company-normaliser.test.ts + src/lib/channels/email-adapter.ts + .monty/memory/backlog.json + .monty/memory/decisions.md. Throwaway _bl104-prefix-revert.ts + _bl104-verify.ts untracked per convention. EB 92 left in DRAFT in 1210-solutions awaiting PM manual resume decision — skipResume=true honored throughout.

[2026-04-16T20:25:00Z] -- ORCHESTRATOR CLOSE BL-105. Dev commit cb5f6673 shipped; monty-qa independent review verdict: PASS_WITH_FINDINGS (ship). QA exceeded brief: re-ran verifier against FOUR real BlankTag Ltd-suffix leads (Charlotte Wright/Groomi Limited, Ledsone Uk/Ledsone Uk Limited, Amy James/Finnsøn LTD, Johnpaul Manning/Insulation4less Ltd) — all render with suffix stripped (Groomi/Ledsone/Finnsøn/Insulation4less), zero {UPPERCASE} residue across 3 steps × 4 leads. Also enumerated tokens across ALL 8 active LinkedIn campaigns (1210 + Lime C1-C7): only {FIRSTNAME}/{COMPANYNAME}/{JOBTITLE}/{LASTEMAILMONTH} in use — all in VAR_MAP — fix covers every live case. BlankTag blast radius zero (already paused, zero messages shipped pre-fix; parallel pause-linkedin session handled the status transitions). Vitest delta: dev claimed 1020/37/1 but actual post-commit is 1018/39/1 — 2-test delta is pre-existing in src/lib/emailbison/__tests__/company-normaliser.test.ts (BL-103 test expectations contradicted by BL-104's normaliser behavior change that landed in parallel); NOT a BL-105 regression, NOT touched by BL-105. QA filed 7 findings; 3 medium rolled into BL-106 follow-up: (F1) {EMAIL} in VAR_MAP targets unbound key → silent empty-render (no prod LinkedIn template uses {EMAIL} today so no immediate risk); (F2) jobTitle not normalised at render boundary — Lime C4/C5 use {JOBTITLE}, a lead with "X UK Ltd" in jobTitle would leak suffix (needs LEGAL-only variant since "UK Sales Director" is legit job title); (F3) missing lockstep test asserting VAR_MAP right-hand keys ⊂ buildTemplateContext returned keys — the exact coupling the dev's own JSDoc warned about. F4/F5/F6/F7 are low-priority hygiene (adjacent-token tests, malformed-brace tests, verifier robustness, namespace-collision follow-up from BL-104 F5). Backlog: BL-105 closed (done), BL-106 opened (improvement, medium, priority 3). Memory writes honored per governance: orchestrator → decisions.md + backlog.json; dev → decisions.md + architecture.md (implicit — none needed this cycle); qa → incidents.md. Hard constraints all honored across both subagents: no pause-task touches, no campaign resume, no writer prompt changes, no sender-name transformer, no normaliser duplication, no --no-verify / --force.

[2026-04-16T20:15:00Z] -- COMPLETED BL-105 LinkedIn variable + company-name rendering fix (Tier 2). Bugs (1) and (2) from the 19:25Z diagnostic resolved at the render boundary; bug (3) sender-name deferred per PM. (A) NEW src/lib/linkedin/variable-transform.ts — `transformVariablesForLinkedIn(text)` maps canonical Outsignal {UPPERCASE} tokens (FIRSTNAME/LASTNAME/COMPANYNAME/JOBTITLE/EMAIL/LASTEMAILMONTH) to {{camelCase}} Handlebars bindings matching buildTemplateContext keys verbatim. Idempotent — unlike the EB transform (target shape=input shape), LinkedIn target is DOUBLE-curly so a second pass sees no single-curly matches and is a structural no-op (covered by test). Unknown tokens pass through + `[linkedin-variable-transform] unmapped token:` warn (symmetric to EB BL-099). Null/undefined/empty handled. (B) src/lib/linkedin/sequencing.ts modified at two points: (i) compileTemplate() now calls transformVariablesForLinkedIn(template) BEFORE Handlebars.compile — chose render-time injection over DB-write-time (createSequenceRulesForCampaign) because: render-time needs no DB backfill (existing rows with raw {FIRSTNAME} just work), zero ambiguity on DB state, single call site, every render path (including adapter.getSequenceSteps fallback rendering) is covered automatically. This is the conceptual symmetry to email's wire boundary (the email wire is createOrUpdateLeadsMultiple, not DB write; LinkedIn's "wire" is compileTemplate which produces the final text that enqueueAction sends to LinkedIn). (ii) buildTemplateContext() now calls `normalizeCompanyName(person.company)` at the companyName binding. DB stays raw, only render boundary is normalised — identical contract to BL-103 at email wire. Imports normalizeCompanyName from @/lib/emailbison/company-normaliser (cross-namespace import acknowledged in decisions log; future `common/` move is a separate concern). (C) NEW tests src/lib/linkedin/__tests__/variable-transform.test.ts — 24 tests covering all 6 mapped tokens, multi-token bodies, idempotency (both single-token and full-set), unknown-token passthrough + warn assertion, warn-silence on successful map, edge cases (null/undefined/empty/plain-text/lowercase/double-curly guard), realistic BlankTag step-2 body shape. Plus 3 integration tests through compileTemplate + buildTemplateContext: Charlotte Wright @ Groomi Limited asserts no {UPPERCASE_LETTERS} residue AND no "Limited"/"Ltd" suffix in rendered output; plain Handlebars template renders idempotently (transform no-op); mixed-format template renders cleanly. (D) VERIFICATION scripts/maintenance/_bl105-linkedin-render-verify.ts (throwaway, underscore-prefixed): read-only, queries real BlankTag campaign cmnspobtf00d8p8xx8v4ew4jg → Charlotte Wright / Groomi Limited / Owner from target list → runs full buildTemplateContext + compileTemplate pipeline on all 4 steps → asserts no {UPPERCASE_TOKEN} residue AND no legal/geo suffix in rendered text. Live output for step 2: "Hey Charlotte, quick backstory. We ran Google Ads for agencies as a white label for years. Decided to cut out the middleman and go direct with brands. Groomi stood out because you are clearly investing in Google Ads already. Have you thought about getting a second opinion on your account structure?" — Charlotte substituted, Groomi (not "Groomi Limited"), zero residue, suffix stripped. Steps 3 and 4 render cleanly too (step 4 still has hardcoded "James" — that is bug (3), out of scope this cycle). (E) GATES: `npx tsc --noEmit` clean. `npx vitest run src/lib/linkedin/__tests__/variable-transform.test.ts` 24/24 pass. Full suite 1020 pass / 37 fail / 1 todo (baseline 996 pass / 37 fail / 1 todo — delta is +24 new tests, zero new failures). ESLint clean on all 4 touched files. Files (new): src/lib/linkedin/variable-transform.ts (119 lines), src/lib/linkedin/__tests__/variable-transform.test.ts (267 lines), scripts/maintenance/_bl105-linkedin-render-verify.ts (272 lines). Files (modified): src/lib/linkedin/sequencing.ts (+18 lines: two imports + 2 code changes in compileTemplate and buildTemplateContext). Commit on main. Hard constraints honored: did NOT touch pause task [pause-linkedin] (parallel), did NOT resume campaigns, did NOT modify writer prompts, did NOT build sender-name transformer, did NOT duplicate normalizeCompanyName, did NOT modify copy-quality.ts BANNED_PATTERNS, no --no-verify / --force. No memory-governance violations (wrote to decisions.md only).

[2026-04-16T20:10:00Z] -- STARTING BL-104 company-normaliser polish (Tier 2 + Tier 3 pre-fixes, PM pre-authorized end-to-end, CANARY BLOCKER P1). BL-103 landed base normaliser at commit 7a895f4b; EB 91 staged on 1210-solutions. Mid-review PM flagged two new edge cases the base normaliser doesn't catch: (A) trailing brackets e.g. 'C4SS (Contact 4 Support Services)' \u2192 'C4SS', (B) domain-based truncation e.g. 'Sonnic Support Solutions' + 'sonnic.com' \u2192 'Sonnic'. Also folding in original BL-104 QA findings: F1 trim whitespace at entry, F2 warn-log on MAX_ITERATIONS truncate, F3 ampersand preservation ('Bain & Company' atomic, no dangling '&'), F5 audit 3 duplicate normalizeCompanyName fns. Delegating to monty-dev. Parallel task isolation: (i) LinkedIn rendering fix [bpp20zwu2] imports normalizeCompanyName \u2014 export signature MUST stay backward-compat (domain param optional, existing 1-arg callers unaffected); (ii) LinkedIn audit [bqcpdd8ld] read-only, no conflict; (iii) BlankTag LinkedIn pause [pause-linkedin] DB-only, no file conflict. Plan: (1) extend src/lib/emailbison/company-normaliser.ts with optional domain param + iterative bracket strip + trim + warn + ampersand guard; (2) update caller in src/lib/channels/email-adapter.ts Step 4 (ATTACH_LEADS) to pass lead domain (investigate exact schema path: lead.domain vs lead.personWorkspace.domain vs Person.domain); (3) unit tests for every PM test case (Sonnic, Abby Cleaning, Cleanevent, Amazon, Acme, DMW, Foo Holdings x2, null/empty domain, bracket cases, & Co cases); (4) pre-fix Tier 3: delete EB 91 via client.deleteCampaign + revert Campaign cmneqixpv0001p8710bov1fga deployed\u2192approved + emailBisonCampaignId 91\u2192null + deployedAt\u2192null + AuditLog 'campaign.status.bl104_canary_revert'; (5) re-run npx tsx scripts/maintenance/_canary-stage-deploy.ts (skipResume=true hardcoded, verify at script line 105 before run); (6) verification: fetch 44 leads from new EB via paginated client.getCampaignLeads; grep specifically for 'Sonnic' (expect 'Sonnic' not 'Sonnic Support Solutions'), 'C4SS' (expect 'C4SS' not 'C4SS (Contact 4 Support Services)'), zero Ltd/Limited/Inc/UK/Scotland residue; log 15 before/after pairs incl. domain-truncated + bracket + & Co cases; (7) F5 naming collision audit \u2014 grep 3 locations, identify unused ones, rename or delete dead exports without breaking callers; (8) gates: tsc clean, vitest (baseline 996 pass / 37 fail / 1 todo), eslint no-restricted-imports=0; (9) commit + push; monty-qa follow-up for independent refetch + grep. Hard NO: live resume, LinkedIn adapter changes, delete non-canary EB campaigns, break normalizeCompanyName export signature, --force, --no-verify. Expected 20-30 min.

[2026-04-16T19:55:00Z] -- COMPLETED LinkedIn active-campaign render audit (read-only blast-radius check, Tier 1+2 throwaway script). Brief: diagnostic [boh2k3qzp] proved BlankTag's LinkedIn shipping literal `{UPPERCASE}` tokens because writer emits single-brace UPPERCASE but `src/lib/linkedin/sequencing.ts` compileTemplate is Handlebars-only. Adapter is shared across ALL clients → audit remaining active LinkedIn campaigns to decide if additional pre-fix pauses are needed before fix [bpp20zwu2] lands. Script: `scripts/maintenance/_linkedin-active-campaigns-render-audit.ts` (committed). Query: Campaign.status='active' AND channels contains 'linkedin' AND workspaceSlug != 'blanktag' → 8 campaigns found: 1x 1210-solutions (Healthcare - April 2026) + 7x lime-recruitment (C1 Manufacturing + C2 Engineering + C3 Transport+Logistics + C4 Warehouse + C5 Logistics + C6 Factory + C7 Shift Manager). Every one of the 8 campaigns scanned has step-2 (first post-connection message) emitting `{FIRSTNAME}`/`{COMPANYNAME}`/`{JOBTITLE}`/`{LASTEMAILMONTH}` tokens — all fail Rule (a) variable syntax when rendered through real pipeline with real sample lead. 4 of 8 also fail Rule (c) hardcoded sender ("Daniel" on 1210-solutions; "Lucy" on Lime C4/C5). Rule (b) N/A on all 8 (no Ltd/Limited suffixes in sampled companies — cannot assess normalisation from this sample). CRITICAL FINDING — blast radius = ZERO delivered messages across all 8 campaigns: sanity-checked via LinkedInAction.groupBy(actionType, status) filtered to campaignName — EVERY campaign has `actionType='message' count=0`. Connection requests have fired (blank notes, no template leakage possible — BlankTag-style bug hits step 2+ only); profile views + cancelled/pending connection_requests populate the table but ZERO post-connection messages have shipped yet. Full counts per campaign at stdout of sanity query. VERDICT: No additional pauses needed pre-fix. The fix cycle [bpp20zwu2] lands BEFORE any step-2 message renders because these campaigns are all early in their connection-acceptance cycle (no accepts yet → no trigger for step 2). Once fix deploys, step 2 will render correctly on first accept. Files touched: scripts/maintenance/_linkedin-active-campaigns-render-audit.ts (new throwaway, committed). No DB writes, no LinkedIn API, no status changes. Time: ~8 min. No schema surprises (Campaign.channels is JSON-string with `contains: 'linkedin'` substring match as the existing BlankTag diag uses; LinkedInAction.campaignName links to Campaign.name; DELIVERED_STATUSES=['complete'] for actionType='message').

[2026-04-16T19:35:00Z] -- STARTING BL-105 LinkedIn variable + company-name rendering fix (Tier 2, PM pre-authorized via urgent brief). Diagnostic at 19:25Z identified three live bugs; this cycle addresses (1) and (2) only — sender-name hardcoding (3) deferred per PM (BlankTag has one LinkedIn sender, no immediate impact). Plan: (A) NEW src/lib/linkedin/variable-transform.ts — `transformVariablesForLinkedIn(text)` mapping writer's {UPPERCASE} tokens → the exact Handlebars names bound by src/lib/linkedin/sequencing.ts buildTemplateContext (firstName/lastName/companyName/jobTitle/email — confirm by reading before commit). Idempotent + warn on unknown tokens (symmetric to src/lib/emailbison/variable-transform.ts). (B) Import normalizeCompanyName from src/lib/emailbison/company-normaliser.ts (do NOT duplicate) and wrap person.company read at sequencing.ts:~58 — DB stays raw, only normalise at render boundary. (C) Apply BOTH transforms at LinkedIn adapter boundary BEFORE Handlebars.compile — trace connection-poller.ts:264-270 to find exact injection point. Per diagnostic, cleanest location is compileTemplate() call site or createSequenceRulesForCampaign() when writing messageTemplate to DB — dev agent picks based on what minimises blast radius. (D) Unit tests src/lib/linkedin/__tests__/variable-transform.test.ts — every writer token, idempotency, unknown passthrough, empty/null; integration-ish render test with realistic lead asserting no literal {FIRSTNAME} + no Ltd suffix. (E) CRITICAL verification — read-only script that fetches a real BlankTag LinkedIn sequence from DB, runs it through full adapter render with a real lead, asserts no {UPPERCASE_ANYTHING} residue + no legal/geo suffix in rendered company. (F) monty-qa independently re-runs verification script + grep. Hard NO: touch pause task (parallel session owns it), resume campaigns (PM decision), modify writer prompts / sequence content, build sender-name transformer (deferred). State: main=7a895f4b (BL-103 EB company normaliser just landed). Expected 15-25 min. Delegating to monty-dev; monty-qa follow-up.

[2026-04-16T19:05:00Z] -- STARTING LinkedIn rendering diagnostic (read-only Tier 1 + one throwaway Tier 2 script). PM concern: BlankTag's LinkedIn copy may have same data-quality bugs as email (wrong variable syntax, hardcoded sender names, unnormalised company names). Plan: (1) DB query for workspace slug=blanktag Campaign rows where channels includes 'linkedin' — prefer status='pending_approval', else most recently updated. (2) Read Campaign.linkedinSequence (JSON). Identify step 2 (first message after connection accept). (3) Pick first lead by createdAt ascending from Campaign.targetList via TargetListPerson join. (4) Invoke the real render path: buildTemplateContext + compileTemplate from src/lib/linkedin/sequencing.ts (Handlebars-based, noEscape:true). (5) 3-rule assessment: (a) no literal {FIRSTNAME}/{{foo}} left in output, (b) company normalised (no Ltd/Inc/Limited suffixes), (c) sender name not hardcoded. Throwaway script path: scripts/maintenance/_blanktag-linkedin-render-diag.ts (underscore prefix, committed + pushed per carve-out rules). No mutations, no DB writes, no EB/LinkedIn state changes. Read-only access to src/lib/emailbison/* and src/lib/channels/email-adapter.ts (parallel task [btrgd9sfk] is editing those; LinkedIn adapter is ours). Expected duration 5-10 min.

[2026-04-16T19:25:00Z] -- COMPLETED LinkedIn rendering diagnostic. CRITICAL FINDINGS (three bugs, all live): (1) VARIABLE SYNTAX BROKEN — BlankTag's LinkedIn sequences emit email-style `{FIRSTNAME}`/`{COMPANYNAME}` (single-brace UPPERCASE) but the runtime render path in src/lib/linkedin/sequencing.ts uses Handlebars (`compileTemplate`, noEscape:true) which only substitutes `{{firstName}}`/`{{companyName}}` (double-brace camelCase). Result: recipients see the literal text `Hey {FIRSTNAME}, ... {COMPANYNAME} stood out because...` — variables are NOT replaced at all, they hit LinkedIn verbatim. All 4 BlankTag LinkedIn campaigns affected (C1 approved + 3 active variants C1-2C/2D/2E). Selected campaign: cmnspobtf00d8p8xx8v4ew4jg "BlankTag - LinkedIn - C1 (2E)" status=active. Writer enforcement (src/lib/copy-quality.ts BANNED_PATTERNS line 28-29) actively BANS `{{firstName}}` and `{firstName}` — i.e. the copy-quality rule enforces the syntax that DOES NOT WORK for LinkedIn and bans the syntax that DOES. (2) NO COMPANY NORMALISATION at LinkedIn render boundary — buildTemplateContext passes `person.company` verbatim into Handlebars (sequencing.ts:58 `companyName: person.company ?? ""`). BL-103's normalizeCompanyName is only applied at the EmailBison wire boundary in email-adapter.ts — LinkedIn never sees it. 8/156 BlankTag leads have Ltd/Limited suffixes (e.g. Groomi Limited, Ledsone Uk Limited, Finnsøn LTD, Insulation4less Ltd). Rendered messages for those leads WOULD leak the suffix if variable syntax were fixed. Today the bug in (1) masks this bug. (3) SENDER NAME HARDCODED in step 4 — "Hey {FIRSTNAME}, last one from me. I can put together a quick Loom walkthrough of how we'd approach {COMPANYNAME}'s account. Either way, no hard feelings. James". Hardcoded "James" (matches workspace senderFullName first-name "James Bessey-Saldanha") — will not adapt when another BlankTag LinkedIn account sends the message. No sender placeholder exists in buildTemplateContext for LinkedIn (it has firstName/lastName/companyName/jobTitle/linkedinUrl + email/outreach context, but zero sender fields). Sample lead used for rendering: Freya Lincoln, Head of Performance Marketing, Careline365 (first lead by addedAt ascending). Secondary render check against Charlotte Wright / Groomi Limited confirms the suffix would leak if (1) were fixed. Recommended fixes (NOT IMPLEMENTED — diagnostic only): (A) Symmetric to variable-transform.ts — add src/lib/linkedin/variable-transform.ts with `transformVariablesForLinkedIn` that maps `{FIRSTNAME}` → `{{firstName}}`, `{COMPANYNAME}` → `{{companyName}}`, `{JOBTITLE}` → `{{jobTitle}}`, `{LASTNAME}` → `{{lastName}}`, `{SENDER_FIRST_NAME}` → `{{senderFirstName}}` etc. Call it at the render boundary in src/lib/linkedin/sequencing.ts `compileTemplate()` BEFORE Handlebars.compile, OR at `createSequenceRulesForCampaign()` when writing `messageTemplate` to DB (preferred — matches email pattern of transforming once at wire boundary). Alternative: change the writer's copy-quality rules to emit `{{firstName}}` for LinkedIn only (channel-aware validation in src/lib/copy-quality.ts — BANNED_PATTERNS would need to differ per channel). Transform is the cleaner fix (keeps canonical `{FIRSTNAME}` format across channels, vendor-shaped tokens only at wire). (B) Apply normalizeCompanyName in buildTemplateContext (src/lib/linkedin/sequencing.ts:58) — change `companyName: person.company ?? ""` to `companyName: normalizeCompanyName(person.company) ?? ""`. Import from src/lib/emailbison/company-normaliser.ts (consider moving to src/lib/common/company-normaliser.ts if you want to decouple from emailbison namespace). (C) Extend buildTemplateContext signature to accept sender info (name, firstName, jobTitle), and update connection-poller.ts (lines 263-270 callsite) to pull from the Sender row tied to the LinkedInConnection. Update writer to emit `{SENDER_FIRST_NAME}` tokens instead of hardcoded names; add to copy-quality BANNED_PATTERNS the regex for "(Best|Thanks|Cheers|Regards|Warmly|Speak soon),\\s*[A-Z][a-z]+" near end of body to catch raw first-names in sign-offs. Re-approval loop: any fix to (1) or (2) or (3) should flip affected campaigns back to `pending_approval` (all 3 active variants for BlankTag — but this is a writer/backfill question for PM, not a mechanical fix). Files touched (diagnostic only, no prod mutations): scripts/maintenance/_blanktag-linkedin-render-diag.ts (new throwaway, committed + pushed per brief). Verified tsc-clean conceptually; script uses only existing exports (buildTemplateContext, compileTemplate from sequencing.ts; prisma from @/lib/db).

[2026-04-16T18:00:00Z] -- COMPLETED BL-103 (Tier 2 + Tier 3 pre-fix, PM pre-authorized). New file `src/lib/emailbison/company-normaliser.ts` exports `normalizeCompanyName(raw: string|null|undefined): string|null|undefined` — iterative trailing-suffix strip (cap 10 iterations) using two longest-first lists: LEGAL_SUFFIXES (36 entries: Limited/Ltd/LLC/L.L.C./Inc/Incorporated/Corp/Corporation/PLC/P.L.C./LLP/L.L.P./GmbH/SA/S.A./SARL/S.A.R.L./BV/B.V./NV/N.V./AG/A.G./Pty/Pty Ltd/Co/Company/SE/OY/AS/A/S/SpA/S.p.A./KG/PBC/& Co — bare + interior-dot variants; trailing-dot collapsed via `\.?$`) and GEO_SUFFIXES (39 entries: countries + continents + UK/Australasia constituent nations + dotted variants). Per-iteration: detach trailing `(...)` group via `^(.*?)(\s*\([^)]+\))\s*$`; try LEGAL on base (longest-first); if no LEGAL match AND no bracket present, try GEO; trim trailing comma+whitespace; re-attach bracket. Geo-strip suppressed when bracketed tail present so "Bar (USA) Inc" → strip Inc → "Bar (USA)" stays. EXCLUDES US states/UK counties/cities (brand-identity guard — Manchester United, Scottish Widows, Northern Trust all preserved). Applied at email-adapter.ts Step 4 (ATTACH_LEADS) — replaced `entry.person.company ?? undefined` with `normalizeCompanyName(entry.person.company) ?? undefined` in createOrUpdateLeadsMultiple call site; DB value untouched. Tests at src/lib/emailbison/__tests__/company-normaliser.test.ts cover all 21 PM-table cases verbatim + idempotency negative + already-normalised input + bracketed-mid edge case ("Foo Ltd (UK)" → "Foo (UK)") = 24 tests, all pass first-run. Pre-fix Tier 3: state confirmed via DB read — Campaign cmneqixpv0001p8710bov1fga = deployed/EB-90/deployedAt 17:15:24Z + 1210-solutions = 1 EB campaign (id 90 status=draft). client.deleteCampaign(90) succeeded; getCampaign(90) returned 'pending deletion' on first poll then NOT_FOUND on poll 1 (EB async DELETE queue, expected per BL-078 isNotFoundError pattern); getCampaigns confirmed 1210-solutions count=0. Single $transaction reverted Campaign deployed→approved + emailBisonCampaignId 90→null + deployedAt→null (retained contentApproved=true + leadsApproved=true) + AuditLog action='campaign.status.bl103_canary_revert' adminEmail='ops@outsignal.ai' metadata{reason, fromStatus, toStatus, clearedEmailBisonCampaignId:90, clearedDeployedAt, phase:'BL-103 pre-fix'} + appended 'BL-103 rollback 2026-04-16 — EB 90 deleted, DB reverted' to latest CampaignDeploy.emailError. Re-stage via `npx tsx scripts/maintenance/_canary-stage-deploy.ts` (skipResume=true hardcoded, verified at script line 105) succeeded: new EB campaign id=91, status=draft, leadCount=44, sequenceStepsCount=3, scheduleSet=true. Verification (full enumeration of 44 leads via paginated client.getCampaignLeads): ZERO companies end in any LEGAL or GEO suffix (regex assertion mirroring normaliser). Before/after pairs (10 sampled — DB raw vs EB normalised): "Lime Fms Ltd"→"Lime Fms", "Abby Cleaning Scotland Ltd"→"Abby Cleaning" (the original problem case), "Cleanevent Services Ltd"→"Cleanevent Services", "Tib Services"→"Tib Services" (no-op — no suffix), "Dmw Recruitment Ltd"→"Dmw Recruitment", "Cleanevent Services Ltd"→"Cleanevent Services", "Evergreen Facilities Services Ltd"→"Evergreen Facilities Services", "Ers Services Uk Ltd"→"Ers Services" (iterative: Ltd then Uk then "Ers Services" stable), "Frontline Construction Recruitment Limited"→"Frontline Construction Recruitment", "Sonnic Support Solutions"→"Sonnic Support Solutions" (no-op). Sequence steps re-fetched via getSequenceSteps: step 1 body contains literal `{COMPANY}` placeholder (template token preserved at storage — substitution at EB send time, not at storage); steps 2/3 don't reference {COMPANY} by design. Cross-check confirmed no literal company names leaked into any step body. Gates: tsc clean, eslint no-restricted-imports=0, vitest 996 pass / 37 fail / 1 todo (added 24 new passing tests vs prior 972; 37 pre-existing failures unchanged — api-routes/emailbison-client/linkedin-sender/resend-notifications/discovery promotion-icp-gate). Hard constraints honoured: skipResume=true held throughout, no live resume, no DB backfill, no writer-prompt changes, no ooo-reengage.ts/variable-transform.ts/sender-name transformer touches, no `as` casts on EB response data. monty-qa follow-up to independently re-fetch EB 91 leads and grep company column for residual Ltd/Inc/UK/Scotland/USA. Files touched: src/lib/emailbison/company-normaliser.ts (new), src/lib/channels/email-adapter.ts (import + 1 call-site change in Step 4), src/lib/emailbison/__tests__/company-normaliser.test.ts (new), .monty/memory/decisions.md (this entry).

[2026-04-16T14:35:00Z] -- STARTING BL-103 expanded execution per the 14:30Z brief (Tier 2 + Tier 3, PM pre-authorized). Plan-of-record: (1) NEW src/lib/emailbison/company-normaliser.ts — pure `normalizeCompanyName(raw: string|null|undefined)`, iterative strip-until-stable (cap 10 iterations), trims trailing comma+whitespace each iteration, preserves original casing, matches strictly at trailing end. Two ordered longest-first lists: LEGAL_SUFFIXES (Limited/Ltd/Ltd./LLC/L.L.C./Inc/Incorporated/Inc./Corp/Corporation/Corp./PLC/P.L.C./LLP/L.L.P./GmbH/SA/S.A./SARL/S.A.R.L./BV/B.V./NV/N.V./AG/A.G./Pty/Pty Ltd/Co/Company/Co./SE/OY/AS/A/S/SpA/S.p.A./KG/PBC/& Co) and GEO_SUFFIXES (UK/U.K./United Kingdom/USA/U.S.A./US/U.S./United States/EU/Europe/European/EMEA/APAC/LATAM/Americas/Global/International/Worldwide/England/Scotland/Wales/Ireland/Northern Ireland/Canada/Australia/NZ/New Zealand/Germany/France/Spain/Italy/Netherlands/South Africa/China/Japan/India/Singapore/Hong Kong/UAE). Per-iteration: build escaped \\s+SUFFIX\\.?$/i regex per suffix candidate (longest-first), match → strip → trim trailing comma+whitespace; if no match in an iteration, exit stable. (2) Apply at email-adapter.ts Step 4 (ATTACH_LEADS) — map eligibleLeads → leads in createOrUpdateLeadsMultiple call: replace `entry.person.company ?? undefined` with `normalizeCompanyName(entry.person.company) ?? undefined`. DB value untouched. (3) Tests at src/lib/emailbison/__tests__/company-normaliser.test.ts — all 21 PM-table cases + idempotency negatives. (4) Pre-fix Tier 3 recon: confirm Campaign cmneqixpv0001p8710bov1fga = deployed/90 + 1210-solutions = 1 EB campaign (id 90). (5) deleteCampaign(90) on 1210-solutions apiToken; verify 404 via getCampaign + isNotFoundError. (6) Single $transaction: Campaign.update deployed→approved + emailBisonCampaignId 90→null + deployedAt→null (retain content+leadsApproved=true) + AuditLog action='campaign.status.bl103_canary_revert' adminEmail='ops@outsignal.ai' metadata; optionally append note to latest CampaignDeploy. (7) Confirm 1210-solutions = 0 EB campaigns. (8) Re-run scripts/maintenance/_canary-stage-deploy.ts (skipResume=true hardcoded — verified). (9) listLeads on new EB id, assert ZERO companies end in any legal/geo suffix; log 10 before/after pairs from DB raw vs EB normalised; getSequenceSteps and confirm body still contains literal {COMPANY}. (10) tsc + vitest gates (baseline ~832 pass / 37 fail / 1 todo). (11) Commit + push origin main. Hard constraints (DO NOT VIOLATE): no live resume, no DB backfill, no writer-prompt changes, no ooo-reengage.ts/variable-transform.ts/sender-name transformer touches, no `as` casts on EB response data, no --force, no --no-verify, STOP if anything looks off.

[2026-04-16T14:30:00Z] -- EXPANDING BL-103 scope per PM directive. Original 14:00 brief covered legal-suffix stripping only; expanded brief (this entry) adds TRAILING geographic/regional qualifier stripping. Iterative strip-until-stable algorithm: Abby Cleaning Scotland Ltd -> strip Ltd -> Abby Cleaning Scotland -> strip Scotland -> Abby Cleaning. Geographic list: countries (UK/USA/Canada/Australia/Germany/France/Spain/Italy/Netherlands/South Africa/China/Japan/India/Singapore/Hong Kong/UAE), continents/major regions (EU/Europe/EMEA/APAC/LATAM/Americas/Global/International/Worldwide), constituent nations (England/Scotland/Wales/Ireland/Northern Ireland/NZ/New Zealand) + dotted variants. EXCLUDES US states / UK counties / cities (brand-identity risk). PRESERVE: 'The ' prefix, industry descriptors (Cleaning/Solutions/Services/Consulting/Group/Holdings/Partners/Associates/Industries/Systems/Technologies), bracketed mid-name qualifiers (Foo (UK) Ltd -> Foo (UK)). EDGE CASES (must all be in tests): Abby Cleaning Scotland Ltd -> Abby Cleaning, Covenco Limited -> Covenco, Google Inc. -> Google, Google UK -> Google, Google Ireland Ltd -> Google, Microsoft Corporation -> Microsoft, Microsoft UK Ltd -> Microsoft, Ladder Group -> Ladder Group (preserved), The Ladder Group -> The Ladder Group, Foo Holdings Ltd -> Foo Holdings, Bar (USA) Inc -> Bar (USA), Acme Services UK Limited -> Acme Services, Scottish Widows -> Scottish Widows (Widows not in list), Manchester United -> Manchester United (United not in list), Amazon Web Services -> Amazon Web Services, Scotland Insurance -> Scotland Insurance (geographic at START not TRAILING — left untouched), Northern Trust Company -> Northern Trust, ABC, LLC -> ABC, '' -> '', null -> null, undefined -> undefined. Pre-fixes (Tier 3, PM pre-authorized): delete EB 90 from 1210-solutions; revert Campaign cmneqixpv0001p8710bov1fga deployed→approved + emailBisonCampaignId 90→null + deployedAt→null; confirm 1210-solutions = 0 EB campaigns. Re-run npx tsx scripts/maintenance/_canary-stage-deploy.ts (skipResume=true MUST hold — stage only). Verification: client.listLeads on new EB id, assert ZERO companies end in any legal suffix OR geographic qualifier; log 10 before/after pairs from actual canary leads; re-fetch getSequenceSteps and confirm body still contains literal {COMPANY} template token (substitution happens at send time, not at storage). monty-qa follow-up for independent EB re-fetch + suffix grep on Ltd/Inc/UK/Scotland/USA. Hard NO: live resume, DB backfill, writer-prompt changes, ooo-reengage.ts, variable-transform.ts / sender-name transformer touches. State: main=41ba65cd, EB 90 in 1210-solutions, Campaign cmneqixpv DB=deployed/90.

[2026-04-16T14:00:00Z] -- STARTING BL-103 company-name normaliser at EB wire boundary (Tier 2, PM pre-authorized via canary re-stage brief). Context: EB 90 preview on 1210-solutions canary showed "Abby Cleaning Scotland Ltd keeps placing workers" mid-body — legal suffixes read robotic in cold copy. Scope: (1) NEW src/lib/emailbison/company-normaliser.ts (co-locate with variable-transform.ts) — pure function normalizeCompanyName(name: string|null|undefined): string, strips trailing legal suffixes case-insensitive (Ltd/Limited/LLC/Inc/Incorporated/Corp/Corporation/PLC/LLP/GmbH/SA/SARL/BV/NV/AG/Pty/Pty Ltd/Co/Company/SE/OY/AS/A/S/SpA/KG/PBC/& Co + dotted variants), handles trailing commas + whitespace, preserves "The " prefix (The Ladder Group stays), handles parenthetical "Foo Ltd (UK)" → "Foo (UK)", single-pass strip (Foo Holdings Ltd → Foo Holdings, NOT Foo), preserves original casing. (2) Apply ONLY at EB wire boundary in src/lib/channels/email-adapter.ts createLead payload — DB value stays raw legal name, same pattern as variable-transform. (3) Unit tests src/lib/emailbison/__tests__/company-normaliser.test.ts — all 9 brief edge cases + null/empty/undefined handling. (4) Pre-fixes: delete EB 90 from 1210-solutions via client.deleteCampaign; revert Campaign cmneqixpv0001p8710bov1fga status deployed→approved + emailBisonCampaignId 90→null + deployedAt→null via Prisma $transaction with AuditLog. (5) Re-run npx tsx scripts/maintenance/_canary-stage-deploy.ts (stage-only, skipResume=true — MUST NOT go live). (6) Critical verification: fetch all 44 deployed leads via client.listLeads + assert NO company field ends in Ltd/Limited/LLC/Inc/Corp/PLC/GmbH; re-fetch getSequenceSteps + confirm {COMPANY} still a template token (NOT substituted at storage). (7) Commit + push. Hard constraints: stage only (no live resume), no DB backfill (wire boundary only — separate backlog if PM wants), no writer-prompt changes, no ooo-reengage.ts (BL-097). Preserving {FIRST_NAME} variable transformer + {SENDER_FIRST_NAME} sender-name transformer — don't break those. Expected duration 15-25 min. Delegating to monty-dev; monty-qa follow-up for independent EB re-fetch + suffix grep.

[2026-04-16T00:00:00Z] -- COMPLETED Phase 5.5 of deploy pipeline rebuild (Tier 2, PM pre-authorized, DB mutation + AuditLog). ad3105de rolled back 11 email Campaigns deployed→approved and deleted orphan EB drafts (IDs 64-77) but missed clearing `emailBisonCampaignId` on the DB side. Symmetric fix to Phase 0 (which correctly handled the 4 LinkedIn rollbacks). Phase 3's Step 1 CREATE_OR_REUSE idempotency guard (src/lib/channels/email-adapter.ts) would fire 404 safety error on every re-deploy — this blanks the pointer so fresh-create path runs. Script: scripts/maintenance/_deploy-rebuild-phase-5-5-clear-stale-ebid.ts. Dry-run recon matched 11/11 expected prefixes (1210 x5: cmneq92p2/cmneqixpv/cmneq1sdj/cmneqhwo5/cmneqa518, Lime x6: cmnpwzv9e/cmnpwzwi5/cmnpwzxmg/cmnpwzym5/cmnpx037s/cmnq5nivc); old ebIds 64/66/67/70/71/72/73/74/75/76/77. Single $transaction: 11 Campaign.update + 11 AuditLog.create (action='campaign.emailbisoncampaignid.cleared', adminEmail='ops@outsignal.ai', metadata={oldEbId, reason, phase, relatedCommit}). Post-tx: stale-pointer count=0 ✓, AuditLog count=11 ✓. Collateral check (Campaigns.updatedAt within last 2min) = 11 rows, all in-scope, zero unrelated edits. No code-path change (DB-only). 2 already-active campaigns (1210 LinkedIn Healthcare, Lime LinkedIn C5) untouched per scope. Commit pending (maintenance script only).


[2026-04-16T12:00:00Z] -- STARTING Bundle B BL-075 (Tier 2, PM pre-authorized via handover 2026-04-15). Fix zombie-state bug: `executeDeploy` catch (src/lib/campaigns/deploy.ts:174-197) currently only updates CampaignDeploy.status=failed but leaves Campaign.status='deployed' + emailBisonCampaignId + deployedAt set after a terminal deploy failure. Commit 184db22c was a one-shot SQL cleanup for this exact drift; the systemic fix never landed. Plan: atomic prisma.$transaction (callback form) inside the outer catch — findFirst inflight-sibling CampaignDeploy (status IN pending|running) inside the tx (serializable with write), if absent run Campaign.updateMany({where:{id,status:'deployed'},data:{status:'approved',emailBisonCampaignId:null,deployedAt:null}}) + existing CampaignDeploy.update + tx.auditLog.create({action:'campaign.status.auto_rollback_on_deploy_failure', adminEmail=SYSTEM_ADMIN_EMAIL, metadata={fromCampaignStatus:'deployed', toCampaignStatus:'approved', erroredStep, campaignDeployId, clearedEmailBisonCampaignId, reason(clipped 500)}}). Preserve existing `current.status==='running'` guard on CampaignDeploy write (don't clobber channel-level terminal writes). Update email-adapter.ts comment (lines 614-618) from "we deliberately do NOT roll back" to reflect new outer-catch rollback contract — logic stays at deploy.ts boundary. Tests: new file src/lib/campaigns/__tests__/deploy-rollback.test.ts (4 cases + paused-manually edge case); extend src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts case 3 — BUT that test exercises EmailAdapter directly (not executeDeploy), so assertions will need to be added to a new dedicated integration case OR the extended assertion must be in a separate test. Verified: Campaign.emailBisonCampaignId is `Int?` nullable + @unique (schema.prisma:760) ✓, Campaign.deployedAt is `DateTime?` nullable (schema.prisma:794) ✓, schema untouched. adminEmail convention: SYSTEM_ADMIN_EMAIL from @/lib/audit (sibling precedent: operations.ts:880 campaign.contentApproved.reset). Retry-awareness gate: Bundle C will fix trigger retry config separately — do NOT touch trigger/campaign-deploy.ts. BL-075.

[2026-04-16T12:45:00Z] -- COMPLETED Bundle B BL-075 (Tier 2, PM pre-authorized). src/lib/campaigns/deploy.ts executeDeploy catch now runs an atomic prisma.$transaction (callback form) on terminal failure: (a) existing CampaignDeploy.status='failed' write preserved behind the `currentDeploy.status==='running'` guard; (b) tx.campaignDeploy.findFirst retry-awareness gate — status IN ['pending','running'] AND id !== deployId — serialized with write so a retry enqueuing mid-tx can't race through; (c) tx.campaign.findUnique snapshot of emailBisonCampaignId BEFORE the rollback; (d) tx.campaign.updateMany({where:{id,status:'deployed'},data:{status:'approved',emailBisonCampaignId:null,deployedAt:null}}) — race-safe optimistic pattern from deploy-campaign.ts:130-133, count=0 if Campaign already moved (paused/archived); (e) tx.auditLog.create action='campaign.status.auto_rollback_on_deploy_failure', adminEmail=SYSTEM_ADMIN_EMAIL, metadata={fromCampaignStatus, toCampaignStatus, erroredStep (extracted from /\[step:\d+\]/ in err.message), campaignDeployId, clearedEmailBisonCampaignId, reason (clipped 500 chars)}. Audit is SILENT on retry-sibling path and on updateMany-count=0 path — the eventual retry's own catch audits the terminal outcome. email-adapter.ts:614-618 comment updated: rollback lives at deploy.ts boundary, not per-channel adapter. No schema change, no trigger retry config change (Bundle C scope). New tests: src/lib/campaigns/__tests__/deploy-rollback.test.ts (6 cases: happy, full-rollback, inflight-sibling skip, audit-metadata-shape-with-500-clip, paused-mid-deploy count=0, adapter-already-flipped-failed-preserve-guard) + extended src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts with case 5 (Step 6 422 driven through executeDeploy, asserts all rollback fields + AuditLog + preserves case-3 adapter-level emailError [step:6] contract). Gates: tsc clean, eslint no-restricted-imports=0, vitest 832 pass / 37 fail / 1 todo (brief expected 825+ pass, 37 pre-existing fail, 0 NEW — all 37 failures in unrelated pre-existing files: api-routes, emailbison-client, linkedin-sender, resend-notifications, discovery). Pattern worth recording: findFirst-inside-tx for retry-aware rollback — any write that conditionally depends on the absence of a sibling row MUST do the findFirst inside the same $transaction to avoid the read-then-write race hole. BL-075.

<!-- Append entries as: [ISO date] -- [decision summary] -->

[2026-04-16T09:22:00Z] -- Phase 6.5a BL-074 START. PM-authorized, Tier 2 (reversible). Root cause: src/lib/emailbison/client.ts::createSequenceStep posts {position, subject, body, delay_days} to v1 deprecated path /campaigns/{id}/sequence-steps — EB requires {title, sequence_steps:[{email_subject, email_body, wait_in_days}]} envelope. Phase 6a canary got 422 "title/sequence_steps required". Plan: add new batch method createSequenceSteps() targeting v1.1 path (POST /api/campaigns/v1.1/{id}/sequence-steps), migrate email-adapter.ts Step 3 loop to single batch call, keep EB-shape transformation inside client method, preserve Phase 3 idempotency via GET-diff-then-POST-missing. Old createSequenceStep() KEPT (not removed) — two production callers exist outside email-adapter: trigger/ooo-reengage.ts:229 and src/lib/agents/campaign.ts:310. Per spec "If any other production call sites exist, STOP and escalate — don't leave a dead export" → preserving old method avoids dead export; flagging the two legacy callers for follow-up (they'll hit the same 422 on next execution). Contract test at src/lib/emailbison/__tests__/client-sequence-steps.test.ts asserts outbound HTTP body shape read from fetch mock. Integration test src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts needs URL update (v1 → v1.1) to stay passing — outside allowed-files list but required to keep baseline green. LinkedIn grepped: linkedin-adapter.ts uses prisma.createMany via createSequenceRulesForCampaign (sequencing.ts:410) — NOT same bug → BL-079 not filed. Baseline: 818 pass / 37 fail / 1 todo.

[2026-04-16T09:30:00Z] -- Phase 6.5a BL-074 COMPLETE. Commit 17139b10. Shipped: (1) `EmailBisonClient.createSequenceSteps` plural batch method on v1.1 path with Zod-at-boundary response parsing + empty-array short-circuit + `delay_days=0→wait_in_days=1` wire-clamp; (2) `createSequenceStep` singular kept + marked @deprecated for legacy ooo-reengage + agents/campaign.ts callers (Phase 6.5b scope); (3) email-adapter.ts Step 3 loop collapsed to single batched call, Phase 3 GET-diff idempotency preserved via pre-POST filter; (4) contract test (4 cases) asserts outbound HTTP body shape by reading fetchMock.mock.calls[0][1].body; (5) existing adapter + race + integration tests updated to match batch pattern and v1.1 URL. Verification: tsc clean, eslint no-restricted-imports = 0, vitest 822 pass / 37 fail / 1 todo (baseline 818/37/1 — gained 4 new contract tests, zero new failures, same pre-existing 37 failures). LinkedIn verified clean — uses prisma.createMany not per-step POST. BL-079 not filed. Deferred: BL-075 rollback gap, BL-076 Trigger.dev retry, BL-078 error-class mismatch — all Phase 6.5b. Files touched (all in allowed list): src/lib/emailbison/client.ts, src/lib/channels/email-adapter.ts, src/__tests__/email-adapter-deploy.test.ts, src/lib/emailbison/__tests__/client-sequence-steps.test.ts (new), src/lib/channels/__tests__/email-adapter-race.test.ts, src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts, .monty/memory/decisions.md. types.ts NOT touched — consumer CreateSequenceStepParams shape stable, transformation lives inside the new client method.


[2026-04-16T09:30:00Z] -- STARTING Phase 6a-rollback of deploy pipeline rebuild (Tier 3, PM pre-authorized per handover-2026-04-15-deploy-rebuild.md). Same pattern as Phase 0 (4 LinkedIn rollback) + Phase 5.5 (11 stale ebId clear). Scope: 1 EB campaign delete + 1 Campaign row update + 1 CampaignDeploy row update + 1 AuditLog row insert, all for Campaign cmneqixpv0001p8710bov1fga (1210-solutions email canary) whose Phase 6a deploy died at Step 3 (UPSERT_SEQUENCE_STEPS) with EB 422 "title/sequence_steps required" due to createSequenceStep shape mismatch — pre-existing bug to be fixed in Phase 6.5. (6a-r.1) delete orphan EB campaign 79 via client.deleteCampaign with 1210-solutions workspace.apiToken (same key that created it); verify 404 post-delete; STOP+escalate if delete fails. (6a-r.2) Prisma $transaction: UPDATE Campaign cmneqixpv status deployed→approved + emailBisonCampaignId 79→null + deployedAt→null (retain contentApproved=true, leadsApproved=true, targetListId unchanged); UPDATE CampaignDeploy cmo16w3r90001zxlvxr1x5yop status failed→rolled_back + append error suffix "Phase 6a-rollback 2026-04-16 — EB campaign 79 deleted, DB reverted to approved ready for Phase 6.5 fix of createSequenceStep batch shape"; INSERT AuditLog action='campaign.status.phase6a_rollback' entityType=Campaign entityId=cmneqixpv0001p8710bov1fga adminEmail=ops@outsignal.ai metadata per brief. Dry-run print planned updates first, then execute. (6a-r.3) post-tx verification per row + EB 404 re-check + LinkedIn canary cmneqixvz0003p871m8sw9u7o UNCHANGED sanity check + collateral updatedAt window check showing exactly 1 Campaign + 1 Deploy touched. (6a-r.4) NO commit — DB-only + memory log, no tracked-file change expected per Phase 1a / Phase 5.5 precedent. Hard rules: ONLY cmneqixpv + its 1 Deploy + 1 AuditLog + 1 EB call; do NOT touch LinkedIn canary; do NOT touch the other 15 approved campaigns; NO push, NO schema migration. STOP if: EB delete fails, dry-run count != 1+1, post-tx state check fails. Delegating monty-dev via Agent tool; monty-qa after (small scope but brief still requires 3+ findings).

[2026-04-16T09:00:00Z] -- STARTING Phase 5 RETRY of deploy pipeline rebuild (Tier 3, PM pre-authorized end-to-end per handover-2026-04-15-deploy-rebuild.md §0 + §11 Authorization line: "commit + push + `npx vercel --prod`"). Previous Phase 5 session died before running. Delegating to monty-dev directly via Agent tool; skipping monty-qa per PM brief (mechanical push + deploy, QA would only review self-evident push/vercel output). Scope: (1) git status --porcelain sanity check (untracked maintenance scripts + .nova memory + .env.example expected, no blocking staged changes), (2) git log origin/main..HEAD --oneline to list commits to push (expect ~9 inc. f3e831a2, 8016c97f, 9404015f, e2ffe366, 5a13ceba, dfdef8fc, aa0db89b), (3) git fetch origin main + verify HEAD strictly ahead, STOP+escalate if not, (4) git push origin main — no --force, no --no-verify, STOP on pre-push hook failure, (5) npx vercel --prod --yes and capture deploy URL, (6) poll vercel ls --prod until state=READY if CLI non-blocking, (7) smoke-test: grep src/app/api for health/ping/status + curl against new prod URL, expect 200 or 3xx, (8) report in specified format. NO --force, NO --no-verify, NO manual retries, NO campaign work, NO EB API calls. STOP on any error. Decision entry update stays uncommitted (per brief: "Do not push a separate commit just for the decisions.md update").

[2026-04-15T21:15:00Z] -- COMPLETED Phase 3 of deploy pipeline rebuild (Tier 2, PM pre-authorized — NOT committed, PM will commit). Single-file refactor: src/lib/channels/email-adapter.ts (+373 / -18 LOC). NOT touched per scope: client.ts, types.ts (all required EB methods + Zod-shaped types already present in uncommitted Phase 1/2 work), linkedin-adapter.ts (Phase 2 complete), deploy-campaign.ts (initiator already correct), trigger/campaign-deploy.ts + src/lib/campaigns/deploy.ts (orchestrator already sequential email→linkedin with fail-fast). 10-step flow now explicit with `// Step N —` comments + DEPLOY_STEP enum. Per-step implementation: (1) CREATE_OR_REUSE — getCampaign(campaignId) loaded once, preExistingEbId = campaign.emailBisonCampaignId ?? null; if non-null, getCampaign(EB) to verify, on EmailBisonApiError.isRecordNotFound throw "manual delete?" message (no silent re-create); else createCampaign. (2) PERSIST — Promise.all dual-write to Campaign + CampaignDeploy (idempotent — same ID on re-run). (3) UPSERT_SEQUENCE_STEPS — only call getSequenceSteps when preExistingEbId != null (skips on fresh deploy to keep happy-path mock minimal); diff by position via Set lookup; createSequenceStep only for missing positions. EB OpenAPI lines 252-269 + 1326-1334 do NOT document 409/422 dup behaviour, chose GET-then-diff (safer per Phase 3 spec ambiguity rule). (4) ATTACH_LEADS — unchanged from pre-existing impl + zero-leads early exit + WebhookEvent dedup; createLead loop captures createdLeadIds; attachLeadsToCampaign batch. (5) UPSERT_SCHEDULE — fresh deploy → createSchedule directly; idempotent re-run → getSchedule, if non-null updateSchedule (with save_as_template:false per UpdateScheduleParams contract), else createSchedule. (6) ATTACH_SENDERS — unchanged sender query + attachSenderEmails. (7) UPDATE_SETTINGS — unchanged updateCampaign. (8) ATTACH_TAGS — ESCALATION: NO Workspace.ebTagIds field exists (grep schema.prisma — only emailBisonCampaignId/emailBisonSenderId). Per Phase 3 hard rules: did NOT invent silently, did NOT skip silently — left as documented no-op (preserved comment from Phase 1 work) + flagged in report for PM schema follow-up. (9) RESUME_LAUNCH — resumeCampaign via withRetry; throws propagate to outer catch with [step:9] tag. (10) VERIFY_STATUS — getCampaign GET + EbCampaignStatusSchema Zod parse (NEW Zod schema at module scope: { id: number, status: string }.passthrough()) + LAUNCHED_STATUSES set check (queued|launching|active, case-insensitive via toLowerCase). Compensating txn: outer catch (a) console.error logs, (b) writes emailStatus=failed + emailError prefixed with `[step:N]` (no schema change — Phase 3 refactor-only), (c) rethrows so deploy.ts orchestrator's fail-fast skips LinkedIn. NO Campaign.status/emailBisonCampaignId rollback (Tier 3, PM-owned, same as Phase 0 BL-061 pattern). State machine: in_progress → completed (success) | failed (any throw); rolled_back transition documented in catch comments as PM-triggered out-of-scope. Channel separation CONFIRMED: email-adapter handles email-only (LinkedIn methods absent), linkedin-adapter unchanged from Phase 2, deploy.ts loops `["email","linkedin"]` sequentially with adapter.deploy await — email throw skips linkedin (verified by re-reading deploy.ts:106-126). NO `as` casts on EB response data added — Step 10 uses Zod parse; only existing pre-Phase-3 cast in client.ts:241-247 getSequenceSteps preserved (out of scope, was there from Phase 1 uncommitted). Bare catches: 2 added, both log+rethrow OR conditional rethrow (line 201 inner: classifies 404 then unconditionally throws; line 491 outer: console.error + db update + throw). Verification gates: tsc clean (exit 0); eslint . | grep no-restricted-imports = 0; vitest 806 pass / 40 fail / 1 todo / 1 skip across 56 files (identical to baseline pre-Phase-3 with all uncommitted files in place — verified by stash/restore). The 3 failing tests in src/__tests__/email-adapter-deploy.test.ts (untracked, Phase 4 scope per brief) pre-existed at 5 failing; my refactor IMPROVED that file from 1 pass / 5 fail → 3 pass / 3 fail (3 remaining failures are tests that need new mock for getCampaign on ebMock — Phase 4 work). NO new test failures elsewhere. NO commit (PM owns). NO push. NO --no-verify. Ready for Phase 4 (tests + QA).

[2026-04-15T20:45:00Z] -- STARTING Phase 3 of deploy pipeline rebuild (Tier 2, PM pre-authorized). Orient complete. Gap-list: (i) Idempotency guard on Step 1 MISSING — current email-adapter.ts unconditionally calls createCampaign; will add check on campaign.emailBisonCampaignId + GET to EB to verify, 404 → fail with failedAtStep=1 (no silent re-create). (ii) Sequence-step upsert MISSING — EB OpenAPI (docs/emailbison-dedi-api-reference.md lines 252-268, 1326-1334) does NOT document 409/422 duplicate semantics; will use GET-then-diff (safer) via existing client.getSequenceSteps → skip existing positions, POST only missing. (iii) Tag attachment ESCALATION — NO Workspace.ebTagIds / emailBisonTagIds / defaultEbTags field exists in prisma/schema.prisma (grep confirmed — only emailBisonCampaignId at :757/:826 + emailBisonSenderId on Sender). Current code is a documented no-op at line 258-259 ("Step 7 (tags): no-op until workspace-level ebTagIds field is added"). Will PRESERVE existing no-op + flag in report; NOT invent a config field silently. (iv) Schedule PUT-if-exists MISSING — current code unconditionally calls createSchedule; will add getSchedule → if present use updateSchedule (requires save_as_template per UpdateScheduleParams), else createSchedule. (v) Step 10 verify MISSING — resumeCampaign exists but no post-resume GET/status check; will add getCampaign + status assertion ∈ {queued, launching, active} (case-insensitive). (vi) Compensating transaction partial — current code marks emailStatus=failed + writes emailError but no failedAtStep tracking; schema has NO failedAtStep column. Will encode step number into emailError prefix ("[step:N] <msg>") + persist finalEbStatus into existing emailError on success path (no schema change per Phase 3 refactor-only scope). rolled_back transition documented in code comments per BL-061 precedent (out-of-scope for auto-execution). (vii) Channel separation CORRECT — src/lib/campaigns/deploy.ts:106 already loops ["email","linkedin"] sequentially, awaits each adapter.deploy, throws propagate up so LinkedIn never runs on email failure; CampaignDeploy is per-campaign with per-channel status fields. No changes needed. (viii) Step comments 1-10 will be added to email-adapter.ts for auditor traceability. Files to touch: src/lib/channels/email-adapter.ts (add idempotency + upsert + schedule GET-first + verify + step comments + failedAtStep encoding). NOT modifying: linkedin-adapter.ts (Phase 2 complete), deploy-campaign.ts (already correct — initiator only), deploy.ts (orchestrator already sequential + fail-fast), client.ts (all required methods exist), types.ts (all types exist), test files (Phase 4 scope, must still parse). Verification gates after: tsc clean, eslint restricted-imports=0, vitest 809 pass baseline preserved, no new `as` casts on EB data, no bare catches.

[2026-04-15T20:20:00Z] -- COMPLETED Phase 2 of deploy pipeline rebuild. Commit 8016c97f landed 4-file change: (1) src/lib/channels/linkedin-adapter.ts — Zod schema `LinkedInSequenceStepSchema` at adapter boundary replacing the silent-drift `as LinkedInSequenceStep[]` cast (throws with "LinkedIn step must have position or stepNumber (BL-068 shape-drift guard)" on shape failure); sequence mapped through `step.position ?? step.stepNumber ?? idx + 1` before sort; postConnectRules.map also resolves via same fallback. (2) src/lib/linkedin/sequencing.ts — `createSequenceRulesForCampaign` now threads idx through the map and resolves `resolvedPosition = step.position ?? step.stepNumber ?? idx + 1` as defense-in-depth for any caller that bypasses the adapter. LinkedInSequenceStep interface marks `position` optional and adds `stepNumber?`. (3) src/lib/channels/__tests__/linkedin-adapter.test.ts — NEW file, 6 vitest cases: (a) position-only happy path, (b) stepNumber-only BL-068 shape, (c) missing both → Zod throws with BL-068 message + linkedinStatus=failed written + rule builder never invoked, (d) mixed sequence, (e) secondary defensive read in sequencing.ts via dynamic re-import, (f) regression fixture using REAL JSON captured from cmneqa5r50003p8rk322w3vc6 (stepNumber/connection_request/delayDays shape verified via controlled SELECT). (4) .monty/memory/backlog.json — BL-069 filed (medium/priority-2, open) for channel sequence JSON shape-drift normalisation + delayDays/delayHours cleanup. Gates: 6/6 target tests pass; 71/71 channel+linkedin tests pass; full suite 809 pass / 37 fail / 1 todo (37 failures are pre-existing BL-055 discovery ICP + api-routes + emailbison-client + resend-notifications + linkedin-sender failures noted in handover, zero of which touch the modified files); tsc clean; eslint no-restricted-imports=0; pre-commit hook ran `npx tsc --noEmit` clean. No `--no-verify`, no push, no force. All uncommitted working-tree files preserved per scope. READY for Phase 3 re-deploy of the 4 rolled-back 1210 LinkedIn campaigns.

[2026-04-15T20:05:00Z] -- STARTING Phase 2 of deploy pipeline rebuild (BL-068 fix, Tier 2 code-only). Scope: (a) linkedin-adapter.ts — replace `as LinkedInSequenceStep[]` cast at line 64 with Zod `.parse()` that accepts either `position` OR `stepNumber` and throws on neither (so the upstream compensating txn handles it cleanly); change the rule-builder read at ~line 106 to `step.position ?? step.stepNumber ?? idx + 1`. (b) sequencing.ts:387 — second defensive read with same fallback (defense-in-depth). (c) New test file src/lib/channels/__tests__/linkedin-adapter.test.ts — 6 vitest cases including a regression fixture built from the actual stuck 1210 JSON (keys: stepNumber, channel, type, subject, subjectB, body, delayDays, notes — verified via controlled SELECT on cmneqa5r50003p8rk322w3vc6). Real JSON confirmed uses `stepNumber` (not `position`), `type: "connection_request"` (not "connect"), and `delayDays` (not `delayHours`). Preserving existing delayDays vs delayHours inconsistency — flagged for BL-069. Only touching: linkedin-adapter.ts, sequencing.ts, new test file, backlog.json (BL-069 append). All other uncommitted files preserved.

[2026-04-15T19:15:00Z] -- COMPLETED Phase 1 of deploy pipeline rebuild. (1a) Schema verify verdict: CLEAN for scope. `Campaign.emailBisonCampaignId` present at prisma/schema.prisma:757 + index :803 + mirror on CampaignDeploy :826. Controlled SELECT succeeded (`findFirst({select:{id,emailBisonCampaignId}})` returned null-valued row — column exists on live DB). `npx prisma generate` clean. 3 pending migrations (sender_channel, person_email nullable, inboxAlert dedup) pre-existed Phase 1 and DO NOT touch Campaign.emailBisonCampaignId — left unapplied per brief constraint (no `prisma migrate deploy`). (1b) 4 rows updated in single $transaction: cmneq1z3i (deployedAt 2026-04-15T12:18:07.580Z→null), cmneq93i8 (12:18:04.429Z→null), cmneqa5r5 (12:18:11.024Z→null), cmneqixvz (12:18:06.403Z→null). 4 AuditLog rows inserted (action=campaign.deployedat.cleared, entityType=Campaign, adminEmail=ops@outsignal.ai, metadata={reason, phase:'Phase 1', relatedCommit:'f3e831a2'}). Post-update SELECT verified all 4 with deployedAt=null, status=approved, contentApproved=true, leadsApproved=true. Collateral check: 4 rows with deployedAt=null AND updatedAt in txn window [18:46:07.628Z, 18:46:09.934Z] = 4 targets + 0 unexpected. Final verdict OK. (1c) NO commit — Phase 0 landed the schema in a prior commit, `prisma generate` clean, no tracked source files modified in Phase 1. Empty commit avoided per brief §1c. Maintenance script scripts/maintenance/_bl068-clear-deployedat.ts stays untracked (_ prefix convention, one-off tool). (1d) tsc clean (exit 0), eslint no-restricted-imports=0, 4 target rows verified deployedAt IS NULL, collateral confirms no other rows touched. Uncommitted working-tree state preserved: src/lib/channels/email-adapter.ts + src/lib/emailbison/{client,types}.ts + .env.example + .monty/memory/{decisions,incidents}.md + .nova/memory/yoopknows/learnings.md + src/__tests__/email-adapter-deploy.test.ts + src/__tests__/emailbison-client-schedule.test.ts + .monty/memory/audit-logs/ + .monty/memory/handover-2026-04-14-linkedin-debris.md + all scripts/maintenance/_*.ts. READY for Phase 2.

[2026-04-15T19:00:00Z] -- PLANNED Phase 1 of deploy pipeline rebuild (Tier 2). (1a) Schema verify: `Campaign.emailBisonCampaignId Int?` present at prisma/schema.prisma:757 with `@@index([emailBisonCampaignId])` at :803 + mirror on CampaignDeploy at :826. `npx prisma migrate status`: 3 pending migrations (20260331000000_add_sender_channel, 20260331100000_make_person_email_nullable, 20260414000000_add_inbox_alert_dedup_timestamps) — NONE reference Campaign.emailBisonCampaignId (verified by grep, the sender_channel migration touches Sender.emailBisonSenderId not Campaign). Controlled SELECT `prisma.campaign.findFirst({select:{id:true,emailBisonCampaignId:true}})` returned row with emailBisonCampaignId=null — column is live on DB. `npx prisma generate` clean. No `prisma migrate deploy` — pre-existing drift on 3 unrelated migrations is not Phase 1 scope. (1b) PLANNED Tier 2 DB-only cleanup: single Prisma $transaction updating 4 Campaign rows (cmneqa5r50003p8rk322w3vc6, cmneq1z3i0001p8ef36c814py, cmneqixvz0003p871m8sw9u7o, cmneq93i80001p8p78pcw4yg9) setting deployedAt=null (retain status=approved, contentApproved=true, leadsApproved untouched), and inserting 4 AuditLog rows with action='campaign.deployedat.cleared', entityType='Campaign', entityId=<campaignId>, adminEmail='ops@outsignal.ai', metadata={reason:'BL-068 rollback side-effect cleanup', phase:'Phase 1', relatedCommit:'f3e831a2'}. Dry-run print first, then apply, then SELECT re-verify. Script at scripts/maintenance/_bl068-clear-deployedat.ts (untracked, _ prefix convention). (1c) Commit decision: Phase 0 already landed the schema (prior commit), `prisma generate` clean, no source change required — Phase 1 likely ends with NO tracked-file changes (script stays untracked). Empty commit forbidden; will skip commit and report "no tracked-file changes needed". Preserving all uncommitted files per brief §scope.

[2026-04-15T18:40:00Z] -- COMPLETED Phase 0 of deploy pipeline rebuild. (0a) Uncommitted diff inventory complete — methods EXISTING on EmailBisonClient: createCampaign, createSequenceStep, createLead, attachLeadsToCampaign, resumeCampaign, pauseCampaign, deleteCampaign (BL-061), updateCampaign, attachSenderEmails, getCampaignById, getCampaignLeads, getSequenceSteps. Methods NEW in uncommitted diff: createSchedule, updateSchedule, getSchedule, attachTagsToCampaigns. email-adapter.ts uncommitted diff: steps 3b (attach leads to campaign after create), 4 (createSchedule hardcoded Mon-Fri 09-17 Europe/London), 5 (attachSenderEmails from Sender.emailBisonSenderId with channel in [email, both] + health healthy|warning), 6 (updateCampaign with plain_text/open_tracking/reputation_building/can_unsubscribe), 8 (resumeCampaign launch), plus zero-leads early-exit short-circuit. Campaign.emailBisonCampaignId already persists post-create. Gaps vs §5 target: (i) idempotency guard on step 1 missing — createCampaign still unconditional; (ii) sequence-step upsert missing (existing code loops createSequenceStep — 409 on rerun); (iii) compensating transaction on partial failure missing; (iv) tag attachment not wired (method exists, no workspace.ebTagIds); (v) LinkedIn BL-068 position bug unfixed; (vi) getSchedule exists but no "PUT if exists, POST if new" branch in adapter. Test files email-adapter-deploy.test.ts (391 lines) + emailbison-client-schedule.test.ts (225 lines) present untracked. (0b) BL-068 trace found in CampaignDeploy.error + linkedinError columns on all 4 stuck deploy rows — no reproduction needed. Stack verbatim: "Invalid prisma.campaignSequenceRule.createMany() invocation: ... position: undefined ... Argument `position` is missing." Root cause: CampaignSequenceRule.position is non-null Int with no default (schema.prisma:1110); LinkedIn rule builder emits position:undefined for every row. Prisma validates pre-insert and rejects atomically, so no EB campaign ever got created (emailBisonCampaignId null on all 4 deploys). (0c) 4 stuck 1210 LinkedIn Campaign IDs: cmneqa5r50003p8rk322w3vc6 (Industrial/Warehouse), cmneq1z3i0001p8ef36c814py (Green List Priority), cmneqixvz0003p871m8sw9u7o (Facilities/Cleaning), cmneq93i80001p8p78pcw4yg9 (Construction). (0d) Tier 3 rollback executed in single Prisma transaction. Per-campaign: Campaign.status deployed→approved (contentApproved=true + leadsApproved=true retained); CampaignDeploy.status failed→rolled_back with error suffix "BL-068 LinkedIn campaignSequenceRule schema error — position field missing, see BL-068 notes"; AuditLog row created with action='campaign.status.bl068_rollback', metadata {reason, stackTraceExcerpt: "Argument position is missing.", rolledBackBy: "Monty Phase 0", fromStatus, toStatus, deployId, deployFromStatus, deployToStatus, campaignName, workspaceSlug}. Verify fetch confirmed all 4 campaigns + 4 deploys + 4 audit rows. (0e) BL-068 filed in .monty/memory/backlog.json (items count 64→65) with full verbatim trace + root-cause hypothesis + rollback notes + affected campaign IDs. Committed at SHA f3e831a2 ("chore(backlog): file BL-068 (LinkedIn deploy schema error)"). No push. (0f) 1210 EB API key MISMATCH vs b4c37f19^ in-repo history: repo-history fingerprint 17|WZWCUJLe...ace1 (51 chars); live DB fingerprint 31|0E00GRK6o...b6ef (51 chars). Prefix differs, body differs — key already rotated since setup-client.ts deletion. No rotation action. STOPPED after Phase 0 per brief. No src/ or scripts/ modifications. Uncommitted working-tree state on src/lib/emailbison/*.ts + src/lib/channels/email-adapter.ts + test files preserved for Phase 1+.

[2026-04-15T17:30:00Z] -- PLANNED Phase 0 of deploy pipeline rebuild (Tier 3, PM pre-authorized end-to-end). Scope: (0a) orient to ~70% rebuild in uncommitted diffs on src/lib/emailbison/client.ts + email-adapter.ts + types.ts — no writes, no stash; (0b) BL-068 stack trace found in CampaignDeploy.error/linkedinError for all 4 stuck 1210 LinkedIn deploys — root cause: `prisma.campaignSequenceRule.createMany()` called with `position: undefined` in all rule rows. Schema requires `position Int` non-nullable. Hypothesis: LinkedIn adapter's sequence-rule builder doesn't compute or pass `position` per rule, so Prisma's input validation rejects every createMany call; (0c) 4 stuck 1210 Campaign IDs identified: cmneqa5r50003p8rk322w3vc6 (Industrial/Warehouse), cmneq1z3i0001p8ef36c814py (Green List Priority), cmneqixvz0003p871m8sw9u7o (Facilities/Cleaning), cmneq93i80001p8p78pcw4yg9 (Construction); (0d) PLANNED rollback in single Prisma transaction: per-campaign CampaignDeploy status failed -> rolled_back + error suffix "; BL-068 rollback 2026-04-15"; Campaign status deployed -> approved (retain contentApproved=true + leadsApproved=true); AuditLog row per campaign with action='campaign.status.bl068_rollback' and metadata {reason, stackTraceExcerpt: "Argument position is missing.", rolledBackBy: "Monty Phase 0"}; (0e) file BL-068 ticket in backlog.json with trace excerpt + root-cause hypothesis + Phase 2 reference; ONE commit for backlog.json append (no push); (0f) 1210 EB key MISMATCH vs b4c37f19^ (in-repo history showed 17|WZWCUJLe...ace1, live is 31|0E00GRK6o...b6ef) — key already rotated since setup-client.ts deletion, no action. STOP after Phase 0. No src/ or scripts/ changes.

[2026-04-15T16:45:00Z] -- COMPLETED BL-061 cleanup (Tier 3, user pre-approved). Inventory found a worse picture than spec assumed: the buggy deploy path ran TWICE for each email campaign (first attempt + retry?), producing TWO EB draft campaigns per DB campaign, not one. Lime: 6 email DB campaigns -> 12 EB drafts (ebIds 56, 57, 58, 59, 60, 62, 64, 66, 67, 70, 71, 73). 1210: 5 email DB campaigns -> 10 EB drafts (ebIds 61, 63, 65, 68, 69, 72, 74, 75, 76, 77). ALL 22 EB drafts had status=draft, leadCount=0, senderCount=0, steps upload failed with 422 "The title field is...". CampaignDeploy status was 'failed' (not 'complete' as the spec framed it). LinkedIn deploys (1 Lime + 5 1210) are out of EB scope — handled separately. Actions taken: (1) added `deleteCampaign(campaignId)` to src/lib/emailbison/client.ts — minimal DELETE /api/campaigns/{id} wrapper with JSDoc referencing EB docs lines 572-589. (2) Deleted all 22 EB drafts serialized with leadCount===0 pre-check. Final verify: all 22 gone from EB (queue processed within ~5 min). (3) Flipped 11 Campaign.status deployed -> approved + 11 CampaignDeploy.status failed -> rolled_back (transaction per campaign, error field documents the BL-061 context). (4) Final state: Lime EB has 7 pre-existing production campaigns left (ebIds 26,31,32,42,43,44,45 untouched), 1210 EB is empty (no pre-existing campaigns existed). Zero collateral damage. Type-check clean. Root cause for BL-062: deploy path in src/lib/campaigns/deploy-campaign.ts / deploy.ts calls client.createCampaign() instead of reusing an existing emailBisonCampaignId on retry — needs reuse-or-transition logic before next campaign-deploy run. Scripts retained under scripts/maintenance/_bl061-cleanup-{inventory,deep-check,apply,verify}.ts for future forensics.

[2026-04-15T16:00:00Z] -- PLANNED (Tier 3, user pre-approved): BL-061 follow-up cleanup. The new `scripts/cli/campaign-deploy.ts` (commit c1de9dfc) was used earlier today to deploy 17 campaigns across lime-recruitment (7) and 1210-solutions (10). The deploy path creates NEW EmailBison campaigns each invocation instead of reusing/transitioning existing ones, so both workspaces now have duplicate EB campaigns (originals with leads/senders/copy, Monty's with 0 leads/0 senders, created 2026-04-15 12:17-12:18 UTC). Plan: (1) inventory duplicates vs originals in each workspace via CampaignDeploy + EB getCampaigns/getCampaignLeads, (2) add `deleteCampaign(campaignId)` to `src/lib/emailbison/client.ts` (DELETE /api/campaigns/{id}), (3) serialize EB deletes of duplicates with leadCount===0 safety check, (4) DB cleanup: flip CampaignDeploy.status -> 'rolled_back' and Campaign.status deployed -> approved (only if still deployed). Scope: lime-recruitment + 1210-solutions ONLY. Not touching WIP diffs on email-adapter/client/types (BL-066 work).

[2026-04-08T12:00:00Z] -- Built `scripts/batch-reverify.ts`: CLI script for batch re-verification of target list leads via BounceBan (primary) with Kitt fallback for unknowns. Accepts `--workspace` + `--campaignName` (substring match) or `--listId`. Overwrites stale LeadMagic verification data on Person.enrichmentData. Removes invalid leads from the target list after verification. Includes `--dry-run` mode. Uses existing `verifyEmail()` from bounceban.ts and kitt.ts directly (no enrichment waterfall). Compiles clean with `npx tsc --noEmit`.

[2026-04-08T12:30:00Z] -- Fix Prospeo adapter seniority capitalisation bug. Added `mapSeniorityToProspeo()` in `src/lib/discovery/adapters/prospeo-search.ts` mapping internal lowercase seniority values (`manager`, `c_suite`) to Prospeo's expected capitalised format (`Manager`, `C-Suite`). Prospeo silently ignores unrecognised values, so lowercase caused the filter to be dropped. Also added location format warning (missing `#CC` suffix) and full request body debug log. Rebuilt dist via `npm run build:cli`. Type-checked clean.

[2026-04-08T13:00:00Z] -- Auto-transition campaign from `deployed` to `active` after successful deploy. Added logic in `executeDeploy()` (src/lib/campaigns/deploy.ts) after `finalizeDeployStatus()`: when deploy record status is `complete`, atomically update campaign status from `deployed` to `active` using `updateMany` with status guard (only transitions if current status is `deployed` — won't re-activate paused/completed campaigns). Logs transition. Type-checked clean.

[2026-04-08T13:30:00Z] -- P3 deploy notification template (LinkedIn-only "LEADS: 0 pushed"): investigated and confirmed fix already exists in `src/lib/notifications.ts` lines 516-518. The `hasEmailChannel` guard hides the "Leads" section when `channels` does not include "email". The `channels` array is passed from `deploy.ts` line 503. No code change needed. Type-checked clean.

[2026-04-08T14:00:00Z] -- P6 connection poller investigation (Lime Recruitment, 0 acceptances): Root cause analysis complete. The `actionType` bug fix (commit a2ae49db) fixed 2 queries in `connection-poller.ts` that matched `actionType: "connect"` instead of `{ in: ["connect", "connection_request"] }`. This fix is DEPLOYED on origin/main. However, this bug only affected the timeout/retry path (`getConnectionTimeoutDaysForPerson` and retry action lookup), NOT the acceptance detection path. The acceptance detection path (`getConnectionsToCheck` -> worker `pollConnections` -> `processConnectionCheckResult`) queries `LinkedInConnection` records by `status: "pending"` with no actionType filter — it was never broken. DB state: 21 LinkedInConnection records all `pending`, sender (Lucy Marshall) is active+healthy, all 21 persons have LinkedIn URLs, requests sent April 1-7. The VoyagerClient is returning non-connected statuses for all 21 — this is genuine zero acceptance, not a detection failure. No code changes needed.

[2026-04-08T16:00:00Z] -- BL-003 fix: CTA validator sign-off handling. Two changes in `src/lib/copy-quality.ts`: (1) Removed `worth a chat?` from BANNED_CTA_PATTERNS — writer-rules.md explicitly lists it as a good soft CTA example. (2) Added `stripSignOff()` helper that strips common sign-off blocks (single name, greeting+name patterns like "All the best,\nDaniel") from the end of email text before CTA checks. This prevents false hard violations when emails end with a sign-off after the CTA question. Updated 10 tests in `copy-quality.test.ts` (1 modified, 9 added). Type-checked clean, all 99 tests pass.

[2026-04-11T17:10:00Z] -- Weekend audit session: architectural shift on ICP scoring + data integrity invariants + backlog rationalisation. (1) ICP scoring moved off Claude Code CLI onto direct Anthropic API path. Root cause of perceived score variance was temperature=1 default, not inherent LLM non-determinism — raw SDK test confirmed 10/10 byte-identical output at temp=0. Fix: scorer.ts pins temperature=0 + adds persistLowConfidence option so low-confidence scores stay null until upstream data improves. Cost: ~$0.001/lead (~$5 per 5000 leads) vs previously eating 66% of the 5-hour rolling limit. (2) Three data integrity invariants codified in src/lib/audit/invariants.ts: INV1 email integrity, INV2 scoring coverage, INV3 staging path. CLI + daily Trigger.dev task that posts PASS/FAIL to #outsignal-ops. Outsignal excluded pending per-campaign ICP architecture decision (parked). (3) BL-027 rawResponse null bug: earlier commit 9213d7dd claimed to fix all 5 adapter call sites but only Serper Maps actually landed in git — fix completed in commit 0e3dc11a. (4) Historical staging archive backfill: 12,603 Person rows that bypassed DiscoveredPerson now have synthetic archive entries with historical promotedAt (keeps them out of billing window quota counts). INV3 ✓ across all active workspaces. (5) 526 YoopKnows + 655 MyAcq broken job titles nulled. INV1 ✓ across all active workspaces. (6) Rise ICP criteria prompt written to DB based on actual EmailBison campaigns (not aspirational workspace metadata) — targets B2B SaaS/tech with marketing + people ops personas. (7) Backlog hygiene: BL-004 closed (placeholder emails verified zero), BL-010 bumped to P2 (not observed stalling since BL-027 deploy), BL-012 closed as obsolete (superseded by API-path approach). New items BL-015 through BL-024 added covering the rate-limit budget gate (still P1), Monty QA Sonnet routing (P2), daily usage report (P2), Lime title artefacts (P3), LinkedIn crawl fallback (P2), Outsignal per-campaign ICP design (P3), and the items completed this session. Total backlog now 24 items (15 done, 7 open, 2 wont_fix).

<!-- Historical entries recovered from stash@{0} on 2026-04-11. These reference the 2026-04-10 backlog numbering scheme (BL-010..BL-026) which differs from the current numbering (current BL-015..BL-024 were assigned different items in the 2026-04-11T17:10:00Z weekend audit). The stash BL-010..BL-012 status closures reference the same canonical BL-010..BL-012 as the current numbering. See 2026-04-11T19:15:00Z stash recovery entry for reconciliation: stash BL-017 was extracted as audits-2026-04-11/source-first-enrichment-investigation.md + BL-037; stash BL-018 → current BL-035; stash BL-019 → current BL-034; stash BL-026 → current BL-036; stash BL-020/022/023/024/025 mostly obsoleted by the 2026-04-11 API-path scoring shift and preserved only as reference in audits-2026-04-11/token-burn-analysis-2026-04-10.md. -->

[2026-04-10T09:00:00Z] -- Backlog hygiene post session 50 deploy verification. Closed BL-010 (processor stalling fixed — verified 31/31 jobs complete post-deploy), BL-011 (bulk enrichment shipped), BL-012 (batch ICP scoring shipped), BL-015 (campaign approval notifications shipped). Kept BL-016 open (code deployed, 290+ people need Nova re-enrichment) and BL-017 open (code deployed, awaiting first post-deploy discovery run to validate sourceId population end-to-end). Added BL-018: harden AI Ark export webhook with schema validation + louder failure logging + debug payload persistence (priority 3, low severity, follow-up after first real export run).

[2026-04-10T09:30:00Z] -- Added 7 new backlog items (BL-020 through BL-026) targeting Claude Code rate-limit optimisation + Nova CLI entry point fix. Derived from 2026-04-10 rate-limit investigation (354M rlt burn over 48h, ICP scoring was 66% via 3,681 fresh-spawn sessions) and Nova entry point investigation (runner.ts still on @ai-sdk/anthropic, blocking terminal-based Nova invocation). Ship order: Tier 1 behaviours first (free), then BL-023 (safety net) + BL-025 (runner.ts CLI fix), then BL-020 (biggest single win, ~150-200M saving per backfill), then BL-026 + BL-021, then BL-024 + BL-022. Existing BL-019 (LinkedIn A/B variants) was untouched.

[2026-04-10T09:36:00Z] -- BlankTag A/B sibling-clone deploy (BL-019 manual workaround). Pre-approved bypass of Nova orchestrator routing because no existing agent tool covers "clone campaign with sequence-subset + shard target list". Wrote one-shot `scripts/_tmp/blanktag-clone-deploy.ts` running a single Prisma transaction: (1) deterministic shard of list `cmn4x4w4g0000zxicipc08h9b` (468 leads) sorted by id into 3x156 non-overlapping slices with set-intersection proof before commit, (2) 3 new TargetLists + 468 TargetListPerson rows, (3) 3 new Campaigns with parentCampaignId=cmmwei70q0007zxgpvyhwwmua, linkedinSequence = [pos1 blank connect + matched pos2 variant by body text + pos3 + pos4], leadsApproved=contentApproved=true, status=approved. Then per-clone: atomic approved->deployed transition + CampaignDeploy row + tasks.trigger("campaign-deploy"). Variant matching by body text: portal 2C->DB note "2B-v1", portal 2D->"2B-v2", portal 2E->"2B-v3" (portal/DB labels diverge — portal labels used for clone names). Results: 2C=cmnspob3g004ep8xxhm8grecl, 2D=cmnspobho008tp8xxrddnwyeu, 2E=cmnspobtf00d8p8xx8v4ew4jg. All 3 transitioned deployed->active ~1s after trigger fire. Original cmmwei70q0007zxgpvyhwwmua verified untouched (status=approved, deployedAt=null, updatedAt=2026-04-10T08:34:55.291Z unchanged, list size=468, 6 pos2 variants intact). Note to self: original has 9 total steps (1+6+1+1), user brief said "6 messages at pos2" which was accurate re: pos2 variants. James warmup day 0 — 5 connect/day limit means ~31 days runway per clone, total ~94 days before the 468-lead pool exhausts. BL-019 is still the proper long-term fix.

[2026-04-13T11:00:00Z] -- BL-038 + BL-040 implemented together (3 commits). BL-038: ICP scoring gate in promotion flow — scores DiscoveredPerson records before promotion via scoreStagedPersonIcp(), rejects below workspace.icpScoreThreshold (default 40) with status='scored_rejected'. Fail-open on API errors. Pre-fetches domains via prefetchDomains() before scoring loop. Schema: added icpScoreThreshold to Workspace, icpScore/icpReasoning/icpConfidence to DiscoveredPerson (migration Tier 3, not run — prisma generate only). BL-040: AI Ark source-first enrichment — new aiark-source-first.ts uses stored PersonWorkspace.sourceId for direct /export/single lookup. Wired into both batch waterfall (enrichEmailBatch) and single-person waterfall (enrichEmail). Extended EmailAdapterInput with sourceId/discoverySource. Updated enrichment-processor.ts to load sourceId from PersonWorkspace and derive discoverySource from Person.source. 16 tests total (4 scorer, 6 promotion, 6 aiark-source-first), all passing. Type-checked clean.

[2026-04-11T19:15:00Z] -- Stash recovery. Extracted session 50 work from stash@{0} (created 9 hours ago on old main 590e89e7). Three destinations: (1) two reference docs written to the PM memory directory outside git — audits-2026-04-11/source-first-enrichment-investigation.md (full stash BL-017 investigation doc preserved verbatim) and audits-2026-04-11/token-burn-analysis-2026-04-10.md (rate-limit analysis preserved as reference because the `claude --resume` optimization was obsoleted by the 2026-04-11 API-path scoring shift); (2) Nova memory appends — .nova/memory/blanktag/campaigns.md (BlankTag C1 sibling-clone deploy record with live 2C/2D/2E campaign IDs), .nova/memory/yoopknows/learnings.md (290-person re-enrichment cohort), .nova/memory/lime-recruitment/learnings.md (same bug affects Lime, count unknown); (3) Monty memory updates — BL-010 status open→done with VERIFIED FIXED note appended, 7 new backlog items BL-031..BL-037, 3 historical decisions.md entries from 2026-04-10 appended verbatim with provenance header. SKIPPED from stash by PM decision: stash BL-015 (already shipped, git history sufficient), stash BL-020 as a backlog item (obsoleted by API-path shift, kept only as reference doc), stash BL-022 (Nova session pooling — held until BL-032 smoke test determines which specialist wrappers are actually live). Stash@{0} was NOT dropped — kept as 48-hour safety net, scheduled to drop Monday evening after verifying nothing load-bearing was missed. Refs: BL-031, BL-032, BL-033, BL-034, BL-035, BL-036, BL-037.


[2026-04-14] Triage: Two bugs reported by Claudia from Jamie's WhatsApp (Lime E5 client approval). Logged as BL-050 (CTA validator false positive, P2 medium) and BL-051 (writer validation surfacing to client gate, P3 medium). Deferred — Jamie worked around manually. Related; should be considered together.

[2026-04-14] Bug logged: BL-053 — sequence rewrite carries forward contentApproved=true on 1210 Healthcare. Reset performed manually for the 2 Healthcare campaigns via reset-healthcare-content-approved.ts (dry-run + apply, both verified post-reset, audit log entries written). Note: BL-052 ID already taken by 'In Progress' UI item from earlier the same day, so this carry-forward bug was assigned BL-053. Root cause fix needed in Nova Writer's save path (or save-sequence CLI / API endpoint) — overwriting a sequence on a campaign with contentApproved=true must reset that flag and clear contentApprovedAt.

[2026-04-15T00:00:00Z] -- BL-060 migration batch A (2 of 8). Delegating to Dev Agent: delete scripts/generate-copy.ts and scripts/analyze-website.ts. Both carry DEPRECATED headers, zero consumers (verified by user via codebase grep). Replacement path is `npm run chat`. Tier 2 (reversible via git revert). Per-file commits: `chore(scripts): delete deprecated scripts/{name}.ts (BL-060)`. Do NOT run `npm uninstall @ai-sdk/anthropic` — 6 other offender files still reference it. QA Agent will verify after. BL-060 backlog entry stays open until all 8 files done.

[2026-04-15T12:15:00Z] -- BL-060 batch A: delegating deletion of scripts/generate-copy.ts and scripts/analyze-website.ts to monty-dev. Both marked DEPRECATED on line 1, zero consumers (PM verified via grep across src/, scripts/, package.json, .claude/, docs/), replaced by `npm run chat`. Removes 4 `// eslint-disable-next-line no-restricted-imports -- BL-060` comments. 6 of 8 BL-060 files remain after this batch. Tier 2 action (reversible via git revert).

[2026-04-15T12:25:00Z] -- BL-060 batch A complete. monty-dev deleted scripts/generate-copy.ts (commit 6061d9ac, 336 lines) and scripts/analyze-website.ts (commit a4899fa5, 284 lines) on top of b595729b. Both commits: correct subject/body per spec, Co-Authored-By trailer present, exactly 1 file touched each, no --no-verify. Post-state verified: `npx eslint . | grep no-restricted-imports | wc -l` = 0, `npx tsc --noEmit` clean, no live-code references to either filename (8 residual hits all in historical planning/memory docs — acceptable). monty-qa returned PASS with 5 findings (4 info/process, 1 low-severity: BL-060.notes in backlog.json not updated to reflect 2/8 progress — per governance that's orchestrator-only). Not mutating backlog.json per PM instruction (BL-060 stays open, 6 of 8 files remaining).

[2026-04-15T13:00:00Z] -- BL-060 batch B1: delegating thin-launcher rewrite of scripts/chat.ts and scripts/monty.ts to monty-dev. Pattern: scripts/nova.ts (print signpost → exit). Removes interactive REPL (readline) + generateText + @ai-sdk/anthropic + orchestratorConfig/montyOrchestratorConfig imports. Keeps `npm run chat` / `npm run monty` working as signposts to `claude` + `/nova*` / `/monty` slash commands. Per-file commits. Tier 2 (reversible via git revert). 4 of 8 BL-060 files remain after this batch.

[2026-04-15T13:15:00Z] -- BL-060 batch B1 complete. monty-dev rewrote scripts/chat.ts (commit fffe6413, 56 lines, signposts all 8 /nova* commands) and scripts/monty.ts (commit d99c0d83, 34 lines, signposts /monty). Both: thin launchers, chalk-only deps, process.exit(0), no REPL, match nova.ts shape. Co-Authored-By present, per-file commits, no --no-verify, zero unrelated files touched. Post-state: `npx eslint . | grep no-restricted-imports | wc -l` = 0, `npx tsc --noEmit` clean, `npm run chat` and `npm run monty` both exit 0 with signpost in sub-second. monty-qa GO with 6 findings (all info/low, none blocking): cross-launcher duplication acceptable at N=3, decorative shebang (npm run bypasses it), branding drift (chat.ts says "Outsignal Orchestrator" but all slash commands are /nova*), minor header/body redundancy, monty.ts doesn't list /monty-dev/qa/security specialists. 4 of 8 BL-060 files remain; backlog.json unchanged per PM instruction.

[2026-04-15T00:00:00Z] -- BL-060 batch B2: (1) brand color fix in scripts/chat.ts (#F0FF7A -> #635BFF to match nova.ts/monty.ts), (2) delete scripts/setup-client.ts (one-off seed, zero consumers, had hardcoded API tokens flagged separately), (3) delete scripts/lime-linkedin-writer.ts (one-off lime LinkedIn writer run, hardcoded campaign IDs). Three separate commits, not pushed.

[2026-04-15T10:00:00Z] -- Triaged BL-061 (CLI deploy surface for Campaign Agent). Gap: Campaign Agent and ops have no CLI to trigger `POST /api/campaigns/{id}/deploy`; 17 approved campaigns (7 Lime + 10 1210) blocked. Plan: (B1) extract deploy initiation logic from `src/app/api/campaigns/[id]/deploy/route.ts` into shared helper `src/lib/campaigns/deploy-campaign.ts` so both the route and a new CLI call the same code path; (B2) build `scripts/cli/campaign-deploy.ts` with --ids/--dry-run flags, serialized execution, stop-on-first-failure, post-verify GET; (B3) QA; (B4) commit + push; (B5) dry-run then live on 17 IDs; (B6) file "Automate EB UI completion" brief. Delegating B1+B2 to monty-dev now.

[2026-04-15T14:00:00Z] -- BL-060 step 7 (7/8): relocate extractIcpCriteria from src/lib/agents/campaign.ts to new src/lib/icp/extract-criteria.ts so the anthropic() call lives in ESLint allow-listed territory (alongside scorer.ts). Drops 2 eslint-disable comments + both `ai`/`@ai-sdk/anthropic` imports from campaign.ts. Moves icpCriteriaSchema + extractIcpCriteria verbatim (dropping stale "Claude Haiku" JSDoc line — actual model is NOVA_MODEL=opus-4-6). Adds unit tests (mocked happy path + schema validation). NOVA_MODEL still used at campaign.ts:419 for campaignConfig.model, so import stays. Tier 2.

[2026-04-15T14:30:00Z] -- BL-061 B1+B2 (planned, Tier 2): extract non-retry deploy-initiation logic from src/app/api/campaigns/[id]/deploy/route.ts into new shared helper src/lib/campaigns/deploy-campaign.ts exporting initiateCampaignDeploy({campaignId, adminEmail, dryRun}). Discriminated-union return ({ok:true, deployId, beforeStatus, afterStatus, channels} | {ok:false, code: 'not_found'|'not_approved'|'already_deployed'|'missing_approvals', reason}). Dry-run short-circuits after validation — no mutation, no trigger fire. Refactor route.ts to delegate non-retry path to the helper, mapping codes back to 400/404/409. Retry branch stays inline. New CLI scripts/cli/campaign-deploy.ts runs IDs serially, live default, --dry-run opt-in (matches task spec verbatim, NOT patch-content-approved's --apply default). Parses --ids CSV or stdin; stops on first failure; post-verifies each success via getCampaign. JSON envelope via runWithHarness with per-campaign results + summary counts. Tests: src/__tests__/deploy-campaign.test.ts (helper paths) + src/__tests__/campaign-deploy-cli.test.ts (arg parsing, classify). No schema change. No commit — orchestrator handles.

[2026-04-15T15:00:00Z] -- BL-061 B1+B2 complete. Files: (1) NEW src/lib/campaigns/deploy-campaign.ts (189 lines) exporting initiateCampaignDeploy + deployFailureHttpStatus. (2) MODIFIED src/app/api/campaigns/[id]/deploy/route.ts (117->85 lines) — retry branch unchanged, non-retry branch delegates to helper with the original error strings preserved verbatim ("Campaign not found" / "Both leads and content must be approved before deploying" / "Campaign is not in approved status (may have already been deployed)") so portal-visible response shape is byte-identical. (3) NEW scripts/cli/campaign-deploy.ts (277 lines) — parses --ids/--dry-run/--admin-email/--incident, also accepts stdin, dedupes preserving order, runs serially, stops on first failure (LIVE only — dry-run reports every verdict), post-verifies each success via getCampaign, final JSON envelope via runWithHarness. Default admin = ops@outsignal.ai. (4) NEW src/__tests__/deploy-campaign.test.ts (313 lines, 12 tests) covering happy path, channel skip marking, all 4 failure codes, and 4 dry-run scenarios (no-mutation assertion is the load-bearing check). (5) NEW src/__tests__/campaign-deploy-cli.test.ts (197 lines, 22 tests) covering splitStdinIds, parseCliArgs (incl. LIVE-default, dedup, positional rejection), classifyResult discriminated-union flattening. Results: 34/34 tests green, `npx tsc --noEmit` clean, `npx eslint` on changed files clean, adjacent tests (patch-content-approved, save-campaign-sequences-reset) still green. Design trade-offs: (a) default LIVE not dry-run (matches task spec, not patch-content-approved convention — user speccd --dry-run explicitly); (b) positional args rejected (forces explicit --ids= or stdin sourcing so the input source is unambiguous); (c) post-verify failure is non-fatal (deploy already landed, just logs a stderr warn). No commit — orchestrator handles after QA.

[2026-04-15T15:30:00Z] -- BL-061 escalation: Phase A refactor of email-adapter.ts for API-native end-to-end deploy. Earlier session 2026-04-15T15:00:00Z shipped CLI + helper (B1+B2) that fires trigger.dev task — but the trigger.dev task currently stops after createCampaign + sequenceSteps + createLead loop. Campaigns end up as DRAFT on EB, not launching. User reconfirmed via OpenAPI spec that schedule/attach-senders/update/resume/attach-tags endpoints ALL exist (prior verification was reading stale spike doc, WRONG). Refactor scope: (1) extend client.ts with createSchedule/updateSchedule/getSchedule + attachTagsToCampaigns (4 new methods; attachSenderEmails + updateCampaign + resumeCampaign already exist); (2) refactor email-adapter.deploy() to add steps 4-8: createSchedule (hardcoded Mon-Fri 09-17 Europe/London default — no schema change), attachLeadsToCampaign (fixes gap where createLead'd leads weren't being attached), attachSenderEmails (via Sender.emailBisonSenderId), updateCampaignSettings (plain_text=true, open_tracking=false, reputation_building=true, can_unsubscribe=true), resumeCampaign (DRAFT→QUEUED launch op); (3) skip attachTagsToCampaigns wiring (no tag IDs configured anywhere, method ships unused for future use); (4) tests. Delegating to monty-dev with full EB spec references. Schema unchanged — all defaults live in adapter code.

[2026-04-15T15:45:00Z] -- BL-061 Phase A (Tier 2, planned): extending src/lib/emailbison/client.ts with 4 new methods (createSchedule, updateSchedule, getSchedule, attachTagsToCampaigns), adding CreateScheduleParams + UpdateScheduleParams + ScheduleResponse types to types.ts, and refactoring src/lib/channels/email-adapter.ts::deploy() to add steps 3b (attachLeadsToCampaign with captured lead IDs — fixes gap where createLead'd leads were orphaned in global pool), 4 (createSchedule Mon-Fri 09:00-17:00 Europe/London default), 5 (attachSenderEmails from Sender.emailBisonSenderId for healthy+warning channel=email|both senders), 6 (updateCampaign with plain_text/open_tracking/reputation_building/can_unsubscribe defaults), 8 (resumeCampaign as final launch op; step 7 tags=no-op per spec). Zero-leads path: skip attach+launch, mark complete with emailError='no_leads_to_deploy'. Zero-senders path: throw. Adds src/__tests__/emailbison-client-schedule.test.ts + src/__tests__/email-adapter-deploy.test.ts. No schema change. No trigger/vercel deploy. No commit (orchestrator owns). Source of truth for endpoint bodies: docs/emailbison-dedi-api-reference.md lines 152-198 (schedule), 1118-1127 (tags).

[2026-04-15T16:00:00Z] -- BL-061 QA Finding A fix (Tier 2). Wrap `initiateCampaignDeploy` call in scripts/cli/campaign-deploy.ts main() loop in try/catch so a helper throw (e.g. tasks.trigger rejection mid-batch) does not propagate out and discard prior per-ID outcomes. On catch: push synthetic failure row with code='helper_threw', errorReason=err.message, errorWarning=zombie-deploy warning pointing operator at getCampaign + campaignDeploy.findFirst for manual reconciliation; log loudly to stderr; set stoppedEarly=true; break out of loop to preserve stop-on-first-failure semantics. Extend PerCampaignOutcome with optional `errorWarning` field. classifyResult untouched — synthetic row built inline at the catch site (no InitiateDeployResult produced). Add one vitest in src/__tests__/campaign-deploy-cli.test.ts mocking helper to throw on 2nd of 3 IDs; assert result[0]=deployed, result[1]={ok:false,code:'helper_threw',errorWarning:/zombie/}, result.length===2, stoppedEarly===true. Requires exporting main() behind the same invokedAsScript guard. NOT touching deploy-campaign.ts — compensating transaction is a separate follow-up.

[2026-04-15T16:15:00Z] -- BL-061 Phase A complete. Files: (1) src/lib/emailbison/types.ts +38 lines (CreateScheduleParams + UpdateScheduleParams + ScheduleResponse). (2) src/lib/emailbison/client.ts +91 lines (createSchedule POST, updateSchedule PUT, getSchedule GET with 404→null, attachTagsToCampaigns POST /tags/attach-to-campaigns with EMPTY_TAG_LIST + EMPTY_CAMPAIGN_LIST guards). (3) src/lib/channels/email-adapter.ts +94 lines (DEFAULT_SCHEDULE + DEFAULT_CAMPAIGN_SETTINGS constants, deploy() refactored: captures lead IDs into createdLeadIds[], zero-leads short-circuit with emailError='no_leads_to_deploy', attachLeadsToCampaign → createSchedule → Sender query filter channel in (email,both) + emailBisonSenderId not null + healthStatus in (healthy,warning) → attachSenderEmails → updateCampaign defaults → resumeCampaign as final launch op; tags noted as no-op awaiting Workspace.ebTagIds). (4) NEW src/__tests__/emailbison-client-schedule.test.ts (11 tests, 215 lines). (5) NEW src/__tests__/email-adapter-deploy.test.ts (6 tests, 336 lines; uses vi.hoisted for mock ref sharing + class-constructor stub for EmailBisonClient). Test results: 17 new + 35 adjacent (deploy-campaign.test.ts + campaign-deploy-cli.test.ts) = 52/52 green. `npx tsc --noEmit` clean. `npx eslint` on changed files shows zero NEW errors (2 pre-existing lint issues unchanged: client.ts any-at-line-892 shifted from line-787, email-adapter.ts unused-DeployResult-import warning). No schema change, no commit, no trigger/vercel deploy. Orchestrator handles commit after QA. Deviations from spec: none — the spec was followed verbatim including channel filter in (email,both) which wasn't in the spec Prisma query literal but matches the canonical getActiveSendersForChannel pattern in src/lib/channels/sender-helpers (sender-helpers.test.ts:75).

[2026-04-15T17:00:00Z] -- BL-060 post-ship polish (Tier 2, docs only, no behavior change). Addressing QA F1+F2 on commit b9004bfd. (F1) Rewriting `.monty/memory/backlog.json` BL-060 notes with accurate 8-file breakdown per `git log 6061d9ac..b9004bfd`: 4 deletions (generate-copy, analyze-website, setup-client, lime-linkedin-writer), 2 thin launchers kept as npm run chat/monty signposts (chat.ts, monty.ts), 1 relocation (src/lib/agents/campaign.ts → src/lib/icp/extract-criteria.ts), 1 allow-list expansion (src/lib/agents/** added to eslint allow-list, 2 eslint-disables removed from runner.ts). Status stays `done`. (F2) Dropping trailing sentence in eslint.config.mjs allow-list comment block that references `memory/feedback_nova_no_api.md` and `memory/bl-060-handover.md` — those files only live in the PM's personal Claude memory dir, not in the repo. Rest of comment is self-contained. Single NEW commit, not amending b9004bfd. Only 2 files touched.

[2026-04-15T12:20:00Z] -- BL-061 shipped. Commit c1de9dfc pushed to origin/main. Phase B5 batch deploy executed via `npx tsx scripts/cli/campaign-deploy.ts --ids=<17-csv> --incident=BL-061 --admin-email=jonathan@outsignal.ai`. Dry-run: 17/17 would-deploy, zero anomalies. Live: 17/17 success, zero failures, not stopped early. All post-verified via getCampaign -> verifiedStatus=deployed, verifiedDeployedAt populated. 7 Lime + 10 1210. Deploy IDs in `/tmp/bl061-deploy-envelope.json` (ephemeral — summary recorded in BL-061 backlog entry). Follow-ups filed: BL-062 (zombie deploy compensating txn), BL-063 (CLI main/stdin coverage), BL-064 (race test), BL-065 (unused export), BL-066 (Automate EB UI completion — schedule/senders/tracking via undocumented EB API; research phase not started). Remaining ops action: manually complete EB UI config per existing runbook on each of the 17 campaigns until BL-066 lands.

[2026-04-15T18:00:00Z] -- Routing BL-068 cleanup (Tier 3, pre-approved by user — same pattern as BL-061). Scope: 4 stuck 1210-solutions LinkedIn campaigns in status=deployed due to prisma.campaignSequenceRule.createMany() schema error. Actions: (a) investigate schema mismatch + document as BL-068 (do NOT fix), (b) rollback DB state (Campaign deployed->approved, CampaignDeploy failed->rolled_back), (c) check+delete any EB artifacts created today. Constraint: ONLY these 4 campaigns; no deploy-code changes today. Full pipeline rebuild is pending per project_campaign_deploy_audit.md. Delegating to monty-dev.

[2026-04-15T19:45:00Z] -- PLANNED Phase 1a amendment: clear spurious `deployedAt` on 11 email Campaign rows (Lime 6 + 1210 5) that were rolled back deployed→approved in cleanup commit ad3105de but never had timestamps cleared. Lime IDs: cmnpwzv9e010np8itsf3f35oy, cmnpwzwi5011sp8itj20w1foq, cmnpwzxmg012gp8itxv4dvmyb, cmnpwzym5014op8it2cpupfwx, cmnpx037s01dcp8itzzilfdfb, cmnq5nivc0001p8534g0k4wr6. 1210 IDs: cmneq92p20000p8p7dhqn8g42, cmneqixpv0001p8710bov1fga, cmneq1sdj0001p8cg97lb9rhd, cmneqhwo50001p843r5hmsul3, cmneqa5180001p8rkwyrrlkg8. Plan: single $transaction, 11× Campaign.update setting deployedAt=null (preserve status, contentApproved, leadsApproved), 11× AuditLog.create (action=campaign.deployedat.cleared, adminEmail=ops@outsignal.ai, metadata{reason,phase:'Phase 1a amendment',relatedCommit:'ad3105de'}). Post-verify SELECT 0 rows deployedAt IS NOT NULL AND status=approved in Lime/1210. Then delete untracked throwaway scripts/maintenance/_bl068-clear-deployedat.ts. Single commit follows.

[2026-04-15T20:00:00Z] -- PLANNED Phase 2 execution (Tier 2, delegate to monty-dev). Scope: fix BL-068 `position: undefined` bug in LinkedIn rule builder via three-part code-only fix — (F1) primary defensive read at src/lib/channels/linkedin-adapter.ts ~106-118 `position: step.position ?? step.stepNumber ?? idx + 1`; (F2) Zod parse replacing `as LinkedInSequenceStep[]` cast at ~line 64, schema accepts either `position` or `stepNumber` key, `.refine()` rejects steps missing both, `.parse()` throws cleanly; (F3) secondary defensive read at src/lib/linkedin/sequencing.ts:387 for defense-in-depth. Tests: new src/lib/channels/__tests__/linkedin-adapter.test.ts with 6 cases (happy-path position, BL-068 stepNumber shape, missing-both rejection, mixed sequence, idx+1 fallback, real 1210 JSON regression fixture). Gates: vitest target + linkedin dir + full suite (37 BL-055 failures OK per handover §6), `npx tsc --noEmit` clean, `npx eslint . | grep no-restricted-imports | wc -l` = 0. Commit message specified by brief; Co-Authored-By Claude Opus 4.6 trailer. Also filing BL-069 (channel sequence JSON shape-drift normalisation, severity medium, priority 2) in .monty/memory/backlog.json — bundled in same commit per dev's call. Hard rules: ONLY touch linkedin-adapter.ts + sequencing.ts + new test file + backlog.json; preserve all other uncommitted files; NO push, NO vercel deploy, NO schema migration, NO Phase 3. Delegating to monty-dev now; monty-qa adversarial review after.

[2026-04-15T19:50:00Z] -- COMPLETED Phase 1a amendment. (1a-A) SELECT returned 11 rows (expected 11-13, matches QA Finding 3 count exactly — the "13" ambiguity resolved as 11 on tight filter of status=approved AND workspace∈{lime-recruitment,1210-solutions} AND updatedAt≥2026-04-15T00:00Z). IDs: cmnpwzv9e, cmnpwzwi5, cmnpwzxmg, cmnpwzym5, cmnpx037s, cmnq5nivc (Lime 6), cmneq92p2, cmneqixpv, cmneq1sdj, cmneqhwo5, cmneqa518 (1210 5). All deployedAt timestamps in 12:17:39Z-12:18:02Z window. All contentApproved=true, leadsApproved=true. (1a-B) Single prisma.$transaction applied 22 ops (11 Campaign.update setting deployedAt=null + 11 AuditLog.create action=campaign.deployedat.cleared, adminEmail=ops@outsignal.ai, metadata.priorDeployedAt captured per row). Status/contentApproved/leadsApproved untouched. Guard pre-check asserted status=approved AND deployedAt≠null for all 11 before applying. (1a-C) Post-verify: all 11 rows now deployedAt=null, status=approved, contentApproved=true, leadsApproved=true. Collateral check: 0 rows remain with deployedAt IS NOT NULL AND status=approved in Lime+1210. (1a-D) Script deletion: scripts/maintenance/_bl068-clear-deployedat.ts was untracked (never in git index) — `rm` applied. No `git rm` needed. (1a-E) Commit: NO COMMIT — same Phase 1 precedent (§1c), no tracked-file change exists to commit. The deleted script was untracked; the .monty/memory/decisions.md edits remain uncommitted per brief's "preserve uncommitted working-tree files" clause. Empty commit avoided. Phase 1a complete. READY for Phase 2 recon.

[2026-04-15T20:50:00Z] -- Tier 2: Phase 3 QA fix pass on src/lib/channels/email-adapter.ts. Fixed F2 (silent `as EmailSequenceStep[]` cast at former line 241) by adding `StoredEmailSequenceStepSchema` + `StoredEmailSequenceSchema` Zod schemas (position required, all content fields optional, passthrough extras) mirroring Phase 2's `LinkedInSequenceStepSchema` discipline. Replaced cast with `StoredEmailSequenceSchema.parse(campaign.emailSequence ?? [])` wrapped in try/catch that rethrows with BL-068-tagged message identifying the campaignId — outer deploy catch encodes as `[step:2] <msg>` on emailError. Position is required (not optional-with-refine) because the Step 3 loop keys idempotency on `existingPositions.has(step.position)` — `stepNumber`-only drift would make `.has(undefined)` always false → re-POST every step → silent EB duplication. Fixed F4 (two bare catches) at getSequenceSteps lines 710 and 747: added `console.warn` with full context (EB campaign ID, campaign name, workspace slug, error object) before each fallback path. No logic changed. Verification: `npx tsc --noEmit` clean; `npx eslint . | grep no-restricted-imports | wc -l` = 0; `npx vitest run` = 806 passed (matches QA baseline — pre-existing 40 unrelated failures unchanged); email-adapter-deploy.test.ts improved from 5 failed/1 passed (baseline with stash) to 3 failed/4 passed (my Zod parse unblocked 2 previously-failing tests). Zero bare catches remain in the whole file; diff introduces zero new `as`-casts on stored-JSON/EB-response. Surprise: pre-existing `as Array<{...}>` cast at line 733 in getSequenceSteps fallback read path (not F2, not in my diff, out of Phase 3 scope) — could be tightened in a future Zod-boundary sweep but brief forbids logic changes beyond F2/F4. Not committing — PM commits.

[2026-04-15T20:15:00Z] -- COMPLETED Phase 3.5 (BL-070 fix). (3.5f-preflight) Verified all 3 pre-existing migration files MATCH live Neon DB state via information_schema + pg_indexes probes before resolving as applied: mig1 Sender.channel column (text NOT NULL default 'email' + Sender_channel_idx) MATCH; mig2 Lead.email column (nullable=YES, default=null — table name "Lead" per @@map in schema) MATCH; mig3 InboxStatusSnapshot 5× lastXxxAlertAt TIMESTAMP(3) nullable MATCH. _prisma_migrations table absent (project was on `prisma db push`). (3.5f) `prisma migrate resolve --applied` × 3 after killing stale schema-engine advisory-lock holder (pid 28168); `prisma migrate deploy` applied the 4th (DROP old @@index + CREATE UNIQUE INDEX on Campaign.emailBisonCampaignId). Spot-check live DB: only `Campaign_emailBisonCampaignId_key` (UNIQUE) exists; old `_idx` gone. (3.5g) Fixed implicit-any TS7006 on email-adapter-race.test.ts:222 (warnSpy.mock.calls.map((c: unknown[]) => ...)) — test file was a pre-existing passing-per-decisions-md claim but tsc regressed. Post-fix: tsc clean, eslint no-restricted-imports=0, vitest 807 pass / 40 fail (matches baseline: 806 + 1 new race test; 40 pre-existing BL-055 ICP mock failures unchanged). Race test 1/1 green in isolation. (3.5h) Filed BL-072 (orphan EB cleanup worker, P3/medium/open). Committed 5 files at SHA 5a13ceba: schema.prisma, migration.sql, email-adapter.ts, email-adapter-race.test.ts (NEW), backlog.json. Pre-commit hook ran tsc clean. Phase 4 test files (src/__tests__/email-adapter-deploy.test.ts + emailbison-client-schedule.test.ts) and ~30 maintenance scripts preserved untracked per brief. No push, no vercel. BL-070 race is now closed at the DB constraint level.

[2026-04-15T21:50:00Z] -- Phase 3.5 QA + checkpoint. monty-qa reviewed commit 5a13ceba with 9 findings. GO-with-caveat verdict for Phase 4. Highest finding (F1, HIGH): src/lib/agents/campaign.ts:283-329 activateSignalCampaign is the BL-070 sibling bug on Campaign.signalEmailBisonCampaignId — schema line 785 is Int? with NO @unique, exact same read-createEBCampaign-write race. Filed BL-073 (severity high, priority 2, open) prescribing the Phase-3.5-mirror fix. F2 (MEDIUM): P2002 catch in email-adapter.ts doesn't inspect err.meta.target — safe today but would misfire if another Campaign @unique is added; recommend narrowing to target.includes("emailBisonCampaignId") when BL-073 lands (need dual-target handling). F3/F4 (MEDIUM, testing): race test is not a true Promise.all concurrency test and doesn't cover the 3 defensive rethrow branches (delete-fails / re-read-null / P2002-in-reuse-path) — BL-072 fallback scenario is unexercised. F5 (MEDIUM, migration-safety): unique-create preamble is a comment not a DO-block guard; ACCESS EXCLUSIVE lock on Campaign was fine at 56 rows but flag the pattern before applying uniques to Person/TargetListPerson. F6-F9: LOW/INFO (transaction atomicity widened, no downstream idempotency test on winner switchover, comment phrasing, schema clean). Full findings at .monty/memory/incidents.md [2026-04-15T21:30:00Z]. Checkpoint to PM: Phase 3.5 GO for Phase 4 with BL-073 as the gating follow-up for any Phase 4 work touching signal-campaign concurrency.

[2026-04-15T22:00:00Z] -- STARTING Phase 4 (tests-only, PM pre-authorized). Scope: (4a) commit 2 untracked test files src/__tests__/email-adapter-deploy.test.ts + src/__tests__/emailbison-client-schedule.test.ts. First file has 3 failures (TypeError: ebClient.getCampaign is not a function at Step 10 VERIFY_STATUS) — stale mocks from Phase A, missing `getCampaign` on ebMock hoisted stub. Surgical update: add getCampaign mock + explicit emailBisonCampaignId:null on stubCampaign + update sequential-order expectation to include getCampaign at the end. NOT a rewrite — targeted mock additions to catch up with Phase 3's 10-step flow. Schedule tests all green as-is. (4b) Second race test case in src/lib/channels/__tests__/email-adapter-race.test.ts using orchestrated-mock (not real DB) to exercise true Promise.all concurrency with P2002 on the loser. (4c) 3 defensive branch tests (delete-fails, re-read-null, P2002-from-reuse). (4d) Integration E2E — FIRST check docs/emailbison-dedi-api-reference.md for workspace create/delete endpoints; if unavailable, STOP and escalate or fall back to pure HTTP-mock per brief. (4e) Full suite verification. Tier 2 throughout. Commits: 1 for 4a (stale-test updates), 1 for 4b+4c+4d. No push, no vercel, no production DB migrations.

[2026-04-15T22:30:00Z] -- COMPLETED Phase 4 (tests-only). (4a) Stale tests updated — email-adapter-deploy.test.ts: added getCampaign+getSequenceSteps+getSchedule+updateSchedule+deleteCampaign mocks to ebMock, added findUnique to prismaMock.campaign, mocked EmailBisonApiError export, explicit emailBisonCampaignId:null on stubCampaign, added "getCampaign" to sequential-order expectation. emailbison-client-schedule.test.ts all green as-is. Commit dfdef8fc. 17/17 green. (4b) True Promise.all concurrent-deploy race — new describe block in src/lib/channels/__tests__/email-adapter-race.test.ts. Orchestrated-mock strategy: latched `winningEbId` in prisma.campaign.update stub mirrors UNIQUE-constraint semantics; two Promise.all deploys converge on winner; exactly 1 EB.deleteCampaign fires on loser's orphan; both CampaignDeploy rows end on winner. (4c) 3 defensive branch tests — (i) orphan delete fails → BL-072 warn + continue + no throw, (ii) re-read returns null after P2002 → throw "Refusing to proceed" + [step:2] tag + no delete + no resume, (iii) P2002 from REUSE path → rethrows unmodified via else branch + no recovery + [step:2] tag. (4d) Integration E2E — EB OpenAPI docs confirm both POST /api/workspaces/v1.1 and DELETE /api/workspaces/v1.1/{team_id} exist BUT delete requires super-admin token (not in repo) + would touch live EB resources. Per brief fallback: HTTP-mock strategy via global.fetch stub, new file src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts with 4 cases — (1) first-time deploy happy path (asserts relative order of all 10 EB HTTP calls), (2) re-deploy idempotency (verifies POST /campaigns never fires, POST sequence-steps never fires for position 1, PUT schedule fires for reuse branch), (3) mid-flight 422 at Step 6 (asserts emailError tagged [step:6], resume+verify never called, state recoverable via persisted emailBisonCampaignId), (4) resume-after-failure (verifies idempotent re-run completes cleanly — createCampaign never called, sequence-steps not re-posted, but Step 6 fires successfully). (4e) Full suite: 818 pass / 37 fail / 1 todo — matches baseline (807+11 new = 818 pass; 37 unchanged pre-existing BL-055/api-routes/emailbison-client/resend-notifications/linkedin-sender failures). tsc clean, eslint no-restricted-imports=0. No src/ production code touched in Phase 4. No push, no vercel, no prod DB migrations. Ready for PM to move to Phase 5 (commit + push + prod deploy).

[2026-04-16T00:00:00Z] -- STARTING Phase 6a CANARY deploy (Tier 3, PM pre-authorized per handover 2026-04-15 §0). Scope: fire 2 production deploys via scripts/cli/campaign-deploy.ts — (1) email canary cmneqixpv0001p8710bov1fga (1210 Email Facilities/Cleaning, 44 leads), (2) LinkedIn canary cmneqixvz0003p871m8sw9u7o (1210 LinkedIn Facilities/Cleaning, 27 leads, BL-068 rollback — directly validates Phase 2 LinkedIn Prisma fix). CLI syntax correction: uses --ids=<csv> flag, NOT positional arg (task-brief example was wrong). Sequence: 6a.1 pre-flight read-only per-canary table, 6a.2 email canary fire + 15s poll of CampaignDeploy.status (10min max) + EB GET verification, 6a.3 LinkedIn canary fire (only on 6a.2 success), 6a.4 EB-side verification (leads count, senders, schedule, steps, status), 6a.5 report + HARD STOP before Phase 6b (remaining 15). Hard rules: stop on first failure; no push/migration/vercel; no mid-flight fixes; EB-state mismatch = NO-GO even if DB says success. Delegation: monty-dev for execution + polling, monty-qa after for ≥3 findings (idempotency exercised? compensating-tx fire? EB vs DB parity?). Checkpoint to PM before Phase 6b.

[2026-04-16T00:05:00Z] -- PHASE 6a monty-dev starting execution. Planned Tier 2 action: create scripts/maintenance/_phase6a-preflight.ts (read-only) per brief §6a.1. Verifies both canaries (cmneqixpv0001p8710bov1fga email + cmneqixvz0003p871m8sw9u7o LinkedIn) meet all 10 preflight gates: status=approved, contentApproved=true, leadsApproved=true, emailBisonCampaignId=null, deployedAt=null, targetListId non-null + people>0, emailSequence/linkedinSequence parseable w/ steps>0, channels includes expected channel, workspace.enabledModules includes channel, >=1 active+healthy sender. Will STOP on any fail and not fire Tier 3 deploys. Tier 3 CLI call will be `npx tsx scripts/cli/campaign-deploy.ts --ids=<id> --incident=BL-phase-6a` (no --dry-run = LIVE) with 15s DB polling loop on CampaignDeploy.status, 10min timeout, then EB-side verification via ebClient.getCampaign/getCampaignLeads/getSequenceSteps/getSchedule/getCampaignSenderEmails.

[2026-04-16T08:05:00Z] -- PHASE 6a CANARY FAILED — STOPPED. Preflight 6a.1 both canaries PASS (12/12 gates each). Fired email canary cmneqixpv0001p8710bov1fga via scripts/cli/campaign-deploy.ts --ids=<id> --incident=BL-phase-6a. Trigger.dev task completed in ~50s. CampaignDeploy cmo16w3r90001zxlvxr1x5yop terminal state: status=failed, emailStatus=failed, emailBisonCampaignId=79, error="Email Bison API error 422: The title field is required. (and 1 more error) — errors.title + errors.sequence_steps". Failure is at Step 3 (POST /api/campaigns/{id}/sequence-steps). Campaign row: status=deployed, emailBisonCampaignId=79, deployedAt=2026-04-16T08:00:27.470Z. EB-side verification: BOTH EB campaign 78 (created 08:00:53) AND EB campaign 79 (created 08:01:10) exist on EB, both status=draft, both 0 leads/0 senders/no schedule/no steps. This is the Phase 3 regression signature — idempotency+compensating-tx did NOT fire correctly. Two orphan EB drafts from a single CampaignDeploy. Per hard rules: STOP before LinkedIn canary (cmneqixvz0003p871m8sw9u7o — CONFIRMED UNTOUCHED: status=approved, ebId=null, deployedAt=null). NO mid-flight fix. NO rollback (PM decides). Logs at /tmp/phase6a-email-deploy.log + /tmp/phase6a-eb-verify-email.log + /tmp/phase6a-final-state.log. Preflight script kept at scripts/maintenance/_phase6a-preflight.ts. GO/NO-GO: NO-GO for Phase 6b. Handing back to PM / orchestrator for triage; likely need BL-phase-6a ticket + Phase 3.6 compensating-tx fix before re-attempt.

[2026-04-16T08:30:00Z] -- Phase 6a CANARY FAILED + QA review complete. HARD STOP before Phase 6b. Email canary (1210 email, 44 leads) failed 422 at Step 3 (createSequenceStep) ~50s after CLI fire. Identical error string to pre-rebuild 2026-04-15 failure (CampaignDeploy cmo00nd6g000pp8j53qk6h1ky) — Phase 3 rebuild (e2ffe366 + dfdef8fc + aa0db89b) did NOT fix root cause, only wrapped scaffolding around an untested wire-payload bug. LinkedIn canary NOT fired per stop-on-first-failure. Residual state: 2 orphan EB drafts (78, 79 — both empty, preserved as forensic evidence), Campaign cmneqixpv...v1fga stuck status=deployed + ebId=79 + deployedAt=non-null, CampaignDeploy.status=failed. Compensating-tx did NOT fire (per QA F3/F7). QA 8 findings: F1 CRITICAL (createSequenceStep wire shape + deprecated endpoint) filed as BL-074 (blocks Phase 6b); F2 HIGH (zero contract tests — all mocks return success regardless of payload — test theater); F3 HIGH (Trigger.dev retry produced dual orphans, serial-retry path uncovered by race tests) filed as BL-076; F4 MEDIUM (optimistic Campaign.status rollback gap — Phase 5.5 was manual SQL fix, systemic fix missing) filed as BL-075; F5 MEDIUM (CLI reports success=1 for failed async task); F6 MEDIUM (_phase6a-preflight.ts hard-coded IDs, should be generalized for Phase 6b); F7 LOW (Step 3+ orphan cleanup gap); F8 LOW (no retry-idempotency test). Next PM action: (1) approve BL-074 scope + fix before any retry, (2) BL-075+BL-076 in parallel, (3) rollback Campaign cmneqixpv...v1fga to approved + clear ebId + clear deployedAt (Phase-5.5-mirror, single-row) before re-attempt, (4) do NOT delete EB 78 + 79 until F3 root-cause understood. Full QA findings at .monty/memory/incidents.md [2026-04-16T08:00:00Z]. Phase 6b NO-GO confirmed.

[2026-04-16T08:15:00Z] -- COMPLETED Phase 6a-rollback (Tier 3, PM pre-authorized). Same pattern as Phase 0 (4 LinkedIn rollback) + Phase 5.5 (11 stale ebId clear). Script: scripts/maintenance/_phase-6a-rollback-email-canary.ts (untracked, `_` prefix convention). Preflight gates all pass: Campaign cmneqixpv status=deployed/ebId=79/deployedAt=2026-04-16T08:00:27.470Z/contentApproved=true/leadsApproved=true/targetListId=cmnrplw2u004izxildck6yutd/workspace=1210-solutions; Deploy cmo16w3r9 status=failed/ebId=79/campaignId=cmneqixpv; LinkedIn canary cmneqixvz untouched (status=approved, ebId=null). (6a-r.1) EB delete campaign 79 via EmailBisonClient.deleteCampaign using 1210-solutions workspace apiToken (prefix 31|…). First --execute attempt: DELETE 79 returned 204 success, but post-delete GET on the same client instance threw non-404 error (status=undefined — likely transient fetch error or retry-exhaustion race). Second --execute attempt (after fresh-process re-verification externally confirmed EB 79 genuinely 404 with proper EmailBisonApiError.isRecordNotFound=true, body record_not_found.campaign="79"): pre-delete GET returned 404, script skipped redundant delete, proceeded to DB mutation. Root cause of first-run error benign — EB 79 genuinely deleted (verified externally + by pre-delete GET on retry), DB state was unchanged from first run (transaction never reached because script's pre-tx post-delete verify threw). (6a-r.2) Dry-run matched row counts: 1 Campaign + 1 CampaignDeploy (expect 1 + 1). Single prisma.$transaction committed at 2026-04-16T08:13:16.000Z: Campaign.update (status deployed→approved, emailBisonCampaignId 79→null, deployedAt 2026-04-16T08:00:27.470Z→null; contentApproved=true/leadsApproved=true/targetListId preserved) + CampaignDeploy.update (status failed→rolled_back, error appended with suffix " --- Phase 6a-rollback 2026-04-16 — EB campaign 79 deleted, DB reverted to approved ready for Phase 6.5 fix of createSequenceStep batch shape", new length=365) + AuditLog.create (id=cmo17cktr0000zxierpzqd91x, action=campaign.status.phase6a_rollback, entityType=Campaign, entityId=cmneqixpv0001p8710bov1fga, adminEmail=ops@outsignal.ai, metadata={reason, phase:"Phase 6a-rollback", deletedEbCampaignId:79, originalDeployId, fromCampaignStatus:deployed, toCampaignStatus:approved, fromDeployStatus:failed, toDeployStatus:rolled_back}). (6a-r.3) Post-state verified: Campaign {status=approved ✓, emailBisonCampaignId=null ✓, deployedAt=null ✓, contentApproved=true ✓, leadsApproved=true ✓}; Deploy {status=rolled_back ✓, error suffix present ✓}; EB 79 {404 ✓ — EmailBisonApiError.isRecordNotFound=true}; LinkedIn canary cmneqixvz UNCHANGED (status=approved, ebId=null, updatedAt=2026-04-15T18:46:09.496Z strictly before tx start 2026-04-16T08:13:16.000Z, byte-identical to preflight read ✓). Collateral check: exactly 1 Campaign row with updatedAt >= tx_start-5s, in-scope (cmneqixpv) — zero unrelated edits to the other 15 approved campaigns or the 2 already-active campaigns (1210 LinkedIn Healthcare, Lime LinkedIn C5). (6a-r.4) NO COMMIT per DB-only scope (Phase 1a / Phase 5.5 precedent): maintenance script stays untracked (`_` prefix), no tracked source files modified. OUT-OF-SCOPE OBSERVATION (NOT touched per brief hard rule "ONLY cmneqixpv + its 1 CampaignDeploy + 1 AuditLog + 1 EB delete"): EB campaign 78 — the first-attempt orphan from the Phase 6a run (duplicate-createCampaign regression that Phase 3's idempotency guard was supposed to prevent, per QA F3/BL-076) — remains on EB (status=draft, 0 leads/0 senders, not persisted on any DB row, unreachable from app data — only from EB admin UI). Flagged for orchestrator as a separate cleanup decision; Phase 6.5 fix of createSequenceStep batch shape should also address the duplicate-createCampaign/Trigger.dev-retry bug that produced 2 EB drafts from 1 CampaignDeploy.

[2026-04-16T09:45:00Z] -- Phase 6a-rollback QA reconciliation (orchestrator wrap-up). monty-qa returned CONCERN verdict (does NOT block Phase 6.5) with 7 genuine findings + 1 positive on the rollback script. F1 HIGH corrects the dev's own [08:15Z] entry: first-run failure was NOT "transient fetch retry-exhaustion race" — real cause is that scripts/maintenance/_phase-6a-rollback-email-canary.ts:150-161 narrows its catch to `EmailBisonApiError` but `client.getCampaign` can also throw `EmailBisonError` (src/lib/emailbison/types.ts:336) with code=CAMPAIGN_NOT_FOUND/statusCode=404 when EB returns 200-with-empty-data during the DELETE async-queue window. That class has `status=undefined/isRecordNotFound=undefined`, so the catch rethrows spuriously. Bug class likely lives in other callers using the same `isRecordNotFound || .status === 404` pattern. Filed as BL-078 (blocks clean BL-077 execution). F2/F3/F5/F7 are script-quality nits (not idempotent, collateral check misses CampaignDeploy axis, AuditLog metadata unconditionally records deletedEbCampaignId, canary check uses literals not preflight-snapshot). F4 MEDIUM is strategic — zero tests on a maintenance script that hits external API + DB; a 10-line mocked vitest would have caught F1 pre-execution. F6 LOW governance: (a) BL-074 notes said "DO NOT delete EB 79" — PM's Phase 6a-rollback brief superseded that; reconciled by appending [UPDATE 2026-04-16T09:45Z] clarification to BL-074.notes explaining EB 79 was deleted with authorization while EB 78 is still retained for BL-076 forensics. (b) EB 78 "flagged for orchestrator" lived only in dev's ephemeral return message + prose in [08:15Z] entry; filed as BL-077 (improvement, P3, low — cleanup after BL-076 root-cause captures). Net backlog delta: +BL-077 (EB 78 cleanup), +BL-078 (error-class mismatch). Full QA findings persisted to .monty/memory/incidents.md [2026-04-16T08:45:00Z]. NEXT: READY for Phase 6.5 (fix createSequenceStep batch shape per BL-074). F2/F3/F4/F5/F7 do NOT block Phase 6.5; any future maintenance script (starting with BL-077) should incorporate F1 fix (catch both error classes) and F4 discipline (mocked smoke test before --execute).

[2026-04-16T10:00:00Z] -- PHASE 6.5b START. PM-authorized compound fix covering BL-074 follow-through (QA F1/F2/F7 from Phase 6.5a) + BL-075 (Campaign.status atomic rollback on deploy failure) + BL-076 (Trigger.dev retry preExistingEbId reuse) + BL-078 (shared isNotFoundError helper covering EmailBisonApiError AND EmailBisonError). Four separate commits targeted (A, B, D, C — order matters: A self-contained; B lands the rollback; D lands the error helper; C coordinates with B + uses D). NO push, NO vercel deploy this phase — PM fires Phase 6.5c separately. Hard constraints: 2 canary campaigns (cmneqixpv, cmneqixvz) UNTOUCHED, EB 78 orphan UNTOUCHED (retained for BL-076 forensics, BL-077 tracks deferred cleanup), 15 Phase 6b campaigns UNTOUCHED, no Trigger.dev retry-semantics change without escalation, uncommitted maintenance scripts preserved. Delegation via monty-dev per bundle → monty-qa adversarial review (min 3 findings each) → next bundle. Final gates: tsc clean, eslint no-restricted-imports=0, vitest 825+ pass / 37 pre-existing fail / 0 new fail, git log shows exactly 4 commits on top of 17139b10.

[2026-04-16T09:42:51Z] -- STARTING Phase 6.5b Bundle A (BL-074 follow-through, Tier 2 reversible, PM pre-authorized via handover 2026-04-15). LOCAL COMMIT ONLY — no push, no deploy. Scope: (A1) Migrate getSequenceSteps READ path src/lib/emailbison/client.ts:235-249 from deprecated v1 `/campaigns/{id}/sequence-steps` to v1.1 `/campaigns/v1.1/{campaignId}/sequence-steps` (docs line 1318); preserve Zod-at-boundary shape validation matching createSequenceSteps pattern (client.ts:381-402) + preserve defensive field-name fallbacks (order??position, email_subject??subject, email_body??body, wait_in_days??delay_days) — schema goal is catching drift loudly, not forcing specific shape. (A2) trigger/ooo-reengage.ts:229 migrate createSequenceStep(id, step) → createSequenceSteps(id, campaignName, [step]); title from existing campaignName local var at line 212-214. (A3) src/lib/agents/campaign.ts:310 migrate createSequenceStep(ebCampaign.id, step) → createSequenceSteps(ebCampaign.id, campaign.name, [step]); title from campaign.name already in scope at line 298. (A4) After A2+A3 commit, grep createSequenceStep repo-wide; if clean, DELETE deprecated singular method at client.ts:250-291 including its JSDoc. (A5) backlog.json BL-074 status open→done + updatedAt bumped + append [CLOSED] note preserving existing [UPDATE 2026-04-16T09:45Z] prose. Gates: tsc clean, eslint no-restricted-imports = 0, vitest 822 pass / 37 pre-existing fail / 0 new fail. Constraints: no DB mutations, no canary touches (cmneqixpv/cmneqixvz), no EB 78 touch, no schema change, no trigger/campaign-deploy.ts retry change, no maintenance script stage. Single commit A with exact title `refactor(emailbison): migrate remaining callers off deprecated createSequenceStep + GET v1.1 (BL-074 follow-through / Phase 6.5b A)`.

[2026-04-16T10:24:19Z] -- Phase 6.5b Bundle D COMPLETE. Commit pending. Shipped: (D1) Helper `isNotFoundError(e: unknown): boolean` at src/lib/emailbison/errors.ts — `e instanceof EmailBisonApiError && (e.status === 404 || e.isRecordNotFound === true)` OR `e instanceof EmailBisonError && (e.code === 'CAMPAIGN_NOT_FOUND' || e.statusCode === 404)`. Plain boolean return (no type predicate — callers that need class-narrowed property access use their own `instanceof` in the branch). JSDoc block references BL-078 + Phase 6a-rollback QA F1 + EB async-DELETE queue docs. (D2) 1 tracked-source caller migrated: src/lib/channels/email-adapter.ts:228 Step 1 manual-delete guard (the canonical site from the BL-078 brief). Outcome: import narrowed from `EmailBisonApiError` → `isNotFoundError`, check collapsed from `err instanceof EmailBisonApiError && err.isRecordNotFound` → `isNotFoundError(err)`, now correctly catches EmailBisonError {CAMPAIGN_NOT_FOUND, 404} during EB async-DELETE windows. EmailBisonApiError import removed from email-adapter.ts (no other references survived). Internal EmailBisonClient 404→null sites (client.ts:601/993/1011) intentionally NOT migrated — circular-dep (errors.ts imports EmailBisonApiError FROM client.ts). Functional risk zero since these internal methods only ever see EmailBisonApiError. Admin/portal inbox action routes intentionally NOT migrated — catch uses EmailBisonApiError.{status,body} beyond the 404 branch; mechanical replacement would lose narrowed type. Flagged for later restructure. Maintenance script `scripts/maintenance/_phase-6a-rollback-email-canary.ts` NOT touched per hard rule (preserved untracked); BL-077 will import the helper when authored. (D3) 10 unit tests in src/lib/emailbison/__tests__/errors.test.ts: EmailBisonApiError status=404 (plain), status=404 + record_not_found body (isRecordNotFound=true), status=500, status=400; EmailBisonError CAMPAIGN_NOT_FOUND/404, UNEXPECTED_RESPONSE/200, other-code + statusCode=404-only; plain Error → false; null/undefined/string/plain objects with false-positive shape → false; symbols + numbers → false. (D4) Zero existing tests needed updates — mocks of `EmailBisonApiError` (class extends Error with isRecordNotFound=false) remain compatible: `instanceof` still holds, helper reads `status/isRecordNotFound` which are undefined/false → returns false. No existing test throws an `EmailBisonApiError` with status=404 in the Step 1 catch path, so behavior preserved. (D5) backlog.json BL-078 open→done, updatedAt 2026-04-16T10:25:00Z, [CLOSED] note appended documenting migration count + intentional non-migrations + gate results. Tests added: 10 (in new file). Gates: tsc clean, eslint no-restricted-imports=0, vitest 842 pass (832 baseline + 10 new) / 37 pre-existing fail / 1 todo / 0 new fail. Files changed: 4 (errors.ts new, errors.test.ts new, email-adapter.ts edit, backlog.json edit). Architecture.md appended with the cross-class narrowing-helper pattern for future reuse. NO canary touch, NO schema change, NO maintenance script stage, NO push, NO deploy. BL-078 closed.

[2026-04-16T10:20:26Z] -- STARTING Phase 6.5b Bundle D (BL-078, Tier 2 reversible, PM pre-authorized via handover). LOCAL COMMIT ONLY — no push, no deploy. Scope: (D1) Add shared helper `isNotFoundError(e: unknown): boolean` at src/lib/emailbison/errors.ts — returns true for EmailBisonApiError {status:404 OR isRecordNotFound:true} AND EmailBisonError {code:'CAMPAIGN_NOT_FOUND' OR statusCode:404}; false for non-Error values (null/undefined/strings/plain objects/numbers/symbols); instanceof-based narrowing, no `any`, boolean return (no type predicate). (D2) Migrate callers from single-class narrow → helper: src/lib/channels/email-adapter.ts:228 (Step 1 manual-delete guard — primary migration per BL-078 brief), src/lib/emailbison/client.ts:601/993/1011 (internal schedule + blacklisted-domain + blacklisted-email 404→null patterns — migrate for future-proofing against EB semantic-404). Admin/portal inbox action routes (src/app/api/{admin,portal}/inbox/email/actions/route.ts) NOT migrated — catch structure uses narrowed EmailBisonApiError.{status,body} for logging + HTTP response mapping beyond the 404 branch; mechanical replacement would lose narrowed type. Flagged in return summary. Maintenance script scripts/maintenance/_phase-6a-rollback-email-canary.ts:150-161/183-194/356 NOT touched — preservation rule for untracked scripts; BL-077 (future orphan cleanup) will use helper on next iteration. (D3) Unit tests at src/lib/emailbison/__tests__/errors.test.ts covering 10 required cases (both classes, hit/miss variants, non-Error inputs). (D4) Existing test mocks of EmailBisonApiError still valid — helper works on stubs that extend Error with matching property shape. No existing test updates expected. (D5) backlog.json BL-078 open→done + [CLOSED] note. Gates: tsc clean, eslint no-restricted-imports=0, vitest 835+ pass (832 baseline post-B + helper tests) / 37 pre-existing fail / 0 new fail. Constraints per brief: no canary (cmneqixpv/cmneqixvz), no EB 78 touch, no 15 Phase 6b campaigns, no trigger/campaign-deploy.ts, no schema, no maintenance stage, no push/deploy. Single commit D with exact title `refactor(emailbison): shared isNotFoundError helper covering both error classes (BL-078 / Phase 6.5b D)`.

[2026-04-16T14:00:00Z] -- COMPLETED Phase 6.5b Bundle C (BL-076, Tier 2 reversible, PM pre-authorized). Commit pending. Shipped: (C1) Retry semantics documented in code comments at deploy.ts:84-111 — Trigger.dev retry.maxAttempts=2 re-invokes executeDeploy(campaignId, deployId) with same payload; payload unchanged, DB state reflects Bundle B rollback effects. (C2) Root cause identified as Phase 6a dual-orphan = withRetry-induced duplicate create (email-adapter.ts:243 wrapped createCampaign in 3-attempt withRetry with 15s final delay — matches the 17s EB 78→79 gap exactly). Trigger.dev retry DID reuse correctly via preExistingEbId branch; the duplicate was PRE-Trigger.dev, at the client layer. (C3) Fix = Option α+γ combined: (i) removed withRetry wrapper from createCampaign in email-adapter.ts — non-idempotent POSTs should never auto-retry at the client layer; transient failures now propagate to Trigger.dev task-level retry which takes the idempotent reuse branch on re-entry; (ii) reordered Step 2 writes so CampaignDeploy.emailBisonCampaignId persists FIRST (retry anchor), Campaign second — closes the process-death-between-writes window; (iii) new executeDeploy entry logic detects Trigger.dev retry via CampaignDeploy.status='failed' + emailBisonCampaignId!=null, atomically restores Campaign state (status→deployed, ebId from CampaignDeploy anchor, deployedAt→now()) in a $transaction with CampaignDeploy 'failed'→'running' guarded updateMany (error+completedAt cleared); (iv) defensive guard throws on any other entry status (complete/running/failed-without-anchor) to prevent silent duplicate creates. (C4) Bundle B QA F2 (state-machine violation at deploy.ts:85-88 unguarded status='running' write): RESOLVED — entry write is now race-safe `updateMany({where:{id,status:'pending'}})` for first attempt and a $transaction with `updateMany({where:{id,status:'failed'}})` for retry. Stale fields cleared on retry transition. (C5) 4 new retry integration tests added to src/lib/campaigns/__tests__/deploy-rollback.test.ts (lines 448+ Bundle C describe block): retry-happy (prove 2 executeDeploy invocations with same deployId produce 1 createCampaign — the dual-orphan regression gate), retry-still-fails (final terminal state is cleanly rolled-back with 2 audit rows), defensive-guard-complete, defensive-guard-no-anchor. (C6) Bundle B tests updated: added `updateMany` to tx mock + `findUniqueOrThrow` implementation-once default in beforeEach (for the new entry read). No Bundle B assertions changed — same contract, same 6 cases still green. email-adapter-race.test.ts: updated 2 tests to acknowledge new Step 2 write order (CampaignDeploy-first = orphan ID written then corrected to winner; final persisted ID per-deployId is still the winner — the retry-anchor invariant). deploy-campaign.integration.test.ts case 5: added the new `findUniqueOrThrow` entry mock + `updateMany` to tx/prismaMock. Bundle B QA F1 CRITICAL (retry-after-rollback unretryable): RESOLVED — retry restore brings Campaign back to 'deployed' before status guard fires; if retry also fails, Bundle B's rollback re-fires and eventually terminal state is rolled-back with no zombie. Pattern worth recording: when a terminal rollback clears fields that a retry needs, anchor the retry-recovery on a SIBLING row (CampaignDeploy) that the rollback doesn't touch — gives the retry a durable state-restore path without coupling retry semantics to the rollback. Gates: tsc clean, eslint no-restricted-imports=0, vitest 846 pass / 37 pre-existing fail / 1 todo (baseline 842/37/1 Bundle D — gained 4 new retry tests, zero new failures, same 37 pre-existing). Files changed: 5 tracked (src/lib/campaigns/deploy.ts, src/lib/channels/email-adapter.ts, src/lib/campaigns/__tests__/deploy-rollback.test.ts, src/lib/channels/__tests__/email-adapter-race.test.ts, src/lib/campaigns/__tests__/deploy-campaign.integration.test.ts) + 3 memory/backlog. NO schema change, NO trigger/campaign-deploy.ts touch (retry config untouched), NO canary touches (cmneqixpv/cmneqixvz), NO EB 78 touch (BL-077 preserved), NO push, NO deploy. BL-076 closed.

[2026-04-16T13:45:00Z] -- STARTING Phase 6.5b Bundle C (BL-076 Trigger.dev retry preExistingEbId reuse, Tier 2 reversible, PM pre-authorized via handover 2026-04-15). LOCAL COMMIT ONLY — no push, no deploy, no Trigger.dev config change (retry.maxAttempts=2 stays). Root-cause analysis: Phase 6a dual-orphan (EB 78 created 08:00:53, EB 79 created 08:01:10, 17s apart) most likely NOT caused by Trigger.dev-retry-duplicating-create (the reuse branch would have fired on retry with preExistingEbId=78 persisted from first attempt's Step 2). Most plausible cause: `withRetry(() => ebClient.createCampaign(...))` at email-adapter.ts:243 wraps `createCampaign` in 3-attempt exponential backoff (delays 1s/5s/15s). If EB creates the campaign but the client doesn't see a 2xx (timeout, transient 5xx, network flap), withRetry retries, creating ANOTHER EB draft. The 15s delay between attempts 2 and 3 matches the 17s gap between EB 78 and EB 79. The second `createCampaign` call succeeds and returns EB 79; Step 2 persists 79; earlier 78 becomes orphan with no cleanup hook. Trigger.dev retry then ran (after Step 3 422) and correctly reused 79 via the preExistingEbId branch. Defensive fix: (C3 Option γ+α combined) persist emailBisonCampaignId onto CampaignDeploy AS EARLY AS POSSIBLE (right after createCampaign succeeds, BEFORE withRetry wraps any subsequent call), and (ii) teach the executeDeploy retry-entry path to restore Campaign.emailBisonCampaignId + Campaign.status from CampaignDeploy when Bundle B's rollback cleared them. This also closes Bundle B QA F1 (retry-after-rollback) and F2 (state-machine transition failed→running). New retry integration test proves single createCampaign call across two executeDeploy invocations with the same deployId. Gates: tsc clean, eslint no-restricted-imports=0, vitest 842+ baseline (Bundle D), 0 NEW fail. BL-076. Shipped: (A1) src/lib/emailbison/client.ts getSequenceSteps migrated from v1 `/campaigns/{id}/sequence-steps` to v1.1 `/campaigns/v1.1/{id}/sequence-steps` (docs line 1318) with Zod-at-boundary union schema accepting both {data:[...]} and bare-array shapes, UNEXPECTED_RESPONSE thrown on drift matching createSequenceSteps pattern (client.ts:381-402); defensive field-name fallbacks (order??position, email_subject??subject, email_body??body, wait_in_days??delay_days) preserved per brief — schema guards drift, fallbacks guard inconsistency. (A2) trigger/ooo-reengage.ts:228-242 createSequenceStep loop collapsed to single batched createSequenceSteps call, title=campaignName (local var line 212-214). (A3) src/lib/agents/campaign.ts:301-323 createSequenceStep loop collapsed to single batched call, title=campaign.name (already in scope line 298). (A4) Deprecated createSequenceStep + JSDoc block DELETED from client.ts after repo-wide `createSequenceStep` grep confirmed zero active callers (only docstring/comment refs in src/__tests__, src/lib/emailbison/__tests__, src/lib/channels/__tests__, src/lib/campaigns/__tests__, src/lib/agents, trigger, .monty, .planning, docs, scripts/maintenance — ALL comments). Legacy mock stubs + mockResolvedValue setup + not.toHaveBeenCalled assertion tripwires removed from email-adapter-deploy.test.ts + email-adapter-race.test.ts (tripwire rationale moot — method gone at type level). Integration test GET regex relaxed to `/\/campaigns\/(v1\.1\/)?{id}\/sequence-steps/` for defensive robustness. (A5) .monty/memory/backlog.json BL-074 status open→done, updatedAt 2026-04-16T09:48:09Z, [CLOSED 2026-04-16T09:48:09Z Phase 6.5b Bundle A] note appended preserving [UPDATE 2026-04-16T09:45Z] prose. Tests added: 3 new getSequenceSteps contract tests in src/lib/emailbison/__tests__/client-sequence-steps.test.ts — (i) URL hits v1.1 path + field-name normalization, (ii) bare-array response shape tolerated, (iii) UNEXPECTED_RESPONSE thrown on drift. Gates: tsc clean, eslint no-restricted-imports=0, vitest 825 pass / 37 pre-existing fail / 1 todo (baseline 822/37/1 — gained 3 new contract tests, zero new failures, same 37 pre-existing failures in discovery/promotion + emailbison-client.test.ts's 6 outdated tests that already reflected pre-Phase-6.5 URL expectations). 9 files changed, 319+/86-. NO canary touch, NO schema change, NO maintenance script stage, NO push, NO deploy. BL-074 closed across all call sites.

[2026-04-16T14:15:00Z] -- PHASE 6.5b COMPLETE. 4 commits landed locally on top of 17139b10 (Phase 6.5a head). Order per brief: A → B → D → C. COMMITS: (A) ad22e6db refactor(emailbison): migrate remaining callers off deprecated createSequenceStep + GET v1.1 — QA PASS. (B) be5e54fa fix(deploy): auto-rollback Campaign.status + ebId + deployedAt on terminal failure — QA BLOCK resolved by Bundle C (B+C interaction QA F1 critical + F2/F3 high resolved in C). (D) e1f7e253 refactor(emailbison): shared isNotFoundError helper — QA SHIP-WITH-FIXES (1 medium semantic-broadening at email-adapter.ts:228, non-blocking). (C) 3cdd5c4a fix(deploy): retry reuses preExistingEbId — QA SHIP-WITH-FIXES (2 HIGH findings filed as BL-079 + BL-080, 1 medium forensic correction filed as BL-081, non-blocking for 6.5c). FINAL GATES: tsc clean, eslint no-restricted-imports=0, vitest 846 pass / 37 pre-existing fail / 1 todo / 0 new fail (baseline 822/37/1 Phase 6.5a head → +24 tests net across 4 bundles). BACKLOG DELTA: BL-074 open→done (A), BL-075 open→done (B), BL-076 open→done (C), BL-078 open→done (D), +BL-079 (Bundle C F1 step-1 failure regression, HIGH), +BL-080 (Bundle C F2 retry-happy test gap, HIGH), +BL-081 (Bundle C F3 forensic math correction, LOW). BL-077 (EB 78 cleanup) stays open per brief. HARD-RULE COMPLIANCE VERIFIED: schema.prisma untouched, trigger/campaign-deploy.ts retry config untouched (maxAttempts=2 preserved), canary campaigns cmneqixpv/cmneqixvz have zero DB mutations in the diff (14 ID references all in .monty/memory prose + 1 test comment, none operational), EB 78 orphan untouched, 15 Phase 6b campaigns untouched, 65 untracked maintenance scripts preserved in working tree. NEXT: PM fires Phase 6.5c (push to origin/main + `npx vercel --prod`). Recommend PM reviews BL-079/BL-080 (both HIGH) before Phase 6b rollout — a Step-1-failure retry regression could fire under EB load during the 17-campaign launch; 6.5c push itself is SAFE because the coordinated B+C contract is coherent on the canonical retry path.

[2026-04-16T15:00:00Z] -- STARTING Phase 6a-retry CANARY (Tier 3, PM pre-authorized per handover 2026-04-15 §0 + Phase 6.5c deploy aayzlcex9 now live, both prod domains 200 OK). Scope: re-fire SAME 2 canaries against fixed pipeline — (1) email cmneqixpv0001p8710bov1fga (1210 Email Facilities/Cleaning, 44 leads, currently status=approved/ebId=null/deployedAt=null post-Phase 6a-rollback) and (2) LinkedIn cmneqixvz0003p871m8sw9u7o (1210 LinkedIn Facilities/Cleaning, 27 leads, never fired — untouched since rollback preflight). Fixes under test: BL-074 (createSequenceSteps v1.1 wire-shape + remaining callers), BL-075 (atomic Campaign.status/ebId/deployedAt rollback on terminal failure), BL-076 (Trigger.dev retry preExistingEbId reuse via CampaignDeploy anchor), BL-078 (shared isNotFoundError helper covering both error classes). Shipped commits 17139b10 + ad22e6db + be5e54fa + e1f7e253 + 3cdd5c4a (Phase 6.5c deploy aayzlcex9). Execution: 6a-r.1 preflight read-only per campaign (12 gates each); 6a-r.2 fire email canary + 15s poll of CampaignDeploy.status (10min cap) + EB verification; 6a-r.3 fire LinkedIn canary ONLY on 6a-r.2 success; 6a-r.4 EB-side verification (Campaign status auto-transition, emailBisonCampaignId populated, deployedAt populated, EB GET campaign status ∈ {queued,launching,active}, leads=44/27, sequence steps match, schedule set); 6a-r.5 orphan check (exactly 1 EB campaign per canary — validates BL-076 retry-reuse); 6a-r.6 per-canary report with verdict. Hard rules: ONLY 2 canary IDs (no touch on other 15 Phase 6b campaigns, no touch on EB 78 orphan preserved for BL-077); STOP on first failure (email fail → NO LinkedIn fire); if Trigger.dev >10min → STOP + alert PM (potential BL-079 manifesting); NO push, NO schema migration, NO mid-flight fixes. Delegation: monty-dev for execution + polling + EB verification, monty-qa adversarial review focused on idempotency-actually-fired/compensating-tx-did-not-fire-spuriously/EB-DB parity/orphan-count-validation. Checkpoint to PM after canary regardless of outcome; do NOT cascade to Phase 6b.

[2026-04-16T14:30:00Z] -- Phase 6a-retry START: PM-authorized Tier 3 canary re-fire against Phase 6.5c-deployed pipeline (aayzlcex9 live on prod). Validates BL-074 (v1.1 sequence-steps payload), BL-075 (atomic rollback), BL-076 (retry reuses preExistingEbId, no dual-create), BL-078 (shared isNotFoundError). Canaries: email cmneqixpv (44 leads, 1210 Email Facilities/Cleaning), LinkedIn cmneqixvz (27 leads, 1210 LinkedIn Facilities/Cleaning). Flow: preflight -> fire email -> poll 10min -> EB verify -> fire LinkedIn -> poll -> EB verify -> orphan check. STOP on first failure. Do NOT cascade to 6b. Do NOT touch EB 78 (BL-077 deferred).

[2026-04-16T14:32:00Z] -- Phase 6a-retry HALT AT PREFLIGHT: Email canary cmneqixpv is GO (all 12 gates PASS: 44 leads, 3 email steps, 59/58 healthy email senders). LinkedIn canary cmneqixvz BLOCKED at sender-health gate: sole LinkedIn-capable sender cmmxon6vi (Daniel / 1210-solutions, linkedinEmail=daniel@1210solutions.co.uk, channel=both) has status=active + sessionStatus=active but healthStatus="session_expired" (healthFlaggedAt=null, lastKeepaliveAt=2026-04-16T09:46:58Z ~4.75h stale, lastPolledAt=2026-04-16T10:56:51Z ~3.5h stale). Per spec hard rule "If ANY gate fails for EITHER canary: STOP immediately", NEITHER canary fired. Tier 3 fire NOT executed. EB 78 not touched. No new orphans created. Returning to orchestrator — needs PM decision on (a) refresh session / re-login sender before retry, (b) fire email-only and defer LinkedIn, or (c) abort Phase 6a-retry and investigate BL-XXX session-expired root cause.

[2026-04-16T15:20:00Z] -- PHASE 6a-retry HALTED AT PREFLIGHT. No Tier 3 fire occurred. Email canary cmneqixpv 12/12 gates PASS (44 leads, 3 steps, 59 active/58 healthy email senders). LinkedIn canary cmneqixvz 11/12 gates PASS; gate-12 (sender health) FAIL — sole LinkedIn-capable sender cmmxon6vi0001p85cr1g1l124 (Daniel, daniel@1210solutions.co.uk, channel='both') has sessionStatus='active' + healthStatus='session_expired' (contradictory state). lastKeepaliveAt ~4h45m stale, lastPolledAt ~3h35m stale, pendingConnectionCount=5, acceptanceRate=0. Per brief's hard rule "If ANY gate fails for EITHER canary: STOP immediately", dev correctly halted BOTH fires (did not narrow to email-only). QA 5 findings written to .monty/memory/incidents.md [2026-04-16T15:15:00Z]: F1 INFO halt-was-correct-by-spec; F2 HIGH real data-integrity bug (worker/src/keepalive.ts:146 → updateSenderHealth handler at src/app/api/linkedin/senders/[id]/health/route.ts:39-47 writes Sender.healthStatus ONLY, does not co-write sessionStatus — while session-refresh.ts:58-64 and extension/expiry/route.ts:47-64 DO co-write both fields correctly); F3 INFO option-2-email-only leaves BL-068 LinkedIn path unexercised (prisma.campaignSequenceRule.createMany in src/lib/channels/linkedin-adapter.ts); F4 MEDIUM option-1 false-positive trap (src/app/api/senders/[id]/reactivate/route.ts flips healthStatus='healthy' without refreshing sessionData — INVERSE of F2 bug, would pass gate-12 with dead cookie); F5 INFO other adversarial concerns (no stale Next.js cache risk, no in-flight Trigger.dev runs, no EB 78 reuse risk, gate-8 asserts people>0 not exact 44/27 so spot-check worth doing). Backlog delta: +BL-082 (keepalive sessionStatus co-write fix, medium/P3). BL-077 (EB 78 orphan cleanup) unchanged. BL-079/080 (Bundle C gaps — Step-1 failure retry dead-end + retry-happy test is mock-theater) unchanged, still open. Orphan check 0 new orphans (no fire occurred). EB 78 untouched. 15 Phase 6b campaigns untouched. NO push, NO schema migration, NO vercel deploy, NO trigger.dev deploy. Three PM options documented (ranked): (1) extension re-auth Daniel then re-run preflight — must verify via sessionConnectedAt bumped today, NOT admin-reactivate, to avoid F4 trap; (2) fire email-only now — requires PM to narrow Phase 6b.1 to email campaigns AND gate LinkedIn canary separately to close BL-068 coverage; (3) abort + fix BL-082 first then retry. QA ranking: option 3 safest, option 1 acceptable with F4 verification step, option 2 acceptable only with Phase 6b.1 email-only scope narrowing. PAUSE for PM decision.

[2026-04-16T16:00:00Z] -- STARTING skipResume stage-deploy option (Tier 2, PM pre-authorized). Adds `opts?: { skipResume?: boolean }` thread-through on executeDeploy → EmailAdapter.deploy + LinkedInAdapter.deploy. When skipResume=true: run Steps 1-8 (createCampaign/reuse, persist ebId, upsert sequence steps, attach leads, upsert schedule, attach senders, updateCampaign settings, tags no-op), SKIP Step 9 resumeCampaign + Step 10 verifyStatus, mark CampaignDeploy.emailStatus='complete' with emailError='STAGED — resume pending PM review via EB UI or manual resumeCampaign call'. Outer deploy.ts post-finalize auto-transition Campaign.status deployed→active is ALSO gated by skipResume — stays at 'deployed'. BL-075 terminal-failure rollback semantics unchanged on Steps 1-8 failure path. Files: src/lib/campaigns/deploy.ts, src/lib/channels/email-adapter.ts, src/lib/channels/linkedin-adapter.ts (symmetric, not exercised today), new src/lib/campaigns/__tests__/deploy-skip-resume.test.ts. Then one-off script scripts/maintenance/_canary-stage-deploy.ts to stage cmneqixpv0001p8710bov1fga (1210 Email Facilities/Cleaning canary) bypassing Trigger.dev — hardcoded campaign ID guard, atomic approved→deployed preflight, direct executeDeploy call, single JSON report block. NO commit, NO push, NO Trigger.dev deploy.

[2026-04-16T16:35:00Z] -- COMPLETED skipResume code+tests (Tier 2). Gates: tsc clean ✓, vitest 848 pass / 37 fail (same pre-existing 37 failures — promotion-icp-gate, api-routes, emailbison-client, resend-notifications, linkedin-sender, discovery; 0 new failures) / 1 todo — gained 2 new skipResume tests + 846 baseline = 848 ✓. Files shipped (uncommitted): src/lib/campaigns/deploy.ts (executeDeploy sig extended w/ optional opts?:ExecuteDeployOptions; thread skipResume to adapter; gate deployed→active auto-transition), src/lib/channels/types.ts (DeployParams.skipResume?:boolean added), src/lib/channels/email-adapter.ts (skipResume branch before Step 9 — writes emailStatus='complete' + emailError='STAGED —...' and returns; Steps 1-8 unchanged including BL-075 outer rollback contract), src/lib/channels/linkedin-adapter.ts (symmetric honor — log-only given LinkedIn pull model has no launch analog), src/lib/campaigns/__tests__/deploy-skip-resume.test.ts (2 cases: skipResume=true path + default-no-opts path). Script: scripts/maintenance/_canary-stage-deploy.ts (hardcoded ALLOWED_CAMPAIGN_ID cmneqixpv, refuses argv mismatch, atomic approved→deployed preflight, executeDeploy with skipResume:true, EB verification via shared EmailBisonClient + single JSON report). EXECUTION OUTCOME: script ran but deploy FAILED at Step 2 due to pre-existing BL-068 shape-drift on Campaign.emailSequence — stored JSON steps are missing required `position` field (Zod threw 3 invalid_type errors on positions [0,1,2]). Guard WORKED AS DESIGNED — fail-loud at adapter boundary before any EB state mutation (per Phase 2 BL-068 discipline). BL-075 auto-rollback fired correctly: Campaign.status=approved ✓, emailBisonCampaignId=null ✓, deployedAt=null ✓, CampaignDeploy.status=failed with [step:2] error ✓. HOWEVER, EB campaign 80 was created fresh BEFORE the Step 2 shape-validation check and is now orphaned (same pattern as EB 78 from Phase 6a → BL-077 unresolved). Code-path note: Step 1 createCampaign runs BEFORE the emailSequence validation happens (validation is late in Step 2 after the persist). This is pre-existing ordering, not a regression from this change — BL-074 work already restructured Step 2. New orphan EB 80 should be appended to BL-077 for cleanup. PM needs to fix the emailSequence storage shape (writer/portal path writes steps without `position` field; likely needs a one-off backfill normalizing stepNumber→position on 1210 Email Facilities/Cleaning canary before re-attempting stage-deploy). skipResume code path itself was NOT exercised on the live canary (adapter threw before reaching the skipResume branch at Step 9) — the skipResume unit tests cover the branch contract directly.

## 2026-04-16 — Compound remediation dispatch (1210 data repair + F1 + orphans + canary retry)

**PM brief:** compound 5-part remediation for canary stage-only deploy blocker. All work pre-authorized by PM (Tier 3 DB mutation on 5 Campaign.emailSequence rows + EB deleteCampaign on orphans 78+80).

**Scope:**
- Commit 0: feat(deploy): skipResume option + stage-only script (uncommitted feat from prior task)
- Commit A: fix(deploy): gate notifyCampaignLive behind skipResume (QA F1)
- Commit B: fix(campaign): repair 5 1210 emailSequence rows (LinkedIn-shaped → canonical StoredEmailSequenceStepSchema)
- Commit C: chore(backlog): file BL-084 (writer audit — bounded, no writer fix this phase)
- Commit D: chore(bl-077): cleanup EB 78 + EB 80 + close BL-077
- Run E: `npx tsx scripts/maintenance/_canary-stage-deploy.ts` → canary cmneqixpv lands status=deployed, EB draft, NOT active

**Classification:** bug fix + data remediation + housekeeping. Blocking Lime+1210 launch unblock. Critical.

**Hard rules:** preserve canary rolled-back state, don't touch Lime 6 or LinkedIn canary cmneqixvz or non-canary 15. No push/vercel/migration. Stop + escalate if data repair fails for any of the 5.

**Delegation:** monty-dev for execution (sequential commits + script run). monty-qa adversarial review after.

[2026-04-16T17:00:00Z] -- STARTING BL-085 fix (Tier 2 code + Tier 3 canary retry + EB delete, PM pre-authorized via /monty brief referencing handover-2026-04-15-deploy-rebuild.md). Symptom: Phase 6a canary stage retry died at Step 3 (UPSERT_SEQUENCE_STEPS) with EB v1.1 422 "email_subject required" — adapter passes empty string on follow-up (step 2) steps whose subjectLine is null/empty per Outsignal threading convention (feedback_email_threading_subject.md: "email 2 always empty subject so it threads under email 1"). Data is canonical post-Phase 6.5c; adapter is the gap. Affects all 11 email campaigns (Lime 6 + 1210 5). BL-075 auto-rollback confirmed fired cleanly on the retry; canary cmneqixpv now at status=approved, ebId=null. Orphan EB 81 (1210) to delete inline. Scope 9 steps: (1) read docs/emailbison-dedi-api-reference.md for v1.1 sequence_steps threading flag (inherit_subject / reply_in_thread / thread) — pick Option 1 (flag-based) if exists, else Option 2 (Re:{firstSubject} fallback). (2) audit 11 email campaigns' emailSequence JSON for empty-subject step distribution. (3) fix src/lib/channels/email-adapter.ts Step 3 batch build — apply chosen fix per-step + handle subjectVariantB. (4) extend src/lib/emailbison/__tests__/client-sequence-steps.test.ts or add src/lib/channels/__tests__/email-adapter-reply-thread.test.ts with 4 cases (happy/edge/regression/real-canary-fixture). (5) gates: tsc clean, eslint no-restricted-imports=0, vitest baseline 848 preserved. (6) single commit. (7) npx tsx scripts/maintenance/_canary-stage-deploy.ts — expect Steps 1-8 clean, Campaign.status=deployed, emailBisonCampaignId=new EB id, EB draft with 44 leads + 3 sequence_steps + senders + Mon-Fri 09-17 UK schedule. (8) client.deleteCampaign(81) via 1210 apiToken + verify EB workspace has exactly 1 draft (the new canary). (9) close BL-085 in backlog. Hard rules: preserve canary cmneqixpv state during investigation; do NOT touch Lime's 6 emailSequence JSON (canonical); do NOT touch LinkedIn canary cmneqixvz or Daniel's sender; NO push/vercel/Trigger.dev deploy/schema migration; STOP + escalate if docs ambiguous; STOP after stage retry (no resume). Delegating monty-dev for all 9 steps; monty-qa adversarial review after focused on (a) EB v1.1 semantics, (b) all 11 campaigns now deploy clean, (c) no regression on non-threading paths, (d) tests use real data not synthetic.

[2026-04-16T13:50:00Z] -- BL-085 cascade fixes A/B/D/E/F executed. Fix A (src/lib/emailbison/client.ts createSequenceSteps): replaced throw-on-Zod-mismatch with tolerant parse — on HTTP 200 w/ shape drift, log descriptive console.warn (Zod error + first 500 chars raw body, handles res=undefined empty-body case) and return []. POST response body NOT consumed by email-adapter.ts (Step 3 idempotency reads getSequenceSteps GET), so returning [] is safe. 4xx/5xx still throw. Fix B (src/lib/channels/email-adapter.ts L475): removed withRetry wrap around createSequenceSteps — POST is non-idempotent (EB appends every batch), any client-side throw (Zod, transient) was retrying-and-duplicating. Transient failures now route through Trigger.dev task retry -> reuse path (preExistingEbId set by Step 2) -> GET-then-diff finds inserted positions and short-circuits. Audit of remaining withRetry wraps: all 13 other sites reviewed, none need removal (getCampaign/getSequenceSteps/getSchedule are GET idempotent; createLead EB-dedupes by email; attachLeadsToCampaign/attachSenderEmails EB-dedupe; updateSchedule/updateCampaign/resumeCampaign are PATCH/PUT idempotent; createSchedule is 1:1-per-campaign singleton URL so a duplicate POST returns non-retryable 422/409 with no amplifier risk). Fix D: added 7 new test cases in client-sequence-steps.test.ts covering the tolerant parse path — null data, success envelope, empty object, empty string, v1.1-likely {data:{}, success:true}, array-with-missing-id, regression guard — all assert single fetch call, no throw, returns [], warn emitted with BL-085 signature. 14/14 tests pass, tsc clean, 80/80 channels tests pass. Fix E: commit df0ed71d2b24b28517cbed0c09b0f05503e0e631, body documents both bugs + audit matrix + BL-076/BL-085 references, Co-Authored-By present. Fix F stage-deploy: Step 3 SUCCEEDED on a single POST — EB 83 has EXACTLY 3 sequence steps (raw probe confirmed via scripts/maintenance/_bl085_probe_eb83_raw.ts), validating Fix A+B neutralized the amplifier. But deploy failed at Step 5 createSchedule with 422 "The save as template field is required" — NEW bug discovered, not part of BL-085 cascade. EB v1.1 createSchedule now requires save_as_template in POST body (the fresh-deploy path sends DEFAULT_SCHEDULE which omits it; the re-run path already sets it explicitly at L606). Also discovered: EB v1.1 getSequenceSteps GET response is ACTUALLY {data: {sequence_id, sequence_steps: [...]}} — nested object containing the array, NOT {data: [...]} flat. Current client Zod for getSequenceSteps rejects this shape (caught during inspect script). Need future work: (1) fix DEFAULT_SCHEDULE / adapter createSchedule call to always include save_as_template:false, (2) update getSequenceSteps Zod schema to handle the nested {data: {sequence_steps}} shape or make it tolerant similarly. Orphan cleanup: EB 82 deleted + verified via isNotFoundError/BL-078 helper after ~2s async delete window; EB 81 (pre-BL-085 422-failure orphan from cmo1hnrmu) also deleted since it had no live Campaign pointer; EB 83 (Fix F Step 5 failure orphan) deleted immediately per hard rules. Final 1210-solutions state: 0 EB campaigns, canary cmneqixpv rolled back to approved/ebId=null (BL-075 worked), zero lingering orphans.

[2026-04-16T18:35:00Z] -- BL-085 cascade QA review complete. monty-qa adversarial pass on commit df0ed71d + Fix F run surfaced 7 findings (2 HIGH, 3 MEDIUM, 2 LOW) written to incidents.md lines 235-251. Verified: tsc clean, 14/14 client-sequence-steps tests pass, 80/80 channels tests pass, no dead code from withRetry removal, warn log is discoverable (console.warn is the codebase convention — no pino/winston/bunyan present). Significant findings: (F1 HIGH) Dev's audit matrix reasoning for KEEPing withRetry on createSchedule/createLead/attachLeadsToCampaign/attachSenderEmails is FLAWED — outer withRetry (src/lib/utils/retry.ts:11-29) catches ALL thrown errors regardless of status, so non-retryable statuses like 422 still waste ~21s on 3 retries. For non-idempotent POSTs this is the same amplifier class BL-085 Fix B just removed for Step 3. Particularly urgent because BL-087 (newly-discovered Step 5 422) will burn 21s per deploy through the withRetry wrap. (F2 HIGH) createLead dedupe claim ("EB dedupes by email") contradicts EB docs line 1458 where createLead email "Must be unique" — implies 422 reject, not silent dedupe. attachLeadsToCampaign dedupe claim is undocumented. Both covered by BL-086. (F3 MED) Fresh-deploy path has no post-POST verification — if EB returns 200+garbage-shape AND failed to persist, silent [] from tolerant parse lets deploy proceed to Step 4 with zero steps. Suggested mitigation: post-POST getSequenceSteps GET verification at email-adapter.ts:497. (F4 MED) client.ts:452-454 warn prefix lacks "BL-085:" tag while email-adapter.ts:445-447 includes it — grep inconsistency for ops. Cheap to fix. (F5 MED) Regression test uses fabricated v1.1 response shape because the real canary body was never captured pre-Fix (probe at _bl085-probe-eb82.ts:36-38 explicitly skipped mutation). Next canary attempt should capture real body and pin the test. (F6-F7 LOW) rawPreview three-branch logic could simplify; adjacent Zod-fail behavior in getSequenceSteps vs createSequenceSteps is opposite (strict vs tolerant) with no banner comment documenting the contrast. Actions: (a) updated BL-085 in backlog with cascade fix summary + in_progress status (blocked by BL-087). (b) Filed BL-086 HIGH priority to address withRetry audit per F1/F2 — options are (i) remove withRetry from each non-idempotent POST following BL-076/BL-085 pattern or (ii) make withRetry status-aware to mirror EmailBisonClient RETRYABLE_STATUSES; preferred (ii) + empirical dedupe probe for createLead/attachLeadsToCampaign/attachSenderEmails. (c) Filed BL-087 CRITICAL to fix Step 5 createSchedule 422 save_as_template required — add field to DEFAULT_SCHEDULE at email-adapter.ts:122-133 + wire-format test. (d) Noted latent getSequenceSteps GET response shape drift (actual v1.1 shape is nested {data: {sequence_id, sequence_steps: [...]}} not flat {data: [...]}) under BL-087 notes — only hits on reuse path which Fix F did not exercise; file separately if/when a reuse-path deploy is attempted. STOPPED per PM hard rule ("If createSequenceSteps retry STILL creates duplicates after Fix B, STOP and escalate — do not try a third fix mid-session"): Fix B demonstrably neutralized the Step 3 amplifier (single POST, 3 clean steps, warn fired), so the hard-rule trigger did NOT activate, but Fix F surfaced two distinct NEW failure modes (Step 5 schedule contract drift, reuse-path GET schema drift) — these are escalated to PM via BL-086/BL-087 rather than patched mid-session. Session state preserved: 1210-solutions EB workspace clean (0 campaigns), canary cmneqixpv rolled back to approved/ebId=null, Lime 6 + LinkedIn canary cmneqixvz untouched.

[2026-04-16T19:00:00Z] -- Starting BL-086 + BL-087 combined fix, then canary stage-deploy retry. Plan: (Fix 1) BL-087 — add save_as_template:false to DEFAULT_SCHEDULE in src/lib/channels/email-adapter.ts; remove the redundant inline override at L606 (updateSchedule path) and let the constant supply it. EB docs (lines 152-169) mark save_as_template OPTIONAL on POST and REQUIRED on PUT; live API has flipped POST to required (422 surfaced on EB 83), so we send `false` on both paths. No other fields appear missing per docs. CreateScheduleParams type (src/lib/emailbison/types.ts:190) keeps `save_as_template?: boolean`. (Fix 2) BL-086 — make withRetry status-aware: rethrow EmailBisonApiError on status NOT in {429,500,502,503,504}; rethrow EmailBisonError (Zod/unexpected-shape); retry TypeError (network); retry unknown errors fall through to NO retry (safe default per spec). Factor predicate into isRetryableError helper. (Fix 3) New tests src/lib/utils/__tests__/retry-status-aware.test.ts covering 8 scenarios. (Fix 4) Single commit BL-086+BL-087. (Fix 5) Run scripts/maintenance/_canary-stage-deploy.ts. (Fix 6) Verify 1210-solutions EB clean. (Fix 7) Update backlog. Tier 2 throughout.

[2026-04-16T19:30:00Z] -- BL-086 + BL-087 LANDED commit ca2fe6a3. Stage-deploy Run G result: Steps 1-3 clean, Step 4 NEW failure (BL-088 createLead 422 'email already taken' — workspace-scoped EB lead store retains leads across campaign deletions). BL-086 amplifier neutralization VERIFIED LIVE: Step 4 made a SINGLE POST attempt (no 21s loop, no duplicate side-effects), auto-rollback (BL-075) fired correctly, canary cmneqixpv restored to status=approved + ebId=null. EB 84 orphan deleted via _bl087-cleanup-eb84.ts; 1210-solutions EB workspace clean (0 campaigns). Backlog: BL-086 + BL-087 -> done, BL-085 stays in_progress (blocked by BL-088), BL-088 filed open/critical/P1. Full session report returned to PM.

[2026-04-16T21:30:00Z] -- STARTING pre-flight shape scan + EB orphan sweep (Tier 1 read-only + Tier 2 new scripts). Creating scripts/maintenance/_bl_preflight_shape_scan.ts and scripts/maintenance/_bl_preflight_orphan_sweep.ts as sibling of _email-shape-scan.ts. Purpose: before canary cmneqixpv green-lights the other 10 rolled-back campaigns, verify (a) their emailSequence JSON matches StoredEmailSequenceStepSchema (src/lib/channels/email-adapter.ts:101-113) — no LinkedIn-key drift (stepNumber/subjectB/messageB), position present; (b) each EB-connected workspace has no orphan campaigns (EB-side campaigns not referenced by any Campaign.emailBisonCampaignId in DB). Uses prisma for DB reads + EmailBisonClient.getCampaigns() auto-pagination per api-client-rules.md. Workspace list enumerated from DB (Workspace where apiToken not null) per live-data-rules.md. Outputs to /tmp/eb-preflight-{shape,orphans}-2026-04-16.json + markdown report. READ-ONLY — no mutations. Canary cmneqixpv EB state NOT queried individually; DB row emailSequence IS read (read-only, non-mutating) but its shape result is excluded from recommendations. Commit 1f6e2eaf context noted.

## 2026-04-16 — BL-088 dispatch (createLead idempotency via EB upsert endpoint) + BL-089/BL-090 if-time

**PM brief:** fix the last canary blocker. createLead per-lead POST hits 422 'email already taken' on run G (Step 4) because EB's workspace lead store persists leads across campaign deletions. Prior-run leads (44 from run F 2026-04-16T13:42) block any subsequent canary on the same TargetList. BL-086 amplifier neutralization is verified (single POST, immediate surface, clean rollback), so the cost is purely the 422 itself.

**Scope this session:**
- BL-088 (critical, P1): switch createLead path to EB upsert `/api/leads/create-or-update/multiple` (docs line 1595). Bulk-friendly (500/request cap), supports `existing_lead_behavior='patch'` to preserve existing data and update our fields. Per-lead loop at email-adapter.ts:538-573 becomes ONE batch call.
- BL-089 (P3, if-time): export `RETRYABLE_STATUSES` from `src/lib/emailbison/client.ts` (or extract to `src/lib/emailbison/retry-statuses.ts`), import into `src/lib/utils/retry.ts`, add vitest assertion that both sets are equal. Kills the sync-by-comment discipline.
- BL-090 (P3, if-time): flip `CreateScheduleParams.save_as_template?: boolean` to required (no `?`) in `src/lib/emailbison/types.ts:201`. Cite the BL-087 canary 422 in a short comment.
- BL-091 (P3, DEFER): integration test with real withRetry — not this session.

**Implementation notes for monty-dev:**
1. Read `docs/emailbison-dedi-api-reference.md` around lines 1458 (current createLead) and 1595 (upsert endpoint) to confirm request/response shapes. Tolerant Zod parse per BL-085 lesson — accept mild response drift (warn + return best-effort), throw on HTTP non-2xx.
2. In `src/lib/emailbison/client.ts`: add `createOrUpdateLeadsMultiple()` method hitting the upsert endpoint. Expected request body: `{ leads: Array<{email, first_name?, last_name?, ...}>, existing_lead_behavior: 'patch' }`. Expected response: array of `{id, email, ...}` per lead (shape TBD from docs — use tolerant Zod + warn-on-drift).
3. KEEP `createLead()` method to avoid breaking non-canary callers. Add new public method and switch email-adapter to it. Dead-code cleanup is a follow-up.
4. In `src/lib/channels/email-adapter.ts`: replace per-lead loop at L538-573 with a single batch call to the new upsert method. Capture returned IDs the same way `createdLeadIds.push` currently does. Update the L521 comment — remove the wrong idempotency claim and reference the new endpoint.
5. Tests: (a) unit test `createOrUpdateLeadsMultiple` happy path + 422 surface + tolerant parse; (b) email-adapter Step 4 test mocking the batch call, asserting single invocation with correct body shape, verifying IDs land in createdLeadIds; (c) regression test for the 422-duplicate scenario — previously-existing lead email returns successfully now, not a 422. This change should add >=3 new passing tests, zero new baseline-failures (baseline: 848 pass / 37 pre-existing fail / 1 todo).
6. BL-089: export `RETRYABLE_STATUSES` as top-level const. Update retry.ts to import. Add equality assertion test.
7. BL-090: flip `save_as_template` to required in `CreateScheduleParams`. Verify no callers break (should be clean since DEFAULT_SCHEDULE supplies it).
8. Commit message: `fix(emailbison): idempotent createLead via upsert endpoint (BL-088) + retry-status sync (BL-089) + save_as_template required type (BL-090)`. Include BL-086/BL-087/BL-085 cross-references. Split BL-088 from BL-089+BL-090 if BL-088 stands alone cleanly.
9. `git push` to origin main.
10. Run `npx tsx scripts/maintenance/_canary-stage-deploy.ts`. Expected: Steps 1-4 clean (no 422 on duplicate emails), land on Step 5+ or Step 9 complete (skipResume branch). If a new EB draft is created, note its ID for post-session cleanup (PM will authorize the delete via the existing BL-078 helper pattern).

**Hard rules:**
- Canary cmneqixpv state pre-run: status=approved, ebId=null. MUST preserve if the run fails — BL-075 auto-rollback must fire.
- Lime 6 email campaigns, LinkedIn canary cmneqixvz, Daniel's sender — DO NOT TOUCH.
- NO `vercel deploy` / NO `trigger.dev deploy` / NO schema migration / NO `.env` changes.
- Canary MUST stay stage-only (skipResume=true). Script already enforces this. MUST NOT go live.
- If upsert endpoint behaves unexpectedly (422 on different field, schema drift we didn't anticipate), STOP and escalate.
- If BL-089/BL-090 break tsc or tests, SKIP them and land BL-088 alone.

[2026-04-16T22:30:00Z] -- STARTING BL-093 cascade (4 production-blocking bugs from PM EB 85 review). Tier 2 code-only, Tier 3 canary stage-deploy retry, EB delete EB 85 + DB rollback before retry. Bug 1: replace `Re: <X>` follow-up subject workaround (commit 496d2d9c) with EB `thread_reply` boolean on sequence_steps payload — step 2 (empty subject, threading) gets `thread_reply=true` + `email_subject=''`, step 3 (own subject) and step 1 get `thread_reply=false`. Add field to client createSequenceSteps payload + remove email-adapter Re: fallback. Bug 2: build `transformVariablesForEB(text)` mapping our `{FIRSTNAME}` → EB `{{first_name}}` etc., apply to subject + body in client createSequenceSteps before POST. Audit confirms writers emit single-curly UPPERCASE per copy-quality.ts BANNED_PATTERNS line 29. Variable map: `{FIRSTNAME}`→`{{first_name}}`, `{LASTNAME}`→`{{last_name}}`, `{COMPANYNAME}`→`{{company}}`, `{JOBTITLE}`→`{{title}}`, `{LOCATION}`→`{{notes}}` (defensive — there's no documented EB lead.location field; map to notes which holds free-form text), `{LASTEMAILMONTH}`→passthrough as `{{lastemailmonth}}` (likely a custom variable). Sender refs `{SENDER_FIRST_NAME}`/`{SENDER_FULL_NAME}` are EB-native — pass through unchanged. Idempotent: running transform twice yields same output. Bug 3: flip DEFAULT_CAMPAIGN_SETTINGS.can_unsubscribe from true→false in email-adapter.ts. Bug 4: NO per-campaign sender allocation field exists in prisma/schema.prisma (verified — neither Campaign nor Sender has any allocation field; `Sender.removedFromCampaignIds` exists but is a soft-flag for health, not allocation). Pragmatic fix for canary: hardcoded allocation map keyed by campaignId in email-adapter.ts Step 6, scoped to the 5 1210 campaigns. cmneqixpv (Facilities/Cleaning) gets 12 senders (subset of 58). File BL-094 for proper schema-based allocation. Pre-fix: clean up EB 85 + revert canary DB to status=approved/ebId=null/deployedAt=null via maintenance script. Then run _canary-stage-deploy.ts. Validate per-bug. Tier 2 throughout — NO commit, NO push, NO migration. PM routes to monty-qa after.

[2026-04-16T23:30:00Z] -- BL-093 cascade COMPLETE. Final canary EB 88 verified all 4 fixes. Three runs total: (1) EB 85 cleanup + revert via _bl093-prefix-revert.ts; (2) Run 1 EB 86 — Step 3 422 "email_subject required" because hypothesis "thread_reply alone allows empty subject" was WRONG; (3) Run 2 EB 87 — Step 3 200 OK but stored subject was "Re: Re: cleaners across multiple sites" (double-Re) because EB AUTO-PREPENDS "Re: " server-side when thread_reply=true, AND we were also adding our own "Re: " prefix; (4) Run 3 EB 88 — adapter now sends RAW firstStepSubject (no Re: prefix) when thread_reply=true, EB auto-prepends to render "Re: cleaners across multiple sites" (single Re:), matching live Lime production convention (campaigns 26/31/32/42/43/44/45 all do this). Final EB 88 verified: 3 sequence_steps with correct thread_reply pattern (step 1 false, step 2 true, step 3 false), email_body shows transformed `{{first_name}}` (was `{FIRSTNAME}` in source), can_unsubscribe=false, 11 senders attached (matching CAMPAIGN_SENDER_ALLOCATION map for cmneqixpv exactly: 635,640,645,650,655,660,667,672,677,682,687), 44 leads upserted, 0 tags, status=draft, status=deployed on Outsignal Campaign side, emailError="STAGED — resume pending PM review". GAP found: Workspace.apiToken-scoped EB lead store retains leads across campaign deletions (already covered by BL-088 createOrUpdateLeadsMultiple from prior session — verified working today). New BL-094 needed: proper Campaign.allocatedSenderIds schema field + UI + migration to backfill the 5 1210 campaigns from the hardcoded map. New BL-095 needed: writer-output normalization for {LOCATION} variable — there's no documented EB lead.location field so we map to {{notes}} as a defensive choice; needs proper UX decision (custom variable? location stored on Lead.notes?). Latent: getSequenceSteps GET response shape drift {data: {sequence_id, sequence_steps: [...]}} vs {data: [...]} — same as BL-085 cascade observation; only hits the canary report's verification side, doesn't break deploy; flag as BL-096. Files touched (NOT committed): src/lib/emailbison/types.ts (CreateSequenceStepParams + thread_reply), src/lib/emailbison/client.ts (import variable-transform, apply to email_subject + email_body, emit thread_reply), src/lib/emailbison/variable-transform.ts (NEW), src/lib/channels/email-adapter.ts (Step 3 thread_reply logic + DEFAULT_CAMPAIGN_SETTINGS can_unsubscribe=false + Step 6 resolveAllocatedSenders + CAMPAIGN_SENDER_ALLOCATION map). Tests touched: src/lib/emailbison/__tests__/variable-transform.test.ts (NEW, 20 cases), src/lib/emailbison/__tests__/client-sequence-steps.test.ts (added BL-093 + thread_reply expectation in happy path), src/lib/channels/__tests__/email-adapter-reply-thread.test.ts (rewrote 4 cases for new thread_reply semantics), src/lib/channels/__tests__/email-adapter-allocation.test.ts (NEW, 6 cases), src/__tests__/email-adapter-deploy.test.ts (added orderBy + thread_reply + can_unsubscribe expectations). Maintenance scripts: scripts/maintenance/_bl093-prefix-revert.ts (NEW). Gates: tsc clean ✓, 931 tests pass / 37 fail (37 = pre-existing baseline, 0 new failures) / 1 todo. Memory rules honoured: no hyphens used as separators in code/docs, no links/images cold (can_unsubscribe disabled), inboxes terminology preserved (kept "senders" since that's the existing schema convention but allocation logic respects the per-domain Azure/Google split via stable emailBisonSenderId-asc ordering), Azure/Google split via round-robin-by-EB-ID gives natural distribution since IDs were assigned in domain order during sender provisioning. STOPPED before commit per PM hard rule — orchestrator routes to monty-qa first.

**Classification:** bug fix (critical, P1) + housekeeping (P3 x2). Tier 2 throughout.

**Delegation:** monty-dev for implementation + canary run. monty-qa adversarial review after.

[2026-04-16T21:45:00Z] -- COMPLETED pre-flight shape scan + EB orphan sweep (Tier 1 read-only, Tier 2 scripts only). RESULT: 0 shape-drift campaigns of 10 non-canary targets (canary cmneqixpv also clean, excluded from recommendations), 0 orphans across 7 EB-connected workspaces (27 EB campaigns swept, all owned by DB). Commit 1f6e2eaf's LinkedIn-key drift class DOES NOT present in the remaining 10 rolled-back emailSequences (zero stepNumber/subjectB/messageB on any step, all positions present). 4 Lime campaigns (E1/E2/E4/OOO) carry extra bodyHtml key — schema.passthrough() legal, informational only. 1210-solutions EB is a clean slate (0 campaigns) — confirms Phase 5.5 + BL-075 rollback machinery is symmetric. Ladder-group EB returned 401 stale token — NOT an orphan concern, separate backlog-worthy issue. Recommendation to PM: canary may progress independently, no pre-emptive DB scrub needed, no cleanup DELETE calls to stage. Report: /tmp/eb-preflight-report-2026-04-16.md. Scripts: scripts/maintenance/_bl_preflight_shape_scan.ts + _bl_preflight_orphan_sweep.ts. Zero mutations performed.

[2026-04-16T16:20:00Z] -- STARTING BL-088 (Tier 2, PM dispatched). Plan: (1) Add EmailBisonClient.createOrUpdateLeadsMultiple() hitting POST /api/leads/create-or-update/multiple per docs line 1595. Body: {existing_lead_behavior:'patch', leads:[{email, first_name?, last_name?, title?, company?, ...}]}. Tolerant Zod parse on 200 (BL-085 pattern) — log [BL-088] warn on shape drift, return best-effort array. Throw on HTTP non-2xx (request helper does this). Preserve createLead() for non-canary callers. (2) email-adapter.ts Step 4: replace per-lead createLead loop (L538-573) with one batch call to createOrUpdateLeadsMultiple. Capture returned IDs into createdLeadIds. Update L521 comment — drop wrong "createLead is idempotent by email" claim, reference BL-088 + new endpoint. Keep withRetry wrap (BL-086 makes it status-aware so transient 5xx still retries, 422 surfaces immediately). (3) Tests: new client-create-or-update-leads.test.ts (happy/422/drift/retryable) + new email-adapter-step4-upsert.test.ts (single batch POST, body shape, IDs land, regression for pre-existing lead returns 200 not 422). If-time: BL-089 (export RETRYABLE_STATUSES from client.ts → import in retry.ts → equality assertion test); BL-090 (flip CreateScheduleParams.save_as_template to required, comment-cite BL-087). DEFER BL-091. Hard rules: no schema migration, no env, no deploy. Single commit. Then push + run canary stage-deploy. Pre-canary state: cmneqixpv approved/ebId=null. Baseline tests: 885 pass / 37 fail / 1 todo (37 fails are pre-existing — promotion-icp-gate + similar; preserve count). tsc clean baseline.


[2026-04-16T16:30:00Z] -- BL-088 + BL-089 + BL-090 LANDED (Tier 2). (1) BL-088: EmailBisonClient.createOrUpdateLeadsMultiple() new public method hits POST /api/leads/create-or-update/multiple with body {existing_lead_behavior:'patch', leads:[{email, first_name?, last_name?, title?, company?}]}. Tolerant Zod parse (BL-085) — 200 with shape drift returns [] + console.warn '[BL-088] createOrUpdateLeadsMultiple response drift: <zod> raw=...'. Bare-array OR {data:[...]} both accepted via z.union. createLead() preserved untouched (non-canary callers in leads/operations.ts L823 + pipeline/signal-campaigns.ts L370). email-adapter Step 4 (L538-573 → ~L516-602): pre-filter eligible leads BEFORE batch (preserves WebhookEvent EMAIL_SENT dedup + null-email skip), then ONE batch upsert call (withRetry-wrapped, status-aware via BL-086). createdLeadIds.push captures returned IDs in order; existing zero-leads early exit on createdLeadIds.length === 0 unchanged; attachLeadsToCampaign batch unchanged. L521 comment rewritten — dropped wrong "createLead is idempotent by email" claim, added BL-088 + EB docs reference. (2) BL-089: exported RETRYABLE_STATUSES from src/lib/emailbison/client.ts as top-level const; private static reuses the same Set reference. retry.ts imports `RETRYABLE_STATUSES as RETRYABLE_EB_STATUSES` directly — comment-discipline duplicate gone. Added BL-089 set-equality assertion test in src/lib/utils/__tests__/retry-status-aware.test.ts (predicate accepts canonical set + rejects non-canonical 4xx; canonical set has documented 5 members). (3) BL-090: CreateScheduleParams.save_as_template flipped from optional to required with comment "BL-090 + BL-087: EB v1.1 POST /campaigns/:id/schedule rejects 422 if absent; required on both POST + PUT." UpdateScheduleParams collapsed to type alias of CreateScheduleParams (was an Omit + re-add hack pre-BL-090). emailbison-client-schedule.test.ts createSchedule fixture updated to include save_as_template:false (matches DEFAULT_SCHEDULE in email-adapter Step 5 set by BL-087). Tests: +12 client-create-or-update-leads.test.ts (happy/empty/422-non-retryable/drift x6/canary regression/503 retryable/bare-array) + 5 email-adapter-step4-upsert.test.ts (single batch POST + body shape/regression pre-existing leads/dedup pre-batch/null-email filter/all-filtered short-circuit) + 2 BL-089 set-equality. Updated 4 existing test files to swap createLead mocks for createOrUpdateLeadsMultiple. tsc clean. vitest: 904 pass / 37 fail / 1 todo (was 885/37/1 → +19 new pass, ZERO new failures, same 6 pre-existing failed test files). Commit pending. BL-091 deferred per brief.

[2026-04-16T22:00:00Z] -- BL-088 + BL-089 + BL-090 SHIPPED commit 33a9c3c4 (pushed to origin/main refs 3cdd5c4a..33a9c3c4). CANARY LIVE-VALIDATED: npx tsx scripts/maintenance/_canary-stage-deploy.ts (2026-04-16T15:31:00Z) → Campaign cmneqixpv status=deployed, emailBisonCampaignId=85 (NEW EB draft), deployedAt set, 44 retained workspace leads upserted via /api/leads/create-or-update/multiple with ZERO 422s, Step 8 reached, skipResume branch fired correctly (emailError="STAGED — resume pending PM review via EB UI or manual resumeCampaign call", emailStatus=complete, executeError=null). The exact BL-088 failure mode (prior-run leads in workspace store blocking fresh canary) is neutralized. BL-086 amplifier neutralization + BL-087 save_as_template fix + BL-088 upsert composed cleanly to unblock the full Steps 1-8 path. PM cleanup pending: EB 85 in 1210-solutions workspace (1 draft campaign, 44 leads, schedule SET, 3 sequence_steps).

QA REVIEW (monty-qa, sonnet) on commit 33a9c3c4: 7 findings total, 0 critical, 0 high, 1 medium, 4 low, 2 info. Verdict: SHIP. No revert warranted. Actionable findings filed to backlog: BL-092 (>500 lead chunking gap in createOrUpdateLeadsMultiple, F2 low) + BL-093 (signal-campaigns.ts still uses per-lead createLead — silent 422 on second signal-campaign run in retained workspace, F4 medium/operational risk) + BL-094 (BATCH 422 failure-mode + raw-preview-capture test coverage gap, F5 low) + BL-095 (DEPLOY_STEP.ATTACH_LEADS stale comment — trivial patch-forward, F1 medium). Info findings (F6 attachLeadsToCampaign undocumented cap, F7 pre-existing getSequenceSteps v1.1 drift) tracked observationally in BL-094 and canary notes — not filed as separate items since both are pre-existing or require production telemetry to confirm. BL-085 cascade CLOSED: Step 3 tolerant-parse (df0ed71d) implicitly live-validated by canary reaching Step 8 — no duplicate sequence_steps, no drift warn triggered against real EB v1.1 response. BL-091 stays open (integration test with real withRetry, deferred per brief). Workspace 1210-solutions EB state at session end: 1 campaign (EB 85, draft), 44 leads (unchanged from pre-run — upsert preserved them).


[2026-04-16T17:00:00Z] -- TRIAGE: Canary EB 85 PM-reviewed, 4 bugs surfaced, delegating single-cycle fix to monty-dev. BL-096 thread_reply flag (drop 'Re: X' workaround from 496d2d9c, use EB thread_reply boolean + empty subject for step 2+). BL-097 variable-format transformer (EB uses {{first_name}} double-curly snake_case for lead vars, {SENDER_FIRST_NAME} single-curly UPPER_SNAKE for sender sig vars — build boundary transform in email-adapter, DON'T modify writer prompts). BL-098 unsubscribe link default=OFF (deliverability memory rule: no links/images in cold). BL-099 inbox allocation enforcement (Step 6 currently attaches all 58 workspace inboxes; should respect per-campaign allocation — investigate Prisma model + campaign cmneqixpv allocation). Pre-fixes: delete EB 85 via ebClient.deleteCampaign(85) in 1210-solutions workspace + revert canary DB (campaign cmneqixpv → status='approved', emailBisonCampaignId=null, deployedAt=null). After fixes: stage-only retry via _canary-stage-deploy.ts (skipResume=true, MUST NOT go live). Then QA review before commit. Pre-authorized Tier 2.

[2026-04-16T17:35:00Z] -- BL-093 monty-qa REVISE addressed (F1/F2/F3 surgical bundle). NO commit, NO push, NO canary re-run.

**F1 (CRITICAL — allocation map wrong vs live DB).** Verified inversion via new read-only script `scripts/maintenance/_bl093-derive-allocation.ts` which mirrors the email-adapter Step 6 query exactly (workspaceSlug=1210-solutions, channel in [email,both], healthStatus in [healthy,warning], emailBisonSenderId not null, orderBy emailBisonSenderId asc) and computes idx % 5 buckets. Live DB pool is 58 EB IDs: 631..660, 661, 662, 663, 666..690 (664/665 absent — not healthy / not channel-eligible / no EB ID). Round-robin produced:
  - bucket 0 (Construction): [631, 636, 641, 646, 651, 656, 661, 668, 673, 678, 683, 688]
  - bucket 1 (Green):        [632, 637, 642, 647, 652, 657, 662, 669, 674, 679, 684, 689]
  - bucket 2 (Healthcare):   [633, 638, 643, 648, 653, 658, 663, 670, 675, 680, 685, 690]
  - bucket 3 (Industrial):   [634, 639, 644, 649, 654, 659, 666, 671, 676, 681, 686]
  - bucket 4 (Facilities):   [635, 640, 645, 650, 655, 660, 667, 672, 677, 682, 687]
Buckets 0/1/2 in the prior map had 663/664/665 at idx-30/31/32 instead of 661/662/663 — exact monty-qa inversion call. Bucket 3 (Industrial) and bucket 4 (Facilities/CANARY) MATCHED live DB; that's why EB 88 worked by accident (canary IS bucket 4). Updated `CAMPAIGN_SENDER_ALLOCATION` in `src/lib/channels/email-adapter.ts` to the correct map; updated `ALL_1210_SENDER_IDS` fixture in `email-adapter-allocation.test.ts` to the live pool; replaced the count-only smoke check with explicit per-bucket pin assertions (4 dedicated tests, one per non-canary campaign) so future map drift is caught at the test layer; added a union assertion that the 5 buckets fully partition the live pool (guards against new "phantom EB ID" inversions). Verified all 5 campaign IDs in the map are real (`prisma.campaign.findMany channels contains 'email'` returned the same 5 cuids).

**F2 (HIGH — signal-campaign pre-provision bypasses thread_reply).** Took Option A: extracted `buildSequenceStepsForEB(fullSequence, contextLabel?, shouldEmit?)` from `email-adapter.ts:534-589` into a top-level exported helper in the same file (avoids deeper module split — keeps EmailSequenceStep type local). Refactored Step 3 deploy callsite to use it (passing the GET-then-diff predicate as `shouldEmit` so the helper still gets the FULL sequence and identifies step 1 correctly even on partial re-deploys — caught a subtle bug in my first refactor draft where filtering BEFORE the helper would mis-thread). Refactored `agents/campaign.ts:296-323` signal-campaign-activate path to call the helper via dynamic `await import(...)` (sidesteps a potential circular-import risk between agents and channels). New test file `src/lib/channels/__tests__/build-sequence-steps-for-eb.test.ts` with 9 cases: 3 signal-path scenarios (incl. the previously-422'd empty-subject-on-step-2 case), 2 deploy-path predicate scenarios (re-deploy with step 1 already in EB still threads step 2 correctly), 4 small-bits assertions (sort, body fallback, delay defaulting, delayDays=0 preservation).

**F3 (HIGH — _bl093-prefix-revert.ts dangerously idempotent).** Took Option A: `rm scripts/maintenance/_bl093-prefix-revert.ts`. Script was one-shot (already executed Apr 16 across the 3 prefix-cleanup cycles for EB 85/86/87). Removing it eliminates the future re-run risk where the docstring's stale ORPHAN_EB_ID=87 would silently NULL the canary's live EB 88 reference. Replaced with `_bl093-derive-allocation.ts` which is read-only and safe to keep.

**F4/F5/F6 — backlog only, NOT implemented this session.**
- BL-097 (allocation drift detection): Add a CI integration test (or monthly cron) that re-runs the derivation query against the live DB snapshot and asserts the result matches `CAMPAIGN_SENDER_ALLOCATION` exactly. Catches the F1 class of bug at PR time instead of at deploy time. Will need a DB connection in CI — likely a nightly job rather than per-PR. Source: monty-qa BL-093 F4.
- BL-098 (subjectVariantB → EB variant): Currently `subjectVariantB` on stored sequence steps is unused at the EB wire layer — writers produce A/B variants but only A reaches EB. Investigate EB's documented variant support (if any) and either route subject_b through createSequenceSteps OR document that A/B is admin-side only. Source: monty-qa BL-093 F5.
- BL-099 (variable transform stray-brace warn logs): `transformVariablesForEB` silently passes through unrecognised single-curly `{XXX}` patterns. Add a console.warn when an unmapped UPPER_SNAKE variable survives the transform — surfaces writer drift before it reaches recipients as raw `{XXX}`. Source: monty-qa BL-093 F6.

**Re-asserting prior-session backlog items (still open).**
- BL-094: proper `Campaign.allocatedSenderIds` schema field + UI + migration to backfill the 5 1210 campaigns from `CAMPAIGN_SENDER_ALLOCATION`. The hardcoded map is the stop-gap; BL-094 is the durable fix.
- BL-095: writer-output normalization for `{LOCATION}` variable — currently mapped to `{{notes}}` defensively because EB has no documented `lead.location` field. Needs an explicit UX decision (custom EB variable, or store on Lead.notes with proper formatting).
- BL-096: `getSequenceSteps` GET response shape drift `{data: {sequence_id, sequence_steps: [...]}}` vs `{data: [...]}` — same as BL-085 cascade observation; canary verification side only, doesn't break deploy.

**Files touched this session.**
- `src/lib/channels/email-adapter.ts` — corrected `CAMPAIGN_SENDER_ALLOCATION` (buckets 0/1/2), extracted `buildSequenceStepsForEB`, refactored Step 3 to use it.
- `src/lib/channels/__tests__/email-adapter-allocation.test.ts` — corrected `ALL_1210_SENDER_IDS` fixture, replaced smoke-check with 4 per-bucket pin tests, added union-equals-pool assertion.
- `src/lib/agents/campaign.ts` — signal-campaign-activate now imports + calls `buildSequenceStepsForEB`.
- `src/lib/channels/__tests__/build-sequence-steps-for-eb.test.ts` — NEW; 9 cases covering signal-path + deploy-path-predicate scenarios.
- `scripts/maintenance/_bl093-prefix-revert.ts` — DELETED (one-shot, already executed).
- `scripts/maintenance/_bl093-derive-allocation.ts` — NEW; read-only allocation map verifier.

**Gates.** `npx tsc --noEmit` clean. `npx vitest run src/lib/emailbison src/lib/channels src/lib/campaigns src/__tests__/email-adapter-deploy.test.ts` → 184/184 passing (was 171/171 in PM brief; +13 from the new tests). Full suite: 944 pass / 37 fail (same 37 pre-existing baseline) / 1 todo. Zero new failures. NO canary re-run — F1/F2/F3 are structural and don't change the EmailAdapter.deploy behaviour for the already-staged EB 88.

**Awaiting orchestrator commit.** Suggested commit message: `fix(email-adapter): correct 1210 sender allocation map + extract shared sequence-step builder (BL-093 F1/F2/F3)`.

[2026-04-16T23:55:00Z] -- TRIAGE: PM correction of commit da7fdf60. BL-093 variable transformer built against WRONG spec. EB vendor-authoritative syntax is SINGLE-curly UPPER_SNAKE: {FIRST_NAME}, {LAST_NAME}, {EMAIL}, {TITLE}, {COMPANY}, {SENDER_FIRST_NAME}, {SENDER_FULL_NAME}, {SENDER_EMAIL_SIGNATURE}. Current transformer emits {{first_name}} double-curly snake_case — EB will NOT substitute; recipients would see literal "{{first_name}}" text. Severity: critical. The prior "validator" false-positive likely came from grepping the transformer's own output rather than fetching EB's stored body. Classification: bug (critical) — correction of da7fdf60, not a new feature. Tier 2 throughout (code + DB maintenance); canary stage-only (skipResume=true, will NOT go live). Delegation: monty-dev for (a) rewrite VAR_MAP + SENDER_SIG set + variable-transform.test.ts; (b) pre-fix EB 88 delete + canary cmneqixpv DB revert (status=approved, ebId=null, deployedAt=null); (c) re-run _canary-stage-deploy.ts stage-only; (d) fetch EB body via ebClient.getSequenceSteps + log LITERAL body content string (not transformer output — that was the prior validation flaw); (e) commit + push. monty-qa adversarial after: verify test table matches vendor spec + verify fetched EB body contains single-curly UPPER_SNAKE. Hard rules: no live canary, no writer prompt changes, no schema migration, no env changes.

[2026-04-17T00:15:00Z] -- BL-093 CORRECTION SHIPPED commit 14bb69ba (da7fdf60..14bb69ba pushed to origin/main). Rewrote src/lib/emailbison/variable-transform.ts against PM-provided vendor-authoritative EB spec: SINGLE-curly UPPER_SNAKE. VAR_MAP_UPPER now maps FIRSTNAME->{FIRST_NAME}, LASTNAME->{LAST_NAME}, COMPANYNAME->{COMPANY}, JOBTITLE->{TITLE}, EMAIL->{EMAIL} passthrough. LOCATION + LASTEMAILMONTH intentionally unmapped (trigger BL-099 warn — vendor undocumented, surfaces gap rather than silently mis-mapping). KNOWN_EB_TOKENS set for idempotency (vendor-confirmed + defensive sender variants pass through without warn on second pass). 28 rewrite tests + BL-099 warn coverage + idempotency-with-all-8-vendor-tokens assertion. Also extended getSequenceSteps Zod union to accept v1.1 nested shape `{data:{sequence_id,sequence_steps:[...]}}` (closes BL-096 latent drift — was blocking canary verification read-back).

Maintenance: scripts/maintenance/_bl093-eb88-revert.ts deleted EB 88 + reverted Campaign cmneqixpv to status=approved/ebId=null/deployedAt=null. Canary re-run EB 89 created cleanly (skipResume=true, draft, 3 steps, 11 senders, 44 leads, STAGED emailError).

CRITICAL VERIFICATION (task 5): fetched EB 89 sequence_steps via ebClient.getSequenceSteps(89) and logged LITERAL body strings. ALL 5 assertions passed: contains {FIRST_NAME}, does NOT contain {{first_name}}, does NOT contain {FIRSTNAME}, does NOT contain {firstName}, does NOT contain {{FIRSTNAME}}. Real EB-stored step 1 body: "Hi {FIRST_NAME},<br><br>Running payroll for {cleaners and FM operatives|security and cleaning staff}...{COMPANY} keeps placing workers...". This is EB's own storage, not our code's output — the prior-cycle false-positive validation flaw is addressed.

Gates: tsc clean, vitest 952 pass / 37 fail (baseline 37 preserved, 0 new) / 1 todo, +8 new passing tests.

New BL items to file: (a) LOCATION vendor decision (writer still emits {LOCATION}; now warns every transform); (b) LASTEMAILMONTH vendor decision (same); (c) trigger/ooo-reengage.ts emits `{{first_name}}` double-curly in writer prompts (lines 414/421/429/461) + fallback body (L461) — reaches EB unsubstituted via transformer (single-curly regex won't match double-curly). Out of scope for BL-093 correction per "do not touch writer prompts" rule. (d) src/lib/placement/send-test.ts uses `{{first_name}}` in placement HTML — placement emails are static test content, not templated; low priority.

EB 89 left in draft for PM review. Workspace 1210-solutions EB state at session end: 1 campaign (EB 89, draft), 44 leads retained, 3 sequence_steps, schedule set.

[2026-04-17T00:45:00Z] -- monty-qa review of 14bb69ba: SHIP verdict confirmed. Independent EB 89 fetch verified step 1/2/3 bodies contain {FIRST_NAME}/{COMPANY} literal (3 + 1 occurrences); all wrong-syntax variants (double-curly, old FIRSTNAME, lowercase, wrapped double-curly) confirmed 0 across all 3 steps. tsc clean, 952/37/1 preserved, 28/28 transformer tests pass. Canary DB state confirmed: cmneqixpv=deployed+ebId=89+deployedAt set, 1210-solutions=1 EB campaign. BL-093 correction cycle CLOSED. New backlog items filed per qa recommendations: BL-097 (HIGH — trigger/ooo-reengage.ts emits {{first_name}} double-curly, bypasses transformer, ships raw on every OOO reply — active path, needs separate cycle), BL-098 (MEDIUM — {LOCATION}/{LASTEMAILMONTH} in writer-rules.md but not in vendor spec; latent decision), BL-099 (MEDIUM — BL-096 nested-shape Zod lacks unit tests; happy-path covered by canary fetch only). Deferred per PM "unless necessary" rule: qa F4 (defensive sender tokens silent pass-through — low), F5 (placement/send-test.ts dead code + raw fetch violation — low), F6 (incidents.md 200-line governance cap — informational). EB 89 left in draft in 1210-solutions for PM review via EB UI before any resume decision.

[2026-04-16T17:15:00Z] -- STARTING BL-100 sender-name substitution (Tier 2 code + Tier 3 EB delete + canary DB revert, PM pre-authorized end-to-end in task brief). Classification: bug (high severity, priority 2). Follow-up to commit 14bb69ba (BL-093). Problem: EB 89 canary body ends with hardcoded 'Daniel Lazarus' — baked into the writer's template output. All workspaces use multi-sender inbox pools (per BL-093 allocation map), so any non-Daniel inbox ships wrong name to prospects. PM decision: fix at the EB adapter boundary (NOT writer prompts — that is BL-097 / separate cycle). Plan delegating to monty-dev: (1) add sender-name transformer (new file or extend src/lib/emailbison/variable-transform.ts) detecting signature-line pattern on last 5 lines only, case-insensitive, full-name exact match → {SENDER_FULL_NAME}, first-name exact match → {SENDER_FIRST_NAME}; no-match path = passthrough + warn; (2) source roster by querying inboxes allocated to the campaign per BL-093 allocation (pass distinct first+last+full names); (3) apply in email-adapter.ts AFTER lead variable transform, before createSequenceSteps POST; (4) unit tests: signature detection (Daniel Lazarus last line → full; Daniel last line → first; Daniel\\nSenior Consultant → first on first line; Hi Daniel in body → NOT replaced; no signature → passthrough+warn; multiple senders; idempotent); (5) pre-fixes: delete EB 89 via client.deleteCampaign with 1210-solutions workspace.apiToken; revert canary DB (Campaign status deployed→approved, emailBisonCampaignId=null, deployedAt=null); confirm 1210-solutions = 0 EB campaigns post-delete; (6) re-run canary via npx tsx scripts/maintenance/_canary-stage-deploy.ts stage-only skipResume=true MUST NOT go live; (7) CRITICAL boundary verification (BL-093 lesson): ebClient.getSequenceSteps(newEbId) for steps 1/2/3 — assert signature portion contains literal '{SENDER_FIRST_NAME}' or '{SENDER_FULL_NAME}', NOT 'Daniel Lazarus' or 'Daniel'; log actual EB-stored body for PM review. Hard rules: stage only, NO go-live, NO writer-prompt changes (BL-097), NO ooo-reengage.ts touch (BL-097), NO campaign deletes outside EB 89. Expect 15-25 min. monty-qa will review after: transformer test suite + independent EB refetch + sender tokens present.

[2026-04-16T18:00:00Z] -- STARTING BL-100 (Tier 2 code + Tier 3 pre-fix, PM pre-authorized end-to-end). Sender-name signature substitution transformer at the EB adapter boundary. PM identified on EB 89 verification: writer template has hardcoded 'Daniel Lazarus' at the end of each email body; all 58 1210-solutions email senders currently resolve to 'Daniel Lazarus' so canary is benign, but a multi-name roster would ship the wrong name to recipients. Same principle as BL-093: normalize at vendor edge, NO writer prompt changes. Plan: (1) Create src/lib/emailbison/sender-name-transform.ts with `transformSenderNames(body, roster): {transformed, matched}` — operates ONLY on last 5 non-empty "lines" of body where line separator is `\n` OR `<br\s*/?>` (EB bodies are HTML-formatted with <br> separators). Full-name match first, first-name fallback, case-insensitive, exact-line-after-trim only. Mid-body "Hi Daniel," NEVER rewritten because it's not in the last-5-non-empty-lines signature region. Empty roster / no signature match → passthrough + console.warn with campaignId + body-preview. Idempotent: already-transformed body (contains {SENDER_*} tokens) is a no-op second pass. (2) Co-located vitest test file with 9 min cases (full match, first match, mid-body protect, signature block with title on trailing line, no match warn, multi-sender any-match, case-insensitive, idempotent, empty roster). (3) Wire into email-adapter.ts Step 3 between buildSequenceStepsForEB and createSequenceSteps: build SenderRoster from the already-resolved allocated senders (reuse resolveAllocatedSenders + existing allSenders query; add sender names to select). Apply transformSenderNames to each step body before POST. On no-match warn, include campaignId in the context label. Tier 3 pre-fixes (post code + test green): use existing _bl093-eb88-revert.ts as template → new one-shot script _bl100-eb89-revert.ts (hardcoded scope: EB 89 in 1210-solutions + Outsignal cmneqixpv); delete EB 89, revert Campaign status=deployed→approved + emailBisonCampaignId 89→null + deployedAt→null, flip latest CampaignDeploy cmo1ptrrj status=complete→rolled_back with error suffix, create AuditLog action='campaign.status.bl100_rollback'. Then re-run _canary-stage-deploy.ts (script is unchanged; it supports skipResume via direct executeDeploy call; hardcoded to cmneqixpv). Boundary verification: fetch newEbId sequence steps via ebClient.getSequenceSteps, isolate last ~5 lines of each body, assert contains {SENDER_FIRST_NAME} or {SENDER_FULL_NAME} AND does NOT contain literal 'Daniel Lazarus'/'Daniel' in signature region. Commit scope: src/lib/emailbison/sender-name-transform.ts + test file + email-adapter.ts changes ONLY. Exclude all scripts/maintenance/_bl100*.ts from commit (throwaway per repo convention). Baseline: 952 pass / 37 fail / 1 todo from commit 14bb69ba.

[2026-04-16T18:30:00Z] -- COMPLETED BL-100 (Tier 2 code + Tier 3 pre-fix, PM pre-authorized). Sender-name transformer shipped + canary re-verified. Files: (1) NEW src/lib/emailbison/sender-name-transform.ts — `transformSenderNames(body, roster, context?)` operates ONLY on last 5 non-empty lines where "line" splits on BOTH `\n` and HTML `<br\s*/?>` variants (EB bodies are HTML-formatted with <br>, not newlines — confirmed via EB 89 body read-back). Full-name preferred over first-name on same line. Case-insensitive exact-line match (trimmed). Mid-body "Hi Daniel," never rewritten because outside signature window. Idempotent: body containing `{SENDER_(?:FIRST_NAME|FULL_NAME)\}` skips the no-match warn + no-ops. Empty roster silent no-op. Unmatched roster emits console.warn with campaignId + tail(200)→preview(120) so PM can chase writer drift. (2) NEW src/lib/emailbison/__tests__/sender-name-transform.test.ts — 20 tests covering all mandatory BL-100 brief cases + <br> separator variants (<br>, <br/>, <BR/>, <br />) + EB 89 step-1 full-body regression. All 20 pass. (3) src/lib/channels/email-adapter.ts — added `buildSenderRoster(senders)` helper (splits Sender.name into first/last/full tokens; dedupes; multi-word last-name support) and wired into Step 3 between buildSequenceStepsForEB and createSequenceSteps. New prisma.sender.findMany scoped to allocated subset (reuses resolveAllocatedSenders). Empty roster is intentionally no-op here — Step 6 remains authoritative for the zero-sender throw. Docstring explains the transform ordering on the wire: sender-name (adapter, here) → lead-variable (client.createSequenceSteps internal) → POST. Gates: tsc clean, eslint no new errors (only pre-existing 'DeployResult' unused-import warning), vitest 972 pass / 37 fail / 1 todo (baseline 952/37/1 — +20 from the new transformer tests, zero new failures, same 6 pre-existing failed files). Tier 3 pre-fix: scripts/maintenance/_bl100-eb89-revert.ts (throwaway, untracked) deleted EB 89 + reverted Campaign cmneqixpv to status=approved/ebId=null/deployedAt=null + flipped CampaignDeploy cmo1ptrrj to status=rolled_back with BL-100 suffix on emailError + created AuditLog row cmo1qpnux action=campaign.status.bl100_rollback. EB-side post-delete check accepted status='pending deletion' as "confirmed deleted" (EB async DELETE window, same as BL-078 isNotFoundError semantic — script log + run scoped to this cycle). Residual Campaign count in 1210-solutions with non-null ebId = 0. Re-canary via _canary-stage-deploy.ts (skipResume=true, direct executeDeploy call, hardcoded to cmneqixpv) created EB 90 — status=draft, 3 sequence steps, 44 leads, 11/58 allocated senders, schedule set, no executeError. Boundary verification via ebClient.getSequenceSteps(90): step 1 body ends `{SENDER_FULL_NAME}<br>07376 643884` (phone preserved); step 2 body ends `{SENDER_FIRST_NAME}` (Daniel alone became first-name token); step 3 body ends `{SENDER_FULL_NAME}`. No literal 'Daniel Lazarus' anywhere, no standalone 'Daniel' line, mid-body `Hi {FIRST_NAME},` and `{COMPANY}` tokens intact, spintax preserved verbatim. Commit scope: transformer + test + email-adapter only; _bl100-eb89-revert.ts kept untracked. Baseline promoted 952→972; no regressions. BL-100 closed for code scope.

[2026-04-16T18:50:00Z] -- COMPLETED BL-100 sender-name substitution (Tier 2 code + Tier 3 EB delete + canary DB revert, PM pre-authorized). Dev delivered commit 41ba65cd (pushed to origin/main): new src/lib/emailbison/sender-name-transform.ts with transformSenderNames(body, roster) detecting signature-region name patterns on last-5 non-empty-lines, case-insensitive, full-name preferred over first-name, idempotent, warn-on-no-match; new buildSenderRoster(senders) helper in email-adapter.ts wiring Sender table query into roster dedup + null-skip + first/last/full splitting; transform wired into Step 3 of email-adapter.ts immediately after BL-093 lead variable transform, before createSequenceSteps POST. Pre-fix: EB 89 deleted from 1210-solutions, canary Campaign cmneqixpv reverted (status=approved, ebId=null, deployedAt=null + AuditLog cmo1qpnux0000zx2xnl0664tx). Re-canary stage-only via scripts/maintenance/_canary-stage-deploy.ts produced EB 90 in DRAFT (3 steps, 44 leads upserted, 11/58 senders allocated, schedule set, Step 9/10 skipped). QA CONDITIONAL PASS (4 findings): F1 HIGH short-body edge case (≤5 non-empty lines → whole body is signature region → mid-body salutation line matching a sender first-name gets corrupted) filed as BL-101, gates wider rollout but safe for current 1210-solutions canary (6+ line bodies); F2 MEDIUM SenderRoster.lastNames dead-stored (populated, never consulted) filed as BL-102; F3 MEDIUM buildSenderRoster exported 'for unit test visibility' but zero tests reference it filed as BL-102; F4 LOW console.warn convention drift vs sibling variable-transform.ts folded into BL-102. QA independently re-fetched EB 90 via ebClient.getSequenceSteps(90) + asserted: zero literal 'Daniel Lazarus', zero standalone 'Daniel' in signatures, {SENDER_FIRST_NAME}/{SENDER_FULL_NAME} tokens present on all 3 steps, mid-body {FIRST_NAME} + spintax preserved, phone number after step-1 name untouched; EB 89 confirmed 404 (isNotFoundError=true). Tests: 20/20 new pass, full suite 972/37/1 vs baseline 952/37/1 (zero new regressions, same 6 pre-existing failed files), tsc clean, eslint no new errors. EB 90 awaits PM manual resume decision — stage only per brief hard rule.

[2026-04-16T19:00:00Z] -- STARTING BL-105 URGENT PAUSE of 4 active BlankTag LinkedIn campaigns (Tier 3, PM pre-authorized). Classification: bug (critical severity, priority 1). Production incident: BlankTag LinkedIn campaigns delivering LITERAL {FIRSTNAME}/{COMPANYNAME} placeholders to prospects — zero variable substitution on wire. Diagnostic boh2k3qzp confirmed. Continues BL-097 theme (variable-substitution failure modes on outbound channels) but distinct: BL-097 is ooo-reengage.ts double-curly, this is LinkedIn-wire substitution failure across all 4 active BlankTag campaigns. Delegating to monty-dev: (1) find all campaigns where workspaceSlug='blanktag' AND channel ∈ ('linkedin','both') AND status='active' — confirm exactly 4 before mutations; (2) enumerate the pause mechanism (campaign.status enum values, any LinkedInAction / linkedinActionQueue schema, Trigger.dev linkedin-*.ts tasks that need freezing, any worker-side pre-queued jobs); (3) flip campaign.status→paused on all 4 AND freeze pending queued LinkedIn actions (exact target status depends on schema — paused/cancelled/on_hold); (4) re-query to VERIFY status=paused + zero pending/scheduled actions; (5) damage-window audit: GROUP BY campaign+step count of already-delivered broken messages (no modification); (6) commit pause script + incident report + push to main. Hard rules: NO campaign deletion, NO sequence content modification (that's a separate cycle), NO touching non-BlankTag campaigns. Expect 10-15 min.

[2026-04-17T01:00:00Z] -- STARTING BL-104 company-normaliser polish + trailing-bracket strip (Tier 2 code + Tier 3 EB delete + canary DB revert, PM pre-authorized end-to-end). Classification: improvement (priority 1, canary blocker — EB 91 staged after 7a895f4b, 10 other campaigns waiting). Follow-up to BL-103. PM review of EB 91 body surfaced 5 edge cases not in base ruleset: (1) trailing bracket strip — iterate `(...)` detach + strip alongside legal/geo ('C4SS (Contact 4 Support Services)' → 'C4SS'; 'Foo Ltd (UK)' → 'Foo'; 'Bar (USA) Inc' → 'Bar'; but 'Amazon (EMEA) Services Ltd' → 'Amazon (EMEA) Services' keeps mid-string `(EMEA)`; 'Acme [UK] Ltd' does NOT strip square brackets). (2) Trim whitespace at entry + after every iteration ('Acme Corp  ' trailing space currently bypasses regex anchor). (3) Warn on MAX_ITERATIONS truncate (don't silently pass). (4) Ampersand preservation — 'Bain & Company' → PREFER preserve 'Bain & Company' as-is (it IS the brand; make '& Co'/'& Company' atomic — strip only if preceded by word+space, not alone). (5) Naming collision audit — 3 normalizeCompanyName surfaces (src/lib/normalize.ts, src/lib/normalizer/company.ts, src/lib/emailbison/company-normaliser.ts); if different purposes rename (normalizeCompanyForEB vs normalizeCompanyForMatch); if dead delete; DO NOT break existing callers. Delegating to monty-dev end-to-end with hard rules: stage only (skipResume=true, MUST NOT go live), NO LinkedIn adapter touch ([bpp20zwu2] owns that), NO non-canary EB deletes, MUST NOT break normalizeCompanyName import signature (other cycles depend on it — [bpp20zwu2] may import it). Pre-fix scope: delete EB 91, revert Campaign cmneqixpv0001p8710bov1fga to status=approved/ebId=null/deployedAt=null, confirm 1210-solutions = 0 campaigns. Re-run _canary-stage-deploy.ts stage-only. Verification: fetch 44 deployed leads, grep company field zero trailing Ltd/Limited/Inc/Corp/etc + zero trailing geo + zero trailing `(...)`; specifically search for 'C4SS' (must be 'C4SS' alone no parens); log 10 fresh before/after pairs. monty-qa adversarial after. Parallel tasks NOT to conflict: [brcfciljn] BlankTag LinkedIn pause, [bpp20zwu2] LinkedIn variable+company fix, [bqcpdd8ld] read-only LinkedIn audit.

[2026-04-16T18:07:31Z] -- STARTING BL-105 PAUSE EXECUTION (Tier 3, PM option-b authorized — all 4 BlankTag LinkedIn campaigns regardless of status). Scope: flip Campaign.status→paused on cmnspob3g004ep8xxhm8grecl (active, 2C), cmnspobho008tp8xxrddnwyeu (active, 2D), cmnspobtf00d8p8xx8v4ew4jg (active, 2E), cmmwei70q0007zxgpvyhwwmua (approved, C1 original); flip LinkedInAction.status pending→cancelled joined by campaignName; AuditLog per BL-100 pattern; verify 0 pending remain; read-only damage count grouped by campaign+step (status in sent/delivered/successful). Hard rules: NO unpause, NO non-BlankTag touches, NO deletes — status flips + audit log only. Commit + push. Delegating to monty-dev. Concurrent tasks [bpp20zwu2] LinkedIn fix + [brcfciljn] previous pause cycle + [bqcpdd8ld] render audit all read-only on this scope or complete — no write conflicts expected.

[2026-04-16T18:15:00Z] -- COMPLETED BL-105 PAUSE EXECUTION (Tier 3, PM option-b pre-authorized). All 4 BlankTag LinkedIn campaigns flipped to status=paused via scripts/maintenance/_bl105-pause-execute.ts. Pre-state: 3 active (2C/2D/2E) + 1 approved (C1 original). Post-state: all 4 paused, workspaceSlug='blanktag' verified on every row pre-mutation. Mutation A (Campaign.status flip) executed in single $transaction with 4 auditLog rows `action='campaign.status.bl105_pause'` inside same tx — preserved contentApproved/deployedAt/ebId untouched (status+updatedAt only). Mutation B (LinkedInAction freeze) executed per-campaign tx (4 txs) flipping pending→cancelled by campaignName join per src/lib/linkedin/queue.ts — all 4 campaigns had 0 pending at flip time (worker had drained before pause fired) + 4 auditLog rows `action='linkedin.action.bl105_cancel'` still written per brief §4 (log op even if count=0). Post-verify: all 4 status=paused confirmed, residual pending=0 confirmed. Damage audit (read-only, status='complete' = only delivered enum value in src/lib/linkedin/types.ts; brief's sent/delivered/successful not valid here): 42 profile_view (linkedin_0) + 17 connection_request (linkedin_1) + 0 message across all 4 campaigns — zero confirmed literal-token messages on wire because no message-step (linkedin_2+) actions completed before pause. Profile views render no template; connection requests were blank-connect per diagnostic. Pause caught the campaigns before any connection-acceptance triggered a message send. AuditLog total: 8 rows (4 campaign + 4 linkedin). Script scope hardcoded to 4 campaign IDs + workspaceSlug='blanktag' guard — REFUSE paths on missing row or wrong workspace. Sibling _bl105-damage-audit.txt captures the groupBy snapshot. Commit: pending push. Baseline unchanged: 972 pass / 37 fail / 1 todo (no code changes, only a throwaway maintenance script). Parallel fix cycle [bpp20zwu2] now has a frozen target to patch.

[2026-04-16T18:12:01Z] -- STARTING BL-104 monty-dev execution (Tier 2 code + Tier 3 EB delete + canary DB revert, PM pre-authorized end-to-end). Scope: (F-bracket) extend src/lib/emailbison/company-normaliser.ts with trailing round-bracket strip lane (iterate alongside legal+geo, round parens only, square brackets preserved); (F-domain) NEW optional `domain?: string|null` 2nd arg — after legal+geo+brackets settle, compare cleaned tokens vs domain stem tokens (strip www., take portion before first '.', lowercase, '-' → ' '), truncate cleaned to the matched prefix length when domain has a strict prefix match of cleaned (preserve original casing); (F1) trim whitespace at entry + after each iteration; (F2) warn-on-MAX_ITERATIONS with existing `eslint-disable-next-line no-console` convention matching variable-transform.ts:153; (F3) ampersand preservation — 'Bain & Company' → 'Bain & Company' (guard against the '& Co'/'& Company' strip when the '&' sits immediately before the matched suffix and the preceding token is a brand word, not empty); (F5) naming-collision audit — identified: src/lib/normalize.ts exports normalizeCompanyName (title-case normaliser, 3 callers: normalizer/company.ts, api/people/enrich/route.ts, api/companies/enrich/route.ts), src/lib/normalizer/company.ts exports classifyCompanyName (NOT normalizeCompanyName — false positive in brief), src/lib/emailbison/company-normaliser.ts exports normalizeCompanyName (wire-boundary, 2 callers: email-adapter.ts + linkedin/sequencing.ts). Only TWO functions actually collide by name (normalize.ts title-caser + company-normaliser.ts wire-stripper); normalizer/company.ts is not a collision. Action: leave wire-boundary normalizeCompanyName name untouched (backward-compat contract with [bpp20zwu2]); no rename needed for normalizer/company.ts (different export). Action for normalize.ts: keep as-is — it's the title-case normaliser with distinct purpose (called from enrichment/discovery pipelines to fix "DHL INC" → "Dhl Inc"); renaming would cascade through 3 route handlers + normalizer/company.ts + 2 test files. Proposing in commit body: "TODO: future rename of src/lib/normalize.ts#normalizeCompanyName to normalizeCompanyDisplayCase to stop autocomplete picking the wrong one" — flag for follow-up backlog, do not rename this cycle (risk/benefit ratio). Call-site change: src/lib/channels/email-adapter.ts line 879 add `entry.person.companyDomain` as 2nd arg (Person.companyDomain per prisma/schema.prisma line 149 — indexed, nullable string). Test extension: every PM acceptance case from rules 1-5 + idempotency with domain. Tier 3 pre-fix: `client.deleteCampaign(91)` on 1210-solutions (existing apiToken resolve), verify 404, assert getCampaigns count = 0, single Prisma $tx updating Campaign cmneqixpv0001p8710bov1fga status=deployed→approved + emailBisonCampaignId=91→null + deployedAt→null (preserving contentApproved/leadsApproved) + AuditLog row action='campaign.status.bl104_canary_revert' metadata per brief + append 'BL-104 rollback 2026-04-16 — EB 91 deleted, DB reverted' to latest CampaignDeploy emailError. Re-stage: VERIFY scripts/maintenance/_canary-stage-deploy.ts line ~105 hardcoded skipResume:true BEFORE invoking; npx tsx runs the stage-only path. Verification: paginated getCampaignLeads on new EB id, assert zero trailing LEGAL/GEO/bracket residue on ALL 44 leads, specifically grep 'Sonnic' appears as 'Sonnic' (proves domain truncation), 'C4SS' as 'C4SS' if present in real 44-lead fixture (else explicitly note it's unit-test-only); log 15 before/after pairs; re-fetch getSequenceSteps to confirm {COMPANY} token still present in step 1 body. Gates: tsc clean, vitest baseline 996/37/1 (no new failures), eslint no new violations. Baseline source: most recent completion was BL-103 at commit 7a895f4b. Commit: single commit on main `fix(emailbison): normaliser trailing brackets + domain-based truncation + trim + warn + ampersand (BL-104)` + F5 audit note in body. Push origin/main, no --force, no --no-verify. Hard rules: skipResume=true MUST hold, DO NOT touch src/lib/linkedin/* (parallel [bpp20zwu2]), backward-compat signature (optional 2nd arg), NO non-canary EB deletes, NO writer prompt / ooo-reengage.ts touches.

[2026-04-16T18:13:21Z] -- COMPLETED BL-105 pause execution (monty-dev, commit 61366fd0). 4/4 Campaign.status flipped to paused (3 from active, 1 from approved); 0 pending LinkedInAction across 4 campaign names (worker had drained, pause is future-enqueue guard via Campaign.status); 8 AuditLog entries written. Damage audit: 42 profile_views + 17 connection_requests complete, ZERO message actions delivered — pause caught the campaigns before any connection-accept triggered step-2 template render. Parallel fix [bpp20zwu2] (cb5f6673) landed first, no conflict. NOT doing: unpause (PM decision pending re-approval loop after fix verification). Next: QA verification of the variable-transform fix before any unpause.

## 2026-04-16 — Triage: 1210-solutions EB inbox daily_limit correction
- Classification: improvement, priority 1 (blocks PM-approved canary resume on EB 92)
- Tier: 2 (reversible — EB PATCH, no schema/DB writes; AuditLog entries for trail)
- Routing: Dev Agent (monty-dev)
- Target: Outlook/Azure -> 5/day; Google/Gmail -> 8/day. Ambiguous providers flagged, not updated.
- Scope guard: 1210-solutions workspace only; NO campaign settings, NO resume, NO writer/LinkedIn adapter files (other cycle bw14ydcxv holds those).
- API surface confirmed: ebClient.getSenderEmails() + patchSenderEmail(id, { daily_limit }).

[2026-04-17T02:00:00Z] -- INVESTIGATION (Tier 1 read-only, Task A from PM orchestrator brief): LASTEMAILMONTH wire-up + Lime C1/C3 step-2 "duplication." SURPRISE FINDINGS that contradict PM's premise — REPORTING BEFORE ANY CODE CHANGES. (1) Wire-up ALREADY EXISTS: src/lib/linkedin/sequencing.ts:245-264 inside evaluateSequenceRules parses Campaign.description for `lastEmailMonth:(\w+)` and passes it to buildTemplateContext at line 303. buildTemplateContext already accepts outreachContext.lastEmailMonth and binds it at line 88. The transformer already maps {LASTEMAILMONTH} → {{lastEmailMonth}} (BL-105, variable-transform.ts:75). Production render path (connection-poller:264 and webhook route:200) fully resolves the token. (2) Lime C1 description = "LinkedIn retargeting of email non-responders. lastEmailMonth:February" and C3 = "…lastEmailMonth:March". Simulated production render on first target-list lead for each: C1 Simon@Garolla → "Hi Simon, I sent you an email back in February about…" ✓ C3 Nova@NG Transport → "Hi Nova, I emailed you back in March about…" ✓ The token DOES resolve in prod. (3) PM saw "renders empty" because _bl105-verify-1210-lime-render.ts:219-221 calls buildTemplateContext(person, undefined, undefined) — verifier never passes outreachContext, so `{{lastEmailMonth}}` always binds to "" in the verifier. That's a verifier bug, not a prod bug. (4) COVERAGE CHECK for PM's proposed dynamic spec (query WebhookEvent eventType=EMAIL_SENT by leadEmail, take most recent, extract month): Lime C1 target list has 517 leads total; sampled 50, only 2 (4%) have any EMAIL_SENT webhook events — and those 2 show "March", NOT the description's "February". C3 sample: 4/38 (10.5%) have events — March(3) + April(1), NOT description's "March". Implementing PM's literal spec would render empty for ~90% of Lime prospects AND render the wrong month for the ~10% with webhook data. Regression on an active campaign. (5) 1210-solutions (where {LASTEMAILMONTH} is NOT currently used in any campaign body) has ZERO EMAIL_SENT webhook events at all — dynamic lookup returns null for every 1210 lead regardless. (6) No Nova spec doc found for dynamic per-lead lookup via grep across .nova/memory, .monty/memory, docs/. The only written spec is writer-rules.md line 209 which lists {LASTEMAILMONTH} as a canonical variable without defining resolution source. BL-098 explicitly notes this is an unresolved vendor/resolution decision. RECOMMENDATION: do NOT implement the literal dynamic per-lead spec as briefed — it regresses the only two campaigns that use the token today. Three viable paths for PM decision: (A) KEEP description parser, fix the verifier to pass outreachContext derived from description so diagnostics match prod (~15 min, zero risk). (B) AUGMENT with dynamic-primary + description-fallback — dynamic lookup (WebhookEvent EMAIL_SENT for this leadEmail, most-recent month) used if non-null, else description. Still works for Lime today (fallback wins when webhook empty). Risk: for leads WITH webhook data, the rendered month changes from what Nova set in description to what webhooks say. PM must authorize that semantic shift. (C) REPLACE with dynamic (PM's literal ask) — regresses Lime C1/C3 today, blocks until webhook history backfills. Awaiting PM ruling before any Tier 2 code write.

[2026-04-17T02:00:00Z] -- TASK B INVESTIGATION (Tier 1 read-only): Lime C1 (cmmwei6pf0001zxgpbsvbbsp1) + C3 (cmmwei6y80005zxgpptn4wd08) position=2 "duplication." FINDING: NOT data corruption. CampaignSequenceRule rows DO have variantKey='A' and variantKey='B' set for the two step-2 entries in each campaign — correct BL-034 A/B variant pattern. evaluateSequenceRules (sequencing.ts:270-295) deterministically picks ONE variant per person via djb2 hash; only one message fires at position 2 per prospect. BUT: the source linkedinSequence JSON on Campaign record is MISSING the variantKey field on both step-2 entries — they appear as two naked duplicates. createSequenceRulesForCampaign (sequencing.ts:395-429) reads `step.variantKey ?? null`, so if the sequence is re-deployed from the current JSON the rules will be recreated WITHOUT variantKey and both messages will fire (regression). This is latent. Data shape drift between the write path that populated variantKey on the rules and the JSON that was stored alongside. Confirmed other Lime campaigns (C2, C4-C7) have NO step-2 duplication — this is scoped to C1+C3 only (the two that use {LASTEMAILMONTH} + reference the earlier email campaign). RECOMMENDATION: Option (b) from PM brief — it IS an intentional A/B pattern, not a duplicate message bug. Fix path: backfill Campaign.linkedinSequence JSON for C1 + C3 to write variantKey:"A" / "B" on step-2 entries (matching existing CampaignSequenceRule state). Zero prospect impact — rules are already correctly set up. Protects against re-deploy regression. Tier 2 write, needs PM approval because Lime C1/C3 are active campaigns. NOT mutating until PM decides.

[2026-04-17T02:00:00Z] -- TASK C: PM confirmation logged — LinkedIn sequences intentionally use hardcoded sender names (e.g. "Daniel" on 1210, "Lucy" on Lime, "James" on BlankTag step 4) BY DESIGN. LinkedIn is 1-to-1 chat from a specific sender profile — the Person viewing the InMail sees that sender's name anyway, so baking the name into copy matches the on-screen identity. Email is multi-sender pooled via {SENDER_FIRST_NAME}/{SENDER_FULL_NAME} tokens (BL-100) because any of N senders can own a given send. No backlog item exists today labeled "LinkedIn sender placeholder" — this decision closes the informal todo that surfaces as "LOG:Daniel/Lucy" in the _bl105-verify-1210-lime-render.ts output and the BL-105 decisions-log note "step 4 still has hardcoded 'James' — that is bug (3), out of scope this cycle". With this ruling, future verifier runs can drop Rule (c) hardcoded-sender scans or re-frame them as informational-only (not a lint failure). Deferring the verifier update until PM signs off on Task A direction (same file touched).


[2026-04-17T02:15:00Z] -- COMPLETED 1210-solutions EB sender daily_limit correction (Tier 2, PM pre-authorized). New script `scripts/maintenance/_1210_daily_limits_2026_04_16.ts` fetched 59 senders via EB API (auto-paginated), classified 50 Outlook (`type="microsoft_oauth"`) + 9 Google (`type="google_workspace_oauth"`) + 0 ambiguous using primary `type` heuristic. All 59 oldLimit=10; PATCHed sequentially to target (Outlook→5, Google→8); re-fetched and verified all 59 at target. 0 failures, 0 skipped, 0 ambiguous. Summary AuditLog row id=cmo1us1li0000zx4ifolbngdl (action=`eb.sender.daily_limit.updated`, entityType=Workspace, entityId=1210-solutions) contains full per-inbox change set. NO campaign/schedule/sequence/writer-path touches; EB 92 NOT resumed. tsc gate clean. `PatchSenderEmailParams.daily_limit?: number` was already defined in types.ts — no type change needed. Roster ready for PM to authorise EB 92 canary resume.
